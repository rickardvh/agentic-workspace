repos:
  - repo: local
    hooks:
      - id: validation
        name: format, lint, and typecheck
        entry: uv run python src/tooling/git_hooks/pre_commit.py
        language: system
        pass_filenames: false
        always_run: true
      - id: no-absolute-paths
        name: no absolute paths
        entry: uv run python src/tooling/check/check_no_absolute_paths.py
        language: system
        pass_filenames: false
        always_run: true
