version = 1

[rules]
routing_only = ["memory/index.md"]
high_level = ["memory/index.md", "memory/current/project-state.md"]
canonical_dirs = [
  "memory/domains",
  "memory/invariants",
  "memory/runbooks",
  "memory/mistakes",
  "memory/decisions",
]
task_board_globs = ["TODO.md", "tasks/**", ".github/issues/**"]
core_doc_globs = ["README.md", "docs/**/*.md", "CONTRIBUTING.md", ".github/**/*.md"]
core_doc_exclude_globs = ["AGENTS.md", "memory/**/*.md", ".agentic-workspace/memory/**/*.md"]
forbid_core_docs_depend_on_memory = false
# Recommended audience conventions for planner-agnostic routing:
# - agent_bootstrap
# - human_operator
# - shared_invariant
# Existing repos may keep their current audience values; these labels are guidance only.
# Optional improvement-pressure fields:
# - memory_role = "durable_truth" | "improvement_signal"
# - symptom_of = "workflow_friction" | "guidance_drift" | "missing_guardrail" | "architecture_friction" | "operator_complexity"
# - preferred_remediation = "docs" | "skill" | "script" | "test" | "validation" | "refactor" | "code"
# - improvement_candidate = true
# - improvement_note = "short advisory hint"
# - elimination_target = "shrink" | "promote" | "automate" | "refactor_away"
# - retention_justification = "short reason the note should remain despite being an improvement signal"
# Existing `routes_from`, `stale_when`, and `related_validations` fields also act as
# the first evidence-backed trust anchors for durable notes: they say what currently
# grounds the note and what should make it questionable or stale when repo evidence changes.
# These fields are advisory only; existing manifests remain valid without them.

[notes."memory/index.md"]
note_type = "routing"
canonical_home = "memory/index.md"
authority = "canonical"
audience = "human+agent"
canonicality = "agent_only"
task_relevance = "required"
subsystems = ["memory-system"]
surfaces = ["decision", "runtime", "api", "retrieval", "tests", "architecture"]
routes_from = ["AGENTS.md", "memory/**/*.md"]
stale_when = ["memory/**/*.md"]
routing_only = true
high_level = true

[notes."memory/current/project-state.md"]
note_type = "current-overview"
canonical_home = "memory/current/project-state.md"
authority = "advisory"
audience = "human+agent"
canonicality = "agent_only"
task_relevance = "optional"
subsystems = ["current-context"]
surfaces = []
routes_from = ["README.md", ".agentic-workspace/memory/**/*.md", "bootstrap/**/*.md"]
stale_when = ["README.md", ".agentic-workspace/memory/**/*.md", "bootstrap/**/*.md"]

[notes."memory/current/task-context.md"]
note_type = "current-context"
canonical_home = "memory/current/task-context.md"
authority = "advisory"
audience = "agent"
canonicality = "agent_only"
task_relevance = "optional"
subsystems = ["current-context"]
surfaces = []
routes_from = []
stale_when = []
# Use this note only for optional continuation compression:
# active goal, touched surfaces, blocking assumptions, and next validation.

[notes."memory/current/routing-feedback.md"]
note_type = "routing-feedback"
canonical_home = "memory/current/routing-feedback.md"
authority = "advisory"
audience = "agent"
canonicality = "agent_only"
task_relevance = "optional"
subsystems = ["memory-system", "routing"]
surfaces = []
routes_from = []
stale_when = []
# Use this note only for compact routing calibration:
# concrete missed-note or over-routing cases worth revisiting after manifest or index changes.

[notes."memory/domains/example-runtime-boundary.md"]
note_type = "domain"
canonical_home = "memory/domains/example-runtime-boundary.md"
authority = "advisory"
audience = "human+agent"
canonicality = "agent_only"
task_relevance = "optional"
subsystems = ["starter-example", "runtime"]
surfaces = ["runtime", "architecture"]
routes_from = ["memory/index.md", "bootstrap/**/*.md"]
stale_when = ["memory/index.md", "bootstrap/**/*.md"]

[notes."memory/invariants/example-response-contract.md"]
note_type = "invariant"
canonical_home = "memory/invariants/example-response-contract.md"
authority = "advisory"
audience = "human+agent"
canonicality = "agent_only"
task_relevance = "optional"
subsystems = ["starter-example", "api"]
surfaces = ["api", "tests"]
routes_from = ["memory/index.md", "bootstrap/**/*.md"]
stale_when = ["memory/index.md", "bootstrap/**/*.md"]

[notes."memory/runbooks/example-release-check.md"]
note_type = "runbook"
canonical_home = "memory/runbooks/example-release-check.md"
authority = "advisory"
audience = "human+agent"
canonicality = "agent_only"
task_relevance = "optional"
subsystems = ["starter-example", "operations"]
surfaces = ["runtime", "tests"]
routes_from = ["memory/index.md", "bootstrap/**/*.md"]
stale_when = ["memory/index.md", "bootstrap/**/*.md"]

[notes."memory/decisions/example-cli-selection.md"]
note_type = "decision"
canonical_home = "memory/decisions/example-cli-selection.md"
authority = "advisory"
audience = "human+agent"
canonicality = "agent_only"
task_relevance = "optional"
subsystems = ["starter-example", "tooling"]
surfaces = ["architecture"]
routes_from = ["memory/index.md", "bootstrap/**/*.md"]
stale_when = ["memory/index.md", "bootstrap/**/*.md"]

[notes."memory/current/active-decisions.md"]
note_type = "current-decisions"
canonical_home = "memory/current/active-decisions.md"
authority = "advisory"
audience = "human+agent"
canonicality = "agent_only"
task_relevance = "optional"
subsystems = ["orchestration", "validation"]
surfaces = ["decision", "architecture"]
routes_from = ["AGENTS.md", "Makefile", ".github/workflows/**/*.yml", ".agentic-workspace/**/*.md"]
stale_when = ["AGENTS.md", "Makefile", ".github/workflows/**/*.yml", ".agentic-workspace/**/*.md"]

[notes."memory/domains/memory-package-context.md"]
note_type = "domain"
canonical_home = "memory/domains/memory-package-context.md"
authority = "canonical"
audience = "human+agent"
canonicality = "agent_only"
task_relevance = "optional"
subsystems = ["memory-package", "orchestration"]
surfaces = ["architecture", "runtime"]
routes_from = ["packages/memory/**", "packages/memory/README.md"]
stale_when = ["packages/memory/**"]
memory_role = "durable_truth"

[notes."memory/domains/vague-prompt-domain-understanding.md"]
note_type = "domain"
canonical_home = "memory/domains/vague-prompt-domain-understanding.md"
authority = "canonical"
audience = "human+agent"
canonicality = "agent_only"
task_relevance = "optional"
subsystems = ["memory-system", "planning", "routing"]
surfaces = ["decision", "routing"]
routes_from = [
  "docs/intent-contract.md",
  "docs/default-path-contract.md",
  "docs/reporting-contract.md",
  "docs/delegated-judgment-contract.md",
  "src/agentic_workspace/cli.py",
]
stale_when = [
  "docs/intent-contract.md",
  "docs/default-path-contract.md",
  "docs/reporting-contract.md",
  "docs/delegated-judgment-contract.md",
  "src/agentic_workspace/cli.py",
]
memory_role = "durable_truth"

[notes."memory/domains/planning-package-context.md"]
note_type = "domain"
canonical_home = "memory/domains/planning-package-context.md"
authority = "canonical"
audience = "human+agent"
canonicality = "agent_only"
task_relevance = "optional"
subsystems = ["planning-package", "orchestration"]
surfaces = ["architecture", "decision"]
routes_from = ["packages/planning/**", "packages/planning/README.md"]
stale_when = ["packages/planning/**"]
memory_role = "durable_truth"

[notes."memory/runbooks/package-context-inspection.md"]
note_type = "runbook"
canonical_home = "memory/runbooks/package-context-inspection.md"
authority = "canonical"
audience = "human+agent"
canonicality = "agent_only"
task_relevance = "optional"
subsystems = ["memory-package", "planning-package", "orchestration"]
surfaces = ["runtime"]
routes_from = ["packages/memory/**", "packages/planning/**"]
stale_when = ["packages/memory/**", "packages/planning/**"]
memory_role = "durable_truth"

[notes."memory/runbooks/dogfooding-feedback-routing.md"]
note_type = "runbook"
canonical_home = "memory/runbooks/dogfooding-feedback-routing.md"
authority = "canonical"
audience = "human+agent"
canonicality = "agent_only"
task_relevance = "optional"
subsystems = ["memory-system", "planning"]
surfaces = ["review", "routing"]
routes_from = [
  "docs/contributor-playbook.md",
  "docs/lazy-discovery-measurements.md",
  "memory/runbooks/dogfooding-usage-ledger.md",
]
stale_when = [
  "docs/contributor-playbook.md",
  "docs/lazy-discovery-measurements.md",
  "memory/runbooks/dogfooding-usage-ledger.md",
]
memory_role = "durable_truth"

[notes."memory/runbooks/dogfooding-usage-ledger.md"]
note_type = "runbook"
canonical_home = "memory/runbooks/dogfooding-usage-ledger.md"
authority = "canonical"
audience = "human+agent"
canonicality = "agent_only"
task_relevance = "optional"
subsystems = ["memory-system", "planning"]
surfaces = ["review", "architecture"]
routes_from = ["memory/runbooks/dogfooding-feedback-routing.md", "docs/lazy-discovery-measurements.md"]
stale_when = ["memory/runbooks/dogfooding-feedback-routing.md", "docs/lazy-discovery-measurements.md"]
memory_role = "durable_truth"

[notes."memory/runbooks/vague-instruction-failure-capture.md"]
note_type = "runbook"
canonical_home = "memory/runbooks/vague-instruction-failure-capture.md"
authority = "canonical"
audience = "human+agent"
canonicality = "agent_only"
task_relevance = "optional"
subsystems = ["memory-system", "planning", "routing"]
surfaces = ["review", "routing"]
routes_from = [
  "docs/intent-contract.md",
  "docs/default-path-contract.md",
  "docs/reporting-contract.md",
  "docs/delegated-judgment-contract.md",
  "memory/runbooks/dogfooding-usage-ledger.md",
]
stale_when = [
  "docs/intent-contract.md",
  "docs/default-path-contract.md",
  "docs/reporting-contract.md",
  "docs/delegated-judgment-contract.md",
  "memory/runbooks/dogfooding-usage-ledger.md",
]
memory_role = "improvement_signal"
symptom_of = "guidance_drift"
preferred_remediation = "docs"
improvement_candidate = true
improvement_note = "If repeated vague prompts keep requiring the same clarification, capture the missing domain knowledge or promote it into canonical docs."
elimination_target = "promote"

[notes."memory/decisions/installed-system-consolidation-2026-04-05.md"]
note_type = "decision"
canonical_home = "memory/decisions/installed-system-consolidation-2026-04-05.md"
authority = "canonical"
audience = "human+agent"
canonicality = "agent_only"
task_relevance = "optional"
subsystems = ["orchestration", "validation"]
surfaces = ["decision", "architecture"]
routes_from = ["AGENTS.md", "README.md", "Makefile", ".github/workflows/**/*.yml"]
stale_when = ["AGENTS.md", "README.md", "Makefile", ".github/workflows/**/*.yml"]
memory_role = "durable_truth"

[notes."memory/decisions/path-consolidation-agentic-workspace-2026-04-05.md"]
note_type = "decision"
canonical_home = "memory/decisions/path-consolidation-agentic-workspace-2026-04-05.md"
authority = "canonical"
audience = "human+agent"
canonicality = "agent_only"
task_relevance = "optional"
subsystems = ["orchestration", "packaging"]
surfaces = ["decision", "architecture"]
routes_from = ["packages/memory/**", "packages/planning/**", "README.md", "Makefile"]
stale_when = ["packages/memory/**", "packages/planning/**", "README.md", "Makefile"]
memory_role = "durable_truth"

[notes."memory/decisions/foundation-stability-2026-04-05.md"]
note_type = "decision"
canonical_home = "memory/decisions/foundation-stability-2026-04-05.md"
authority = "canonical"
audience = "human+agent"
canonicality = "agent_only"
task_relevance = "optional"
subsystems = ["planning", "orchestration"]
surfaces = ["decision", "architecture"]
routes_from = ["docs/execplans/archive/**/*.md", "README.md", "AGENTS.md"]
stale_when = ["docs/execplans/archive/**/*.md", "README.md", "AGENTS.md"]
memory_role = "durable_truth"

[notes."memory/decisions/workspace-orchestrator-ownership-ledger-2026-04-05.md"]
note_type = "decision"
canonical_home = "memory/decisions/workspace-orchestrator-ownership-ledger-2026-04-05.md"
authority = "canonical"
audience = "human+agent"
canonicality = "agent_only"
task_relevance = "optional"
subsystems = ["orchestration", "planning", "memory-system"]
surfaces = ["decision", "architecture"]
routes_from = ["AGENTS.md", ".agentic-workspace/**/*.md", ".agentic-workspace/OWNERSHIP.toml", "packages/**"]
stale_when = ["AGENTS.md", ".agentic-workspace/**/*.md", ".agentic-workspace/OWNERSHIP.toml", "packages/**"]
memory_role = "durable_truth"

[notes."memory/decisions/mature-repo-jumpstart-seed-candidates-2026-04-14.md"]
note_type = "decision"
canonical_home = "memory/decisions/mature-repo-jumpstart-seed-candidates-2026-04-14.md"
authority = "canonical"
audience = "human+agent"
canonicality = "agent_only"
task_relevance = "optional"
subsystems = ["planning", "memory-system"]
surfaces = ["decision", "architecture"]
routes_from = [
  "docs/delegated-judgment-contract.md",
  "docs/resumable-execution-contract.md",
  "docs/capability-aware-execution.md",
  "docs/execution-summary-contract.md",
  "docs/reporting-contract.md",
  "docs/jumpstart-contract.md",
]
stale_when = [
  "docs/delegated-judgment-contract.md",
  "docs/resumable-execution-contract.md",
  "docs/capability-aware-execution.md",
  "docs/execution-summary-contract.md",
  "docs/reporting-contract.md",
  "docs/jumpstart-contract.md",
]
memory_role = "durable_truth"

[notes.".agentic-workspace/memory/WORKFLOW.md"]
note_type = "workflow-policy"
canonical_home = ".agentic-workspace/memory/WORKFLOW.md"
authority = "canonical"
audience = "human+agent"
canonicality = "agent_only"
task_relevance = "optional"
subsystems = ["memory-system"]
surfaces = []
routes_from = ["AGENTS.md", "memory/index.md", "memory/manifest.toml", ".agentic-workspace/memory/**/*.md"]
stale_when = ["AGENTS.md", "memory/index.md", "memory/manifest.toml", "bootstrap/AGENTS.md"]
related_validations = ["python scripts/check/check_memory_freshness.py"]
memory_role = "durable_truth"

[notes.".agentic-workspace/memory/VERSION.md"]
note_type = "version-marker"
canonical_home = ".agentic-workspace/memory/VERSION.md"
authority = "canonical"
audience = "agent"
canonicality = "agent_only"
task_relevance = "optional"
subsystems = ["installer", "payload"]
surfaces = ["api"]
routes_from = ["src/**/*.py", "pyproject.toml"]
stale_when = ["src/**/*.py", "pyproject.toml", "bootstrap/memory/system/VERSION.md"]

[notes."memory/mistakes/recurring-failures.md"]
note_type = "recurring-failures"
canonical_home = "memory/mistakes/recurring-failures.md"
authority = "canonical"
audience = "human+agent"
canonicality = "agent_only"
task_relevance = "optional"
subsystems = ["memory-system"]
surfaces = ["tests", "architecture"]
routes_from = ["tests/**/*.py", "scripts/check/**/*.py"]
stale_when = ["tests/**/*.py", "scripts/check/**/*.py"]
related_validations = ["python scripts/check/check_memory_freshness.py"]
memory_role = "improvement_signal"
symptom_of = "missing_guardrail"
preferred_remediation = "test"
improvement_candidate = true
improvement_note = "If recurring failures persist, consider a regression test, validation, or lint rule before growing the note."
elimination_target = "shrink"
