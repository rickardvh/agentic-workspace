# Repo Memory Skills

Repo-owned memory skills live here.

`REGISTRY.json` is the explicit repo-owned memory skill registry for this repository. Keep it in sync with the checked-in skill directories here and record the activation hints that help workspace-level skill recommendation stay explainable.

Use this directory for repeatable procedures that operate on checked-in memory or maintain the repo's memory system.
Do not put general coding or planning workflows here.

Current repo-specific skills:

- `package-context-inspection`
- `memory-reporting`
- `vague-prompt-domain-understanding`
