{
  "schema_version": "agentic-workspace/operation/v1",
  "id": "memory.install.lifecycle",
  "command_surface": {
    "program": "agentic-memory",
    "command": "install",
    "format_option": "format"
  },
  "summary": "Install Memory bootstrap files into a repository.",
  "classification": "lifecycle-mutation",
  "inputs": [
    {
      "name": "target",
      "source": "cli-option",
      "required": false
    },
    {
      "name": "dry_run",
      "source": "cli-option",
      "required": false
    },
    {
      "name": "force",
      "source": "cli-option",
      "required": false
    },
    {
      "name": "project_name",
      "source": "cli-option",
      "required": false
    },
    {
      "name": "project_purpose",
      "source": "cli-option",
      "required": false
    },
    {
      "name": "key_repo_docs",
      "source": "cli-option",
      "required": false
    },
    {
      "name": "key_subsystems",
      "source": "cli-option",
      "required": false
    },
    {
      "name": "primary_build_command",
      "source": "cli-option",
      "required": false
    },
    {
      "name": "primary_test_command",
      "source": "cli-option",
      "required": false
    },
    {
      "name": "other_key_commands",
      "source": "cli-option",
      "required": false
    },
    {
      "name": "policy_profile",
      "source": "cli-option",
      "required": false
    },
    {
      "name": "format",
      "source": "cli-option",
      "required": false
    }
  ],
  "output": {
    "kind": "text-or-json"
  },
  "effects": {
    "read_only": false,
    "destructive": false,
    "idempotent": true,
    "writes_repo_state": true,
    "requires_preflight_gate": false
  },
  "locality": {
    "requires_repo": true,
    "network": "forbidden",
    "outside_repo_writes": "forbidden"
  },
  "reads": [
    {
      "surface": "target repository Memory install state",
      "required": false
    }
  ],
  "writes": [
    {
      "surface": "target repository Memory managed surfaces",
      "required": false,
      "when": "not a dry run"
    }
  ],
  "steps": [
    {
      "id": "apply_memory_lifecycle",
      "uses": "memory.install.apply",
      "description": "Install Memory bootstrap files into a repository."
    },
    {
      "id": "emit_output",
      "uses": "output.emit",
      "description": "Emit the install result in the requested format."
    }
  ],
  "guards": [
    "Generated code owns parser/help/dispatch shape; memory-owned lifecycle policy remains behind the declared primitive.",
    "Dry-run conformance must prove generated lifecycle adapters can execute without repository writes."
  ],
  "proof": [
    "operation contract validates against operation.schema.json",
    "generated memory install dry-run process conformance passes"
  ],
  "migration_status": "runtime-consumed",
  "ir_plan": {
    "status": "representative",
    "summary": "Memory install executes generated operation IR through codegen-owned primitive dataflow while the memory module owns lifecycle policy.",
    "steps": [
      {
        "id": "apply_memory_lifecycle",
        "uses": "memory.install.apply",
        "description": "Install Memory bootstrap files into a repository.",
        "arguments": {},
        "outputs": [
          "result"
        ],
        "on_error": "fail"
      },
      {
        "id": "emit_output",
        "uses": "output.emit",
        "description": "Emit the install result in the requested format.",
        "arguments": {},
        "outputs": [
          "emitted"
        ],
        "on_error": "emit_usage_error"
      }
    ]
  }
}
