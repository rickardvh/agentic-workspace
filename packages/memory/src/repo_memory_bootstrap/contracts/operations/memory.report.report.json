{
  "schema_version": "agentic-workspace/operation/v1",
  "id": "memory.report.report",
  "command_surface": {
    "program": "agentic-memory",
    "command": "report",
    "format_option": "format"
  },
  "summary": "Read-only agentic-memory report; non-verbose JSON uses generated manifest-count payload construction while verbose/text behavior remains on the explicit source fallback.",
  "classification": "read-only-report",
  "inputs": [
    {
      "name": "target",
      "source": "cli-option",
      "required": false
    },
    {
      "name": "verbose",
      "source": "cli-option",
      "required": false
    },
    {
      "name": "format",
      "source": "cli-option",
      "required": false
    }
  ],
  "output": {
    "kind": "text-or-json"
  },
  "effects": {
    "read_only": true,
    "destructive": false,
    "idempotent": true,
    "writes_repo_state": false,
    "requires_preflight_gate": false
  },
  "locality": {
    "requires_repo": false,
    "network": "forbidden",
    "outside_repo_writes": "forbidden"
  },
  "reads": [
    {
      "surface": "package-local runtime state",
      "required": false
    },
    {
      "surface": "verbose flag for broad diagnostic output",
      "required": false
    }
  ],
  "writes": [],
  "steps": [
    {
      "id": "load_report",
      "uses": "memory.report.load",
      "description": "Load package-local report payload."
    },
    {
      "id": "emit_output",
      "uses": "output.emit",
      "description": "Emit report in the requested format."
    }
  ],
  "guards": [
    "report must remain read-only."
  ],
  "proof": [
    "operation contract validates against operation.schema.json",
    "process conformance remains compatible"
  ],
  "migration_status": "runtime-consumed",
  "ir_plan": {
    "status": "representative",
    "summary": "Memory report executes generated operation IR through codegen-owned primitive dataflow. The generated Python runtime projection builds the non-verbose JSON tiny report from manifest TOML counts and falls back to the package-domain report source for verbose/text behavior.",
    "steps": [
      {
        "id": "load_report",
        "uses": "memory.report.load",
        "description": "Load the tiny or verbose memory report payload for the requested target.",
        "arguments": {},
        "outputs": [
          "result"
        ],
        "on_error": "fail"
      },
      {
        "id": "emit_output",
        "uses": "output.emit",
        "description": "Emit the memory report payload in the requested format.",
        "arguments": {},
        "outputs": [
          "emitted"
        ],
        "on_error": "emit_usage_error"
      }
    ]
  }
}
