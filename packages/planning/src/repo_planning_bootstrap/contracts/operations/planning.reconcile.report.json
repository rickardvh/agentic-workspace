{
  "schema_version": "agentic-workspace/operation/v1",
  "id": "planning.reconcile.report",
  "command_surface": {
    "program": "agentic-planning",
    "command": "reconcile",
    "format_option": "format"
  },
  "summary": "Report planning reconciliation state and compile stale target-authority proposals into one exact preview/apply transaction.",
  "classification": "lifecycle-mutation",
  "inputs": [
    {
      "name": "target",
      "source": "cli-option",
      "required": false
    },
    {
      "name": "format",
      "source": "cli-option",
      "required": false
    },
    {
      "name": "apply_safe_prune",
      "source": "cli-option",
      "required": false,
      "description": "When true, remove only cleanup targets already marked safe_to_prune by reconcile."
    },
    {
      "name": "dry_run",
      "source": "cli-option",
      "required": false,
      "description": "Preview safe-prune application without writing repo state."
    },
    {"name": "lane", "source": "cli-option", "required": false},
    {"name": "apply_lane_reconcile", "source": "cli-option", "required": false},
    {"name": "apply_lane_current_slice_reconcile", "source": "cli-option", "required": false},
    {"name": "owner_surface", "source": "cli-option", "required": false},
    {"name": "relation_identity", "source": "cli-option", "required": false},
    {"name": "subject", "source": "cli-option", "required": false},
    {"name": "expected_lane_revision", "source": "cli-option", "required": false},
    {"name": "transition", "source": "cli-option", "required": false},
    {"name": "expected_execplan", "source": "cli-option", "required": false},
    {"name": "issue", "source": "cli-option", "required": false},
    {"name": "external_ref", "source": "cli-option", "required": false},
    {"name": "priority", "source": "cli-option", "required": false},
    {"name": "depends_on", "source": "cli-option", "required": false},
    {"name": "rationale", "source": "cli-option", "required": false},
    {"name": "maturity", "source": "cli-option", "required": false},
    {"name": "expect_relation_revision", "source": "cli-option", "required": false},
    {"name": "apply_issue_relation_reconcile", "source": "cli-option", "required": false},
    {"name": "apply_issue_relation_migration", "source": "cli-option", "required": false},
    {
      "name": "apply_pending_integrations",
      "source": "cli-option",
      "required": false,
      "description": "Apply all eligible pending integration proposals in one target-authority CAS transaction."
    },
    {
      "name": "preview",
      "source": "cli-option",
      "required": false,
      "description": "Compile and persist one bounded reusable proposal, including target-authority CAS refresh for mechanically stale integration proposals, without mutating authoritative Planning state."
    },
    {
      "name": "apply",
      "source": "cli-option",
      "required": false,
      "description": "Apply a previously previewed reconciliation proposal as one exact compare-and-swap transaction."
    },
    {
      "name": "proposal",
      "source": "cli-option",
      "required": false,
      "description": "Exact proposal identity returned by the reconciliation preview."
    },
    {
      "name": "expected_planning_revision",
      "source": "cli-option",
      "required": false,
      "description": "Compare-and-swap Planning revision required when applying a proposal."
    }
  ],
  "conditional_inputs": [
    {"selector": "transition", "selector_value": "restore", "required_inputs": [], "semantics": "Reuse the currently declared execplan unless an exact source is explicitly confirmed."},
    {"selector": "transition", "selector_value": "relink", "required_inputs": ["expected_execplan"], "semantics": "Require the exact existing owner to relink."},
    {"selector": "transition", "selector_value": "supersede", "required_inputs": ["expected_execplan"], "semantics": "Require the exact declared replacement owner."},
    {"selector": "transition", "selector_value": "cancel", "required_inputs": [], "semantics": "Do not require a replacement owner."},
    {"selector": "transition", "selector_value": "human", "required_inputs": [], "semantics": "Do not mutate until an exact owner is chosen."}
  ],
  "output": {
    "kind": "text-or-json"
  },
  "effects": {
    "read_only": false,
    "destructive": false,
    "idempotent": true,
    "writes_repo_state": true,
    "requires_preflight_gate": false
  },
  "locality": {
    "requires_repo": false,
    "network": "forbidden",
    "outside_repo_writes": "forbidden"
  },
  "reads": [
    {
      "surface": "package-local runtime state",
      "required": false
    }
  ],
  "writes": [
    {
      "surface": ".agentic-workspace/planning/state.toml",
      "required": false,
      "when": "--apply-safe-prune removes exact safe_to_prune roadmap targets"
    },
    {
      "surface": ".agentic-workspace/planning/decompositions/",
      "required": false,
      "when": "--apply-safe-prune removes exact safe_to_prune decomposition records"
    },
    {
      "surface": ".agentic-workspace/local/planning/reconciliation-{proposals,receipts}/",
      "required": false,
      "when": "a preview records a reusable proposal or apply records its idempotence receipt"
    },
    {
      "surface": ".agentic-workspace/planning/issue-relations/",
      "required": false,
      "when": "--apply-issue-relation-reconcile updates one semantic relation or --apply-issue-relation-migration migrates legacy strategic fields"
    },
    {
      "surface": ".agentic-workspace/planning/{state.toml,decompositions,lanes}/",
      "required": false,
      "when": "--apply-issue-relation-migration demotes duplicate legacy strategic authority to derived references"
    },
    {
      "surface": ".agentic-workspace/planning/execplans/<owner>.plan.json",
      "required": false,
      "when": "a fresh admitted completed external observation makes the selected owner's observation-dependent next action obsolete; only the exact proof/intent blocker is refreshed"
    },
    {
      "surface": ".agentic-workspace/planning/integration-{proposals,receipts}/ and affected owner records",
      "required": false,
      "when": "--apply-pending-integrations or --apply of a target-authority preview applies eligible pending proposals as one target transaction; the owner records, refreshed proposals, integration receipts, and reconciliation receipt publish atomically"
    },
    {
      "surface": ".agentic-workspace/planning/lanes/<lane>.lane.json and .agentic-workspace/planning/state.toml",
      "required": false,
      "when": "--apply-lane-current-slice-reconcile restores an exact existing execplan onto the named lane relation"
    }
  ],
  "ir_plan": {
    "status": "representative",
    "summary": "Load planning reconcile payload and emit text or JSON through the generated operation executor.",
    "steps": [
      {
        "id": "load_report",
        "uses": "planning.reconcile.load",
        "description": "Load package-local reconcile payload.",
        "arguments": {},
        "outputs": [
          "result"
        ]
      },
      {
        "id": "emit_output",
        "uses": "output.emit",
        "description": "Emit reconcile in the requested format.",
        "arguments": {},
        "outputs": [
          "emitted"
        ]
      }
    ]
  },
  "steps": [
    {
      "id": "load_report",
      "uses": "planning.reconcile.load",
      "description": "Load package-local report payload."
    },
    {
      "id": "apply_safe_prune",
      "uses": "planning.reconcile.load",
      "description": "When requested, remove exact cleanup targets already marked safe_to_prune and then reload verification state."
    },
    {
      "id": "emit_output",
      "uses": "output.emit",
      "description": "Emit report in the requested format."
    }
  ],
  "guards": [
    "--apply-safe-prune must only mutate targets already present in cleanup_targets with safe_to_prune=true.",
    "--dry-run must not write repo state.",
    "--apply requires the exact proposal id and expected Planning revision returned by preview.",
    "Target-authority previews preserve unrelated owners, relations, worktree-local selection, and human-authored intent while refreshing only CAS-stale proposals whose semantic subjects remain current.",
    "Target-authority apply fails closed for same-owner or semantic conflicts; disjoint eligible proposals remain independently applicable.",
    "--apply-issue-relation-reconcile requires the current relation revision for semantic edits.",
    "--apply-issue-relation-migration preserves membership, priority, dependencies, rationale, and maturity before demoting duplicate legacy fields.",
    "--apply-pending-integrations applies the eligible disjoint owner set atomically on an admitted target branch, preserves stale unrelated proposals as explicit skips, and fails closed for same-owner overlap.",
    "Fresh external completion may refresh an obsolete owner action to the exact semantic proof/intent blocker, but cannot itself satisfy proof, intent, or closeout authority.",
    "--apply-lane-current-slice-reconcile must guard the exact owner surface, relation identity, subject, lane revision, transition, and execplan source, and must not fabricate a missing execplan."
  ],
  "proof": [
    "operation contract validates against operation.schema.json",
    "process conformance remains compatible",
    "safe-prune tests cover exact roadmap/decomposition cleanup and dry-run behavior",
    "proposal/apply tests cover revision guards, idempotence, and transaction rollback",
    "issue-relation tests cover supported conflict repair and legacy authority migration",
    "pending integration tests cover one-invocation target apply, stale disjoint skips, same-owner failure, and transaction rollback",
    "target-authority reconciliation tests cover bounded preview, exact regeneration after CAS drift, semantic conflict isolation, atomic publication, preserved owner-scoped state, and idempotent replay",
    "provider-neutral external-observation tests cover stale action refresh plus ambiguous-completion non-authority",
    "workspace CLI tests cover missing-source block and exact source restore through the root planning reconcile front door"
  ],
  "migration_status": "runtime-consumed"
}
