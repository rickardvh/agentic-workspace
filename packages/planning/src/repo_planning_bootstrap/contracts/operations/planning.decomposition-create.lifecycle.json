{
  "schema_version": "agentic-workspace/operation/v1",
  "id": "planning.decomposition-create.lifecycle",
  "command_surface": {"program": "agentic-planning", "command": "decomposition-create", "format_option": "format"},
  "summary": "Create a first-class Planning decomposition record for epic-shaped work.",
  "classification": "lifecycle-mutation",
  "inputs": [
    {"name": "id", "source": "cli-option", "required": true},
    {"name": "title", "source": "cli-option", "required": true},
    {"name": "outcome", "source": "cli-option", "required": true},
    {"name": "promotion_rule", "source": "cli-option", "required": false},
    {"name": "target", "source": "cli-option", "required": false},
    {"name": "expect_planning_revision", "source": "cli-option", "required": false},
    {"name": "dry_run", "source": "cli-option", "required": false},
    {"name": "format", "source": "cli-option", "required": false}
  ],
  "output": {"kind": "module-result"},
  "effects": {"read_only": false, "destructive": false, "idempotent": false, "writes_repo_state": true, "requires_preflight_gate": false},
  "locality": {"requires_repo": false, "network": "forbidden", "outside_repo_writes": "forbidden"},
  "reads": [{"surface": "target repository Planning schemas", "required": true}],
  "writes": [{"surface": ".agentic-workspace/planning/decompositions/", "required": false, "when": "not a dry run"}],
  "steps": [
    {"id": "apply_lifecycle_operation", "uses": "planning.decomposition-create.apply", "description": "Create a schema-valid decomposition owner record."},
    {"id": "emit_output", "uses": "output.emit", "description": "Emit planning lifecycle result in the requested format."}
  ],
  "guards": ["Respect dry-run mode by avoiding writes.", "Create an actual decomposition owner rather than substituting an execplan or lane."],
  "proof": ["operation contract validates against operation.schema.json", "generated decomposition-create process conformance passes"],
  "migration_status": "runtime-consumed",
  "ir_plan": {
    "status": "representative",
    "summary": "Planning decomposition-create executes generated operation IR while Planning owns decomposition mutation policy.",
    "steps": [
      {"id": "apply_lifecycle_operation", "uses": "planning.decomposition-create.apply", "description": "Create the decomposition owner.", "arguments": {}, "outputs": ["result"], "on_error": "emit_usage_error"},
      {"id": "emit_output", "uses": "output.emit", "description": "Emit the result.", "arguments": {}, "outputs": ["emitted"], "on_error": "emit_usage_error"}
    ]
  }
}
