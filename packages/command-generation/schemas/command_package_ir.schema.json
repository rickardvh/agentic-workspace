{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/command-package-ir.schema.json",
  "title": "Agentic Workspace Command Package IR",
  "description": "Intermediate representation for generating command package metadata and language/runtime adapters.",
  "type": "object",
  "required": [
    "schema_version",
    "summary",
    "schema",
    "source_contracts",
    "generation_policy",
    "packages"
  ],
  "properties": {
    "schema_version": {
      "const": "agentic-workspace/command-package-ir/v1",
      "description": "Command package IR contract version."
    },
    "summary": {
      "type": "string",
      "minLength": 1,
      "description": "Short summary of the generated command package boundary."
    },
    "schema": {
      "const": "schemas/command_package_ir.schema.json",
      "description": "Schema reference carried by the IR document."
    },
    "source_contracts": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1,
        "description": "Item string accepted by source contracts in Agentic Workspace Command Package IR."
      },
      "description": "Contract files that feed or constrain command package generation."
    },
    "generation_policy": {
      "type": "object",
      "required": [
        "authority_rule",
        "runtime_boundary",
        "ordinary_development_environment",
        "test_environment",
        "custom_codegen_boundary",
        "shell_adapter_policy",
        "direct_cli_edit_policy",
        "generated_package_maturity",
        "non_python_runtime_binding"
      ],
      "properties": {
        "authority_rule": {
          "type": "string",
          "minLength": 1,
          "description": "The authority rule string field in Agentic Workspace Command Package IR."
        },
        "runtime_boundary": {
          "type": "string",
          "minLength": 1,
          "description": "The runtime boundary string field in Agentic Workspace Command Package IR."
        },
        "ordinary_development_environment": {
          "type": "string",
          "minLength": 1,
          "description": "The ordinary development environment string field in Agentic Workspace Command Package IR."
        },
        "test_environment": {
          "type": "string",
          "minLength": 1,
          "description": "The test environment string field in Agentic Workspace Command Package IR."
        },
        "custom_codegen_boundary": {
          "type": "string",
          "minLength": 1,
          "description": "The custom codegen boundary string field in Agentic Workspace Command Package IR."
        },
        "shell_adapter_policy": {
          "type": "string",
          "minLength": 1,
          "description": "The shell adapter policy string field in Agentic Workspace Command Package IR."
        },
        "direct_cli_edit_policy": {
          "type": "string",
          "minLength": 1,
          "description": "The direct cli edit policy string field in Agentic Workspace Command Package IR."
        },
        "generated_package_maturity": {
          "type": "object",
          "required": [
            "routing_rule",
            "levels"
          ],
          "properties": {
            "routing_rule": {
              "type": "string",
              "minLength": 1,
              "description": "The routing rule string field in Agentic Workspace Command Package IR."
            },
            "levels": {
              "type": "array",
              "minItems": 1,
              "items": {
                "$ref": "#/$defs/maturity_level",
                "description": "Item reference accepted by levels in Agentic Workspace Command Package IR."
              },
              "description": "The levels array field in Agentic Workspace Command Package IR."
            }
          },
          "additionalProperties": false,
          "description": "The generated package maturity object field in Agentic Workspace Command Package IR."
        },
        "non_python_runtime_binding": {
          "type": "object",
          "required": [
            "selected_model",
            "default_runtime_command",
            "scope",
            "universal_contract_owns",
            "target_projection_owns",
            "runtime_owns",
            "error_mapping"
          ],
          "properties": {
            "selected_model": {
              "type": "string",
              "minLength": 1,
              "description": "The selected model string field in Agentic Workspace Command Package IR."
            },
            "default_runtime_command": {
              "type": "string",
              "minLength": 1,
              "description": "The default runtime command string field in Agentic Workspace Command Package IR."
            },
            "scope": {
              "type": "string",
              "minLength": 1,
              "description": "The scope string field in Agentic Workspace Command Package IR."
            },
            "universal_contract_owns": {
              "$ref": "#/$defs/non_empty_strings",
              "description": "The universal contract owns reference field in Agentic Workspace Command Package IR."
            },
            "target_projection_owns": {
              "$ref": "#/$defs/non_empty_strings",
              "description": "The target projection owns reference field in Agentic Workspace Command Package IR."
            },
            "runtime_owns": {
              "$ref": "#/$defs/non_empty_strings",
              "description": "The runtime owns reference field in Agentic Workspace Command Package IR."
            },
            "error_mapping": {
              "$ref": "#/$defs/non_empty_strings",
              "description": "The error mapping reference field in Agentic Workspace Command Package IR."
            }
          },
          "additionalProperties": false,
          "description": "The non python runtime binding object field in Agentic Workspace Command Package IR."
        }
      },
      "additionalProperties": false,
      "description": "Policy for what generation owns, what runtime code owns, and how adapters stay safe."
    },
    "packages": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/package",
        "description": "Item reference accepted by packages in Agentic Workspace Command Package IR."
      },
      "description": "Generated command packages and their command bindings."
    }
  },
  "additionalProperties": false,
  "$defs": {
    "package": {
      "type": "object",
      "required": [
        "id",
        "program",
        "package_role",
        "targets",
        "commands"
      ],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-z0-9][a-z0-9_.-]*$",
          "description": "The id string field in Agentic Workspace Command Package IR."
        },
        "program": {
          "type": "string",
          "minLength": 1,
          "description": "The program string field in Agentic Workspace Command Package IR."
        },
        "package_role": {
          "enum": [
            "root-workspace-cli",
            "planning-module-cli",
            "memory-module-cli"
          ],
          "description": "The package role enumerated value field in Agentic Workspace Command Package IR."
        },
        "targets": {
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/target",
            "description": "Item reference accepted by targets in Agentic Workspace Command Package IR."
          },
          "description": "The targets array field in Agentic Workspace Command Package IR."
        },
        "commands": {
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/command",
            "description": "Item reference accepted by commands in Agentic Workspace Command Package IR."
          },
          "description": "The commands array field in Agentic Workspace Command Package IR."
        }
      },
      "additionalProperties": false,
      "description": "Reusable package object definition for Agentic Workspace Command Package IR."
    },
    "target": {
      "type": "object",
      "required": [
        "kind",
        "package_name",
        "generated_root",
        "entrypoints",
        "test_environment",
        "maturity_level_ref"
      ],
      "properties": {
        "kind": {
          "enum": [
            "python",
            "typescript",
            "bash",
            "powershell"
          ],
          "description": "The kind enumerated value field in Agentic Workspace Command Package IR."
        },
        "package_name": {
          "type": "string",
          "minLength": 1,
          "description": "The package name string field in Agentic Workspace Command Package IR."
        },
        "generated_root": {
          "type": "string",
          "minLength": 1,
          "description": "The generated root string field in Agentic Workspace Command Package IR."
        },
        "entrypoints": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "minLength": 1,
            "description": "Item string accepted by entrypoints in Agentic Workspace Command Package IR."
          },
          "description": "The entrypoints array field in Agentic Workspace Command Package IR."
        },
        "test_environment": {
          "enum": [
            "python-dev",
            "docker"
          ],
          "description": "The test environment enumerated value field in Agentic Workspace Command Package IR."
        },
        "maturity_level_ref": {
          "type": "string",
          "minLength": 1,
          "description": "The maturity level ref string field in Agentic Workspace Command Package IR."
        },
        "generation_status": {
          "enum": [
            "supported-now",
            "proof-fixture",
            "runnable-read-only-adapter",
            "deferred"
          ],
          "description": "The generation status enumerated value field in Agentic Workspace Command Package IR."
        }
      },
      "additionalProperties": false,
      "description": "Reusable target object definition for Agentic Workspace Command Package IR."
    },
    "maturity_level": {
      "type": "object",
      "required": [
        "id",
        "summary",
        "runnable",
        "runtime_backed",
        "weak_agent_routing",
        "promotion_requires"
      ],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-z0-9][a-z0-9-]*$",
          "description": "The id string field in Agentic Workspace Command Package IR."
        },
        "summary": {
          "type": "string",
          "minLength": 1,
          "description": "The summary string field in Agentic Workspace Command Package IR."
        },
        "runnable": {
          "type": "boolean",
          "description": "The runnable boolean field in Agentic Workspace Command Package IR."
        },
        "runtime_backed": {
          "type": "boolean",
          "description": "The runtime backed boolean field in Agentic Workspace Command Package IR."
        },
        "weak_agent_routing": {
          "enum": [
            "forbidden",
            "review-required",
            "allowed-read-only",
            "allowed-mutation-with-review"
          ],
          "description": "The weak agent routing enumerated value field in Agentic Workspace Command Package IR."
        },
        "promotion_requires": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "The promotion requires reference field in Agentic Workspace Command Package IR."
        }
      },
      "additionalProperties": false,
      "description": "Reusable maturity level object definition for Agentic Workspace Command Package IR."
    },
    "command": {
      "type": "object",
      "required": [
        "adapter_id",
        "status",
        "command",
        "operation_ref",
        "runtime_binding",
        "schemas",
        "effect_hints",
        "conformance_refs",
        "projection_boundary"
      ],
      "properties": {
        "adapter_id": {
          "type": "string",
          "pattern": "^[a-z0-9][a-z0-9_.-]*$",
          "description": "The adapter id string field in Agentic Workspace Command Package IR."
        },
        "status": {
          "enum": [
            "generated",
            "proof-fixture",
            "deferred"
          ],
          "description": "The status enumerated value field in Agentic Workspace Command Package IR."
        },
        "command": {
          "type": "object",
          "required": [
            "name"
          ],
          "properties": {
            "name": {
              "type": "string",
              "minLength": 1,
              "description": "The name string field in Agentic Workspace Command Package IR."
            },
            "subcommand": {
              "type": "string",
              "minLength": 1,
              "description": "The subcommand string field in Agentic Workspace Command Package IR."
            },
            "manifest_ref": {
              "type": "string",
              "minLength": 1,
              "description": "The manifest ref string field in Agentic Workspace Command Package IR."
            }
          },
          "additionalProperties": false,
          "description": "The command object field in Agentic Workspace Command Package IR."
        },
        "operation_ref": {
          "type": "object",
          "required": [
            "id",
            "path"
          ],
          "properties": {
            "id": {
              "type": "string",
              "minLength": 1,
              "description": "The id string field in Agentic Workspace Command Package IR."
            },
            "path": {
              "type": "string",
              "pattern": "^operations/[A-Za-z0-9_.-]+\\.json$",
              "description": "The path string field in Agentic Workspace Command Package IR."
            }
          },
          "additionalProperties": false,
          "description": "The operation ref object field in Agentic Workspace Command Package IR."
        },
        "runtime_binding": {
          "type": "object",
          "required": [
            "kind",
            "primitive_refs"
          ],
          "properties": {
            "kind": {
              "const": "operation-primitive-sequence",
              "description": "The kind constant field in Agentic Workspace Command Package IR."
            },
            "primitive_refs": {
              "type": "array",
              "minItems": 1,
              "items": {
                "type": "string",
                "pattern": "^[a-z0-9][a-z0-9_.-]*$",
                "description": "Item string accepted by primitive refs in Agentic Workspace Command Package IR."
              },
              "description": "The primitive refs array field in Agentic Workspace Command Package IR."
            }
          },
          "additionalProperties": false,
          "description": "The runtime binding object field in Agentic Workspace Command Package IR."
        },
        "schemas": {
          "type": "object",
          "required": [
            "input",
            "output"
          ],
          "properties": {
            "input": {
              "$ref": "#/$defs/schema_refs",
              "description": "The input reference field in Agentic Workspace Command Package IR."
            },
            "output": {
              "$ref": "#/$defs/schema_refs",
              "description": "The output reference field in Agentic Workspace Command Package IR."
            }
          },
          "additionalProperties": false,
          "description": "The schemas object field in Agentic Workspace Command Package IR."
        },
        "effect_hints": {
          "type": "object",
          "required": [
            "read_only",
            "destructive",
            "idempotent",
            "writes_repo_state",
            "requires_preflight_gate"
          ],
          "properties": {
            "read_only": {
              "type": "boolean",
              "description": "The read only boolean field in Agentic Workspace Command Package IR."
            },
            "destructive": {
              "type": "boolean",
              "description": "The destructive boolean field in Agentic Workspace Command Package IR."
            },
            "idempotent": {
              "type": "boolean",
              "description": "The idempotent boolean field in Agentic Workspace Command Package IR."
            },
            "writes_repo_state": {
              "type": "boolean",
              "description": "The writes repo state boolean field in Agentic Workspace Command Package IR."
            },
            "requires_preflight_gate": {
              "type": "boolean",
              "description": "The requires preflight gate boolean field in Agentic Workspace Command Package IR."
            }
          },
          "additionalProperties": false,
          "description": "The effect hints object field in Agentic Workspace Command Package IR."
        },
        "conformance_refs": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "minLength": 1,
            "description": "Item string accepted by conformance refs in Agentic Workspace Command Package IR."
          },
          "description": "The conformance refs array field in Agentic Workspace Command Package IR."
        },
        "projection_boundary": {
          "type": "object",
          "required": [
            "universal",
            "target_specific",
            "runtime_owned"
          ],
          "properties": {
            "universal": {
              "$ref": "#/$defs/non_empty_strings",
              "description": "The universal reference field in Agentic Workspace Command Package IR."
            },
            "target_specific": {
              "$ref": "#/$defs/non_empty_strings",
              "description": "The target specific reference field in Agentic Workspace Command Package IR."
            },
            "runtime_owned": {
              "$ref": "#/$defs/non_empty_strings",
              "description": "The runtime owned reference field in Agentic Workspace Command Package IR."
            }
          },
          "additionalProperties": false,
          "description": "The projection boundary object field in Agentic Workspace Command Package IR."
        }
      },
      "additionalProperties": false,
      "description": "Reusable command object definition for Agentic Workspace Command Package IR."
    },
    "schema_refs": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[A-Za-z0-9_.-]+\\.schema\\.json$",
        "description": "Item string accepted by schema refs in Agentic Workspace Command Package IR."
      },
      "description": "Reusable schema refs array definition for Agentic Workspace Command Package IR."
    },
    "non_empty_strings": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1,
        "description": "Item string accepted by non empty strings in Agentic Workspace Command Package IR."
      },
      "description": "Reusable non empty strings array definition for Agentic Workspace Command Package IR."
    }
  },
  "x-agentic-workspace-doc-role": "contract-reference"
}
