# Resume through current owners

After return, use the packet's exact re-entry in current `start`. The owner checks
identity, source revisions, baseline, scope and transport; a worker's successful
exit or persuasive summary is not admission. Supply the requested semantic return
judgment against the assigned outcome and the actual result.

```agentic-owner-reference
{"kind":"request","owner":"assignment","id":"assignment/judge-return/v1"}
```

Use only the exact admitted integration and Planning continuation actions. A
Planning update records the progress it owns; Verification and independent review
still own their evidence and claim boundaries. Then resolve the current procedure
again, reusing only still-current judgments. Lost disposable answers are unknown;
recover useful facts from their owner, not from an imagined conversation.

On interruption or worker/provider replacement, first inspect current custody and
effect disposition. Preserve committed integration even if continuation failed.
An unknown dispatch outcome does not justify relaunch; a replacement must use the
current owner's supersession/reassignment/recovery route. Retain unique returned
work until its owner consumes it. Missing runtime or recovery keeps that dependent
action unresolved and never creates a substitute skill-local session authority.
