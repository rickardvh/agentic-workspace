# Testing And Packaging

Use `uv run pytest` for Python tests in this repository. Plain `pytest` may run outside the managed environment and miss installed package behavior.

Capture repeated test-command confusion here instead of rediscovering it in chat.

