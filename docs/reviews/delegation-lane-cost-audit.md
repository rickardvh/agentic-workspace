# Delegation lane cost audit

2026-09-07 residual sweep: reassessment began from the remote integrated foundation `d8e3cb12`, after discovering that the local checkout was still on an older assignment branch. Fetch and a new stack branch preserved existing untracked receipts. No old worker was reclassified. Read-only capability discovery created no worker conversations. Initial overly broad file reads caused truncation and rereads; one guessed startup selector and two guessed paths failed. These are avoidable implementation-agent orientation costs, not provider failures. The ordinary policy-document link-check exposed path-keyword task classification; a second documentation review reached a manual best-fit handoff but projected unrelated active Planning context. No inference was spent on that mismatched subject.

The evidence layer's focused tests passed, then the routed complete orchestration/config suite passed 235 tests in 19.82 seconds. Unknown metric dilution and invalidated-predecessor burden were product computation defects; neither correction establishes monetary savings or a successful supported-host lifecycle.



This records observed avoidable engineering and execution costs during the delegation reconstruction stack. Measurements, inferred design opportunities and agent mistakes are distinguished. Account-wide credit depletion does not establish this task’s monetary cost. Exact credits and dollars remain unknown.



Early small-probe counters were last-response snapshots, not proven whole-worker totals. The second ordinary inspection used cumulative fresh-turn counters. These scopes are not a valid savings comparison. No provider transcripts, hidden reasoning or provider conversation identifiers are retained in this report.



The record preserves failures and subsequent corrections. Later entries supersede earlier statements about what remained to fix; passing executable proof does not constitute independent acceptance.



- Broad proof selection repeatedly ran a 461-test CLI suite for small delegation changes. Direct pass 926.50s; aggregate default 900s timed out; extended aggregate passed it in 658.789s. Passing proof not cheaply reusable across immaterial commit/worktree/subject changes led to reexecution. Improve material proof dependencies, resumable validated receipts, realistic per-command timeout and focused-owner coverage without weakening proof.

- Extended aggregate passed nine commands then a relative performance check failed while concurrent tests ran. A repeat failed a different case. Accepted base passed all six in 67.92s; stack isolated passed all six in 72.10s. Concurrency amplified timing noise and forced diagnostics; serialize timing-sensitive tests or declare exclusive resource requirements. Do not relabel failed aggregate as passed.

- Native selected aggregate passed four commands then hit inherited structured-inventory gaps, reproducible on accepted base: two unclassified files and 430929-byte generated resource against 430000 budget. Run cheap static/preflight blockers before expensive suites; baseline relevant validation early.

- Tiny Codex probes cost 13.8k–18.6k effective input tokens per call despite short JSON output. Recorded four-mode round: input 13775/13804/13833/13833, cached 12544/13568/1792/13568, output 39/15/27/26, elapsed 5805/4802/5685/3998ms. Earlier probes and restarts incurred additional usage. No credit price or savings inferred. Prefer metadata-only protocol checks, one bounded opt-in live matrix per material contract change, cheap model sufficient for transport proof, and lifecycle proof reuse.

- Codex exec resume with a nonexistent reference unexpectedly started fresh and incurred 18568 input, 13056 cached, 7 output tokens. Validate exact continuation admission before launching a turn; no silent fresh fallback.

- Four persisted test conversations accumulated in the user's app list; user interrupted work and they were manually archived. Add explicit disposable/retained lifecycle capabilities and owned cleanup; user attention is nonzero but unpriced.

- Archived conversations cannot directly resume on this installed provider; a metadata-only check returned an explicit unarchive requirement. Cleanup/continuation compatibility must be discovered, not assumed; avoid inference probes for a protocol/currentness question.

- Every native dispatch starts a control-plane process, and discovery launches version/schema/catalog processes. Version/implementation fingerprint changes or fifteen-minute TTL can repeat discovery even if only non-capability adapter logic changed. Measure and narrow invalidation to material capability dependencies; cheap fingerprint alone is not remote freshness proof. Retained live attachment remains unavailable, not faked.

- Root orchestrator spent many read/patch/test iterations and large tool outputs navigating huge runtime files, duplicated generated adapters and scattered owner surfaces. Tool output truncation caused rereads; some edits required correction. Compact owner APIs/currentness views and focused generated conformance would reduce orientation/review burden. Exact root token and credit attribution is unavailable; this is an observed workflow design and execution limitation, not a fabricated dollar estimate.

- Multiple worktrees require separate environment/build/cache setup and duplicate native compilation. This protects proof subjects and reviewable stacking but has overhead. Reuse immutable dependencies/build artifacts only with proven source/toolchain binding; do not share mutable proof subjects or sacrifice isolation.



Remaining design work includes selective proof reuse, narrower capability invalidation, retained live control and independently admitted successful-completion economics. Agent execution mistakes include speculative path/selector/fixture guesses and premature broad validation.

- The choice layer reused its existing checkout after predecessor proof, avoiding another venv/build setup. Hard-ineligible native routes now skip remote discovery. A dry-run was found to materialize an assignment before its guard; fixed with no-write proof. Tests required correction for guessed fixture kind, missing live Planning fixture and CLI-helper boolean/repeated-path encoding. These were agent/test-design mistakes, not product capability gaps. An external suite was started before inspecting the prior focused test failure; retain its unaffected results and rerun only the corrected focused case.

- Adding two assignment inputs invalidated published executed conformance for unrelated config.report and delegation-outcome.append because receipts bind the global external profile. The current publisher only permits a complete unfiltered all-target run. This caused one external-suite readiness failure and required a full publication refresh. A future per-operation material-dependency receipt contract could avoid unrelated reruns without accepting stale client/runner semantics. The proof selection projection itself took over 100 seconds for this changed set.

- Editable-source decision requests call cargo build --locked --message-format=json before each Rust request. The observed no-op build messages and repeated child invocations are avoidable validation/startup overhead; exact aggregate time is unmeasured. A source/toolchain-bound reusable core binary or in-process validation cache would help. Configuration choice also exposes the repeated active-Planning projection needed to bind source work; a compact already-validated work identity could avoid duplicate owner reads.

- Re-reading current #2210 criteria during the choice aggregate found that the offer bound feasibility and work but omitted contextual outcome evidence. The obsolete selected proof was cancelled before further costly commands. This was a design-review sequencing mistake: verify the current acceptance checklist before freezing/running the full subject. Preserve the cancelled receipt; it is not passing evidence.

- The router advertised projection cancellation separately from selected-proof execution, which requires an explicit --proof-cancel-file argument. The original run lacked that argument, so the projection sentinel did not stop its tests. After verifying the owned proof process, its process tree was stopped; the run is interrupted/incomplete, not a canonical cancelled or passed aggregate. Use the selected-proof cancellation argument on future runs and unify the cancellation contract in future design.

- Outcome-layer inspection exposed label-only assignment context and proof-result-to-target calibration without causal attribution; these can teach routing the wrong lesson and cause avoidable downstream retries. Not yet fixed end to end. Comparable configuration grouping and explicit unknown transport burden are now covered by focused tests. During this continuation, stale summary path shorthand caused two failed output redirections; several over-budget tool reads were truncated and a guessed test result key caused one focused failure. These are avoidable agent execution costs, not external blockers; exact credit impact is unknown.

- The prior turn ended with only a cost-audit acknowledgement despite unfinished authorized implementation. The maintainer had to ask for resumption. This was avoidable agent control-flow/attention cost, not a capability or authority blocker. Successful closeout now nominates canonical evidence; standalone proof publication no longer fabricates target quality/handoff/review burden. Isolated legacy TypeScript outcome nomination and failure-heavy causal admission remain unresolved.

- Frozen outcome run 6d8ddfd2ef497ea6cf6b failed its first aggregate command after 208.78 seconds: 491 tests passed and one legacy sidecar equality failed because a new optional configuration-context field was emitted as null. Corrected by omitting the field for legacy packets, preserving the generic transport floor. Whole-command/subject proof granularity requires rerunning unaffected cases; this is both a missed compatibility precheck and a proof-reuse design cost. A Rust-to-sidecar/schema digest-prefix mismatch was also caught late, during the pre-freeze conformance run; the focused end-to-end test now covers it. No provider inference was involved.

- The corrected selected outcome proof runs a combined external-client/orchestration/shared-core/configuration suite first, then later repeats external-client/shared-core tests with native transport, configuration tests with instruction/scoped-instruction tests, and orchestration again. These overlapping plain pytest sets are observable duplicate proof work, not provider inference. A coverage-aware command planner could share proven test coverage across owners while retaining source/currentness and selector distinctions; no proof was skipped here.

- Corrected frozen outcome run bffaf37aee8c70957f0e passed 22 commands, then the generated conformance check failed in 3.18 seconds on stale external contract bundles and delegation outcome input mirrors. The command-package generator does not regenerate the separate external-consumer profile/bundles. This was another missed cheap generation check before expensive proof; run all owning generators plus their --check surfaces before freezing. The earlier standalone 302-case conformance run did not detect stale mirrors and reused unchanged publication, illustrating split generation/readiness contracts.

- The public unknown-review case then exposed a separate literal CLI enum in command_package_ir.json, despite the source config constant and external schema being updated. The maintained refresh_command_surfaces.py pipeline now refreshes command packages, external bundles and command adapters together; the public ingress test covers the cross-surface value. Current packaged readiness tests pass without another 302-case publication run, so that repetition was avoided.

- Resumed CI exposed a layer-4 test missed by the selected 30-command proof: review-stack transitions still expected fabricated target feedback. CI spent 235.55s (463 passed, one skipped, one failed); corrected the expectation and no-receipt negative. My first corrections assumed compact projection fields and a list instead of tuple, and did not inspect later closeout assertions, causing several avoidable 5-10s focused reruns. One commit hook ran before its preceding test failure was inspected because I chained dependent commands without guarding exit status; corrected before publication. Future commands must inspect each dependent result.

- I invoked `ty check` without the repository's `src` scope, producing 1860 irrelevant generated/test diagnostics and a huge truncated output. The maintained command is `ty check src`; it found only two actionable new receipt-typing errors. This was avoidable agent execution cost, not an inherited product failure count.

- Existing-assignment dry-run could launch transports, and isolated Node export also dispatched. Deterministic negative cases now prevent both; no measured real provider launch is attributed to these gaps in this sweep.

- Native worker launch lacked exact bounded-entry environment, and new dispatch state was written only after completion. A worker therefore could not resolve its own canonical assignment at startup. Failed dispatch observations were discarded even after potential provider spend. Layer 5 adds prelaunch exact assignment state, adapter-owned early custody, confirmed process-release state and retained failed observations. Token savings from these changes are not yet measured.

- Layer 4 omitted the required release changeset despite carrying semver:minor. The label-named check actually validates both. Two CI runs repeated unaffected runtime/package/workspace checks before the missing metadata was diagnosed and corrected. Layer 5 includes its changeset before publication. This was a missed repository preflight requirement, not a product regression.

- The focused layer-5 suite completed with 179 passes/one opt-in skip in 202.98s, but the tool session continued reporting running after no owned test processes remained and the redirected file was still empty. A stdin follow-up released the completed exit-0 output. This is observed execution-tool/output lifecycle friction; no extra provider inference or fabricated test pass is inferred.

- Real human-policy use exposed a registered selector returning no delegation decision for retained-local work. The previous regression selected configurations through a private helper, so it missed the ordinary acquisition gap. The public selector now provides current offers; the test obtains them publicly before Python/TypeScript export.

- First public projection copied broad legacy decision detail, exceeded the 64 KiB serialization budget and omitted later configurations (including the intended disposable Sol route). The full verbose fallback measured 2,046,293 bytes; corrected complete selected output measured 21,584 bytes. These are serialized AW output bytes, not provider-token savings. A >8-candidate regression now checks completeness. My failed selector guesses before consulting inventory added avoidable calls.

- New-assignment dry-run preserves no-write behavior but still reports missing assignment/target inputs instead of a useful preview when materialization is intentionally suppressed. Actual public export constructs the assignment successfully. This misleading preview remains an observed design gap; do not treat its error as an unavailable provider.

- The single substantive native inspection used unchanged human policy and a disposable Sol route selected through the public API. After 719347 ms without a structured return, the orchestrator verified the exact owned process ancestry and stopped only that provider process to bound further cost. Preserved observation: packet 11327 bytes, rendered prompt 4274 bytes (exported CRLF file 4383 bytes), effective input 56512, cached input 54784, output 1913. Orientation/retry/repair counters and exact monetary/account-credit attribution are unknown. This is an interrupted/censored observation, not target-failure evidence or successful ordinary delegation. No provider transcript was read or stored. Exact public cleanup returned already-absent after process release, preserving AW semantic state.

- The selected native configuration did not expose one-off reasoning-effort/timeout adjustment; it used provider-default effort of unknown value. The adapter provides no live steering and no useful in-flight burden/phase report, so the parent could not cheaply distinguish useful progress, provider delay or context overhead. Do not infer which caused the long run. These are concrete remaining execution-configuration/observability design gaps; no additional inference probe was launched to guess.

- Failure custody initially recorded ephemeral=false until successful completion even though the provider guarantee had been validated before starting its turn. Current code captures the reported guarantee immediately, preserves unknown before observation, and deterministic failed-ephemeral cleanup needs no archive call. The earlier actual cleanup is still reported as already-absent, not relabeled not-stored.

- New-assignment dry-run now returns a no-write selection-preview with construction explicitly unperformed. The first focused validation caught missing required lifecycle state/run fields in that preview; fixed against the actual public schema before frozen proof. Both public-language cases pass.

- Frozen layer-5 proof passed ten commands then failed because taskless assurance projection inherited an unrelated probe assignment. Exact subject matching fixes the leak; the Planning resume case remains covered. The immutable failed run is 7767e3ec635b0dcdafcc. My first new assertion incorrectly expected no automatic configuration rather than the retained orchestrator configuration, adding one avoidable focused rerun.

- Matching installed-version provider source confirms native tokenUsage.last is the latest model response, whereas total accumulates responses. The interrupted observation cannot be used as whole-worker or billed cost. Earlier small one-response probes do not establish multi-step totals. Effective config/read later observed medium default effort, but this does not retroactively prove the interrupted turn effort or its cause. No additional inference was used for this diagnosis.

- The next proof failed after 13 commands because a pre-existing default-policy test read the actual checkout and assumed no config.local.toml. It now uses an isolated checked-in-config fixture; human policy was not removed. Frozen revalidation repeats earlier passing commands because aggregate subject binding does not reuse them across this test-only change. Exact owned-thread account/usage/read returned null (zero new provider turns), so thread credit estimates remain unavailable.

- Layer 6 public native parameter fixtures initially used the normalized transport method field in human config, which correctly rejected it; corrected the fixture. A second failure exposed that the validated parameter variant had not been added to the target eligibility projection. Source-host errors hid this behind an unavailable-host message; the bridge now surfaces a bounded exact reason. These avoidable implementation/debugging reruns are separate from provider capability failures.

- A single source evaluation repeatedly invoked the same CLI version check for profiles and native variants. An operation-scoped snapshot now reuses it only within that evaluation, rechecking at the next operation and honoring explicit refresh. Synthetic audit verifies one version query inside a repeated lookup, another on the next operation, and rediscovery after a version change. This is call-count proof, not measured credit savings.

- The new public input invalidated six unrelated external-operation receipt fingerprints; the required all-target refresh reran 302 conformance cases (all passed, nine unchanged unavailable). This was an unfrozen preflight, not the final implementation proof. The later 4096-character direct-entry guard and frozen proof remain separately verified.

- A second, distinct ordinary check used explicit low effort and an enforced 90-second native turn budget, with ephemeral history and no concurrent local test workload. It timed out after 91347 ms without structured return: cumulative fresh input 103262, cached input 73088, output 1522; packet 11721 bytes and rendered prompt 4244 bytes. These totals cannot be compared as savings against the earlier 56512 last-response input counter. Exact credits, orientation and repair burden remain unknown. Cleanup reported not-stored in 1 ms, without provider calls or deletion. No further inference was started to guess the cause.

- The second timeout was labeled provider-response-timeout even after an active turn had started. The adapter now distinguishes its turn deadline. Source inspection also found that forced shutdown previously killed only the launcher, risking unconfirmed descendant lifetime; no orphan or extra spend was observed in the live checks. An owned Windows launcher/child fixture proves the corrected tree shutdown. This is not live-provider interrupt proof.



- Layer 7 source inspection found assignment currentness coupled to a raw private plan file and manually maintained revision, while our canonical plan still contained scaffold text and an obsolete no-independent-candidate explanation. These could cause stale choices or repeated orientation; neither is proven to have caused the two incomplete workers. The layer consumes the selected owner projection, binds content and relevant semantics, excludes unrelated integration receipt churn, and tightens our plan. Bundled-owner projection consumption is not proof of arbitrary replacement Planning providers.

- In the same pass I repeated invalid PowerShell glob/path guesses, requested an unsupported nested summary selector, and produced truncated reads despite prior knowledge of those hazards. I also started the full implementation CLI suite before the final proof freeze, so it is diagnostic and may require repeated validation. These are avoidable agent execution costs; credit attribution is unknown.



- The layer-7 diagnostic implementation suite found 19 failures / 227 passes in 277.03 seconds. Compact task-disposition assertions, selector invocation, legacy transport setup and manual export arguments had not been reconciled in that test module; the new owner projection also exposed an incomplete plan fixture. Several incremental fixture guesses caused additional focused reruns before using owner diagnostics to identify the missing proof claim. A badly quoted search expanded to roughly 250k tokens of tool output (truncated by the tool), another avoidable execution mistake. Final proof separately repeats the corrected broad module and overlaps public-client groups; no monetary or credit total is available.



- Layer 8 adds hard provider-history eligibility without changing human local policy. A policy-change test exposed a stale retained choice crashing a read; current offers now remain inspectable with an explicit execution block and preserved assignment custody. The first new test imported a generated operation from the wrong module, and a later fixture omitted capability expiry; both were avoidable test-authoring errors. Fourteen focused guarantee/parameter/public-route cases passed in 47.34 seconds after correction.

- The layer-8 conformance check accepted the existing executed profile, so no all-target 302-case refresh was needed. An owned archived-provider metadata read confirmed ephemeral=false with zero returned turns, zero new conversations and zero new inference. Persistence guarantees now require explicit request and response confirmation before starting a turn. None of these observations establishes a never-visible guarantee or measured credit savings.


- While preparing the final cost report on Windows, an implicit text encoding wrote a smart apostrophe as a non-UTF-8 byte and made the AW implementation route fail before action. The tracked report was repaired without changing valid UTF-8 content; subsequent writes use explicit UTF-8. This was an avoidable agent file-authoring error.

Layer 8 frozen proof 026f92e25e7bb4388df7 stopped at the shared adapter group after 330 passed, one skipped and one failed in 221.22 seconds. The failure was an older exact-shape assertion missing the new default execution_guarantees field; the focused correction passed in 0.68 seconds. Running the full affected shared-core fixture before freezing would have avoided this late proof restart. No provider inference was involved.

Layer 8 revalidation passed all 29 selected commands in receipt ea63b9437f7bb5d1c782. The affected-test selection overlaps the delegation-domain test selection, so shared-core, external-client and guarantee cases run twice. That proof-selection duplication is observed execution burden, not a measured credit saving. The frozen selection was preserved instead of changing proof authority mid-run.

A deterministic, zero-provider-call reproduction then confirmed that a successful resumed dispatch published new lineage metadata which removed its own selected resume offer before admission. This could force needless reconciliation after successful work. Layer 9 preserves the consumed lineage view only for admission of the exact completed packet; future selections still use current lineage. Fresh, resume, fork and restart fixtures include mismatched custody, live process, altered publication and capability negatives. This is synthetic contract evidence; it neither explains nor rehabilitates the two incomplete real ordinary worker attempts.

The local reproduction initially omitted required shared-core fixture fields and needed one correction. This turn also spent speculative analysis on locks before reading the existing OS lock, which already releases on process death. Both were avoidable orientation work; no credit attribution is available. Further provider inference was not used to guess the cause of the earlier worker failures.

Layer 9's first freeze missed the ordinary retained-read entry path into the same consumed-offer check. Run 41cf3122cdee382c2b9b was cancelled through its explicit cancellation file after six passed commands, before the expensive combined group. The revised synthetic public-client fixture now selects a persisted resume route, publishes a completed return locally, and checks ordinary retained re-entry before testing source invalidation. Catching this entry path before freezing would have avoided even that partial restart. No cancelled run is counted as completed proof.

Layer 9's final run 790a3fbcda68ea3750e3 passed all 27 selected commands, but the aggregate remained blocked by unresolved maintained acceptance evidence. The prescribed generated-adapter Docker conformance also passed on Docker 29.5.2. Executable evidence does not supply organic dogfood or independent acceptance. PR #3060 omitted its label at creation; #3061 was created with its label, yet its initial label-policy job still saw no label while a later label job passed. Re-running that small failed job once retained the failure. This is observed event/currentness overhead; it is not a code regression or a reason to rerun the whole suite.

Layer 10 found that repair recorded a request without a constructible next attempt, and that reassignment metadata still described local-only writes despite canonical assignment/structural-receipt updates. It now uses the existing repair and reassignment owners, with explicit current offers and preserved semantic work. The implementation initially duplicated an existing replacement-result projection, overlooked required target/reason options in the separate command IR, omitted a required preview state field, and found a second export admission branch tied only to human configuration. Two-case groups took 23.28, 23.24, 28.30 and 29.85 seconds before direct inspection of the retained failed fixture located the remaining export check. Revised cases passed in 39.20 seconds; expanded safety/authority negatives passed in 47.69 seconds; 13 current/source compatibility cases passed in 125.75 seconds. These are local test times, not provider-credit measurements.

Avoidable editing/orientation work in that layer included a wrong generator path, an indentation typo, a duplicate import, a malformed Windows search glob and stale patch contexts after formatting. A broad JSON key-order rewrite was removed after proving that only reassignment options changed semantically, reducing the source IR diff to 23 additions and five deletions. No further provider inference was used to diagnose incomplete workers. The next PR should attach its label while draft and become ready afterward to avoid initial publication races where supported by the repository workflow.

The completed repair/re-entry fixture passed both clients in 62.18 seconds, including explicit fresh and resume choices across successive attempts and a proved pre-launch refusal. Native/orchestration checks passed 65 cases with one opt-in host skip in 16.04 seconds. The generated-conformance precheck then required a whole-profile operation-receipt refresh after the small reassignment interface change; that broad invalidation remains a design cost. A bounded worker startup short circuit and regression test already existed. Speculating about a broad-startup loop before reading those surfaces added avoidable orientation work and did not establish the cause of either incomplete real worker.

- During the final repair layer, an unnecessarily broad process-command-line query and a full nested conformance receipt print produced truncated output and extra context recovery. Bounded identity/status projections would have answered both questions more cheaply. No provider inference was involved.

- Final repair-layer selected proof passed all 21 commands (336 delegation cases in 245.73s), but the aggregate remained blocked because reassign/repair operation files lacked focused-route declarations, in addition to pending manual acceptance. This metadata gap was visible in the pre-execution route and should have been fixed before freezing. Added both paths to the existing delegation proof lane; preserve the original receipt rather than calling it fully passed.
