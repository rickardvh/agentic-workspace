# Delegation lane cost audit

2026-09-07 residual sweep: reassessment began from the remote integrated foundation `d8e3cb12`, after discovering that the local checkout was still on an older assignment branch. Fetch and a new stack branch preserved existing untracked receipts. No old worker was reclassified. Read-only capability discovery created no worker conversations. Initial overly broad file reads caused truncation and rereads; one guessed startup selector and two guessed paths failed. These are avoidable implementation-agent orientation costs, not provider failures. The ordinary policy-document link-check exposed path-keyword task classification; a second documentation review reached a manual best-fit handoff but projected unrelated active Planning context. No inference was spent on that mismatched subject.

The evidence layer's focused tests passed, then the routed complete orchestration/config suite passed 235 tests in 19.82 seconds. Unknown metric dilution and invalidated-predecessor burden were product computation defects; neither correction establishes monetary savings or a successful supported-host lifecycle.



This records observed avoidable engineering and execution costs during the delegation reconstruction stack. Measurements, inferred design opportunities and agent mistakes are distinguished. Account-wide credit depletion does not establish this task’s monetary cost. Exact credits and dollars remain unknown.



Early small-probe counters were last-response snapshots, not proven whole-worker totals. The second ordinary inspection used cumulative fresh-turn counters. These scopes are not a valid savings comparison. No provider transcripts, hidden reasoning or provider conversation identifiers are retained in this report.



The record preserves failures and subsequent corrections. Later entries supersede earlier statements about what remained to fix; passing executable proof does not constitute independent acceptance.



- Broad proof selection repeatedly ran a 461-test CLI suite for small delegation changes. Direct pass 926.50s; aggregate default 900s timed out; extended aggregate passed it in 658.789s. Passing proof not cheaply reusable across immaterial commit/worktree/subject changes led to reexecution. Improve material proof dependencies, resumable validated receipts, realistic per-command timeout and focused-owner coverage without weakening proof.

- Extended aggregate passed nine commands then a relative performance check failed while concurrent tests ran. A repeat failed a different case. Accepted base passed all six in 67.92s; stack isolated passed all six in 72.10s. Concurrency amplified timing noise and forced diagnostics; serialize timing-sensitive tests or declare exclusive resource requirements. Do not relabel failed aggregate as passed.

- Native selected aggregate passed four commands then hit inherited structured-inventory gaps, reproducible on accepted base: two unclassified files and 430929-byte generated resource against 430000 budget. Run cheap static/preflight blockers before expensive suites; baseline relevant validation early.

- Tiny Codex probes cost 13.8k–18.6k effective input tokens per call despite short JSON output. Recorded four-mode round: input 13775/13804/13833/13833, cached 12544/13568/1792/13568, output 39/15/27/26, elapsed 5805/4802/5685/3998ms. Earlier probes and restarts incurred additional usage. No credit price or savings inferred. Prefer metadata-only protocol checks, one bounded opt-in live matrix per material contract change, cheap model sufficient for transport proof, and lifecycle proof reuse.

- Codex exec resume with a nonexistent reference unexpectedly started fresh and incurred 18568 input, 13056 cached, 7 output tokens. Validate exact continuation admission before launching a turn; no silent fresh fallback.

- Four persisted test conversations accumulated in the user's app list; user interrupted work and they were manually archived. Add explicit disposable/retained lifecycle capabilities and owned cleanup; user attention is nonzero but unpriced.

- Archived conversations cannot directly resume on this installed provider; a metadata-only check returned an explicit unarchive requirement. Cleanup/continuation compatibility must be discovered, not assumed; avoid inference probes for a protocol/currentness question.

- Every native dispatch starts a control-plane process, and discovery launches version/schema/catalog processes. Version/implementation fingerprint changes or fifteen-minute TTL can repeat discovery even if only non-capability adapter logic changed. Measure and narrow invalidation to material capability dependencies; cheap fingerprint alone is not remote freshness proof. Retained live attachment remains unavailable, not faked.

- Root orchestrator spent many read/patch/test iterations and large tool outputs navigating huge runtime files, duplicated generated adapters and scattered owner surfaces. Tool output truncation caused rereads; some edits required correction. Compact owner APIs/currentness views and focused generated conformance would reduce orientation/review burden. Exact root token and credit attribution is unavailable; this is an observed workflow design and execution limitation, not a fabricated dollar estimate.

- Multiple worktrees require separate environment/build/cache setup and duplicate native compilation. This protects proof subjects and reviewable stacking but has overhead. Reuse immutable dependencies/build artifacts only with proven source/toolchain binding; do not share mutable proof subjects or sacrifice isolation.



Remaining design work includes selective proof reuse, narrower capability invalidation, retained live control and independently admitted successful-completion economics. Agent execution mistakes include speculative path/selector/fixture guesses and premature broad validation.

- The choice layer reused its existing checkout after predecessor proof, avoiding another venv/build setup. Hard-ineligible native routes now skip remote discovery. A dry-run was found to materialize an assignment before its guard; fixed with no-write proof. Tests required correction for guessed fixture kind, missing live Planning fixture and CLI-helper boolean/repeated-path encoding. These were agent/test-design mistakes, not product capability gaps. An external suite was started before inspecting the prior focused test failure; retain its unaffected results and rerun only the corrected focused case.

- Adding two assignment inputs invalidated published executed conformance for unrelated config.report and delegation-outcome.append because receipts bind the global external profile. The current publisher only permits a complete unfiltered all-target run. This caused one external-suite readiness failure and required a full publication refresh. A future per-operation material-dependency receipt contract could avoid unrelated reruns without accepting stale client/runner semantics. The proof selection projection itself took over 100 seconds for this changed set.

- Editable-source decision requests call cargo build --locked --message-format=json before each Rust request. The observed no-op build messages and repeated child invocations are avoidable validation/startup overhead; exact aggregate time is unmeasured. A source/toolchain-bound reusable core binary or in-process validation cache would help. Configuration choice also exposes the repeated active-Planning projection needed to bind source work; a compact already-validated work identity could avoid duplicate owner reads.

- Re-reading current #2210 criteria during the choice aggregate found that the offer bound feasibility and work but omitted contextual outcome evidence. The obsolete selected proof was cancelled before further costly commands. This was a design-review sequencing mistake: verify the current acceptance checklist before freezing/running the full subject. Preserve the cancelled receipt; it is not passing evidence.

- The router advertised projection cancellation separately from selected-proof execution, which requires an explicit --proof-cancel-file argument. The original run lacked that argument, so the projection sentinel did not stop its tests. After verifying the owned proof process, its process tree was stopped; the run is interrupted/incomplete, not a canonical cancelled or passed aggregate. Use the selected-proof cancellation argument on future runs and unify the cancellation contract in future design.

- Outcome-layer inspection exposed label-only assignment context and proof-result-to-target calibration without causal attribution; these can teach routing the wrong lesson and cause avoidable downstream retries. Not yet fixed end to end. Comparable configuration grouping and explicit unknown transport burden are now covered by focused tests. During this continuation, stale summary path shorthand caused two failed output redirections; several over-budget tool reads were truncated and a guessed test result key caused one focused failure. These are avoidable agent execution costs, not external blockers; exact credit impact is unknown.

- The prior turn ended with only a cost-audit acknowledgement despite unfinished authorized implementation. The maintainer had to ask for resumption. This was avoidable agent control-flow/attention cost, not a capability or authority blocker. Successful closeout now nominates canonical evidence; standalone proof publication no longer fabricates target quality/handoff/review burden. Isolated legacy TypeScript outcome nomination and failure-heavy causal admission remain unresolved.

- Frozen outcome run 6d8ddfd2ef497ea6cf6b failed its first aggregate command after 208.78 seconds: 491 tests passed and one legacy sidecar equality failed because a new optional configuration-context field was emitted as null. Corrected by omitting the field for legacy packets, preserving the generic transport floor. Whole-command/subject proof granularity requires rerunning unaffected cases; this is both a missed compatibility precheck and a proof-reuse design cost. A Rust-to-sidecar/schema digest-prefix mismatch was also caught late, during the pre-freeze conformance run; the focused end-to-end test now covers it. No provider inference was involved.

- The corrected selected outcome proof runs a combined external-client/orchestration/shared-core/configuration suite first, then later repeats external-client/shared-core tests with native transport, configuration tests with instruction/scoped-instruction tests, and orchestration again. These overlapping plain pytest sets are observable duplicate proof work, not provider inference. A coverage-aware command planner could share proven test coverage across owners while retaining source/currentness and selector distinctions; no proof was skipped here.

- Corrected frozen outcome run bffaf37aee8c70957f0e passed 22 commands, then the generated conformance check failed in 3.18 seconds on stale external contract bundles and delegation outcome input mirrors. The command-package generator does not regenerate the separate external-consumer profile/bundles. This was another missed cheap generation check before expensive proof; run all owning generators plus their --check surfaces before freezing. The earlier standalone 302-case conformance run did not detect stale mirrors and reused unchanged publication, illustrating split generation/readiness contracts.

- The public unknown-review case then exposed a separate literal CLI enum in command_package_ir.json, despite the source config constant and external schema being updated. The maintained refresh_command_surfaces.py pipeline now refreshes command packages, external bundles and command adapters together; the public ingress test covers the cross-surface value. Current packaged readiness tests pass without another 302-case publication run, so that repetition was avoided.

- Resumed CI exposed a layer-4 test missed by the selected 30-command proof: review-stack transitions still expected fabricated target feedback. CI spent 235.55s (463 passed, one skipped, one failed); corrected the expectation and no-receipt negative. My first corrections assumed compact projection fields and a list instead of tuple, and did not inspect later closeout assertions, causing several avoidable 5-10s focused reruns. One commit hook ran before its preceding test failure was inspected because I chained dependent commands without guarding exit status; corrected before publication. Future commands must inspect each dependent result.

- I invoked `ty check` without the repository's `src` scope, producing 1860 irrelevant generated/test diagnostics and a huge truncated output. The maintained command is `ty check src`; it found only two actionable new receipt-typing errors. This was avoidable agent execution cost, not an inherited product failure count.

- Existing-assignment dry-run could launch transports, and isolated Node export also dispatched. Deterministic negative cases now prevent both; no measured real provider launch is attributed to these gaps in this sweep.

- Native worker launch lacked exact bounded-entry environment, and new dispatch state was written only after completion. A worker therefore could not resolve its own canonical assignment at startup. Failed dispatch observations were discarded even after potential provider spend. Layer 5 adds prelaunch exact assignment state, adapter-owned early custody, confirmed process-release state and retained failed observations. Token savings from these changes are not yet measured.

- Layer 4 omitted the required release changeset despite carrying semver:minor. The label-named check actually validates both. Two CI runs repeated unaffected runtime/package/workspace checks before the missing metadata was diagnosed and corrected. Layer 5 includes its changeset before publication. This was a missed repository preflight requirement, not a product regression.

- The focused layer-5 suite completed with 179 passes/one opt-in skip in 202.98s, but the tool session continued reporting running after no owned test processes remained and the redirected file was still empty. A stdin follow-up released the completed exit-0 output. This is observed execution-tool/output lifecycle friction; no extra provider inference or fabricated test pass is inferred.

- Real human-policy use exposed a registered selector returning no delegation decision for retained-local work. The previous regression selected configurations through a private helper, so it missed the ordinary acquisition gap. The public selector now provides current offers; the test obtains them publicly before Python/TypeScript export.

- First public projection copied broad legacy decision detail, exceeded the 64 KiB serialization budget and omitted later configurations (including the intended disposable Sol route). The full verbose fallback measured 2,046,293 bytes; corrected complete selected output measured 21,584 bytes. These are serialized AW output bytes, not provider-token savings. A >8-candidate regression now checks completeness. My failed selector guesses before consulting inventory added avoidable calls.

- New-assignment dry-run preserves no-write behavior but still reports missing assignment/target inputs instead of a useful preview when materialization is intentionally suppressed. Actual public export constructs the assignment successfully. This misleading preview remains an observed design gap; do not treat its error as an unavailable provider.

- The single substantive native inspection used unchanged human policy and a disposable Sol route selected through the public API. After 719347 ms without a structured return, the orchestrator verified the exact owned process ancestry and stopped only that provider process to bound further cost. Preserved observation: packet 11327 bytes, rendered prompt 4274 bytes (exported CRLF file 4383 bytes), effective input 56512, cached input 54784, output 1913. Orientation/retry/repair counters and exact monetary/account-credit attribution are unknown. This is an interrupted/censored observation, not target-failure evidence or successful ordinary delegation. No provider transcript was read or stored. Exact public cleanup returned already-absent after process release, preserving AW semantic state.

- The selected native configuration did not expose one-off reasoning-effort/timeout adjustment; it used provider-default effort of unknown value. The adapter provides no live steering and no useful in-flight burden/phase report, so the parent could not cheaply distinguish useful progress, provider delay or context overhead. Do not infer which caused the long run. These are concrete remaining execution-configuration/observability design gaps; no additional inference probe was launched to guess.

- Failure custody initially recorded ephemeral=false until successful completion even though the provider guarantee had been validated before starting its turn. Current code captures the reported guarantee immediately, preserves unknown before observation, and deterministic failed-ephemeral cleanup needs no archive call. The earlier actual cleanup is still reported as already-absent, not relabeled not-stored.

- New-assignment dry-run now returns a no-write selection-preview with construction explicitly unperformed. The first focused validation caught missing required lifecycle state/run fields in that preview; fixed against the actual public schema before frozen proof. Both public-language cases pass.

- Frozen layer-5 proof passed ten commands then failed because taskless assurance projection inherited an unrelated probe assignment. Exact subject matching fixes the leak; the Planning resume case remains covered. The immutable failed run is 7767e3ec635b0dcdafcc. My first new assertion incorrectly expected no automatic configuration rather than the retained orchestrator configuration, adding one avoidable focused rerun.

- Matching installed-version provider source confirms native tokenUsage.last is the latest model response, whereas total accumulates responses. The interrupted observation cannot be used as whole-worker or billed cost. Earlier small one-response probes do not establish multi-step totals. Effective config/read later observed medium default effort, but this does not retroactively prove the interrupted turn effort or its cause. No additional inference was used for this diagnosis.

- The next proof failed after 13 commands because a pre-existing default-policy test read the actual checkout and assumed no config.local.toml. It now uses an isolated checked-in-config fixture; human policy was not removed. Frozen revalidation repeats earlier passing commands because aggregate subject binding does not reuse them across this test-only change. Exact owned-thread account/usage/read returned null (zero new provider turns), so thread credit estimates remain unavailable.

- Layer 6 public native parameter fixtures initially used the normalized transport method field in human config, which correctly rejected it; corrected the fixture. A second failure exposed that the validated parameter variant had not been added to the target eligibility projection. Source-host errors hid this behind an unavailable-host message; the bridge now surfaces a bounded exact reason. These avoidable implementation/debugging reruns are separate from provider capability failures.

- A single source evaluation repeatedly invoked the same CLI version check for profiles and native variants. An operation-scoped snapshot now reuses it only within that evaluation, rechecking at the next operation and honoring explicit refresh. Synthetic audit verifies one version query inside a repeated lookup, another on the next operation, and rediscovery after a version change. This is call-count proof, not measured credit savings.

- The new public input invalidated six unrelated external-operation receipt fingerprints; the required all-target refresh reran 302 conformance cases (all passed, nine unchanged unavailable). This was an unfrozen preflight, not the final implementation proof. The later 4096-character direct-entry guard and frozen proof remain separately verified.

- A second, distinct ordinary check used explicit low effort and an enforced 90-second native turn budget, with ephemeral history and no concurrent local test workload. It timed out after 91347 ms without structured return: cumulative fresh input 103262, cached input 73088, output 1522; packet 11721 bytes and rendered prompt 4244 bytes. These totals cannot be compared as savings against the earlier 56512 last-response input counter. Exact credits, orientation and repair burden remain unknown. Cleanup reported not-stored in 1 ms, without provider calls or deletion. No further inference was started to guess the cause.

- The second timeout was labeled provider-response-timeout even after an active turn had started. The adapter now distinguishes its turn deadline. Source inspection also found that forced shutdown previously killed only the launcher, risking unconfirmed descendant lifetime; no orphan or extra spend was observed in the live checks. An owned Windows launcher/child fixture proves the corrected tree shutdown. This is not live-provider interrupt proof.



- Layer 7 source inspection found assignment currentness coupled to a raw private plan file and manually maintained revision, while our canonical plan still contained scaffold text and an obsolete no-independent-candidate explanation. These could cause stale choices or repeated orientation; neither is proven to have caused the two incomplete workers. The layer consumes the selected owner projection, binds content and relevant semantics, excludes unrelated integration receipt churn, and tightens our plan. Bundled-owner projection consumption is not proof of arbitrary replacement Planning providers.

- In the same pass I repeated invalid PowerShell glob/path guesses, requested an unsupported nested summary selector, and produced truncated reads despite prior knowledge of those hazards. I also started the full implementation CLI suite before the final proof freeze, so it is diagnostic and may require repeated validation. These are avoidable agent execution costs; credit attribution is unknown.



- The layer-7 diagnostic implementation suite found 19 failures / 227 passes in 277.03 seconds. Compact task-disposition assertions, selector invocation, legacy transport setup and manual export arguments had not been reconciled in that test module; the new owner projection also exposed an incomplete plan fixture. Several incremental fixture guesses caused additional focused reruns before using owner diagnostics to identify the missing proof claim. A badly quoted search expanded to roughly 250k tokens of tool output (truncated by the tool), another avoidable execution mistake. Final proof separately repeats the corrected broad module and overlaps public-client groups; no monetary or credit total is available.



- Layer 8 adds hard provider-history eligibility without changing human local policy. A policy-change test exposed a stale retained choice crashing a read; current offers now remain inspectable with an explicit execution block and preserved assignment custody. The first new test imported a generated operation from the wrong module, and a later fixture omitted capability expiry; both were avoidable test-authoring errors. Fourteen focused guarantee/parameter/public-route cases passed in 47.34 seconds after correction.

- The layer-8 conformance check accepted the existing executed profile, so no all-target 302-case refresh was needed. An owned archived-provider metadata read confirmed ephemeral=false with zero returned turns, zero new conversations and zero new inference. Persistence guarantees now require explicit request and response confirmation before starting a turn. None of these observations establishes a never-visible guarantee or measured credit savings.


- While preparing the final cost report on Windows, an implicit text encoding wrote a smart apostrophe as a non-UTF-8 byte and made the AW implementation route fail before action. The tracked report was repaired without changing valid UTF-8 content; subsequent writes use explicit UTF-8. This was an avoidable agent file-authoring error.

Layer 8 frozen proof 026f92e25e7bb4388df7 stopped at the shared adapter group after 330 passed, one skipped and one failed in 221.22 seconds. The failure was an older exact-shape assertion missing the new default execution_guarantees field; the focused correction passed in 0.68 seconds. Running the full affected shared-core fixture before freezing would have avoided this late proof restart. No provider inference was involved.

Layer 8 revalidation passed all 29 selected commands in receipt ea63b9437f7bb5d1c782. The affected-test selection overlaps the delegation-domain test selection, so shared-core, external-client and guarantee cases run twice. That proof-selection duplication is observed execution burden, not a measured credit saving. The frozen selection was preserved instead of changing proof authority mid-run.

A deterministic, zero-provider-call reproduction then confirmed that a successful resumed dispatch published new lineage metadata which removed its own selected resume offer before admission. This could force needless reconciliation after successful work. Layer 9 preserves the consumed lineage view only for admission of the exact completed packet; future selections still use current lineage. Fresh, resume, fork and restart fixtures include mismatched custody, live process, altered publication and capability negatives. This is synthetic contract evidence; it neither explains nor rehabilitates the two incomplete real ordinary worker attempts.

The local reproduction initially omitted required shared-core fixture fields and needed one correction. This turn also spent speculative analysis on locks before reading the existing OS lock, which already releases on process death. Both were avoidable orientation work; no credit attribution is available. Further provider inference was not used to guess the cause of the earlier worker failures.

Layer 9's first freeze missed the ordinary retained-read entry path into the same consumed-offer check. Run 41cf3122cdee382c2b9b was cancelled through its explicit cancellation file after six passed commands, before the expensive combined group. The revised synthetic public-client fixture now selects a persisted resume route, publishes a completed return locally, and checks ordinary retained re-entry before testing source invalidation. Catching this entry path before freezing would have avoided even that partial restart. No cancelled run is counted as completed proof.

Layer 9's final run 790a3fbcda68ea3750e3 passed all 27 selected commands, but the aggregate remained blocked by unresolved maintained acceptance evidence. The prescribed generated-adapter Docker conformance also passed on Docker 29.5.2. Executable evidence does not supply organic dogfood or independent acceptance. PR #3060 omitted its label at creation; #3061 was created with its label, yet its initial label-policy job still saw no label while a later label job passed. Re-running that small failed job once retained the failure. This is observed event/currentness overhead; it is not a code regression or a reason to rerun the whole suite.

Layer 10 found that repair recorded a request without a constructible next attempt, and that reassignment metadata still described local-only writes despite canonical assignment/structural-receipt updates. It now uses the existing repair and reassignment owners, with explicit current offers and preserved semantic work. The implementation initially duplicated an existing replacement-result projection, overlooked required target/reason options in the separate command IR, omitted a required preview state field, and found a second export admission branch tied only to human configuration. Two-case groups took 23.28, 23.24, 28.30 and 29.85 seconds before direct inspection of the retained failed fixture located the remaining export check. Revised cases passed in 39.20 seconds; expanded safety/authority negatives passed in 47.69 seconds; 13 current/source compatibility cases passed in 125.75 seconds. These are local test times, not provider-credit measurements.

Avoidable editing/orientation work in that layer included a wrong generator path, an indentation typo, a duplicate import, a malformed Windows search glob and stale patch contexts after formatting. A broad JSON key-order rewrite was removed after proving that only reassignment options changed semantically, reducing the source IR diff to 23 additions and five deletions. No further provider inference was used to diagnose incomplete workers. The next PR should attach its label while draft and become ready afterward to avoid initial publication races where supported by the repository workflow.

The completed repair/re-entry fixture passed both clients in 62.18 seconds, including explicit fresh and resume choices across successive attempts and a proved pre-launch refusal. Native/orchestration checks passed 65 cases with one opt-in host skip in 16.04 seconds. The generated-conformance precheck then required a whole-profile operation-receipt refresh after the small reassignment interface change; that broad invalidation remains a design cost. A bounded worker startup short circuit and regression test already existed. Speculating about a broad-startup loop before reading those surfaces added avoidable orientation work and did not establish the cause of either incomplete real worker.

- During the final repair layer, an unnecessarily broad process-command-line query and a full nested conformance receipt print produced truncated output and extra context recovery. Bounded identity/status projections would have answered both questions more cheaply. No provider inference was involved.

- Final repair-layer selected proof passed all 21 commands (336 delegation cases in 245.73s), but the aggregate remained blocked because reassign/repair operation files lacked focused-route declarations, in addition to pending manual acceptance. This metadata gap was visible in the pre-execution route and should have been fixed before freezing. Added both paths to the existing delegation proof lane; preserve the original receipt rather than calling it fully passed.

## Residual sweep on the integrated foundation (2026-09-07)

The sweep starts from foundation d8e3cb12a9e42272b090051114e636176d89aa30, preserving the previous ten merged delegation layers. It reassesses their actual owners rather than rebuilding obsolete JSON migration mechanisms. The previous two incomplete provider workers remain censored; no later observation establishes their success or a target-quality failure.

One new ordinary bounded document review selected the eligible Luna native non-persisted configuration through public assignment operations, without delegation keywords or a task-specific policy change. The request compared the Verification README/routing note against current command help. Discovery created no conversations. Dispatch used low effort and a 180-second turn bound. The adapter returned after 51761 ms with a structured required-input-missing result: the worker could read the documents but could not initialize the configured runtime or access the installed Verification executable under the admitted read-only sandbox. Its empty changed-path list was accompanied by a header-only placeholder diff. Return admission correctly rejected that malformed patch; no integration, Verification success, task completion, or target-quality evidence is claimed.

Observed costs for this attempt: semantic packet 12258 bytes; rendered prompt 4413 bytes; cumulative fresh input 93914 tokens; cached input 69632 tokens; output 1813 tokens; elapsed 51761 ms. Orientation, repair/retry, review, and monetary/credit costs remain unknown. Cleanup reported not-stored in 2 ms and provider_state_deleted=false. Cleanup did not remove AW custody or turn the attempt into task success. No provider conversation identifier or transcript is retained in this audit.

A zero-inference installed-host probe confirmed that Codex CLI 0.153.4's read-only sandbox could not start either the checkout Python interpreter or its resolved managed interpreter (access denied). The current app-server readOnly schema exposes no confirmed bounded readable-root capability. No safety widening was attempted. Supplying a parent-generated help snapshot was considered, but it would leave ordinary checkout-runtime access unresolved; no further paid worker was launched for that workaround. A matched public retained-local control selected Astra, materialized no assignment, left the assignment store byte-identical, and launched no worker.

The malformed no-change return exposed a product instruction ambiguity: both worker renderers demanded a diff even when nothing changed. The sweep clarifies empty-patch returns while preserving strict admission and the original failed observation. Existing assignment status/close also now expose at most 20 exactly linked same-work attempts, with per-attempt configuration/continuity, sparse bound metrics, admitted repairs and cleanup. They do not add cumulative resume counters or attribute earlier failure to the final target; missing/corrupt history and causal attribution remain unknown.

Avoidable implementation-agent costs included broad/truncated searches and repeated field/path guesses, a first dispatch driver that omitted identities already returned by export (refused before inference), duplicated lint/type checks alongside commit hooks, fixture assumptions about title-only Planning edits and legacy reentry, and a Python public-test helper missing its new status operation. The corrected status case passed in 40.27 seconds. A new public proof fixture initially omitted its established active-owner pointer; another positive fixture advertised unavailable Make targets and had to expose real executable routes rather than dismiss manual coverage. These are implementation/test-authoring costs, not provider-quality observations. The implementation session also paused at an account usage limit and resumed on user instruction; no reset or monetary estimate is inferred.

An initial aggregate proof freeze was premature: run 1a3388944654d59ef05c was explicitly cancelled after 13 of 28 commands passed, when a public counterexample showed generic validation could grant an unrelated task's slice-complete claim. Its first command passed 380 tests in 226.55 seconds. The run remains partial/cancelled, with no completed aggregate claim. Final validation follows the completed owner fixes and declared focused coverage; repeated overlapping selected groups remain observable proof-selection cost.

Public-owner parity work exposed real TypeScript placeholder reports/claim admission and ordinary start/implement entries. Replacing them with current owner transport is product work, not new proof semantics. The Planning negative in the proof fixture took three avoidable extra deterministic reruns because the agent edited shadowed intent fields before changing the established goal authority; final proof/report/claim cases passed 8 tests in 49.41 seconds. This orientation cost is retained separately from target economics.

Final conformance preflight initially rejected obsolete TypeScript placeholder field expectations in three existing fixtures. Removing those TS-only exceptions made both languages use the already declared current-owner assertions; the Node-required precheck then passed. A commit briefly encountered an index lock while a proof projection was active; it was retried after the lock cleared, without deleting the lock or attributing its cause to a provider.

The maintainer's remaining-PR comments exposed avoidable stack construction cost: placing those conformance fixes only in the final layer left earlier public-owner PRs unmergeable. The stack was rebuilt in an isolated worktree with proof conformance at #3072 and ordinary conformance at #3074. Four generated fingerprint hashes also required correction at #3073. Every layer passed normal hooks and generated freshness; exact leases protected publication. The first restack preserved the final tree exactly. The maintainer, not the implementation agent, merged #3063 through #3071.

One tool-session-bound aggregate execution disappeared during an interruption without producing a result or receipt. It remains unproved. A local hidden driver then preserved process output across session interruptions. Run `12fcc879ca1c61610827` passed all 33 selected commands: 1399.382 seconds summed command duration, including 1077 tests in 789.19 seconds in the first group. Groups overlap and are not a unique-test total. Manual obligations kept the aggregate blocked. This local validation time is neither provider inference nor a measured credit/monetary cost. Further broad receipt projections produced avoidable truncated output; bounded field projections were sufficient.

An explicit Docker check after the 33-command run found another implementation-agent assumption: the public TypeScript bridge required the executable name `python`, although the supported Debian image supplies `python3`. Native TypeScript tests passed, but five owner operations failed before launch. The repair adds only an ENOENT-bound discovery fallback and a launch diagnostic, at #3072, with deterministic interpreter regressions. No fallback occurs after an owner starts or rejects a request. Both Docker tests and conformance then passed on `b2f96ae5d77378a9457063cc1ebc6e0fe2aa3a49`. The failed Docker run remains evidence of the defect; it is unrelated to the earlier Codex worker's read-only sandbox denial.

Final scoped proof and exact publication identity are linked from the residual sweep report and PR #3076. No further provider worker was launched during these repairs. Monetary costs, savings and causal target-quality effects remain unknown.

The following reconstruction sweep began from reviewed foundation `1a9932473e5992ba04a44c963c959fab0f924e79`. PRs #3078/#3079 align the candidate and correct current intent/disposition; #3080 preserves independently admitted restrictions when their target capability is absent. Its isolated exact-head selected run `601bcd0a89f45044a7ce` passed 12/12 commands. Preparing that isolated checkout required the router's declared dependency recovery because its environment lacked jsonschema. This is checkout/proof orientation cost, not provider or target quality evidence.

Native boundary development used no provider workers. Fresh executable tests with Python/Node absent proved empty-target quietness and real registry request/currentness behavior; independent language/JSON consumers exercised the same Rust ingress. These are deterministic boundary checks, not the complete stateful or supported-provider delegation lifecycle. The actual configured checkout exposed unsupported native owner controls, and its ADR authority dependencies became stale after the proposed intent change; neither result was converted to successful acceptance.

Avoidable implementation overhead in this sweep included repeated PowerShell wildcard path guesses and oversized reads, one use of an unavailable bare Python command to project output, a lockfile mismatch during an in-progress Cargo dependency addition, and three failed commit-hook attempts before formatting/Clippy corrections in the unstaged CLI layer. A Windows junction fixture also required correcting command path rendering. These failures remain implementation-agent costs; there is no measured monetary total or savings. All previously incomplete real workers remain censored, with no new evidence assigning their failures to target quality.

The native stateful fixture exposed an implementation-agent result mapping error after a real temporary Planning commit: the bridge read an absent outcome field instead of the canonical operation result's status/effects/value. Correcting the mapping restored all four fresh-process consumers; exact replay then proved no additional writes. A subsequent lint run caught unused test-only helpers and unnecessary Rust borrows. These were corrected locally, with no provider inference or target-quality attribution.

Moving proof identity into Rust exposed avoidable transport amplification. Reconciliation now batches candidate admission and freshness: two commands with twenty receipts use two native processes; 129 receipts use two bounded transport chunks. No cross-evaluation cache or monetary saving is inferred. The focused owner run initially failed an inherited template fixture that asserted whole-proof completion while another required command remained missing; comparison with the exact previous implementation confirmed the same missing coverage. The corrected fixture preserves that blocker, and all 28 focused owner cases passed. Native integration separately exposed a real Planning result/view replay-detail mismatch, an obsolete aggregate wrapper line-count guard, and a stale current-source decision fixture; the failed exact-head run remains recorded.

The cumulative native/proof run `328e87f968ff25750488` stopped on its first of 39 commands: 448 tests passed and two failed; 38 commands remained unrun. Both failures reproduced on the restriction-layer baseline. One fixed-point fixture selected the wrong receipt; another exposed broad proof-profile triggers importing unrelated commands. The repair preserves the original bounded command-count assertion, assigns orthogonality to its scoped protocol, and retains genuine additional-owner coverage. Five focused regressions and 26 configuration cases passed; this is not an aggregate pass. Two misplaced TOML table insertions were also corrected in their originating PR layers. These are implementation/proof-selection costs, with unknown monetary cost.

Scoped-instruction migration reuses one Rust applicability owner for Python and native source consumers, and checks the actual Planning write set before execution. Its 49 focused public/scoped cases passed, including all four fresh-process consumers; six focused Rust tests passed. No provider worker was launched. The native repository probe consumes current admitted instruction sources but still reports 42 unsupported configuration-owner controls; it does not establish native dogfood readiness or supported-host success.

The next Planning slice exposed a test-authoring assumption with product consequences: public mutation fixtures manually created a local selector, and the native writer treated its recognizable shape as permission to overwrite it. The correction makes real shared-source continuation acquire absent state through the existing reconciliation operation and preserves source-only local selectors behind an exact custody blocker. Seventeen focused Rust Planning cases and 32 public native/four-consumer cases passed. A test helper initially used the wrong shared-source extension before checking the owner's constant; corrected before execution. No provider inference or economic target attribution follows from these implementation costs.

Run `c5027a95f0a810481145` on frozen `648363d72` selected 46 commands and stopped after four: 464 combined owner tests, lint and typecheck passed; maintainer surfaces failed on a missing generated instruction-applicability reference. Forty-two commands were unrun. The generated page was added in its originating PR (#3084); maintainer surfaces then passed on the following Planning layer. This was an avoidable pre-freeze generation omission, not provider or target cost. No aggregate pass is inferred from its repair.

Task-judgment consolidation exposed a remaining correctness gap: changing the requested task while keeping the same Planning owner/revision and proof fingerprint still returned accepted. The fix binds the judgment to the existing Rust task/change identity as well as the Planning subject; it does not create new Planning work. A first batching draft also recomputed acceptance in the Python bridge; that was caught before freeze and moved into the Rust owner, preserving the existing forwarding guard. These are implementation findings and repair costs, not provider outcome observations.

Native Memory now selects existing advisory sources and exposes exact current read requests without embedding their bodies in ordinary discovery. Forty-eight public native/four-consumer cases passed in 18.88 seconds; 62 Rust owner tests and schema checks passed. Malformed advisory sources remain visible diagnostics rather than vetoing direct work. The historical decision still requires semantic disposition, and stronger-owner promotion is not inferred from prose. Additional wrong-path skill lookup and unavailable bare-Python editing attempts were avoidable implementation orientation costs. No provider inference, target-quality observation or monetary saving is claimed.

Typed task requirements reuse Rust feasibility fields and distinguish executor judgment from evaluator obligations. Fifty-eight public/native identity cases passed in 22.55 seconds, including exact Planning/source drift. A concurrent module declaration briefly made the editable checkout unbuildable before its new source file existed; the ordinary route was rerun only after that implementation state was corrected. A test initially mistook the decision's direct status for absence of effect restrictions; inspecting the actual blocker contract corrected the assertion. The public schema embeds only its two required definitions, avoiding an initial draft's whole-schema expansion. The older ordinary assignment adapter and native provider feasibility still require integration; these tests are not successful delegation or target-cost evidence.

Independent-review consolidation found a second concrete proof defect: unknown actor identities could satisfy separate-actor review, and an absent reviewer provider could appear distinct from a known implementer provider. Rust now owns the exact predicate; Python and Node forward to it. Forty-two focused language/JSON cases and 26 existing review/closeout cases passed (6.99 seconds for the latter). A first schema-generation check exposed missing field descriptions/doc-role, corrected before freeze; maintainer surfaces then passed. This is deterministic owner evidence, not independent review supplied by this implementation session. Historical native receipt runtime compatibility remains explicitly unproven.

CI exposed omitted release changesets across the new package PRs despite their semver labels, plus a stale generated-package proof expectation after protocol trigger narrowing. Both were repaired in their originating layers, with normal hooks and exact-lease publication. The broad cumulative route also combines the explicit Workspace CLI owner suite with an unconditional coordinated-release workspace suite because Verification manifest provenance matches the release helper; this overlap remains a proof-selection cost, not additional provider evidence.

The new shared separation call exposed an installed-package defect: wheels contained no Rust core. A retained installed fixture confirmed the exact missing-core error; it remains recorded as a failed 77.39-second run. Host-platform wheels now contain the compiled core, and the sdist contains its build inputs. Installed fresh-repository proof passed in 76.83 seconds, native artifact/source-input checks passed in 43.18 seconds, extracted sdist rebuilding passed, and same-release-assets installation passed in 49.44 seconds. These establish current Windows packaging behavior, not Linux/manylinux/macOS support or canonical native CLI cutover. No Python semantic fallback, provider inference or monetary estimate was introduced.
Ordinary assignment ingress now requires the exact current task/work judgment instead of empty default feasibility requirements. Six final public startup/lifecycle cases, seven owner/direct/legacy cases and eight transport cases passed. External conformance passed 302 cases with nine declared unavailable before the final compact startup projection correction; the affected startup path subsequently passed. The layer preserves legacy carrier bytes and requires current role-specific Verification obligations for evaluators. Initial omission of the separate client-schema generator and test assumptions about compact startup shape caused avoidable implementation iterations. No provider calls were used.

Cumulative run `2063b3d9a697c9745080`, frozen at `c20259dbd5c0b10930bdca1ed396afc813d3c272` against `e8d910ea4a73f2792de2396f3e9f552fde94dbd4`, completed eleven commands successfully and timed out in command twelve, `make test-workspace`, after 1069.61 seconds in that command; 36 of 48 selected commands remained unrun. Its earlier focused owner group took 283.894 seconds, and the separately selected Workspace CLI suite took 501.442 seconds. The receipt remains incomplete, with no aggregate acceptance claim. This is local proof execution cost, not provider outcome evidence. CI subsequently passed the newer workspace checks and three runtime matrix jobs on the packaging layer, but found fingerprints stale after packaging build-input changes. Regenerating the four fingerprints in that originating layer fixed the omission; rebasing the ordinary layer preserved its exact tested tree.

One zero-inference host capability probe used installed Codex CLI 0.153.4 `command/exec` to run the actual checkout's native `agentic-workspace.exe --help` with unchanged `readOnly` sandbox, network disabled and a 10000 ms timeout. It returned exit code 0, native help and empty stderr. There were no thread/turn calls, provider turns or conversations. The observed executable SHA-256 was `e491f8552469a2556e16b84c8dc6ffe2f75cc761748fd974a49394beb2df72b6`; the checkout at probe time was `3d794158fe91ee5fc295183668cf82f544c6f879`. This establishes executable access, not independently attested binary-build provenance, configured native startup, ordinary worker execution, return admission or successful delegation. Previous incomplete workers remain censored and monetary cost remains unknown.
Current-checkout dogfood of the new requirements ingress exposed a compact `implement` projection omission: it retained the blocked action text but dropped the exact preview operation. The correction forwards that existing operation, without adding another requirement authority. The first manual task judgment used the unsupported result class `patch`; inspecting discovered capabilities showed the existing class is `unapplied-patch`. Correcting that terminology admitted the unchanged task and selected retained-local `codex_astra:internal`, with no materialization or provider execution. This is implementation-agent orientation friction and a current preview/control observation, not a completed admitted delegation lifecycle.
Selected proof `e9e56e4398ca50c99a11` passed three commands and stopped at stale generated assignment input documentation. The one-line generated reference was repaired in its originating layer. Revalidation `10c32d5bf7dc9a968b33` passed eight commands, then found one failure among 263 instruction/configuration cases: unresolved task requirements had erased the available transport methods and falsely changed binding orchestration to transport-unavailable. The origin correction retains current transport facts until feasibility can actually be evaluated, while unresolved requirements still block implementation. The existing fresh-process policy test then passed with an added implementation-block assertion. These failed receipts remain counterevidence until revalidated; neither failure is provider or target-quality evidence.

Revalidation `8e3e55fda9ad6f706093` at `a650edbb841cfc47b02d2e3888b0e0236f682472` passed eleven selected commands, including all 263 instruction/configuration cases. Its next public-owner group failed 12 cases with 56 passing in 135.66 seconds; five commands remained unrun. Updating the old fixtures to consume the current public task judgment exposed a real defect: ordinary manual export lacked the existing packet seal required for valid source-authorized reassignment. The correction reuses that seal. Public Python/TypeScript tests now exercise preview, exact judgment, manual export, reassignment and tamper rejection; the combined 24-case group passed in 201.45 seconds. This repairs deterministic handoff integrity, not supported-host worker execution or independent acceptance.

The later native Verification requirement layer passed 15 focused cases, and the reader-compatibility integration passed 110 cases in 38.33 seconds. CI passed their package/artifact checks and Windows/Ubuntu runtime jobs. An initially incorrect patch label on the minor Verification requirement changeset was corrected to the single matching minor label. Other avoidable implementation costs included fixture setup retries, Windows search-glob mistakes, and repeated discovery of command argument shape. These are implementation-agent costs, distinct from product friction and target quality; monetary cost remains unknown. No new provider worker was launched. Previous incomplete workers remain censored.

The manual handoff/authentication integration at `17e3712c1935c27d5eed1dd136cca5e397ea07e5` revalidated all seven selected follow-up commands: 33 authentication cases (5.645s command), 70 public-owner cases (293.539s), focused shared-core Planning (4.067s), upgrade/direct-claim controls (137.408s), proof-route controls (8.724s), contract tooling (31.211s), and structured inventory (8.438s). This resolves the observed manual-seal regression without rewriting the earlier failed receipt or supplying aggregate/independent acceptance. Authentication inspection also corrected malformed key-lifecycle timestamp handling and bounded optional Git observation to two seconds and 4096 bytes; authentication remains separate from proof sufficiency.

The native artifact layer packages the canonical executable alongside the shared core in host-platform wheels. Two extracted-artifact controls passed in 20.78s, including native operation with empty PATH and no language hosts or checkout, plus fail-closed reader compatibility. Full selected proof `4c3edf2ebb6e782f498c` at `925cac7dbf6d3934d06652f5131a57a0ae8a23ed` stopped with 17 packaging cases passing and one failing in 25.53s: the legacy TypeScript instruction route still preferred a source-checkout Python script. Six commands remained unrun. Passing CI artifact/runtime jobs do not erase this declared-route counterevidence. Native wheel payload availability is not native dogfood cutover or a successful supported-host delegation lifecycle.

Planning custody CI exposed two failures with 428 passing. Caller repair now propagates exact refusal, preserves former Planning state before unsuccessful selection, and leaves decomposition source status unchanged on refused promotion. The final package group passed 432 cases in 73.64s and custody controls passed 16 in 3.97s. An earlier fixture depended on ambient generated-package imports (six failures); using the public Python facade removed that dependence. Independent branch fixtures now use independent worktrees rather than sharing ignored local selection state. General transfer authority and crash-safe promotion remain unresolved.

Avoidable implementation costs in this continuation included attempting to reuse another checkout's virtual environment (correctly rejected by runtime identity before using a freshly synchronized local environment), guessing file paths after discovering the actual filename, repeated Windows search-glob errors, and reading unstaged diffs of staged work. These are implementation-agent orientation costs, not product target-quality observations. No new provider worker was launched. All previously incomplete workers remain censored; implementation-agent inference and local proof have costs, but monetary amounts and savings remain unknown.

The former-route/assurance integration passed the declared native public group (116 cases, 35.11s). CI then caught missing descriptions/doc-role and generated reference for the new assurance schema; proper annotations and `make schema-reference-docs` fixed that omission. Thirty composed cases passed after restacking (13.48s). A new absent-to-unreadable former-source counterexample showed that availability changes must stale earlier route selection; all 24 former-route cases passed after correction (8.20s). A missed Rust formatting step blocked one commit before normal hooks passed. These are proof/implementation costs, not successful delegation evidence.

One read-only native startup using the candidate executable against the actual configured root returned a 182066-byte unresolved packet with 39 Workspace configuration and 12 Verification scope blockers, plus unresolved Planning custody. This is current-checkout execution and context-burden evidence; it does not justify native dogfood cutover or running another provider worker. The first diagnostic print expanded every blocker and was truncated; a compact owner/count projection recovered the relevant finding. The overbroad print was avoidable implementation-agent output cost.

Native route discovery now runs through the same Rust source reader in Python and the existing workspace npm artifact. The full packaging group passed 18 cases in 48.92s, resolving the observed TypeScript source-Python oracle failure without rewriting receipt `4c3edf2ebb6e782f498c`. Thirty-one focused source/packed-consumer cases passed before restacking; the post-rebase combined group passed 43 and failed one fixture import in 30.30s. Loading the maintainer script by its explicit file path removed the ambient-import assumption; that case then passed in 0.08s. The npm consumer works offline with empty PATH; missing/tampered/version/platform artifacts fail closed, and an explicit Rust host target prevents cross-target environment settings from mislabelling the payload. Current-host packaging is not the final platform release matrix.

Avoidable npm implementation work included initially omitting an isolated consumer package.json (npm searched an ancestor), the fixture import assumption above, and a missing capability metadata assertion that exposed and corrected a real discovery port omission. The old persistent TypeScript route write is explicitly unavailable with native request recovery; it is not counted as a successful selection. No provider inference was used to repeat protocol mechanics. Incomplete workers remain censored and monetary cost remains unknown.

CI exact packed-artifact conformance exposed a staging omission: its fallback npm pack still used the generated source directory without the native payload. The proof runner now calls the existing host artifact staging utility. The blocked legacy selection response now uses the declared blocked status/exit mapping while preserving explicit unavailability, no mutation and native request recovery. The complete packed-artifact regression plus focused npm cases passed 12 checks in 61.42s. The earlier CI failure remains counterevidence for its old head. More overbroad CI output, a Windows search glob and an absent scratch-output directory were avoidable implementation-agent costs; no provider work or monetary saving is claimed.
Native selected-proof execution passed 21 bounded producer cases at fc54098c (47.08s), then 29 producer/advisory cases after restacking (50.53s). Legacy publication identity and Unicode cases passed nine checks (7.33s); fixing explicit UTF-8 transport corrected the earlier locale-dependent bridge failure. Immutable output artifacts preserve bounded detail while ordinary results expose sizes and hashes. Existing-index custody and cross-adapter executable compatibility remain explicit implementation gaps; process exit does not grant task judgment or independent acceptance. No provider worker was launched.

An unpublished shared-library build-input identity experiment passed 17 identity/runtime tests (5.42s) and an extracted-wheel control (26.04s), but audit found omitted profile/configuration inputs, dependency-feature unification and wrapper rebuild invalidation. It is not adopted as proof equivalence or shipped as product machinery. Those local build/test and implementation-agent costs bought counterevidence, not proof reuse or target-quality observations. Monetary cost remains unknown.

The dedicated native CLI now forwards canonical JSON to its exact colocated core executable instead of linking a second executable copy of owner semantics. Native-produced current proof is reusable by Python, Node and JSON consumers of that same core without repeating the command. The integrated public/producer group passed 107 cases after restacking (83.43s); the actual extracted wheel passed the no-language-host/no-checkout control (25.99s). Windows interruption, missing sibling, same-path binary drift and separate-location incompatibility are exercised; actual Unix execution remains a platform proof gap. The semantic-regrowth guard now forbids linking the core into the CLI adapter and retains its bounded transport-function inventory (one focused check, 0.19s). A parallel test temporary-name collision was corrected with an atomic suffix; repeated guessed file reads were avoidable orientation cost. No provider work, arbitrary cross-installation equivalence or net proof-cost saving is claimed.

The schema-only native configuration correction passed all 100 public native cases after restacking (33.15s); it removes one redundant already-enforced schema blocker, not the unresolved module/payload/intent semantics. TypeScript activation custody passed 18 cases after restacking (10.63s). Its initial 16-pass/two-failure result exposed an obsolete scaffold missing current canonical fields; acquisition was not weakened. Existing native selection is preserved before draft/state writes, while a rejected absent-carrier activation remains an explicit unselected draft. Canonical Rust creation remains subsequent work.

Linux hosted packed conformance still rejected route discovery after native staging was corrected. Inspection found the custom proof extractor discarded executable mode bits; it now preserves ordinary archive permissions while excluding special bits. The prior Windows 12-case run did not establish Unix extraction behavior. Hosted Linux validation remains the decisive follow-up; no provider capability or target-quality conclusion follows from this maintainer-harness defect. Repeated stack rebases and CI runs are implementation/review costs, with monetary cost unknown.

Explicit native module enablement now gates owner availability without interpreting disabled state. The combined run passed 147 cases and failed four assertions expecting a later error message; those assertions were corrected for earlier capability-currentness rejection and passed (3.42s). A real stale Planning-action counterexample first acquired state after an availability change; current action dependency binding now rejects it without changing semantic work identity. Four restored-owner positives passed (3.88s), and 17 Rust Planning cases passed. Follow-through found a case-sensitive proof-index anchor typo and added the independent-review index to disabled-source observation; 32 focused cases passed after correction (14.46s). This does not establish generic external-module admission, historical transfer or configured-checkout completion.

The later Linux artifact investigation found an additional upstream cause: external-consumer readiness packed the TypeScript source tree into `dist` before conformance reused that archive. Readiness now uses the same native staging utility. The exact readiness-archive/reuse regression passed (25.19s), four readiness contracts passed (0.14s), and the Linux package-artifact job passed at 52d3befd (run 34182678511, job 101924763076). The earlier executable-permission correction remains valid but was insufficient by itself. The declared runtime jobs also passed at that head; these are CI observations, not first-stable or independent acceptance.

Shared publication custody passed 20 affected public cases (65.64s) and nine Rust interruption/boundedness cases (2.13s) after integration. The preceding integrated run passed 38 cases, including all 12 publication and 21 native producer cases, before exposing a Python adapter KeyError when Verification was disabled. That path now returns the current owner blocker without mutation. Audit also caught index-capacity overflow and an insufficient recovery inventory bound; publication now refuses at capacity before receipt/index writes, with separate bounded exceptional recovery. Process-crash recovery is proven; power-loss durability, cleanup and compaction are not. The old manually asserted pass remains an unproven observation, never task proof.

System-intent source ingress passed 16 four-consumer cases (6.77s). A read-only native start against the actual configured checkout emitted 184397 bytes in 2133ms and retained 32 Workspace blockers, 12 Verification blockers and one stale governing-interpretation gap. This measures local diagnostic/context burden; it is not a successful configured lifecycle or provider observation. Repeated guessed paths, an unquoted PowerShell Git revision expression, and mixed-object table rendering were avoidable implementation-agent orientation costs. No new provider workers were launched, earlier incomplete workers remain censored, and monetary cost remains unknown.

The Planning vertical added five public cases (22.05s) and strengthened the actual Planning-bound producer admission/staleness assertions (one affected case, 11.44s). Controlled returned/integration-pending fixture updates remain distinct from actual provider returns. The earlier eight historical-judgment fixtures were corrected in the publication parent without weakening exact-subject/currentness assertions; current manually reported exact judgments remain nested and unproven. A fixture-wrapper selection mistake required one focused rerun (four cases, 5.73s).

Current module CI exposed an obsolete assertion that capability changes preserve an executable Planning commitment. The correction proves unchanged semantic reconciliation, changed action identity and stale-action refusal; the declared shared-core route passed 141 cases (75.71s). Four subsequent stack layers rebased without semantic conflicts. These are implementation/review costs, not target-quality evidence.

Domain-proof integration initially passed 32 cases and failed four packet-size assertions (96.71s): canonical Planning construction increased duplicated public capability fragments, pushing a 41-lane fixture to 103136 bytes. Public composition now exposes its full contract once, retaining request constructibility and the existing discovery bound. The integrated domain, creation and producer group passed 66 cases (117.02s). No byte bound was raised, provider inference was added, or monetary saving inferred. One incorrect patch context and a briefly changed worktree branch were avoidable implementation-agent mistakes; source bytes were preserved and the branch restored before further edits.

### Current proof fixture correction (2026-09-08)

The #3110 workspace check retained three failures (462 passed, one skipped):
old upgrade/identity/PR-cache fixtures reported `passed` without execution and
expected publication or task completion. This was fixture counterevidence, not
permission to bypass current Verification availability or source admission.
The correction executes the existing bounded selected commands and preserves
upgrade/no-Planning and fresh PR-cache reuse/invalidation coverage. Disabled
publication remains a zero-receipt-delta refusal. Source changes stale execution;
restoring exact source permits execution reuse without creating task judgment.

The public exact-task judgment admission positive remains unresolved under
#2334 for this path: selected execution publishes local execution evidence, and
an interoperability report does not establish task sufficiency. Explicit shared
semantic-owner test observations prove exact task/change/fingerprint matching
and manual/independent obligations separately; they are not public acceptance,
human review, or independent Verification evidence. No provider calls or monetary
estimates were used. The first focused two cases took 91.67 seconds including a
source-host Cargo currentness checks; the PR-cache case took 15.92 seconds.
The final focused three-case group passed in 104.67 seconds. The editable
source adapter invokes Cargo to establish current core availability on each
resolution; repeated short Cargo children were observed during this run. This
cost is not attributed to provider inference or task execution quality.


### Native owner integration follow-through (2026-09-08)

The startup source and canonical artifact-profile layers passed their focused
consumer checks; they do not establish instruction compliance or task proof.
The Planning selector-switch integration preserved both retained invocation and
explicit startup-source dependencies: 70 focused cases passed (81.80s), four
additional switch-with-startup consumer cases passed (16.65s), and three Rust
staleness/interruption cases passed (0.95s). The latter includes actual process
exit and fresh-process recovery, not power-loss durability. Historical selector
acquisition remains unavailable.

Verification strategy integration passed 50 strategy/domain/producer cases
(125.69s), resolving the earlier 106202-byte pre-integration discovery failure
through the existing single-contract projection. A further startup-source,
strategy and scope replay case passed (4.07s). No byte bound increased. Full
receipt-index capacity now refuses a new process before it incurs execution
cost; no numerical saving is inferred. Human/domain judgment still needs a
current Assignment-owned review identity as well as actual returned evidence.

Avoidable implementation-agent cost included attempting the ordinary wrapper
while a cherry-pick still contained Rust conflict markers, a new-worktree
runtime dependency synchronization, and another unsupported PowerShell glob
search. Fixture integration detected and restored two accidentally re-encoded
UTF-8 passages before publication. These are implementation costs, not provider
or target-quality evidence. No new supported-host provider worker was launched;
all previously incomplete workers remain censored, and monetary cost is unknown.

### Follow-up report and Planning fixture audit (2026-09-08)

Two exact follow-ups reproduced one failure and one pass in 45.67 seconds.
The dynamic operating-loop fixture now uses actual selected execution and
passes in 17.62 seconds. It preserves projection reuse and current Planning
custody, while closeout remains blocked: the legacy new-plan scaffold still
contains placeholder scope, and its former caller-supplied receipt plan ID
bypassed that scope check. Native typed Planning creation already accepts scope;
this is a legacy adapter/re-entry gap under #2970/#2334, not missing native
creation. No task judgment or terminal acceptance is fabricated.

The route-health test legitimately executes candidate and validation commands
during guarded apply. However, a bounded counterexample changed the validated
source after apply and before reported retirement; the old positive assertions
still passed (28.15 seconds), declaring the finding retired and the route current.
The probe was removed after preserving its log. Apply history records bind
configuration revision and path names but not the executed source contents or
runtime proof subject; retirement reads that JSONL as evidence. This remains
counterevidence under #2334/#2647 and producer custody under #3000/#3001.
Adding a fresh fingerprint only when reporting would not repair it. A genuine
fix needs current, producer-admitted apply execution evidence rather than a
reported pass or file-shaped validation record. No parent closure follows.


### Legacy repair retirement admission subtraction (2026-09-08)

The stale-source counterexample above is corrected by declining reuse at the
existing retirement boundary. Legacy apply execution remains observed, but a
reported pass and raw apply/history files cannot establish a current verified
repair. Both report admission and subsequent history consumption now retain a
stronger-owner-required gap; forged history cannot restore the removed shortcut.
The current strategy and actual candidate/validation execution remain visible,
with affected handoff/closeout claims blocked until the apply owner supplies
producer-admitted execution bound to current sources. No new proof store,
constant-result native API, authority flag, or generic learning layer was added.
Full native apply migration remains #2334/#2647/#3000/#3001 work, not completed
acceptance. This is the #2981 stronger-owner disposition rather than unsupported
reuse or blind re-execution of a potentially effectful apply.

The four focused safety cases passed in 71.22 seconds. Adding the fresh
TypeScript forged-history replay initially failed after 24.89 seconds because
the implementation agent omitted a test import; that avoidable rerun cost is
separate from product friction. The failure log is retained.
The corrected fresh Python/TypeScript forged-history case passed in 26.58 seconds.

Integration of repair-retirement subtraction passed four cases and found one obsolete diagnostic assertion (77.82s). The updated negative covers both reported pass and failure, preserving zero publication when apply producer/source authority is unproven.

### Native Planning update integration

The cumulative native Planning update and proof interaction lane passed 62 tests
in 119.35 seconds. Updates preserve exact creation custody and distinguish
material revision from frontier changes. Interrupted postimage publication can
resume its exact retained invocation; reworded same-semantic recovery remains an
explicit owner-reentry implementation gap. No real provider work, target-quality
observation or monetary saving is inferred from this deterministic run.

### Native maintainer diagnostics (#2995)

Native start/invoke now emits bounded metadata into the existing registered logical stream when current local policy enables capture and explicit identity is available. Actual native CLI capture was discovered by the existing public analysis/export commands; no private-path recovery was required. Nineteen native cases plus the shared forwarder guard passed in 5.70 seconds. A five-pair local timing sample observed median disabled-source startup of 58.38 ms versus 48.21 ms with the explicit disable override (10.16 ms difference); this is host timing, not claimed universal overhead or savings. Native no-identity and explicit-disable paths skip diagnostic source/state reads.

Avoidable implementation-agent mistakes were an initially incomplete schema-validator envelope (nine failures), then a misspelled existing analysis flag (`--origin-scope` instead of `--origin`, one failure); corrected focused reruns passed. No provider inference was used. Existing-registry registration of a new logical identity is still skipped because safe registration custody is unimplemented. Native interruption/rotation and release-artifact acceptance remain separate gaps; diagnostic omissions and transport success never grant task or proof acceptance.

Integration briefly retained conflict markers in an unpublished commit after an unqualified Python command was unavailable and the shell continued. A first merge-repair expression also consumed trailing text; JSON validation caught it. The implementation agent restored complete sources, corrected the merge and amended that unpublished commit before publication. This is avoidable implementation-agent command sequencing waste; no product or target-quality inference follows.

### Material lifetime correction from integrated CI

Linux CI on the native Planning update stack exposed a real Verification request
lifetime regression: changing Planning's returned/integration-pending frontier
invalidated an otherwise exact semantic request. The same failure reproduced
locally. Assurance had hashed an entire Planning subject and physical owner
context rather than the material identity and applicability facts it consumes.
The correction recomputes that small semantic input; no memoisation is needed.
Four native/Python/TypeScript/JSON creation-update-reconciliation cases passed in
21.56 seconds, preserving requests across frontier updates and rejecting changed
scope. The original failing Rust case passed. No proof or task completion is
inferred from preserved request currentness; no provider calls were made.

The integrated Rust boundary also caught an obsolete fixture expecting consumed transport authority to remain unsupported. Its replacement checks the actual Assignment policy and retained safety ceiling: automatic transport with safety false still forbids execution. The initial boundary had89 passes, one stale fixture failure and3 subprocess helpers ignored.

### Native public projection subtraction (#3059/#2981)

The first serialized configured-checkout audit packet contained 193,827 compact UTF-8 bytes. Owner-local contributions and repeated assurance source bodies accounted for 35,499 removable bytes; applying only those final-projection omissions leaves 158,328 bytes. This is a captured-packet measurement, not a claim that all current checkout packets fit a universal size.

A separate real source read with the frozen logging parent and this candidate used the same exact checkout and task: 178,254 bytes before, 144,856 after (33,398 bytes removed). Removing only the identified duplicate fields from the before packet made the two decoded packets exactly equal. Full requirement bodies remain available once by requirement ID; composed blockers, capability declarations, request envelopes and currentness are unchanged. No cache or new detail protocol was introduced, and no 100 KB bound was increased.

The four-consumer applicability/request composition regression passed after correcting an existing stale-source fixture expectation. Its baseline independently failed because current capability admission rejects source drift earlier than the old expected unresolved reply; the updated negative also checks fresh unresolved recovery. Neither these context savings nor deterministic constructibility closes #3059 or #2981.


The public-consumer audit found four existing fixture files reading removed internal contributions. Their assertions now inspect owner-filtered composed blockers/pending actions instead. All 42 affected startup, advisory, domain execution and Verification requirement cases passed in 73.35 seconds; no safety or packet-size bound changed.

### Current policy precedence conformance

Linux runtime CI on the comparative-assignment stack passed154 shared-core tests and found one old fixture expecting a deprecated manual-disabled alias to override canonical automatic transport authority. The fixture now verifies canonical precedence and retains a separate negative without canonical authority, plus the independent unsafe automatic-command ceiling. The CI failure remains recorded; this changes no product behavior or provider evidence.
