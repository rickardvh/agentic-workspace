# Candidate C integrated acceptance input

Date: 2026-09-16. Runtime subject: master
`d0423df70161cb5565792f1789ec707f3e7f376d`, after merged #3357.
This change adds evidence and reconciles documentation/retained interpretation;
it changes no runtime behavior. It is **ready for independent review**.

This is a finite aggregation for [#3277](https://github.com/rickardvh/agentic-workspace/issues/3277)
and the administrative closeout of
[#3260](https://github.com/rickardvh/agentic-workspace/issues/3260).
It does not supply implementation-author approval, preview publication, stable
admission, or whole-#2985 completion. Independent acceptance of this aggregate
remains pending; required leaves already have accepted closure.

## Accepted constituent evidence

Each linked PR retains its exact-head independent review and bounded closure
assessment. Reuse those judgments rather than reviewing our own implementation.

| Integrated obligation | Accepted work and disposition |
| --- | --- |
| Current config grammar, effective local sources, Assignment inputs | [#3320](https://github.com/rickardvh/agentic-workspace/pull/3320), [#3321](https://github.com/rickardvh/agentic-workspace/pull/3321): #3312, #3313, #3303 closed |
| Consequential procedural choices and Planning source contraction | [#3322](https://github.com/rickardvh/agentic-workspace/pull/3322): #3314, #3316 closed |
| Memory declaration, selection and hygiene | [#3323](https://github.com/rickardvh/agentic-workspace/pull/3323): #3317, #3318 closed |
| Verification authoring, semantic applicability and bounded measurement | [#3328](https://github.com/rickardvh/agentic-workspace/pull/3328), [#3333](https://github.com/rickardvh/agentic-workspace/pull/3333): #3319, #3324, #3327 closed |
| Standard skill exposure, same canonical resources, lifecycle and tiny bootstrap | [#3331](https://github.com/rickardvh/agentic-workspace/pull/3331): #3325 closed; includes actual Codex skill discovery, not only fixture registration |
| Direct/composed instruction and resource equivalence | [#3343](https://github.com/rickardvh/agentic-workspace/pull/3343): #3329 closed |
| Lazy current frontier and sole-required versus genuine-choice proof | [#3335](https://github.com/rickardvh/agentic-workspace/pull/3335): #3334, #3315 closed |
| Selected advice, new observable learning and exact Memory recovery | [#3344](https://github.com/rickardvh/agentic-workspace/pull/3344): #3336, #3340, #3341 closed |
| Binding dispatch and admitted Planning continuity | [#3345](https://github.com/rickardvh/agentic-workspace/pull/3345): #3337, #3339 closed |
| External effect truth and fresh affected Configuration consequence | [#3330](https://github.com/rickardvh/agentic-workspace/pull/3330), [#3332](https://github.com/rickardvh/agentic-workspace/pull/3332): #3264, #3266 closed |
| Richer host cost comparison | [#3338](https://github.com/rickardvh/agentic-workspace/pull/3338): #3267 closed with independently accepted `NO_ADAPTER` |
| Native transition consumer repairs and exact System Intent writer | [#3357](https://github.com/rickardvh/agentic-workspace/pull/3357): #3353–#3356 closed at accepted head `801e0e9a2c1e078827ed5fc7fe9ca395ff64246d` |

## Configuration effectiveness and source ownership

The current contract is the native Configuration schema/reader and generated
[configuration reference](../../.agentic-workspace/docs/workspace-config-contract.md),
not this report. The accepted contraction above disposes retired fields. Current
families have the following consumers; this is not a new permanent field ledger:

- Workspace invocation/instruction source, module selection, workflow artifact
  profile, clarification and initiative choices feed startup, instruction
  delivery, domain admission or selected executable preparation. Current tests
  exercise an actual instruction-source change, procedural preference changes,
  Planning policy exposure without creating work, and a module-enable write that
  correctly leaves missing module-owned admission unresolved.
- Shared/local safety, source admission, payload compatibility and assurance
  controls feed native effect/claim restrictions. Local preference cannot waive
  shared policy, unsupported grammar is rejected before state interpretation,
  and an accepted config write does not admit operational state. Instruction and
  decision-record trust revisions were preserved in this reconciliation.
- Assignment policy, target identity/scope, transport feasibility, guarantees and
  cost/confidence inputs feed eligibility and explicit comparative judgment.
  Descriptive priors are decision inputs, not measured economics or dispatch
  authority. This task obtained native `admitted-local` after current comparison;
  no unavailable non-local patch transport was fabricated.
- System Intent source declarations feed exact source reads and reconciliation;
  session logging and privacy controls feed the actual logging sink. The fresh
  Configuration procedure test changes privacy policy and observes redacted
  output, rather than merely echoing a setting.
- Memory declarations/hygiene feed current selection and disposition; Planning
  uses its source-owned continuity rather than a parallel agent manifest;
  Verification protocols, applicability, assurance and measurement declarations
  feed proof selection, evidence admission and remaining claims. Unsupported
  former declarations remain rejected; retained development-package fixtures do
  not resurrect a Python runtime.

## Integrated behavior and partial compliance

The current tests exercise native, JSON, Python and TypeScript entry where those
tests parameterize the public surfaces. Skills use the same current requests and
actions as direct callers; they do not acquire policy or effect authority.

The ordinary learning journey starts with “Check the repaired export and preserve
its result”, without a Memory request. An admitted producer emits an explicit
future-value candidate. The current owner nominates a bounded judgment that
survives carriage/reentry. Separate cases choose no retention, an already-absorbed
stronger source, or authorized advisory capture. A fresh relevant task receives
the captured advice; changing its dependency removes stale advice. Nothing infers
hidden reasoning, and nomination does not authorize publication. These are
deterministic public journeys, not a new live-model compliance experiment.

Other integrated journeys cover exact ready dispatch, in-flight no-relaunch,
retention/return propagation to Planning, direct recovery, and interrupted effects.
Unknown, unavailable, stale and not-applicable remain different. Confirmed effects
survive failed continuation; uncertain effects cannot be blindly retried. A sole
required proof action is carried only after scope/strategy/authority are settled;
multiple legitimate alternatives and sufficiency/review remain judgment.

Quiet controls include absent config/module state, direct resources with no owner
calls, no-signal learning, local Assignment, fresh repository-only readers, and
missing executable methods. The no-runtime path establishes recorded source facts
only. It creates no substitute admission, proof, runtime capability or mutation.

## Observable operating cost

Windows source execution, same runtime subject, fresh public JSON processes.
`test_frontier_expansion_detail_currentness_and_disposable_carriage` was rerun with
output enabled to record its measurements:

| Proof alternatives | Full bytes | Compact bytes | Carriage bytes | Full query ms | Carried query ms |
| --- | ---: | ---: | ---: | ---: | ---: |
| 1 | 105,087 | 4,229 | 4,841 | 107.1 | 106.6 |
| 128 | 280,861 | 4,229 | 4,841 | 119.9 | 106.5 |

Empty carriage was also 4,841 bytes. Exact blocker/action/claim fields match full
output; selected detail matches its full owner; dependent drift rejects the old
reference while unrelated drift and lost carriage need no session recovery.
No local state is created by this fixture. Timings are single observations under
concurrent validation, not a latency benchmark or economic improvement claim.

The instrumented Rust fixture reports full builder events increasing from 4 to
258 while compact and selected-proof events each stay at 1. Receipt admission and
post-effect continuation do not enumerate 128 untaken alternatives. Large selected
Memory stays deferred until expansion, which materializes exactly one body while
preserving authority. This measures actual construction, not JSON omission alone.

Accepted #3338 compares optimized portable and host paths: each needs two caller
interactions, two shell/native invocations and four owner calls. A host projection
only saves ephemeral carriage storage; it does not earn another adapter. Retries,
recovery, currentness and residue remain part of the comparison. No hidden host
context, model token usage, provider economics or longitudinal success rate is
claimed. Those remain with their existing evidence owners.

## Current validation

The paired binaries were built with `cargo build --locked --workspace --bins`.
No Rust or bundled runtime contract changed in this reconciliation.

- `cargo test --locked --workspace`: **139 passed, 3 ignored** process helpers.
- Integrated Configuration/skills/Memory/proof/resources/frontier/System Intent
  selection: **143 passed, 1 skipped** (Windows file-symlink permission; directory
  junction coverage remains exercised).
- Execution configuration, Configuration writes, measurement and external issue
  effect selection: **40 passed**.
- Handoff/Planning/continuation selection: **108 passed**, including the packed
  read-only handoff consumer and current retained Planning adapter.
- The frontier test and instrumented Rust frontier test passed again to expose the
  cost observations above; those repeat runs are not counted as extra coverage.
- Package identity check passed with the unchanged Alpha classifier and release
  ownership contract.

The selections are reproducible as:

```text
python -m pytest -q -n 4 tests/test_native_frontier.py tests/test_native_resources.py tests/test_native_memory_capture.py tests/test_native_memory_declarations.py tests/test_native_memory_disposition.py tests/test_native_proof_procedure.py tests/test_configuration_procedure.py tests/test_selected_procedure_preparation.py tests/test_native_advisory_config.py tests/test_native_config_admission.py tests/test_native_assignment_policy.py tests/test_skills_first_interface.py tests/test_native_system_intent.py
python -m pytest -q -n 4 tests/test_native_execution_configurations.py tests/test_native_configuration_write.py tests/test_native_measurement.py tests/test_bounded_external_issue_route.py
python -m pytest -q -n 4 tests/test_native_readonly_handoff.py tests/test_native_planning_create.py tests/test_native_invoke_continuation.py
```

This reuses the accepted child package/CI evidence rather than claiming a new
whole-repository or release-artifact pass. In particular, #3357 repaired the
Memory, retained TypeScript Planning and isolated-wheel compatibility failures.
Historical strict maintainer Planning residue remains outside this bounded lane;
no operational history was deleted to make acceptance appear clean.

## Source reconciliation and remaining boundaries

README, release/versioning and maturity documentation now describe the single
root Python distribution, single npm package and paired native archive, and the
trusted master preview publisher. Alpha is retained. The published Candidate B
preview and older stable install receipt keep their exact historical subjects;
neither is presented as containing Candidate C.

All declared System Intent sources and the previous retained interpretation were
read through the native owner. Semantic revision replaces the older query-first
preference with the current skills-first responsibility split, lazy actionable
frontier, partial-compliance learning and authority-preserving transitions. The
host-agnostic agent-judgment principle is preserved and its scope includes Rust.
The source-bound proposal and authorized `system-intent.write` returned
`effect_outcome.status=committed`; fresh reentry reports `source_currentness=matched`
and no reconciliation gaps. Task alignment still reports
`unresolved-owner-judgment`: matching sources does not accept the whole task.

Independent aggregate acceptance permits administrative #3260 closure. This
evidence-only PR is the reviewable closeout proposal, not a new unification runtime.
#3277 remains open for final preview and fresh post-C release evidence; #2985
remains open for exact stable preparation/admission/publication. Stale #3283 is
not fresh admission. #3347–#3351 and Set D remain post-v1. No new release, support
platform, maturity label or source trust revision is asserted here.
