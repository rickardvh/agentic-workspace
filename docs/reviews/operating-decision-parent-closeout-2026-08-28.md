# Operating-decision parent closeout — 2026-08-28

## Conclusion

The reopened #2562 lane is satisfied by the existing canonical-decision implementation, the enforcement and delegation repairs, and the fresh generic composed-operation gate. One compatibility-admitted input revision produces one operating-decision identity for owner, task relation, action, effects, proof/claim boundary, terminal posture, and continuation. Purpose-specific packets remain derived projections and provider mechanics remain outside the semantic authority.

## Fresh composed path

The maintained scenario executes cold repository startup, compatibility admission, bounded Planning relation, changed-path implementation, focused proof, provider-neutral handoff/export, untrusted return import, host-owned admission and integration, terminal closeout, and compact resume. The final repair makes the manual handoff explicit in this generic fixture, rejects failed export before downstream claims, and persists the assignment id in Python just as the TypeScript owner already did so run-id-only admission re-resolves the checked-in Planning authority.

The scenario does not require GitHub, a session transcript, or a specific worker provider. The worker-return fields remain evidence only; AW re-resolves current assignment, structural proof, run, and mutation-baseline authorities before admission.

## Acceptance and subtraction

- Compatibility is admitted before managed state becomes decision input.
- `compile_operating_decision` remains the sole semantic owner; start, implement, proof, summary, Planning, Memory, generated clients, and assignment operations carry or derive its identity.
- Relevance is symmetric: applicable Planning/Memory/guidance inputs contribute through typed owner contracts, while unrelated inputs remain absent.
- Projection reuse and invalidation remain revision-keyed and non-authoritative.
- The composed release gate covers the complete generic lifecycle, including return admission and compact resume.
- PR #2782 repairs the late delegation counterexamples and preserves direct-local quietness.
- No universal state store, second workflow command, provider-specific scheduler, transcript requirement, or parallel completion authority was introduced.

The historical closeout evidence predating the reopening remains evidence of its original slice, not the current closure authority. This review plus the current #2763 integration receipt and fresh test run are the post-reopening basis for closing #2562 when the stack merges.
