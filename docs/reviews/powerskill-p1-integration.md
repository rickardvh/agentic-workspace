# P1 migration integration

Implementation sweep under #3411 from the #3409 audit subject
`cc2b236b0d0a546d0c47c9c1e59ede857d679477` through the stacked M1-M5 and
contraction changes. Ready for independent review. This is not an accepted-head
closeout: independent acceptance of the stack and administrative issue closure
remain external. Documentation follows this implemented contract.

## Complete source-group accounting

| #3409 group | Implemented disposition | Retained native meaning / evidence |
| --- | --- | --- |
| Startup | M1 #3465 small entry/shared selected references | Exact source/owner carriage, ordinary and no-runtime boundaries; skills-first suite. |
| Intent discovery and work shape | M1 merge into intent references; old shape entry removed | Agent judgment and native Planning custody remain distinct. |
| Transition gates | M1 retired entry and merged unique interpretation | Native binding requirements survive bypass; no second phase taxonomy. |
| Setup and Configuration behavior | M2 #3466 selected setup references | Native config, authorization, affected-owner observation and post-effect truth; Configuration cases. |
| Correction | M2 selected destination references | Responsible source authority, no-retention and publication/recovery remain native. |
| Resources compose | M3 #3467 removed; this integration adds exact owner adapter | Same resource primitive policy/path/lease/retention/recovery; direct tool retained. Six generic resource-reference and 13 direct lifecycle cases. |
| Proof procedure | M3 native wrapper deleted; generic exact Verification references | Required action, execution, receipts and claim restrictions remain native; no retry on lost continuation. |
| Planning umbrellas | M4d #3471 all three removed | Unique continuation/worker custody in narrow work/Assignment references; explicit umbrella disposition. |
| Planning intake/structure/report/intent/closeout | M4a #3468 work/closeout fragments; reporting stays plain | Current work relation, exact updates, proof/intent separation; creation/catalogue/install proof. |
| Assignment assessment/manual/return | M4b #3469 one narrow bundle | Binding local/non-local scope, sealed packets, return admission and recovery; 28 regression cases plus manual/process handoff. |
| Review findings | M4c #3470 bounded triage/continuation | External reviewer eligibility and claim authority preserved; native impersonation rejection. |
| Memory capture/hygiene | M5 #3472 selected same-source fragments | Explicit candidate consequence, receiving evidence, no-retention, source currentness and publication; 19 native cases. |
| Package lifecycle guidance | M2 common setup reference; duplicate CLI recipes removed | Module state and host edits preserved; installed lifecycle proof. |
| Maintainer skills | M5 keep bounded plain methods; repair ownership-ledger pointer claim | Independent review stays external; no shipped helper runtime added. |
| Routes/source admission | C1-C3 retained generic discovery and lazy resource mechanism | Current qualified identity, selected body dependencies, collision and custody tests. |
| Native owner/frontier/instruction composition | KEEP_NATIVE; #3474 removes obsolete module taxonomy | Facts, policy, effects, evidence and source currentness, not optional method. |
| Adapter/WORKFLOW/knowledge disposition | Small pointer retained, method delegated to startup/correction | Repository instructions unchanged; no trust-pin advance or second promotion flow. |

The sweep searched active Workspace/Planning/Memory skill and bootstrap sources,
maintainer skills and current package/everyday guidance for retired names. Remaining
hits in dated host/extension evidence are historical observations, not active
procedure. Package and generated inventories omit retired entries. All referenced
resources ship with their introducing bundle; canonical/manual/native paths share
one maintained source. No native dispatch selects an M1-M5 skill by name.

The resource adapter completes an M3 omission found during integration: it exposes
`resources/propose/v1` and exact `workspace.resources.*` actions through existing
start/invoke references. Proposal discovery cannot execute, and branch selection
cannot authorize an effect. The dedicated resources tool is unchanged. Route
policy dependencies remain current across both transports. Tests cover stale
replay, changed protection, non-executing proposals and all four public transports.
No accepted disposition was reclassified merely to avoid implementation.

## Final bounded walkthrough and observable cost

Host: this live Codex session on Windows. Source identity: this integration stack,
not a published release. The host's available-skill catalogue advertised the
startup skill; its `.agents/skills/workspace-startup` junction resolves to the
canonical entry. The acting agent read it and used selected owner/resource/proof
sources during this implementation, including real Planning continuation writes.
That is observed activation in this host, not proof of automatic activation in
other hosts or future sessions.

A neutral host-owned change-note bundle was authored under `tools/skills` in a
bounded temporary repository. No module or custom runtime code was needed. The
acting agent inspected the emitted question and alternatives, answered unknown
without patch evidence, then selected visible for the observed change from `12`
to `$12`. The delivered user-note fragment returned a draft only. After cited
patch evidence changed to a behavior-preserving refactor, native re-entry marked
the retained answer stale. The agent reconsidered and selected internal: no user
note was needed. No publication, proof or independent review was inferred.

These were model-authored semantic choices carried by a small script, not a
claim that scripted answer acceptance proves semantic quality. The actual installed
wheel separately passed adoption, complete payload delivery, owner effects,
standard `.agents` exposure, neutral branch delivery, edited-source preservation
and exposure removal. That test proves installed protocol/lifecycle behavior;
it does not measure autonomous host judgment.

For the neutral source snapshot, full entry material was 417 bytes, the shared
question resource 714, and the selected visible/internal branches 184/197 bytes.
The fair plain-skill path reads the same entry, question and one selected branch,
not the whole tree. It needs no AW call for this draft-only task. The AW exercise
used 11 native entries across discovery, semantic answer/reentry and evidence
checks, including two authoring retries from incorrectly hashing CRLF bytes
instead of normalized LF text. Three deliberate semantic dispositions were
unknown, visible and internal. No owner publication or uncertain effect occurred.

AW adds exact source/evidence currentness, explicit unresolved carriage and lazy
selected delivery here; it does not demonstrate lower total cost for this tiny
example. Model token/latency cost, broader completion economics and longitudinal
payoff are unmeasured, not zero. The hash normalization pitfall is carried into the
skill authoring reference so users need not rediscover it. Native/protocol tests
and this bounded host observation do not establish cross-host support or release
admission.

## Evidence and residue boundary

Reuse lower-layer domain proof rather than repeating every owner matrix per skill.
At integration: resource owner/direct checks, skills-first parity, semantic
answer/currentness Rust checks and native frontier bounded-work check pass. The
installed wheel journey passes. Generated payload/reference checks and commit
hooks protect introducing surfaces. Passing validation is distinct from accepted
issue completion, full P1 intent satisfaction and measured operating cost.

The original design audit is retained as a historical design record linked here.
The existing lane plan retains remaining documentation and later external review
custody through its native owner; no new migration registry or knowledge backlog
is shipped. Unrelated unresolved source admissions and repository policy remain
with their owners. This record becomes historical evidence after acceptance, not
a new operating procedure or release gate.
