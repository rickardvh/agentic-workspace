# Current repository decision archive

On 2026-09-09, the real repository's `docs/decisions` archive was revalidated for
#3041/#3040. This is repository evidence for #2909, not a package convention for
other repositories or acceptance of the full decision-capture lifecycle.

The three existing records preserve shared Rust semantic authority, separate
identity lifetimes and selected-outcome terminality. Each retains its rationale,
rejected alternatives, scope, source references and original semantic authorship.
The agent's dated revalidation is an editorial contribution, not new human
authorship or a claim of authenticated human identity.

## Exact source reconciliation

The former archive admission, `ce9d6402c74f326120713e613f97578e1023feec`, still
matched the record bytes but their whole-document `SYSTEM_INTENT.md` dependency
was stale. Ordinary native resolution returned three stale states and no current
consequences. The intervening intent change adds repository-lifetime efficiency
guidance; it does not change these three decisions or their applicability.

Independent scoped review verified unchanged decisions, consequences, actors,
scope and supersession, and the updated dependency hash. The resulting reviewed
archive snapshot is `b4838e325757e523edbdccc51c27a769b8293f43`. The repository's
existing `assurance.decision_record_revision` explicitly admits this fixed
snapshot. It is not an automatically advancing HEAD reference. The admitted
intent dependency is
`sha256:5bce39a4fdbc3dd51e6a88e96f00f1160f8a3efaef14930177650e619fb5801d`.

## Ordinary fresh-process observations

The configured native executable performed each observation in a fresh process
against the lived-in checkout. No fixture admission replaced the real sources.
Use the configured invocation from the repository adapter with these public
`start` arguments to reproduce the bounded source observations:

| Work and changed path | Observed decision continuity |
| --- | --- |
| `--task "Change the shared compiler to preserve current effect identity during retry" --changed crates/agentic-workspace-core/src/lib.rs --format json` | Three current repository consequences, exact path scopes, no stale dependencies; rationale is available through returned requests. |
| `--task "Correct a spelling error in the contributor introduction" --changed CONTRIBUTING.md --format json` | No decision context and no rationale requests. Other ordinary owners retain their own task decisions. |
| First case plus its returned `decision-continuity/read-current-source/v1` request for `docs/decisions/identity-lifetimes.md` | Exact current source body; `authority_effect = no-new-authority`. |

Selection is based on declared exact paths, not words in the task. The quiet case
does not claim that unrelated Planning or Assignment decisions disappear. These
read-only observations did not write decisions or grant completion/proof.

## Historical source and negative controls

The April fallback note remains byte-for-byte present with revision
`sha256:06332182cc9f0085b4c0d6d2bdf132ff607c8eb339b1c403f3700ac9031100fa`.
That exactly matches the existing human-directed `retire` disposition in the
Memory manifest. It is inactive rather than promoted or silently deleted; the
retirement does not endorse historical claims in the note.

The existing public regression
`tests/test_native_public_cli.py::test_public_read_real_repository_decision_preserves_currentness`
passed all eight repository/fallback cases across native, JSON, Python and
TypeScript consumers. The cases preserve real rationale and admitted authority
basis while separately proving quietness, changed-source rejection and receiving
owner admission. Temporary fixture admissions are not independent repository
acceptance.

Nine existing shared-core cases also passed: scoped supersession retains old
rationale without reviving its constraint, native supersession uses the existing
contract, and stale or self-asserted source authority cannot become current.
The archive remains ordinary readable Markdown when AW is absent. No ADR
generator, new registry, archive-wide workflow or copied Memory rationale was
added. Broader #2570/#3040 capture, promotion and learning obligations remain
with their existing owners.

## Stronger-owner output and no-retention contrast

The ordinary `decision_sources.capture` request now nominates material directly
to a configured repository archive supporting the existing decision-source
contract. It does not first create a fallback Memory record. The owner constructs
the complete Markdown postimage and confirm/defer request, including current
scope, dependencies, selected decision context, policy/capability revisions and
the destination convention. `no-retention` returns an exact bounded disposition
without a file or publication attempt. Ordinary work without nominated material
does not acquire a decision review phase.

The repository and fallback owners share bounded proposal/publication checks
but keep separate effect, source and durable ownership. Only fallback publication
updates Memory's manifest. Repository publication remains ordinary Markdown,
and later rationale reads retain their non-authoritative boundary. Existing
immutable publication carriers support recovery and detect a lost repository
source; they are not a second canonical decision archive. Serialized carriers
are bounded before admission, and configured source protection applies to their
exact write destinations, including recovery custody.

`tests/test_native_repository_decisions.py` uses the actual shared-semantic
decision and this repository's existing archive convention in isolated temporary
repositories. It proves repository output, no-retention, fresh affected recall,
unrelated quietness, source/convention loss, bounded publication and recovery
through the public consumers. Fixture answers prove the mechanism, not new
authorization to create a real repository decision. No artificial ADR or April
note promotion is added to the lived-in archive.

This does not establish an agent grant that current policy lacks. The current
bounded human path remains the fallback for that absence; explicit delegated
authority and other repository conventions require their actual admitted owner
contracts. Broader #3040 authority/disposition and #2909 lifetime acceptance remain
open. The archive path here remains repository dogfood, not a portable default.
