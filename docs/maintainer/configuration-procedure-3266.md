# Selected configuration-method adaptation (#3266)

## Selected friction and owner

The issue's pre-implementation selection named `workspace-setup-jumpstart` and
the current Configuration procedure. Accepted #3303/#3313/#3314 and module
#3316–#3319 work showed why saved fields must be distinguished from actual
consumer behavior. This change implements that selected case through the normal
repository/package owner; recurrence did not authorize a running process to
modify its method or approve its replacement.

The old skill told the model to invoke a write, resolve again, then manually
reconstruct each affected behavior using field-specific paragraphs. The new
bundle's `prepare.py` carries exact current requests or one already-authorized
Configuration action. Native Configuration projects the affected owner's fresh
observation from the existing post-effect continuation. A read-only behavior
request provides the same observation without a write. Source publication,
consumer observation, semantic satisfaction and independent review remain
separate facts.

No new configuration writer, source parser, semantic policy interpreter,
workflow language, adaptation store or action loop was added. Existing source,
work, safety, restriction and effect custody checks remain authoritative even
when the optional helper is bypassed. Unsupported writer fields remain gaps.

## Representative fresh-consumer proof

`tests/test_configuration_procedure.py` starts the shipped helper in fresh Python
processes without chat history. Through the paired native binaries it demonstrates:

- An exact authorized instruction-source edit delivers the selected current
  source through the startup-adapter owner, including its actual body/read state.
- A logging privacy edit changes the effective native policy and the transport
  actually captures a redacted event. Enablement alone is not capture success.
- A human-owned Assignment guarantee is observed through the existing current
  task judgment and behavior request set. The native candidate is ineligible with
  `required-execution-guarantee-unavailable`; the method separately reports that
  these declarations are outside the Configuration writer.
- One module enablement example retains the Memory owner's absent/admission gap.
  It cannot manufacture module state or a manifest writer from the saved value.
- Reusing an old action is rejected before effect; native continuation failures
  preserve the confirmed source write and yield unavailable behavior with no
  retry. The existing Rust continuation fixture now checks this consequence too.
- Changed helper/skill material rejects a carried method revision before native
  calls. An unrelated file leaves it current. Native executable material also
  names the same canonical helper and skill dependencies in the existing registry.
- `--no-change` is a successful no-retention disposition with zero owner calls.

The Assignment example deliberately uses the human/source boundary already
named by the owner. The helper does not gain a missing setter. Actual module
proof semantics remain with their owners and the existing module tests; there
is no new all-module mutation matrix.

## Subtraction and operating burden

The configuration skill shrank from the preceding branch's 169-line walkthrough
to a 97-line executable-method guide. The repeated field-by-field save/re-resolve
protocol and manual consumer inference were removed. Current selected branch
detail comes from the native owner; authority guidance and optional deferred,
payload and exposure lifecycles remain concise.

For an already-authorized write, the method performs one CLI invocation. Native
preflight, effect admission and the existing post-effect fresh resolution still
occur internally; they are not zero-cost. The redundant additional model-driven
`start` is no longer prescribed. Read-only concern discovery uses two CLI calls
(discover the exact request, consume it), and carrying an existing request set
uses one. No-change uses none. Python process startup and method hashing are
additional costs; full native output is retained rather than silently dropping
recovery material. No model-token, provider-price or longitudinal ROI claim was
measured from these deterministic fixtures.

Payload declarations, registry resources, canonical/shipped copies, and the
artifact-derived provenance were reconciled together. The provenance refresh
used the current native Configuration writer and its exact authorized proposal;
it did not advance a trust pin or source interpretation. Outstanding source
admissions and aggregate completion claims remain with their owners.

Ready for independent review. The method cannot approve its own replacement;
this evidence is implementation validation, not independent acceptance or
aggregate #3277 completion.
