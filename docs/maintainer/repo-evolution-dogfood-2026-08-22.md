# Repository evolution dogfood — 2026-08-22

This is the real-session companion to `tests/fixtures/repo_evolution_scenario.json`.

The repository began at current `master` and the work proceeded as three ordinary stacked PR slices. No sync phase, background watcher, or manual package-file repair was inserted between product steps.

Observed sequence:

1. Planning shaped issues #2661–#2666 and wrote their issue relations and lane through its canonical operations.
2. The first implementation changed the context-authority registry and regenerated command packages from command IR. A real root Planning adapter defect dropped semantic option values; the source IR and generated projections were corrected together.
3. The structured-file check found that the newly created issue-relation class lacked inventory coverage. The contracts owner received the smallest classification update; no unrelated repository state was rebuilt.
4. Tightening the next execplan changed its Planning revision. Startup rejected the stale local owner selection and named the exact Planning owner-select operation. The acting agent executed that typed transition; an equivalent replay returned the Planning-owned no-op receipt.
5. The second slice admitted machine- and agent-observed coverage facts through bounded adaptations and proved canonical proof, scoped-instruction, and Memory destinations. Stale owner revisions failed closed.
6. The final slice made every advertised instruction choice an actual source mutation. Admit, update, retain, dismiss, and trigger-bounded defer are persisted by `instructions.create`; a new runtime reads the persisted disposition and remains quiet until source semantics or the defer trigger changes.
7. The maintained replay now executes generator publication, Planning selection, git-topology invalidation, module capability removal, validation-manifest reconstruction, machine coverage classification, and semantic persistence through their real owners.

Counts:

- Explicit generic AW-maintenance actions: 0.
- User semantic decisions requested during implementation: 0 (the fixture deliberately exercises one modeled semantic choice).
- Agent-executed typed reconciliation transitions: 2 (Planning owner selection and persisted instruction disposition).
- Redundant rediscovery after canonical admission: 0 in the maintained replay.
- Fresh-runtime semantic replay: quiet.

Follow-up result: the prior semantic-choice mismatch is closed. The runtime no longer advertises metadata-only alternatives or a generic maintenance action. Each advertised choice names the real `instructions.create` contract, supplies only declared inputs, writes a canonical disposition, and is proven quiet from a newly reconstructed projection. Source or trigger changes deliberately reactivate the decision.

Conclusion: the deterministic scenario is representative of the drift and coverage classes encountered in this real stack. Genuine semantics remain intentional user/domain decisions; once chosen, canonical owner state prevents rediscovery while deterministic owner transitions remain ordinary work.
