# Repository evolution dogfood — 2026-08-22

This is the real-session companion to `tests/fixtures/repo_evolution_scenario.json`.

The repository began at current `master` and the work proceeded as three ordinary stacked PR slices. No sync phase, background watcher, or manual package-file repair was inserted between product steps.

Observed sequence:

1. Planning shaped issues #2661–#2666 and wrote their issue relations and lane through its canonical operations.
2. The first implementation changed the context-authority registry and regenerated command packages from command IR. A real root Planning adapter defect dropped semantic option values; the source IR and generated projections were corrected together.
3. The structured-file check found that the newly created issue-relation class lacked inventory coverage. The contracts owner received the smallest classification update; no unrelated repository state was rebuilt.
4. Tightening the next execplan changed its Planning revision. Startup rejected the stale local owner selection and named the exact Planning owner-select operation. The acting agent executed that typed transition; the user was not asked to inspect or edit Planning files.
5. Planning then detected two live lane slices. While building this seam, the completed prior slice was retired by a direct checked-in Planning state edit and the next summary was quiet for that condition.
6. The second slice admitted machine- and agent-observed coverage facts through bounded adaptations and proved canonical proof, scoped-instruction, and Memory destinations. Stale owner revisions failed closed.
7. The final slice added the semantic decision boundary and the maintained multi-change replay. Genuine path-boundary ambiguity is presented as a user/domain choice; deterministic regeneration bypasses that path.

Counts:

- Explicit generic AW-maintenance actions: 1 (the direct prior-slice lifecycle edit).
- User semantic decisions requested during implementation: 0 (the fixtures deliberately exercise two modeled semantic choices).
- Agent-executed typed reconciliation transitions: 1 (Planning owner selection).
- Redundant rediscovery after canonical admission: 0 in the maintained replay.

Mismatch retained honestly: before the #2663 composition seam existed, the real Planning revision change still required the acting agent to execute the exact typed owner-select command returned by startup. Retiring the prior live slice was also performed as one direct checked-in Planning state edit rather than as a typed runtime transition. Neither required user diagnosis, but both were visible maintenance work. The deterministic replay now asserts that equivalent owner-authorized drift is emitted as the ordinary primary action, is baseline guarded, and becomes quiet after currentness is restored. Future dogfood should reopen #2663 if the command is again only advisory, repeatedly reissued, or if lifecycle retirement again needs a direct state edit.

Conclusion: the deterministic scenario is representative of the drift and coverage classes encountered in this real stack. The evidence supports configure-once behavior with one explicit boundary: genuine semantics remain intentional user/domain decisions, while executable owner transitions may still be carried out by the acting agent as part of ordinary work.
