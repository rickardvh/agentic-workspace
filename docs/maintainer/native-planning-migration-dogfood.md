# Real-source native Planning continuation

This 2026-09-09 evidence addresses #2970's migration and continuation outcome on
the #3138 reconstruction stack. It is separate from full #2909 conformance and
exact #2990 release-artifact admission.

## Real sources and retained meaning

The public tests begin with the repository's actual
`.agentic-workspace/planning/execplans/delegation-lane-sweep.plan.json` and
`v1-contraction-2983-2990.plan.json`. They do not create an intermediate
`planning.json.active` representation. Local and shared selectors remain
distinct: recognition of a former local selector cannot grant write custody;
its exact owner-issued transfer answer is bound to the observed source.

Reconciliation preserves scope, constraints, dependencies, stops, declared proof,
frontier and continuation. Controlled return cases add explicit assignment,
returned and integration-pending relationships to the real source and verify
their preservation across fresh processes. Those observations prove continuity,
not worker authorship or successful integration. The separate native patch
lifecycle under #3179 proves actual returned-result integration and typed
Planning adoption before checked target-outcome evidence.

Historical fields are dispositioned explicitly. In the history control,
`drift_log` first makes coverage ambiguous; an explicit irrelevant-history
judgment records that omission without copying its prose into native custody.
Active dependency, assignment, returned and integration-pending relationships
remain preserved. No archive inventory is bulk-imported. The lived-in selected
owner's current coverage reports no ambiguities or omitted history.

## Lived-in update and fresh re-entry

The native public owner updated this repository's selected
`v1-contraction-2983-2990` frontier and next action to retain the published stack
and the decision-archive evidence. The owner returned `planning.update`; the
exact invocation applied before a newly returned `planning.reconcile` acquired
the updated source. Both actions used their original public task/changed context
and returned source requests. No private intent dictionary or direct managed
Planning edit supplied the transition.

The source update at commit `7264cbf0b` changed exact source revision
`sha256:0aeda997913f07c0813bc9061e50d7280899f6ada34da4e068d72eea12c0ca1a`
to `sha256:bdb2cdd4f515bcea1962853566776318afc13914072542bfb082ada0e8867bab`.
After reconciliation, fresh native resolution observed `current = true`, complete
coverage and subject revision
`sha256:734f18da0ad14a725de461c92e7a7ba9a373710be5e2a9426a8827d6364cadfb`.
An additional fresh process retained that semantic revision and the updated
frontier. Local custody is checkout-bound; portable source provenance is not a
grant for another checkout to become its writer.

## Regression protection and limits

CI now explicitly runs the real-source public continuation/mutation cases across
native, JSON, Python and TypeScript, alongside new-owner creation/selection and
unrelated-work quietness. The existing shared-core release step already runs the
semantic-versus-attempt, history-coverage and real-execplan reconciliation cases.
This adds public coverage to that gate instead of another migration fixture or
Planning workflow.

The first focused run passed 28 public cases and exposed four Windows binding
test failures before AW execution: the consumer harness put the complete growing
plan into process arguments. The harness now transports Python/TypeScript input
over stdin without truncating or changing the owner material. Native and JSON
already used bounded input streams. This repairs the evidence carrier; it does
not relax any owner assertion or change the product APIs.

Fifteen focused shared-core/creation/quietness cases passed, including stable
semantic identity across attempts/returns and staleness on material scope change.
The public CI selection is reproduced with:

```text
uv run pytest tests/test_native_public_cli.py tests/test_native_planning_create.py -k "real_former or requirements_preserve_planning or unrelated_claim_request_keeps_planning or public_creation_then_separate_selection or quiescent_selected_owner" -q
```

After the stdin correction, that exact expanded selection passed all 40 cases
on Windows. Independent scoped review found no remaining substantive issue in
the transport correction, CI selection or bounded Planning acceptance evidence.

These cases distinguish task applicability from source transfer, preserve
unrelated bytes, reject stale requests and foreign custody, recover uncertain
publication, and keep direct work stateless. Planning readiness does not grant
Verification success, selected-outcome terminality or broader release readiness.
