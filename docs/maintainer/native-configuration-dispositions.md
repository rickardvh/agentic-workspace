# Native configuration authority dispositions

The native configuration reader preserves current source references and revisions. An unsupported field remains unresolved at its current boundary. Persistence alone does not make an advisory method binding (#2613, #2606, #2767).

This layer handles only `workspace.optimization_bias` and workflow obligations with explicit `force = "recommended"` and known fields. Their full values remain in the source-owned projection with advisory authority, unresolved agent applicability, and no evidence satisfaction. Their revisions change with the source. They grant no effects or claims. The optimization policy limits its meaning to rendering density; it cannot change execution, delegated judgment, proof, or canonical state. Recommended methods remain available to the agent at their declared stage and scope.

| Current control group | Native disposition / remaining owner |
| --- | --- |
| Optimization bias; two recommended workflow obligations | Preserved advisory values; no task veto |
| Required-before-closeout workflow obligations | Existing unresolved boundary retained; stage/scope alone do not identify every affected claim |
| Assurance profiles and route selectors | Verification selection/closeout owner gap; not satisfied by parsing |
| CLI compatibility | Advisory enforcement does not waive structural resource, schema, or reader requirements |
| Payload required-before-work | Genuine source-current compatibility boundary |
| Initiative (`improvement_latitude`) | Preserve human initiative; current #2648 owner transfer remains unresolved |
| Modules, system-intent selectors, adapter/artifact controls | Current intent retained; native owning semantics incomplete |
| Upgrade metadata | Scope-specific upgrade consumption remains unresolved; not silently discarded |
| Delegation policy and targets | Binding assignment, eligibility and transport remain actual owner gaps |
| Runtime capability summaries | Host facts need current owning discovery; configuration cannot manufacture capability |

The audited configured checkout had 39 residuals. This correction changes only three advisory dispositions, leaving the other 36 owner residuals intact. Unknown future fields within a recommended obligation retain the previous unresolved blocker rather than acquiring an assumed advisory meaning. Local command and human-review ceilings remain independent restrictions.

Public native, JSON, Python and Node consumers test actual repository policy values, source drift, unchanged bytes, direct/no-source behavior, and hard/unknown controls. Temporary-source fixtures omit repository-specific admission pins because those name sources outside the fixture. No provider inference, independent acceptance, owner closure or complete native configuration support is claimed.

The canonical `advanced_features` field currently names optional reusable diagnostics, explicitly outside ordinary startup requirements. Native projection retains those source values as advisory diagnostic preferences, with no ordinary task veto or proof/effect grant. This does not revive the old posture framework or retire human initiative: `improvement_latitude` still requires its current #2648 owner. Source revisions and unsupported future values remain validated through the existing schema. The historical residual counts above describe the earlier layer, not the present cumulative checkout.
