---
name: memory-refresh
description: Reassess only Memory selected by the current changed paths and preserve useful lessons while currentness is unresolved.
---

# Memory Refresh

Run the configured AW `start --target . --task "<task>" --format json`, repeating `--changed <path>` for affected paths. Follow the decision packet and exact Memory reads; a code change does not justify a corpus sweep.

For each selected lesson, use [Memory Hygiene](../memory-hygiene/SKILL.md) for current retain, retire or promotion requests. Preserve uncertain former material. A changed source hash is not factual confirmation, and promotion is not established by naming a destination.

If the necessary correction is a prose rewrite, routing change or new capture that the current native owner does not offer, retain the smallest useful finding with the existing owner and state the missing operation. Active work and continuation stay with Planning; command outputs and scratch stay under `.agentic-workspace/local/`.
