interface:
  display_name: "Bootstrap Upgrade"
  short_description: "Upgrade an existing bootstrap install safely."
  default_prompt: "Use $bootstrap-upgrade to upgrade memory for the target repository. Let the tool determine the installation source automatically, run the upgrade, and report any conservative manual-review items."
