description: Finish bootstrap upgrade review from the target repo's temporary bootstrap workspace.
prompt:
  default_prompt: "Use $upgrade from the repo's `memory/bootstrap/skills/` path to finish bootstrap upgrade review conservatively."
