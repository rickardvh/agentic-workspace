interface:
  display_name: "Memory Upgrade"
  short_description: "Upgrade the checked-in memory scaffold for this repo."
  default_prompt: "Use $memory-upgrade to upgrade memory for the current repository. Run `agentic-memory-bootstrap upgrade --target <repo>` for this repo, use the recorded upgrade source automatically, and do not rewrite repo-owned notes unless explicitly asked."
