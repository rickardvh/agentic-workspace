## Memory maintenance

If a change affects behaviour described in `/memory`, update the relevant memory note in the same change.

Review trigger metadata and `Applies to` fields when touched files or interfaces change.
