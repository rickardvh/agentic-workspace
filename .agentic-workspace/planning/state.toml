[todo]
active_items = []
queued_items = []

[roadmap]
lanes = [
  { id = "machine-first-planning-state", title = "Machine-first planning state and closeout hygiene", priority = "first", issues = ["#261", "#280", "#282", "#307", "#323", "#324", "#325"], outcome = "Make planning state, references, retention, closeout learning, archive cleanup, and lower-trust evidence reconciliation machine-first so agents can resume, review, archive, and promote durable knowledge without rereading prose-heavy plan history.", reason = "#324 is the immediate gating audit: prior no-active-plan lane closeouts must be reconciled before existing lanes are treated as satisfied or closed. #323 and #325 add concrete cleanup and evidence-state follow-through.", promotion_signal = "Promote before closing any existing roadmap lane, or when active-plan recovery, archive review, archive cleanup, closeout audit, or follow-on routing still requires reconstructing machine-readable state from prose, commits, or issue history.", suggested_first_slice = "Start with #324: produce a compact checked-in audit that maps each lower-trust closed lane or issue to fully satisfied, bounded slice only, covered by follow-up, or needs reopening." },
  { id = "formal-workflow-substrate-follow-through", title = "Formal workflow substrate follow-through", priority = "second", issues = ["#278", "#327"], outcome = "Move more workflow authority from markdown-first contracts into a stricter, composable substrate without turning the product into a heavyweight workflow engine.", reason = "The structured config substrate landed, but #327 shows branch/workflow posture can still remain implicit before implementation and push.", promotion_signal = "Promote when ordinary work still depends on markdown workflow prose as the primary operational authority, or when branch/workflow posture remains implicit before implementation and push.", suggested_first_slice = "Add the smallest branch-posture or implementation-readiness signal that distinguishes staying on the same branch from being safe to commit directly to the default branch." },
  { id = "local-only-memory-continuity", title = "Local-only memory continuity", priority = "third", issues = ["#328"], outcome = "Add an opt-in local-only memory surface for repo-scoped machine-local knowledge that helps agents on the same machine continue work without promoting private, weak, experimental, or environment-specific context into checked-in Memory.", reason = "#328 identifies a gap between transient chat and checked-in Memory: useful local knowledge can help mixed-agent continuation, but it must stay disabled or explicit through local config, removable, non-authoritative, and promotion-only by deliberate action.", promotion_signal = "Promote after the #324 audit or when same-machine agent handoff keeps losing useful local-only context that is not appropriate for checked-in Memory.", suggested_first_slice = "Define the local config switch, ignored storage path, lightweight record shape, compact report output, and manual promotion guidance without creating a broad new memory subsystem." },
  { id = "python-consumable-contract-extraction", title = "Python-consumable contract extraction", priority = "fourth", issues = ["#258", "#299", "#300", "#301", "#302", "#303", "#305", "#309", "#310", "#311", "#312", "#313", "#314", "#315", "#316", "#320"], outcome = "Make the current Python implementation consume implementation-independent contracts, schemas, manifests, templates, and operation specs as the authoritative source for extractable behavior-adjacent content.", reason = "Most extraction slices landed, but #320 leaves stable authority-marker policy data authored in `cli.py`, and #258 remains the broader declarative-portability parent.", promotion_signal = "Promote after the #324 audit or sooner if Python-owned contract authority blocks reviewed extraction work, generated-interface preparation, or contract parity checks.", suggested_first_slice = "Start with #320: extract authority marker policy data into a checked-in contract while preserving Python-owned path matching behavior." },
  { id = "further-product-compression-on-repeated-pressure", title = "Further product compression on repeated pressure", priority = "fifth", issues = ["#230", "#275", "#281", "#319", "#326"], outcome = "Continue removing, demoting, or internalizing visible product residue only when repeated real use shows another surface still imposes first-line cognitive cost.", reason = "#281, #319, and #326 show remaining visible-residue and adapter-drift pressure across generated compatibility artifacts, maintainer refresh, and memory runbook rationale growth.", promotion_signal = "Promote when repeated dogfooding shows another visible surface behaves like a first-line handbook, leaves startup ambiguity, keeps unnecessary generated adapter residue in the default mental model, leaves checked-in adapters stale, or lets memory runbooks accumulate rationale that belongs in a smaller decision surface.", suggested_first_slice = "Audit generated compatibility artifacts and memory runbook residue, then prove which surfaces should disappear, become static routing tables, move to decision notes, or still earn their keep." },
  { id = "generated-tool-conformance", title = "Generated-tool conformance", priority = "sixth", issues = ["#306"], outcome = "Prepare a black-box conformance layer so generated CLIs, local MCP tools, static skills, or other adapters can be tested against shared operation contracts instead of trusted by implementation path.", reason = "The first conformance slice landed, but the lane should remain visible until #324 verifies whether the broader generated-tool intent is fully satisfied or only bounded-slice satisfied.", promotion_signal = "Promote if the #324 audit finds generated-tool conformance still lacks adapter coverage, negative cases, or parity checks for replaceable implementations.", suggested_first_slice = "After #324, decide whether the read-only conformance harness is enough or whether another generated-tool scenario should be added." },
  { id = "system-intent-operational-follow-through", title = "System intent operational follow-through", priority = "seventh", issues = ["#279"], outcome = "Finish making compiled system intent an explicit planning-time input so slice shaping and larger-intent validation can use it without adding heavy ritual.", reason = "The planning hook landed, but this lane should remain until #324 verifies that system-intent satisfaction was evidenced rather than assumed.", promotion_signal = "Promote if planning still has to infer relevant system intent manually during slice shaping or larger-intent validation after the closeout audit.", suggested_first_slice = "After #324, verify whether the system-intent alignment fields are sufficient or need a smaller follow-up." },
  { id = "pre-ingestion-refinement-templates", title = "Pre-ingestion refinement templates", priority = "eighth", issues = ["#290"], outcome = "Make strong general-purpose reasoning cheaper to apply before coding-agent ingestion through lightweight refinement templates.", reason = "The template slice landed, but this lane stays visible until #324 verifies whether the larger pre-ingestion intent is fully satisfied or needs more templates/proof.", promotion_signal = "Promote if issue refinement still enters coding-agent work with avoidable ambiguity after the closeout audit.", suggested_first_slice = "After #324, inspect whether the initial templates cover the recurring ambiguous intake cases or need one more bounded template." },
  { id = "local-only-integration-area", title = "Local-only integration area", priority = "ninth", issues = ["#287", "#288"], outcome = "Create a sanctioned local-only integration area for vendor/runtime-specific bridges without turning it into shared workflow state.", reason = "The root and boundary slice landed, but this lane should remain visible until #324 verifies the parent intent and any runtime-example follow-up. Keep it distinct from #328: integration shims are local execution aids; local-only memory is advisory machine-local knowledge.", promotion_signal = "Promote if local users still put disposable integrations into shared workflow surfaces or if runtime-specific examples are needed after closeout audit.", suggested_first_slice = "After #324, decide whether the local-only boundary is complete or needs one example/runtime bridge convention." },
  { id = "graceful-partial-compliance", title = "Graceful partial compliance and bypass trust", priority = "tenth", issues = ["#292", "#293", "#295", "#296", "#297", "#298"], outcome = "Make package startup, proof selection, boundary warnings, adapter authority, and compact operating surfaces easier for generic or bounded agents to discover and cheaper to use.", reason = "The visible follow-through slices landed, but the no-active-plan run itself proves this lane should not be closed until #324 audits whether generic-agent compliance and bypass trust were actually improved enough.", promotion_signal = "Promote if #324 or later dogfooding shows startup, compact-surface cost, proof selection, boundary confusion, or lower-trust review still fails materially.", suggested_first_slice = "After #324, either keep this lane open with a concrete follow-up or mark the satisfied slices as evidenced without closing the lane prematurely." },
]
candidates = [
  { priority = "first", summary = "Machine-first planning state and closeout hygiene" },
  { priority = "second", summary = "Formal workflow substrate follow-through" },
  { priority = "third", summary = "Local-only memory continuity" },
  { priority = "fourth", summary = "Python-consumable contract extraction" },
  { priority = "fifth", summary = "Further product compression on repeated pressure" },
  { priority = "sixth", summary = "Generated-tool conformance" },
  { priority = "seventh", summary = "System intent operational follow-through" },
  { priority = "eighth", summary = "Pre-ingestion refinement templates" },
  { priority = "ninth", summary = "Local-only integration area" },
  { priority = "tenth", summary = "Graceful partial compliance and bypass trust" },
]
