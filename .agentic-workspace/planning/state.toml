# Agentic Workspace managed state.
# Do not edit by hand when the CLI is available.
# Inspect: uv run agentic-workspace summary --format json
# Mutate through the package command named by that output.

kind = "agentic-planning-state"
schema_version = "planning-state/v1"

work_items = []

[active]
execplans = []

[todo]
active_items = []
queued_items = []

[roadmap]
lanes = [
  { id = "lane-1392-open-module-participation", title = "Define open module participation and workflow obligations", maturity = "ready", status = "next", surface = ".agentic-workspace/planning/lanes/lane-1392-open-module-participation.lane.json", owner_surface = ".agentic-workspace/planning/lanes/lane-1392-open-module-participation.lane.json", outcome = "Keep direct #1392 runtime posture follow-up slices routable after the model/contract/docs/test PR slice.", reason = "PR #1393 makes the #1392 model slice claimable, but runtime task-posture emission, packet schema synchronization, posture closeout/report visibility, and dynamic AGENTS.md projection semantics must stay discoverable through normal Planning routes until deliberately promoted, dismissed, or superseded.", promotion_signal = "Promote when runtime posture behavior is selected for implementation, especially before #1390/#1391 or runtime work depends on task_posture_packet emission or dynamic instruction projection.", refs = ["GitHub #1389", "GitHub #1392", "PR #1393"], next_action = "Promote one of the four planned #1392 runtime posture slices: runtime task-posture emission; shared task_posture_packet schema synchronization; closeout/report posture visibility; dynamic AGENTS.md projection runtime semantics.", done_when = "The four direct #1392 runtime posture follow-up slices have been promoted, dismissed with evidence, or superseded by an explicit Planning owner.", proof = "Planning summary shows lane-1392-open-module-participation as a roadmap lane; structured inventory and planning doctor pass.", review_role = "planner", handoff_ready = true },
]
candidates = [
  { id = "github-1389-workspace-simplify-agent-facing-surfaces-and-wor", maturity = "candidate", status = "next", priority = "P1", refs = "GitHub #1389", title = "[Workspace]: Simplify agent-facing surfaces and workflows", outcome = "Route the upstream issue into a bounded Agentic Workspace slice before implementation.", reason = "Open upstream issue from refreshed external intent evidence; priority P1 inferred from inferred-lane.", promotion_signal = "Promote when this issue is selected for implementation or grouped into a bounded lane.", suggested_first_slice = "Inspect the issue body, choose the smallest workflow shape, and record exact proof before closeout." },
  { id = "github-1390-workspace-define-the-ordinary-agent-continuity-l", maturity = "candidate", status = "next", priority = "P2", refs = "GitHub #1390", title = "[Workspace]: Define the ordinary agent continuity loop and classify visible surfaces", outcome = "Route the upstream issue into a bounded Agentic Workspace slice before implementation.", reason = "Open upstream issue from refreshed external intent evidence; priority P2 inferred from inferred-slice.", promotion_signal = "Promote when this issue is selected for implementation or grouped into a bounded lane.", suggested_first_slice = "Inspect the issue body, choose the smallest workflow shape, and record exact proof before closeout." },
  { id = "github-1391-workspace-investigate-simplification-of-operatin", maturity = "candidate", status = "next", priority = "P2", refs = "GitHub #1391", title = "[Workspace]: Investigate simplification of operating-loop substeps", outcome = "Route the upstream issue into a bounded Agentic Workspace slice before implementation.", reason = "Open upstream issue from refreshed external intent evidence; priority P2 inferred from inferred-slice.", promotion_signal = "Promote when this issue is selected for implementation or grouped into a bounded lane.", suggested_first_slice = "Inspect the issue body, choose the smallest workflow shape, and record exact proof before closeout." },
]
