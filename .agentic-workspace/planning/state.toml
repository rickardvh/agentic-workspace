# Agentic Workspace managed state.
# Do not edit by hand when the CLI is available.
# Inspect: uv run agentic-workspace summary --format json
# Mutate through the package command named by that output.

kind = "agentic-planning-state"
schema_version = "planning-state/v1"

work_items = []

[active]
execplans = []

[todo]
active_items = []
queued_items = []

[roadmap]
lanes = []
candidates = [
  { id = "github-1227-workspace-define-closeout-reporting-profiles-and", maturity = "candidate", status = "next", priority = "P1", refs = "GitHub #1227", title = "[Workspace]: Define closeout reporting profiles and escalation policy", outcome = "Define the minimal closeout report profiles and the escalation rule that chooses them.", reason = "First implementation dependency: report rendering, startup routing, checks, and docs all need this policy before their behavior can be judged.", promotion_signal = "Promote when starting the operator-facing closeout reporting work.", suggested_first_slice = "Add the profile and escalation contract with targeted tests for default and strict or high-risk selection." },
  { id = "github-1228-workspace-add-derived-operator-facing-closeout-r", maturity = "candidate", status = "next", priority = "P1", refs = "GitHub #1228", title = "[Workspace]: Add derived operator-facing closeout report projection", outcome = "Add a read-only closeout report projection covering completed work, interpreted intent, changes, validation, gaps, and closure boundary.", reason = "Core operator value after #1227: this is the surface that turns the policy into useful closeout output.", promotion_signal = "Promote after #1227 lands or together with #1227 if the policy/projection slice stays bounded.", suggested_first_slice = "Implement the derived report projection without introducing new mutable planning state." },
  { id = "github-1230-workspace-surface-expected-closeout-report-profi", maturity = "candidate", status = "next", priority = "P2", refs = "GitHub #1230", title = "[Workspace]: Surface expected closeout report profile in startup and report routing", outcome = "Expose the expected closeout report profile, reason, escalation source, and next command in startup/report routing.", reason = "Discovery aid that depends on #1227 policy and helps agents choose the right report profile before final closeout.", promotion_signal = "Promote after the profile policy exists and before broad closeout usability validation.", suggested_first_slice = "Add the compact routing field and selector-backed detail command." },
  { id = "github-1229-workspace-add-closeout-traceability-rows-from-in", maturity = "candidate", status = "next", priority = "P2", refs = "GitHub #1229", title = "[Workspace]: Add closeout traceability rows from intent to evidence", outcome = "Add traceability rows that link intent or requirement, surface, evidence, status, and residual risk or follow-up.", reason = "Strengthens the report projection after its base shape exists; useful for non-trivial work and audit profiles.", promotion_signal = "Promote after #1228 establishes the report projection shape.", suggested_first_slice = "Add rows for non-trivial work and tests that prove each row is derived from existing evidence." },
  { id = "github-1231-workspace-add-closeout-report-completeness-check", maturity = "candidate", status = "next", priority = "P2", refs = "GitHub #1231", title = "[Workspace]: Add closeout report completeness checks", outcome = "Flag missing evidence, residual risk, follow-up ownership, and intent-boundary gaps in closeout reports.", reason = "Quality gate that needs the projection and traceability surfaces to exist before completeness can be evaluated reliably.", promotion_signal = "Promote after #1228 and preferably after #1229.", suggested_first_slice = "Add complete, partial, and incomplete report fixtures with strict/high-risk behavior covered." },
  { id = "github-1232-workspace-document-the-operator-facing-closeout-", maturity = "candidate", status = "next", priority = "P3", refs = "GitHub #1232", title = "[Workspace]: Document the operator-facing closeout reporting contract", outcome = "Document the closeout reporting contract, boundaries, escalation expectations, and residue routing.", reason = "Documentation should trail the settled contract enough to avoid recording speculative behavior, while still closing the lane with durable operator guidance.", promotion_signal = "Promote after the core contract and report behavior are implemented.", suggested_first_slice = "Document the implemented profile contract and link the relevant startup/report surfaces." },
]
