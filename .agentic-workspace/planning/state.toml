[todo]
active_items = [
  { id = "payload-audit-drift-classification", status = "in-progress", title = "Classify source payload audit drift instead of reporting raw mismatches", source = "github:#446", why_now = "After #445 reduced manual external-intent refresh work, #446 is the next #442 child slice and reduces false-positive proof friction.", surface = ".agentic-workspace/planning/execplans/payload-audit-drift-classification.plan.json" },
]
queued_items = []

[roadmap]
lanes = [
  { id = "long-session-dogfooding-intent-loop", title = "Address long-session dogfooding findings for intent continuity", priority = "1", issues = ["#442", "#445", "#446", "#447", "#448"], outcome = "make plan -> implement -> evaluate continuation behavior first-class enough that long issue lanes do not depend on chat memory or manual user detection.", reason = "#445 landed the external intent refresh adapter; #446 is now active to reduce false-positive proof friction from raw payload drift.", promotion_signal = "continue the active checked-in execplan before starting another broad lane.", suggested_first_slice = "#446, already active; then #447 and #448 in that order." },
  { id = "planning-payload-boundary", title = "Reclassify planning payload into core, optional, on-demand, and extraction candidates", priority = "2", issues = ["#461", "#462", "#463", "#464", "#465", "#466"], outcome = "make planning smaller by default and prevent planning from absorbing every useful agent-operation capability.", reason = "new intake #461-#466 is the clearest product-shape lane: classify before shrinking, config-gating, or extracting planning-adjacent capabilities.", promotion_signal = "promote after #445 or sooner if planning payload/default-install size blocks the active work.", suggested_first_slice = "#462, because payload classification is the prerequisite for #463, #464, #465, and #466." },
  { id = "feature-tier-and-surface-merge", title = "Make advanced features opt-in through feature tiers and merged surfaces", priority = "3", issues = ["#449", "#450", "#451", "#452", "#453", "#454", "#455", "#456", "#457", "#458", "#459", "#460"], outcome = "make package use cheaper than bypassing it by shrinking default setup and merging advanced maintenance, reconciliation, review, and codegen surfaces behind explicit tiers.", reason = "#450 and children are the broad workspace compression lane that should absorb several overlapping surfaces, but it needs #451 criteria before behavior changes.", promotion_signal = "promote after the active #442/#445 slice or when planning-payload classification needs repo-wide feature-tier decisions.", suggested_first_slice = "#451, then use those criteria to sequence #452-#460." },
  { id = "command-generation-extraction-boundary", title = "Isolate generic command-generation machinery for future extraction", priority = "4", issues = ["#430", "#431", "#432", "#433", "#434", "#435"], outcome = "separate generic command-package generation from Agentic Workspace-specific contracts so later extraction is intentional rather than inertia.", reason = "still valuable, but lower than default-footprint and feature-tier work because generator extraction should not expand shipped or first-contact surfaces.", promotion_signal = "promote when generated-command work is selected or after #459 decides shipped-footprint boundaries.", suggested_first_slice = "#431, because package boundary creation should precede schema and loader decoupling." },
  { id = "operational-footprint-pressure", title = "Add operational footprint pressure for long-running package artifacts", priority = "5", issues = ["#436", "#437", "#438", "#439", "#440", "#441"], outcome = "make artifact growth visible across current memory, archives, reviews, and generated outputs without expanding ordinary startup.", reason = "accepted footprint-control lane, but #455 may supersede part of #437 and #454 may merge some maintenance-pressure views.", promotion_signal = "promote after higher-risk workflow and architecture lanes or when footprint pressure blocks ordinary operation.", suggested_first_slice = "#437 unless #455 removes the stale current-memory surface first." },
]
candidates = []
