[todo]
active_items = []
queued_items = []

[roadmap]
lanes = [
  { id = "product-compression-followthrough", title = "Product compression follow-through", priority = "first", issues = ["#230", "#372", "#374", "#375"], outcome = "Reduce visible product residue and verify whether #230 can honestly close.", reason = "#371 and #373 landed adapter compression/status checks; #372 and #374 remain follow-up implementation slices before #375 closure assessment.", promotion_signal = "Continue with #372 durable artifact authoring commands or #374 lifecycle dry-run plan simplification.", suggested_first_slice = "Use #372 if the next friction target is manual durable JSON/TOML authoring; use #374 if lifecycle dry-run output remains the sharper product-shape burden." },
]
candidates = [
  { id = "visible-product-residue-compression", title = "Continue product-shape subtraction pressure", issues = ["#230"], reason = "#230 remains intentionally open as a strategic product-compression direction; no current active slice is selected.", suggested_first_slice = "Refresh visible-surface inventory only when new first-contact residue appears or a concrete subtraction target is proposed." },
]
