[todo]
active_items = []
queued_items = []

[roadmap]
lanes = [
  { id = "planning-memory-operating-loop", title = "Planning-memory operating loop and posterity discipline", priority = "first", issues = ["#262", "#263", "#264", "#265", "#266"], outcome = "Make ordinary execution recover the right durable context before work, surface relevant planning drift early, and preserve later-useful residue or recurring-friction evidence after work so planning and Memory cooperate tightly at the workflow level while remaining independently usable modules.", reason = "The April 22 issue cluster shows the current chain still loses leverage at both ends of execution: startup and resume rediscovery stay too expensive, and closure still depends too much on the human to rescue useful residue or improvement pressure.", promotion_signal = "Promote when ordinary work still starts without the right durable pull, finishes without posterity capture, or loses recurring-friction evidence that should accumulate product pressure.", suggested_first_slice = "Extend the loop with explicit post-work posterity capture from #266, then reassess whether compact startup/resume drift visibility from #262 should follow immediately." },
  { id = "graceful-partial-compliance", title = "Graceful partial compliance and bypass trust", priority = "second", issues = ["#251", "#253", "#254", "#255", "#256"], outcome = "Make package startup and compact operating surfaces easier for generic agents to discover and cheaper to use, while making likely package bypass or missing planning residue visible as lower-trust review and closure state.", reason = "Live open issues already describe this lane, but the current roadmap was hiding it entirely; the product needs a visible checked-in owner for the follow-on after issue #257 lands.", promotion_signal = "Promote when intent-validation surfaces show open-but-untracked external or repo-known follow-on, or when repeated generic-agent use still makes bypass locally cheaper than package use.", suggested_first_slice = "Use the new intent-validation surface to audit startup discoverability, compact core-surface cost, and lower-trust closeout signals for the current graceful-compliance cluster." },
  { id = "machine-first-planning-chain", title = "Machine-first planning chain", priority = "third", issues = ["#261"], outcome = "Replace prose-first authored execplans with a genuinely machine-first canonical planning state and derived human-readable views.", reason = "The execplan modernization tranche improved the planning chain, but durable planning state is still authored as Markdown and parsed back into projections.", promotion_signal = "Promote when finished-work inspection and delegated-run review both still reveal Markdown-authored state as the limiting factor for cheap queryability and verification.", suggested_first_slice = "Define the minimal canonical data shape for active and archived planning state, plus one derived rendered view that can replace the current authored Markdown contract." },
  { id = "declarative-portability-follow-through", title = "Declarative portability follow-through", priority = "fourth", issues = ["#258"], outcome = "Shift more stable lifecycle, reporting, and runtime truths into declarative contract artifacts with thinner Python adapters.", reason = "Portability improved directionally, but major stable truths still live in large Python implementations instead of more portable contract surfaces.", promotion_signal = "Promote when a bounded lifecycle/reporting truth is stable enough to extract or when another outside-facing integration still requires reverse-engineering large Python branches.", suggested_first_slice = "Identify one stable lifecycle or reporting contract currently trapped in Python and extract it into a smaller shared declarative artifact with adapter-backed validation." },
  { id = "further-product-compression-on-repeated-pressure", title = "Further product compression on repeated pressure", priority = "fifth", issues = ["#230"], outcome = "Continue removing, demoting, or internalizing visible product residue only when repeated real use shows another surface still imposes first-line cognitive cost.", reason = "The bounded startup/front-door/doctrine/query compression tranche is complete; future subtraction should reopen only on repeated evidence rather than on abstract appetite for simplification.", promotion_signal = "Promote when repeated dogfooding shows another visible surface still behaves like a first-line handbook, leaves avoidable startup ambiguity, or keeps unnecessary product residue in the default mental model.", suggested_first_slice = "Capture one repeated remaining residue class and prove whether it should disappear, merge, demote, or move behind a compact query surface." },
]
candidates = [
  { priority = "first", summary = "Planning-memory operating loop and posterity discipline" },
  { priority = "second", summary = "Graceful partial compliance and bypass trust" },
  { priority = "third", summary = "Machine-first planning chain" },
  { priority = "fourth", summary = "Declarative portability follow-through" },
  { priority = "fifth", summary = "Further product compression on repeated pressure" },
]
