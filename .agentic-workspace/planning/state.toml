kind = "agentic-planning-state"
schema_version = "planning-state/v1"

work_items = [
  { id = "structured-file-contract", type = "lane", title = "Schema or validate machine-readable files", maturity = "closed", status = "done", priority = "6", issues = ["#502", "#544"], outcome = "machine-readable JSON/TOML/YAML files are schema-backed, typed-validator-backed, generated/derived, or explicitly classified diagnostics.", reason = "#502 is satisfied after #503, #504, #505, #506, #508, #509, and #544; structured-file inventory has no freeform-prohibited gaps and practical validation claims are executable or delegated through checks.", promotion_signal = "reopen only if a tracked structured file lacks classification or an inventory validation claim becomes non-executable.", suggested_first_slice = "", closure = "archive-and-close", durable_residue = "check", residue_owner = "src/agentic_workspace/contracts/structured_file_inventory.json and scripts/check/check_structured_file_inventory.py", residue_routing = "src/agentic_workspace/contracts/structured_file_inventory.json and scripts/check/check_structured_file_inventory.py", residue_promotion_trigger = "new tracked structured-file formats, inventory statuses, or validation claim classes", residue_retention = "retain" },
  { id = "workflow-friction-polish", type = "lane", title = "Remove small package-use friction discovered during dogfooding", maturity = "closed", status = "done", priority = "7", issues = [], outcome = "common selector guesses, execplan scaffolding, archive cleanup, historical/live tracking distinctions, stale archive-continuation recommendations, external evidence refresh safety, and promotion-state validity are easier to recover from without broad rereads or projection-noise closeout.", reason = "workflow-friction-polish issues #507, #515, #522, #523, #524, #533, #543, and #545 were handled during dogfooding.", promotion_signal = "reopen only when fresh dogfooding friction is captured as a new issue that fits this lane rather than a more specific lane.", suggested_first_slice = "", closure = "archive-and-close", durable_residue = "check", residue_owner = "closed child execplans and their touched implementation/tests", residue_routing = "closed child execplans and their touched implementation/tests", residue_promotion_trigger = "fresh dogfooding friction issue", residue_retention = "retain" },
  { id = "agent-created-aids-lifecycle", type = "lane", title = "Give agent-created reusable aids a lightweight lifecycle", maturity = "closed", status = "done", priority = "8", issues = ["#516", "#521"], outcome = "agents can create, discover, validate, promote, and retire reusable aids without turning the repo into unowned scripts or runtime-specific residue.", reason = "#517 established storage and ownership boundaries, #518 added schema-backed manifests, #519 added executable safety policy, #520 added compact discovery, and #521 added promotion paths and policy checks.", promotion_signal = "reopen only when a new reusable-aid class or promotion route cannot be represented by the manifest schema, checker, compact discovery, or storage docs.", suggested_first_slice = "", closure = "archive-and-close", durable_residue = "contract", residue_owner = "agent aid storage docs, manifest schema, checker, defaults/report discovery, and focused tests", residue_routing = ".agentic-workspace/docs/agent-aids-storage.md; src/agentic_workspace/contracts/schemas/agent_aid_manifest.schema.json; scripts/check/check_agent_aids.py; agent_aids report/defaults surfaces", residue_promotion_trigger = "new aid class, promotion target, discovery route, or canonical proof aid policy", residue_retention = "retain" },
  { id = "structured-first-operating-updates", type = "lane", title = "Reduce prose authoring through correct-by-design structured updates", maturity = "shaped", status = "deferred", priority = "9", issues = ["#525", "#526", "#528", "#530", "#534", "#535"], outcome = "common closeout, handoff, review, Memory, and self-improvement updates are authored from structured fields or constrained templates, with validation confirming rather than teaching shape.", reason = "#531 inventoried prose-heavy operating surfaces and #532 generated closeout/archive prose; continue with constrained templates #534 or structured Memory metadata #535.", promotion_signal = "promote when the next work touches review artifacts, handoff/self-improvement templates, Memory metadata, scaffold helpers, or repeated validation repair.", suggested_first_slice = "#534" },
  { id = "planning-state-transition-closeout", title = "Connect maturity state promotion and closeout transitions", source = "#501", why_now = "planning-state-maturity continues after #500; archive cleanup should preserve future-relevant residue without duplicating implementation detail", decision_owner = "human", strategy_role = "product/architecture", owner_role = "implementation", delivery_role = "implementation", review_role = "validation", knowledge_owner = "planning/docs", handoff_ready = true, type = "slice", maturity = "closed", status = "done", path = ".agentic-workspace/planning/execplans/archive/planning-state-transition-closeout.plan.json", closure = "archive-and-close", durable_residue = "planning", residue_owner = ".agentic-workspace/planning/state.toml", residue_routing = ".agentic-workspace/planning/state.toml", residue_promotion_trigger = "when archive cleanup or work_maturity projections are touched", residue_retention = "shrink" },
  { id = "structured-file-inventory", title = "Add structured-file inventory and no-freeform check", source = "#503", why_now = "the structured-file contract lane needs a checked map before child schema work can proceed without random cleanup.", decision_owner = "human", strategy_role = "product/architecture", owner_role = "implementation", delivery_role = "implementation", review_role = "validation", knowledge_owner = "contracts/checks", handoff_ready = true, type = "slice", maturity = "closed", status = "done", path = ".agentic-workspace/planning/execplans/archive/structured-file-inventory.plan.json", closure = "archive-but-keep-lane-open", durable_residue = "evidence_only", residue_owner = "archive", residue_routing = "archive", residue_promotion_trigger = "none", residue_retention = "retain" },
  { id = "reconstructable-artifact-policy", title = "Add reconstructable artifact storage policy", source = "#541", type = "slice", maturity = "closed", status = "done", path = ".agentic-workspace/planning/execplans/archive/reconstructable-artifact-policy.plan.json", closure = "archive-but-keep-lane-open", durable_residue = "contract", residue_owner = "src/agentic_workspace/contracts/structured_file_inventory.json", residue_routing = "src/agentic_workspace/contracts/structured_file_inventory.json", residue_promotion_trigger = "new structured artifact classes or evidence mirrors", residue_retention = "retain" },
  { id = "external-intent-cache-policy", title = "Treat external intent evidence as reconstructable cache", source = "#536", type = "slice", maturity = "closed", status = "done", path = ".agentic-workspace/planning/execplans/archive/external-intent-cache-policy.plan.json", closure = "archive-but-keep-lane-open", durable_residue = "planning", residue_owner = ".agentic-workspace/planning/state.toml", residue_routing = ".agentic-workspace/planning/state.toml", residue_promotion_trigger = "external evidence refresh behavior changes", residue_retention = "retain" },
  { id = "finished-work-source-owned-reopeners", title = "Replace finished-work sidecar with source-owned reopeners", source = "#537", type = "slice", maturity = "closed", status = "done", path = ".agentic-workspace/planning/execplans/archive/finished-work-source-owned-reopeners.plan.json", closure = "archive-but-keep-lane-open", durable_residue = "planning", residue_owner = ".agentic-workspace/planning/state.toml", residue_routing = ".agentic-workspace/planning/state.toml", residue_promotion_trigger = "finished-work reopening behavior changes", residue_retention = "retain" },
  { id = "audit-artifact-distillation", title = "Distill audit artifacts to decisions and refs", source = "#538", type = "slice", maturity = "closed", status = "done", path = ".agentic-workspace/planning/execplans/archive/audit-artifact-distillation.plan.json", closure = "archive-but-keep-lane-open", durable_residue = "planning", residue_owner = ".agentic-workspace/planning/state.toml", residue_routing = ".agentic-workspace/planning/state.toml", residue_promotion_trigger = "review artifact retention rules change", residue_retention = "retain" },
  { id = "generated-mirror-justification", title = "Require generated mirrors to justify checked-in paths", source = "#539", type = "slice", maturity = "closed", status = "done", path = ".agentic-workspace/planning/execplans/archive/generated-mirror-justification.plan.json", closure = "archive-but-keep-lane-open", durable_residue = "contract", residue_owner = "src/agentic_workspace/contracts/structured_file_inventory.json", residue_routing = "src/agentic_workspace/contracts/structured_file_inventory.json", residue_promotion_trigger = "generated mirror consumers or regeneration paths change", residue_retention = "retain" },
  { id = "focused-contract-proof-lane", title = "Add focused proof lane for contract and inventory changes", source = "#543", type = "slice", maturity = "closed", status = "done", path = ".agentic-workspace/planning/execplans/archive/focused-contract-proof-lane.plan.json", closure = "archive-but-keep-lane-open", durable_residue = "contract", residue_owner = "src/agentic_workspace/contracts/proof_selection_rules.json", residue_routing = "src/agentic_workspace/contracts/proof_selection_rules.json", residue_promotion_trigger = "proof selection lanes change", residue_retention = "retain" },
  { id = "planning-checker-single-source", title = "Collapse duplicate planning checker payload copies", source = "#540", type = "slice", maturity = "closed", status = "done", path = ".agentic-workspace/planning/execplans/archive/planning-checker-single-source.plan.json", closure = "archive-and-close", durable_residue = "check", residue_owner = "scripts/check/check_source_payload_operational_install.py", residue_routing = "scripts/check/check_source_payload_operational_install.py", residue_promotion_trigger = "planning checker compatibility paths or payload mirror consumers change", residue_retention = "retain" },
  { id = "memory-manifest-typed-validator", title = "Add explicit typed validator for memory manifest TOML", source = "#504", why_now = "structured-file-contract next child; existing manifest validation should become an explicit typed-validator-backed contract", type = "slice", maturity = "closed", status = "done", path = ".agentic-workspace/planning/execplans/archive/memory-manifest-typed-validator.plan.json", closure = "archive-but-keep-lane-open", durable_residue = "evidence_only", residue_owner = "archive", residue_routing = "archive", residue_promotion_trigger = "none", residue_retention = "retain" },
  { id = "planning-record-schemas", title = "Add schemas for execplan and review JSON records", source = "#505", why_now = "structured-file-contract next child; planning records carry machine-readable execution, review, and closeout contracts", type = "slice", maturity = "closed", status = "done", path = ".agentic-workspace/planning/execplans/archive/planning-record-schemas.plan.json", closure = "archive-but-keep-lane-open", durable_residue = "contract", residue_owner = ".agentic-workspace/planning/schemas/ and packages/planning/bootstrap/.agentic-workspace/planning/schemas/", residue_routing = ".agentic-workspace/planning/schemas/ and packages/planning/bootstrap/.agentic-workspace/planning/schemas/", residue_promotion_trigger = "when planning record shape, archive closeout requirements, or review artifact structure changes", residue_retention = "retain" },
  { id = "planning-evidence-schemas", title = "Add schemas for external intent and finished-work evidence", source = "#506", why_now = "structured-file-contract next child; evidence records influence reconciliation, closeout, and stale-state detection", type = "slice", maturity = "closed", status = "done", path = ".agentic-workspace/planning/execplans/archive/planning-evidence-schemas.plan.json", closure = "archive-but-keep-lane-open", durable_residue = "contract", residue_owner = "contracts/checks: planning evidence schemas, planning loaders, structured-file inventory, and planning-surface checker", residue_routing = "contracts/checks: planning evidence schemas, planning loaders, structured-file inventory, and planning-surface checker", residue_promotion_trigger = "if another evidence JSON surface needs schema backing, reuse #526 helper/scaffold work rather than hand-copying root/bootstrap/schema/inventory boilerplate", residue_retention = "retain" },
  { id = "root-contract-manifest-schemas", title = "Add schemas for root contract manifests", source = "#508", why_now = "structured-file-contract next child; root contract manifests influence product behavior and some still load without schema validation", type = "slice", maturity = "closed", status = "done", path = ".agentic-workspace/planning/execplans/archive/root-contract-manifest-schemas.plan.json", closure = "archive-but-keep-lane-open", durable_residue = "contract", residue_owner = "contracts/checks: root manifest schemas, contract_tooling validated loaders, python_contract_consumption, and check_contract_tooling_surfaces", residue_routing = "contracts/checks: root manifest schemas, contract_tooling validated loaders, python_contract_consumption, and check_contract_tooling_surfaces", residue_promotion_trigger = "when adding or changing root contract manifests", residue_retention = "retain" },
  { id = "package-local-artifact-schemas", title = "Add schemas for package-local classification and extraction artifacts", source = "#509", why_now = "structured-file-contract next child; package-local classification JSON guides payload and extraction boundaries but still routes as a schema gap", type = "slice", maturity = "closed", status = "done", path = ".agentic-workspace/planning/execplans/archive/package-local-artifact-schemas.plan.json", closure = "archive-but-keep-lane-open", durable_residue = "contract", residue_owner = "packages/planning/schemas plus packages/planning/tests/test_packaging.py and structured-file inventory", residue_routing = "packages/planning/schemas plus package tests and structured-file inventory", residue_promotion_trigger = "if package-local classification artifacts change shape or become shipped payload", residue_retention = "retain" },
  { id = "executable-structured-inventory-claims", title = "Make structured inventory validation claims executable", source = "#544", why_now = "structured-file-contract next child; inventory schema_or_validator claims should be enforced or explicitly delegated", type = "slice", maturity = "closed", status = "done", path = ".agentic-workspace/planning/execplans/archive/executable-structured-inventory-claims.plan.json", closure = "archive-and-close", durable_residue = "check", residue_owner = "scripts/check/check_structured_file_inventory.py and tests/test_structured_file_inventory.py", residue_routing = "scripts/check/check_structured_file_inventory.py and tests/test_structured_file_inventory.py", residue_promotion_trigger = "new inventory statuses or validation claim classes", residue_retention = "retain" },
  { id = "report-section-selector-aliases", title = "Add report selector aliases and recovery guidance", source = "#507", why_now = "workflow-friction-polish next child; common report selector guesses should recover cheaply", type = "slice", maturity = "closed", status = "done", path = ".agentic-workspace/planning/execplans/archive/report-section-selector-aliases.plan.json", closure = "archive-but-keep-lane-open", durable_residue = "check", residue_owner = "src/agentic_workspace/reporting_support.py and tests/test_workspace_cli.py", residue_routing = "src/agentic_workspace/reporting_support.py and tests/test_workspace_cli.py", residue_promotion_trigger = "when report sections, aliases, or selector guidance changes", residue_retention = "retain" },
  { id = "projection-complete-execplan-scaffolds", title = "Make execplan scaffolds projection-complete by default", source = "#515", why_now = "workflow-friction-polish next child; newly scaffolded plans should project cleanly without repair loops.", owner_role = "implementation", review_role = "validation", handoff_ready = true, type = "slice", maturity = "closed", status = "done", path = ".agentic-workspace/planning/execplans/archive/projection-complete-execplan-scaffolds.plan.json", closure = "archive-but-keep-lane-open", durable_residue = "check", residue_owner = "packages/planning/src/repo_planning_bootstrap/installer.py and packages/planning/tests/test_installer.py", residue_routing = "packages/planning/src/repo_planning_bootstrap/installer.py and packages/planning/tests/test_installer.py", residue_promotion_trigger = "when execplan projection sections or promote-to-plan scaffold behavior changes", residue_retention = "retain" },
  { id = "archive-plan-active-execplan-cleanup", title = "Clean state active execplan rows during archive", source = "#522", why_now = "workflow-friction-polish next child; archive-plan should not leave stale active state rows.", owner_role = "implementation", review_role = "validation", handoff_ready = true, type = "slice", maturity = "closed", status = "done", path = ".agentic-workspace/planning/execplans/archive/archive-plan-active-execplan-cleanup.plan.json", closure = "archive-but-keep-lane-open", durable_residue = "none", residue_owner = "archive", residue_routing = "archive", residue_promotion_trigger = "none", residue_retention = "retain" },
  { id = "root-cli-authority-live-tracking", title = "Distinguish historical and live root CLI authority tracking", source = "#523", why_now = "workflow-friction-polish next child; compact root CLI authority output should not imply closed historical issues are live owners.", owner_role = "implementation", review_role = "validation", handoff_ready = true, type = "slice", maturity = "closed", status = "done", path = ".agentic-workspace/planning/execplans/archive/root-cli-authority-live-tracking.plan.json", closure = "archive-but-keep-lane-open", durable_residue = "check", residue_owner = "src/agentic_workspace/contracts/python_runtime_boundary.json and scripts/check/check_contract_tooling_surfaces.py", residue_routing = "src/agentic_workspace/contracts/python_runtime_boundary.json and scripts/check/check_contract_tooling_surfaces.py", residue_promotion_trigger = "when root CLI authority candidates or tracking/provenance fields change", residue_retention = "retain" },
  { id = "promote-to-plan-active-state-validity", title = "Keep promoted active state valid by construction", source = "#545", why_now = "workflow-friction-polish next child; promote-to-plan should not create planning-state schema drift.", owner_role = "implementation", review_role = "validation", handoff_ready = true, type = "slice", maturity = "closed", status = "done", path = ".agentic-workspace/planning/execplans/archive/promote-to-plan-active-state-validity.plan.json", closure = "archive-and-close", durable_residue = "check", residue_owner = "packages/planning/src/repo_planning_bootstrap/installer.py and packages/planning/tests/test_installer.py", residue_routing = "packages/planning/src/repo_planning_bootstrap/installer.py and packages/planning/tests/test_installer.py", residue_promotion_trigger = "when promote-to-plan compact state update behavior changes", residue_retention = "retain" },
  { id = "external-intent-evidence-refresh-safety", title = "Harden external evidence refresh and validation", source = "#533", why_now = "workflow-friction-polish next child; external evidence refresh should be BOM-safe and count-consistent without manual JSON edits.", owner_role = "implementation", review_role = "validation", handoff_ready = true, type = "slice", maturity = "closed", status = "done", path = ".agentic-workspace/planning/execplans/archive/external-intent-evidence-refresh-safety.plan.json", closure = "archive-and-close", durable_residue = "check", residue_owner = "src/agentic_workspace/cli.py, tests/test_workspace_cli.py, packages/planning/src/repo_planning_bootstrap/installer.py, and packages/planning/tests/test_installer.py", residue_routing = "root external-intent refresh and planning external evidence loader tests", residue_promotion_trigger = "when external intent evidence refresh or provider-agnostic evidence loading changes", residue_retention = "retain" },
  { id = "agent-aids-storage-boundaries", title = "Define storage boundaries for agent-created aids", source = "#517", why_now = "agent-created-aids-lifecycle first child; storage boundaries should exist before manifest, safety, discovery, and promotion work.", owner_role = "implementation", review_role = "validation", handoff_ready = true, type = "slice", maturity = "closed", status = "done", path = ".agentic-workspace/planning/execplans/archive/agent-aids-storage-boundaries.plan.json", closure = "archive-but-keep-lane-open", durable_residue = "contract", residue_owner = "agent aid storage defaults, ownership ledgers, structured inventory, and .agentic-workspace/docs/agent-aids-storage.md", residue_routing = "#518 for manifest/schema; #519-#521 for safety, discovery, and promotion", residue_promotion_trigger = "when candidate aid storage, metadata, or promotion behavior changes", residue_retention = "retain" },
  { id = "agent-aid-manifest-schema", title = "Add schema-backed manifests for checked-in agent aids", source = "#518", why_now = "agent-created-aids-lifecycle next child; #517 routed candidate structured aid metadata here so checked-in aids do not become freeform state.", owner_role = "implementation", review_role = "validation", handoff_ready = true, type = "slice", maturity = "closed", status = "done", path = ".agentic-workspace/planning/execplans/archive/agent-aid-manifest-schema.plan.json", closure = "archive-but-keep-lane-open", durable_residue = "contract", residue_owner = "agent aid manifest schema, checker, structured inventory, and compact storage payload", residue_routing = "#519 for executable portability and safety; #520-#521 for discovery and promotion", residue_promotion_trigger = "when checked-in aid metadata or validation requirements change", residue_retention = "retain" },
  { id = "agent-aid-executable-safety", title = "Add portability and safety rules for executable agent aids", source = "#519", why_now = "agent-created-aids-lifecycle next child; #518 created manifests and #519 tightened executable aid safety and portability semantics before discovery or promotion.", owner_role = "implementation", review_role = "validation", handoff_ready = true, type = "slice", maturity = "closed", status = "done", path = ".agentic-workspace/planning/execplans/archive/agent-aid-executable-safety.plan.json", closure = "archive-but-keep-lane-open", durable_residue = "contract", residue_owner = "agent aid manifest schema, executable safety checker, compact defaults payload, proof selection rules, and storage docs", residue_routing = "#520 for discovery and #521 for promotion", residue_promotion_trigger = "when checked-in executable aid safety, portability, review, or proof-role policy changes", residue_retention = "retain" },
  { id = "agent-aid-discovery", title = "Make agent-created aids discoverable without broad file search", source = "#520", why_now = "agent-created-aids-lifecycle next child; #519 made checked-in aids safe enough to expose and #520 added compact discovery without startup burden.", owner_role = "implementation", review_role = "validation", handoff_ready = true, type = "slice", maturity = "closed", status = "done", path = ".agentic-workspace/planning/execplans/archive/agent-aid-discovery.plan.json", closure = "archive-but-keep-lane-open", durable_residue = "contract", residue_owner = "report agent_aids section, skills agent_aid recommendations, report contract, compact defaults payload, and storage docs", residue_routing = "#521 for promotion", residue_promotion_trigger = "when aid discovery, recommendation, retired filtering, or candidate proof-role visibility changes", residue_retention = "retain" },
  { id = "agent-aid-promotion", title = "Promote proven agent aids into repo-native platform-independent surfaces", source = "#521", why_now = "agent-created-aids-lifecycle final child; #520 made aids discoverable and #521 defines how proven aids leave candidate storage for repo-native surfaces.", owner_role = "implementation", review_role = "validation", handoff_ready = true, type = "slice", maturity = "closed", status = "done", path = ".agentic-workspace/planning/execplans/archive/agent-aid-promotion.plan.json", closure = "archive-and-close", durable_residue = "contract", residue_owner = "agent aid manifest schema, checker, compact defaults/report payload, storage docs, and focused tests", residue_routing = "agent-created-aids-lifecycle parent #516 is satisfied; continue with structured-first-operating-updates #531", residue_promotion_trigger = "when aid promotion metadata, discovery routes, module-component aid coverage, or canonical proof aid policy changes", residue_retention = "retain" },
  { id = "prose-heavy-surface-inventory", title = "Inventory prose-heavy operating surfaces and classify essential vs generated prose", source = "#531", why_now = "structured-first-operating-updates first child; inventory recurring prose-writing surfaces before replacing, constraining, generating, structuring, shrinking, or removing them.", owner_role = "implementation", review_role = "validation", handoff_ready = true, type = "slice", maturity = "closed", status = "done", path = ".agentic-workspace/planning/execplans/archive/prose-heavy-surface-inventory.plan.json", closure = "archive-but-keep-lane-open", durable_residue = "contract", residue_owner = "src/agentic_workspace/contracts/prose_surface_inventory.json", residue_routing = "#532 for generated closeout/archive prose; #534 for constrained review/handoff/self-improvement templates; #535 for structured Memory metadata", residue_promotion_trigger = "when recurring prose-authoring surfaces, classifications, or structured-first follow-up issues change", residue_retention = "retain" },
  { id = "generated-closeout-prose", title = "Generate closeout and archive prose from structured fields", source = "#532", why_now = "structured-first-operating-updates next child; #531 identified closeout/archive prose as the highest-frequency generated-prose target.", owner_role = "implementation", review_role = "validation", handoff_ready = true, type = "slice", maturity = "closed", status = "done", path = ".agentic-workspace/planning/execplans/archive/generated-closeout-prose.plan.json", closure = "archive-and-close", durable_residue = "none", residue_owner = "archive", residue_routing = "#534 for constrained review/handoff/self-improvement templates; #535 for structured Memory metadata; #526/#528 for scaffold/helper and validation-friction follow-through", residue_promotion_trigger = "none", residue_retention = "retain" },
]

[active]
execplans = []

[todo]
active_items = []
queued_items = []
