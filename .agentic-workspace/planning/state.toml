[todo]
active_items = []
queued_items = []

[roadmap]
lanes = [
  { id = "graceful-partial-compliance", title = "Graceful partial compliance and bypass trust", priority = "first", issues = ["#292", "#293", "#295", "#296", "#297", "#298"], outcome = "Make package startup, proof selection, boundary warnings, adapter authority, and compact operating surfaces easier for generic or bounded agents to discover and cheaper to use.", reason = "The original graceful-compliance cluster is closed, but the latest external-agent feedback repeats follow-on pressure around competing first-contact surfaces, proof-selection ambiguity, adapter authority, package boundary confusion, and bounded implementer cost.", promotion_signal = "Keep near the top while repeated generic-agent use still makes bypass locally cheaper than package use, startup gates still fail before package routing helps, proof selection remains interpretive, or boundary confusion recurs.", suggested_first_slice = "Audit the April 24 feedback cluster and decide whether a minimum-safe-read startup command, proof selector, or boundary marker is the smallest follow-on slice." },
  { id = "python-consumable-contract-extraction", title = "Python-consumable contract extraction", priority = "second", issues = ["#258", "#299", "#300", "#301", "#302", "#303", "#305", "#309", "#310", "#311", "#312", "#313", "#314", "#315", "#316"], outcome = "Make the current Python implementation consume implementation-independent contracts, schemas, manifests, templates, and operation specs as the authoritative source for extractable behavior-adjacent content.", reason = "The April 25 extraction issue cluster turns the broader declarative-portability gap into an ordered, reviewable milestone with a parent lane and child slices; it is now concrete enough to replace the older generic portability follow-through entry.", promotion_signal = "Promote after the current active local-only boundary slice or sooner if Python-owned contract authority blocks reviewed extraction work, generated-interface preparation, or contract parity checks.", suggested_first_slice = "Start with #310: audit remaining Python-owned extractable contract content and produce the compact extraction map before extracting more surfaces." },
  { id = "local-only-integration-area", title = "Local-only integration area", priority = "third", issues = ["#287", "#288"], outcome = "Create a sanctioned local-only integration area for vendor/runtime-specific bridges without turning it into shared workflow state.", reason = "These are a narrow, coherent boundary-setting slice and should stay local and disposable.", promotion_signal = "Promote when a minimal root name and boundary convention are enough for safe local shortcuts.", suggested_first_slice = "Choose the local-only integration root and vendor/runtime subfolder convention." },
  { id = "machine-first-planning-state", title = "Machine-first planning state and closeout hygiene", priority = "fourth", issues = ["#261", "#280", "#282", "#307", "#323", "#324", "#325"], outcome = "Make planning state, references, retention, and closeout learning more machine-first so agents can resume, review, archive, and promote durable knowledge without rereading prose-heavy plan history.", reason = "The current planning chain is much more queryable, but open issues still identify prose-authored execplans, weak structured references, review-artifact retention, buried lane learning, archive cleanup trust gaps, and lower-trust closeout reconciliation as unresolved planning substrate gaps.", promotion_signal = "Promote when active-plan recovery, archive review, archive cleanup, closeout audit, or follow-on routing still requires reconstructing machine-readable state or durable learning from prose execplan bodies.", suggested_first_slice = "Start with the structured reference model, closeout distillation test, archive cleanup trust regression, or lower-trust closeout reconciliation, whichever most reduces the next active lane's restart/review burden." },
  { id = "pre-ingestion-refinement-templates", title = "Pre-ingestion refinement templates", priority = "fifth", issues = ["#290"], outcome = "Make strong general-purpose reasoning cheaper to apply before coding-agent ingestion through lightweight refinement templates.", reason = "The latest comment on this issue already shows a concrete starter set, making it ready for a bounded slice.", promotion_signal = "Promote when a few reusable templates reduce ambiguity before execution without adding a second workflow layer.", suggested_first_slice = "Land the pre-ingestion issue comment and boundary/proof clarification templates." },
  { id = "system-intent-operational-follow-through", title = "System intent operational follow-through", priority = "sixth", issues = ["#279"], outcome = "Finish making compiled system intent an explicit planning-time input so slice shaping and larger-intent validation can use it without adding heavy ritual.", reason = "The compiled system-intent substrate and extraction boundary are now in place, but planning still lacks the compact hook that takes relevant system intent into account when shaping slices and judging broader directional progress.", promotion_signal = "Promote when planning still has to infer relevant system intent manually during slice shaping or larger-intent validation, even though the compiled declaration and source-hints boundary are already settled.", suggested_first_slice = "Land the smallest planning-side hook that surfaces relevant system intent during slice shaping and larger-intent validation." },
  { id = "formal-workflow-substrate-follow-through", title = "Formal workflow substrate follow-through", priority = "seventh", issues = ["#278", "#327"], outcome = "Move more workflow authority from markdown-first contracts into a stricter, composable substrate without turning the product into a heavyweight workflow engine.", reason = "The structured config substrate landed, but the broader workflow-definition issue remains open and default-branch commit risk shows workflow posture still needs clearer machine-readable guardrails.", promotion_signal = "Promote when ordinary work still depends on markdown workflow prose as the primary operational authority rather than as thin explanation or adaptation, or when branch/workflow posture remains implicit before implementation and push.", suggested_first_slice = "Identify one stable workflow component, obligation family, or branch-posture check that can move from markdown-first prose into a smaller formal substrate without widening into full workflow-engine design." },
  { id = "generated-tool-conformance", title = "Generated-tool conformance", priority = "eighth", issues = ["#306"], outcome = "Prepare a black-box conformance layer so generated CLIs, local MCP tools, static skills, or other adapters can be tested against shared operation contracts instead of trusted by implementation path.", reason = "This draft depends on operation-contract coverage and should stay behind the Python-consumable extraction lane until enough operation contracts and runtime parity checks exist.", promotion_signal = "Promote after operation contracts and Python consumption checks are stable enough that another implementation can be validated as a replaceable adapter.", suggested_first_slice = "Define one narrow process-based conformance schema and generated pytest harness scenario for a stable read-only operation." },
  { id = "further-product-compression-on-repeated-pressure", title = "Further product compression on repeated pressure", priority = "ninth", issues = ["#230", "#275", "#281", "#319", "#326"], outcome = "Continue removing, demoting, or internalizing visible product residue only when repeated real use shows another surface still imposes first-line cognitive cost.", reason = "The bounded startup/front-door/doctrine/query compression tranche is complete, but the new static-routing-table issue, maintainer-surface refresh friction, and memory runbook rationale growth show there are still open subtraction/adapter-drift paths around generated compatibility artefacts and visible adapter residue.", promotion_signal = "Promote when repeated dogfooding shows another visible surface still behaves like a first-line handbook, leaves avoidable startup ambiguity, keeps unnecessary generated adapter residue in the default mental model, exposes a documented adapter refresh path that leaves checked-in adapters stale, or lets memory runbooks accumulate rationale that belongs in a smaller decision surface.", suggested_first_slice = "Audit the remaining generated compatibility artefacts and memory runbook residue, then prove which surfaces should disappear entirely, become static routing tables, move to decision notes, or still genuinely earn their keep." },
]
candidates = [
  { priority = "first", summary = "Graceful partial compliance and bypass trust" },
  { priority = "second", summary = "Python-consumable contract extraction" },
  { priority = "third", summary = "Local-only integration area" },
  { priority = "fourth", summary = "Machine-first planning state and closeout hygiene" },
  { priority = "fifth", summary = "Pre-ingestion refinement templates" },
  { priority = "sixth", summary = "System intent operational follow-through" },
  { priority = "seventh", summary = "Formal workflow substrate follow-through" },
  { priority = "eighth", summary = "Generated-tool conformance" },
  { priority = "ninth", summary = "Further product compression on repeated pressure" },
]
