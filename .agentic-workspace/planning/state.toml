# Agentic Workspace managed state.
# Do not edit by hand when the CLI is available.
# Inspect: uv run agentic-workspace summary --format json
# Mutate through the package command named by that output.

kind = "agentic-planning-state"
schema_version = "planning-state/v1"

work_items = []

[active]
execplans = [
  { id = "github-low-priority-planning-lanes-p3", maturity = "active", status = "active", priority = "P3", refs = "GitHub #595, #627, #960, #961, #962, #963, #964, #989", title = "Defer broader planning, dogfooding, and SkillSpec lanes", outcome = "Lower-priority planning lanes remain visible while future SkillSpec and dogfooding work proves proportionality: compact guidance should prevent over-producing reviews, Memory notes, execplans, or raw context for routine tasks.", reason = "Issue intake found these are low-priority parent lanes, pilots, downstream SkillSpec work, or previously deferred friction lanes; when promoted, they should harden context-reduction behavior through real dogfooding rather than expanding visible methodology.", suggested_first_slice = "For dogfooding, use #595 to measure context burden and artifact footprint on one real host-repo slice; for SkillSpec work, start with #961 and encode proportionality/no-artifact-by-default semantics before generated skill follow-ons.", path = ".agentic-workspace/planning/execplans/github-low-priority-planning-lanes-p3.plan.json" },
]

[todo]
active_items = []
queued_items = []

[roadmap]
lanes = []
candidates = [
  { id = "github-codegen-deferred-p4", maturity = "candidate", status = "deferred", priority = "P4", refs = "GitHub #892, #929, #965, #972, #977, #978, #992, #994, #999, #1001, #1002, #1003, #1004, #1005, #1008", title = "Keep codegen-related issues deferred lowest priority", outcome = "Generated-command-package, generated-resource, Docker conformance, and generated text/runtime issues stay out of near-term selection.", reason = "User explicitly requested codegen-related work be deferred as low priority; these issues already carry codegen and lowest/deferred upstream labels.", promotion_signal = "Promote only after higher-priority non-codegen planning trust and recoverability issues are handled or maintainer direction changes.", suggested_first_slice = "If re-promoted, pick one bounded #892 prerequisite rather than reopening the full codegen epic." },
]
