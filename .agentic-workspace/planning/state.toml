[todo]
active_items = [
  { id = "agent-os-capability-map-refresh", status = "in-progress", title = "Refresh Agent OS capability map against current module boundaries", source = "github:#466", why_now = "#465 defined extraction candidate boundaries; #466 is the remaining #461 child to align long-horizon capability doctrine.", surface = ".agentic-workspace/planning/execplans/agent-os-capability-map-refresh.plan.json" },
]
queued_items = []

[roadmap]
lanes = [
  { id = "planning-payload-boundary", title = "Reclassify planning payload into core, optional, on-demand, and extraction candidates", priority = "1", issues = ["#461", "#466"], outcome = "make planning smaller by default and prevent planning from absorbing every useful agent-operation capability.", reason = "#462 classified the payload, #463 shrank default install, #464 added explicit optional enablement, and #465 defined extraction candidates; #466 now owns capability-map doctrine alignment.", promotion_signal = "continue #466 until docs/agent-os-capabilities.md matches current module boundaries.", suggested_first_slice = "#466 is active; after it lands, validate whether #461 can close." },
  { id = "feature-tier-and-surface-merge", title = "Make advanced features opt-in through feature tiers and merged surfaces", priority = "2", issues = ["#449", "#450", "#451", "#452", "#453", "#454", "#455", "#456", "#457", "#458", "#459", "#460", "#467", "#469", "#471"], outcome = "make package use cheaper than bypassing it by shrinking default setup, capturing improvement signals cheaply, and merging advanced maintenance, reconciliation, review, and codegen surfaces behind explicit tiers.", reason = "#450 and children are the broad workspace compression lane that should absorb several overlapping surfaces; #469 is upstream signal capture, #467 turns those signals into total-operating-cost reduction, and #471 adds closeout-preparation friction from #464 dogfooding.", promotion_signal = "promote after the active #461/#462 payload-boundary lane or when payload classification needs repo-wide feature-tier decisions.", suggested_first_slice = "#451, unless #469 or #471 is needed first to prevent dogfooding signals from disappearing." },
  { id = "command-generation-extraction-boundary", title = "Isolate generic command-generation machinery for future extraction", priority = "3", issues = ["#430", "#431", "#432", "#433", "#434", "#435"], outcome = "separate generic command-package generation from Agentic Workspace-specific contracts so later extraction is intentional rather than inertia.", reason = "still valuable, but lower than default-footprint and feature-tier work because generator extraction should not expand shipped or first-contact surfaces.", promotion_signal = "promote when generated-command work is selected or after #459 decides shipped-footprint boundaries.", suggested_first_slice = "#431, because package boundary creation should precede schema and loader decoupling." },
  { id = "operational-footprint-pressure", title = "Add operational footprint pressure for long-running package artifacts", priority = "4", issues = ["#436", "#437", "#438", "#439", "#440", "#441", "#470"], outcome = "make artifact growth visible across current memory, archives, reviews, and generated outputs without expanding ordinary startup.", reason = "accepted footprint-control lane, with #470 adding dogfooding evidence that historical audit pressure should not re-promote already-routed completed children.", promotion_signal = "promote after higher-risk workflow and architecture lanes or when footprint pressure blocks ordinary operation.", suggested_first_slice = "#437 unless #455 removes the stale current-memory surface first; use #470 when historical audit noise blocks autopilot." },
]
candidates = []
