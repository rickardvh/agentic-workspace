[todo]
active_items = []
queued_items = []

[roadmap]
lanes = [
  { id = "further-product-compression-on-repeated-pressure", title = "Further product compression on repeated pressure", priority = "first", issues = ["#230", "#347", "#348", "#349", "#350", "#351"], outcome = "Continue removing, demoting, internalizing, or making measurable visible product residue so ordinary operation gets cheaper without losing leverage.", reason = "#341-#346 landed and closed as focused child slices; the latest retrospective found #347 legacy-vs-new signal ambiguity, #348 duplicated generated llms.txt adapter residue, #349 missing dogfooding evidence, #350 branch-continuity friction, and #351 unresolved historical audit debt.", promotion_signal = "Promote the smallest fresh slice that reduces default reading, durable residue, or review cost without adding a new first-line concept.", suggested_first_slice = "Start with #349 if package-use closeout trust is the active risk; start with #348 if adapter residue is the cheapest cleanup; start with #347 or #351 if review signals are currently noisy." },
]
candidates = [
  { priority = "first", summary = "Further product compression follow-ups from retrospective (#347-#351 under #230)" },
]
