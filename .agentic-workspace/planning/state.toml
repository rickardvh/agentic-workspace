[todo]
active_items = [
  { id = "preflight-first-takeover-robustness", status = "in-progress", surface = ".agentic-workspace/planning/execplans/graceful-partial-compliance.plan.json", why_now = "Startup audit is complete. Generic agents now have discoverable startup paths. Next critical gap: takeover/interruption recovery is undefended. Issue #277 asks for preflight-first workflow gates and bundled takeover context. Implementing now to harden generic-agent recovery paths.", next_action = "Implement compact preflight command bundling startup defaults + config + active state; add active-state-only selector; add optional strict gating for high-risk commands; add tests.", done_when = "Preflight command works; active state can be queried without full archive; tests cover gate behavior and stale-preflight handling; takeover recovery cost is measurably lower." }
]
queued_items = []

[roadmap]
lanes = [
  { id = "graceful-partial-compliance", title = "Graceful partial compliance and bypass trust", priority = "first", issues = ["#251", "#253", "#254", "#255", "#256", "#277"], outcome = "Make package startup and compact operating surfaces easier for generic agents to discover and cheaper to use, while making likely package bypass or missing planning residue visible as lower-trust review and closure state.", reason = "The startup/compliance cluster remains the main generic-agent adoption lane, and the recent weaker-agent failure reinforces that startup discoverability, preflight-first takeover, and lower-trust review need to harden immediately after the runtime guardrail.", promotion_signal = "Keep near the top while repeated generic-agent use still makes bypass locally cheaper than package use, startup gates still fail before package routing helps, or lower-trust closeout signals recur.", suggested_first_slice = "Use the intent-validation surface to audit startup discoverability, compact core-surface cost, preflight takeover robustness, and lower-trust closeout signals for the current graceful-compliance cluster." },
  { id = "machine-first-planning-chain", title = "Machine-first planning chain", priority = "second", issues = ["#261", "#280", "#282"], outcome = "Replace prose-first authored execplans with a genuinely machine-first canonical planning state, explicit structured references, and proportional review residue, with derived human-readable views.", reason = "The earlier first-slice archive explicitly kept the lane open, and the live follow-on issues still show that typed references, canonical review residue, and broader machine-first planning truth remain unfinished, but the immediate trust failures now sit more directly in runtime and startup posture.", promotion_signal = "Promote once the trust-first runtime and compliance guardrails stop being the tighter constraint, or when planning still depends on Markdown-authored or Markdown-only surfaces for durable active or archived state.", suggested_first_slice = "Use the canonical execplan sidecar path on a real repo slice, then continue extracting remaining Markdown-authored planning truth, typed references, and proportional review residue into machine-first records and derived views." },
  { id = "local-only-integration-area", title = "Local-only integration area", priority = "third", issues = ["#287", "#288"], outcome = "Create a sanctioned local-only integration area for vendor/runtime-specific bridges without turning it into shared workflow state.", reason = "These are a narrow, coherent boundary-setting slice and should stay local and disposable.", promotion_signal = "Promote when a minimal root name and boundary convention are enough for safe local shortcuts.", suggested_first_slice = "Choose the local-only integration root and vendor/runtime subfolder convention." },
  { id = "pre-ingestion-refinement-templates", title = "Pre-ingestion refinement templates", priority = "fourth", issues = ["#290"], outcome = "Make strong general-purpose reasoning cheaper to apply before coding-agent ingestion through lightweight refinement templates.", reason = "The latest comment on this issue already shows a concrete starter set, making it ready for a bounded slice.", promotion_signal = "Promote when a few reusable templates reduce ambiguity before execution without adding a second workflow layer.", suggested_first_slice = "Land the pre-ingestion issue comment and boundary/proof clarification templates." },
  { id = "declarative-portability-follow-through", title = "Declarative portability follow-through", priority = "fifth", issues = ["#258"], outcome = "Shift more stable lifecycle, reporting, and runtime truths into declarative contract artifacts with thinner Python adapters.", reason = "Portability improved directionally, but major stable truths still live in large Python implementations instead of more portable contract surfaces.", promotion_signal = "Promote when a bounded lifecycle or reporting truth is stable enough to extract or when another outside-facing integration still requires reverse-engineering large Python branches.", suggested_first_slice = "Identify one stable lifecycle or reporting contract currently trapped in Python and extract it into a smaller shared declarative artifact with adapter-backed validation." },
  { id = "system-intent-operational-follow-through", title = "System intent operational follow-through", priority = "sixth", issues = ["#279"], outcome = "Finish making compiled system intent an explicit planning-time input so slice shaping and larger-intent validation can use it without adding heavy ritual.", reason = "The compiled system-intent substrate and extraction boundary are now in place, but planning still lacks the compact hook that takes relevant system intent into account when shaping slices and judging broader directional progress.", promotion_signal = "Promote when planning still has to infer relevant system intent manually during slice shaping or larger-intent validation, even though the compiled declaration and source-hints boundary are already settled.", suggested_first_slice = "Land the smallest planning-side hook that surfaces relevant system intent during slice shaping and larger-intent validation." },
  { id = "formal-workflow-substrate-follow-through", title = "Formal workflow substrate follow-through", priority = "seventh", issues = ["#278"], outcome = "Move more workflow authority from markdown-first contracts into a stricter, composable substrate without turning the product into a heavyweight workflow engine.", reason = "The structured config substrate landed, but the broader workflow-definition issue remains open and is still unrepresented in checked-in planning state.", promotion_signal = "Promote when ordinary work still depends on markdown workflow prose as the primary operational authority rather than as thin explanation or adaptation.", suggested_first_slice = "Identify one stable workflow component or obligation family that can move from markdown-first prose into a smaller formal substrate without widening into full workflow-engine design." },
  { id = "further-product-compression-on-repeated-pressure", title = "Further product compression on repeated pressure", priority = "eighth", issues = ["#230", "#275", "#281"], outcome = "Continue removing, demoting, or internalizing visible product residue only when repeated real use shows another surface still imposes first-line cognitive cost.", reason = "The bounded startup/front-door/doctrine/query compression tranche is complete, but the new static-routing-table issue shows there is still one open subtraction path around generated compatibility artefacts and visible adapter residue.", promotion_signal = "Promote when repeated dogfooding shows another visible surface still behaves like a first-line handbook, leaves avoidable startup ambiguity, or keeps unnecessary generated adapter residue in the default mental model.", suggested_first_slice = "Audit the remaining generated compatibility artefacts and prove which should disappear entirely, which should become static routing tables, and which still genuinely earn their keep." },
]
candidates = [
  { priority = "first", summary = "Graceful partial compliance and bypass trust" },
  { priority = "second", summary = "Machine-first planning chain" },
  { priority = "third", summary = "Local-only integration area" },
  { priority = "fourth", summary = "Pre-ingestion refinement templates" },
  { priority = "fifth", summary = "Declarative portability follow-through" },
  { priority = "sixth", summary = "System intent operational follow-through" },
  { priority = "seventh", summary = "Formal workflow substrate follow-through" },
  { priority = "eighth", summary = "Further product compression on repeated pressure" },
]
