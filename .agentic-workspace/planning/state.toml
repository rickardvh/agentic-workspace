[todo]
active_items = []
queued_items = []

[roadmap]
lanes = [
  { id = "feature-tier-and-surface-merge", title = "Make advanced features opt-in through feature tiers and merged surfaces", priority = "1", issues = ["#449", "#450", "#453", "#454", "#455", "#456", "#457", "#458", "#459", "#460", "#467", "#469", "#471", "#472", "#473", "#474"], outcome = "make package use cheaper than bypassing it by shrinking default setup, capturing improvement signals cheaply, and merging advanced maintenance, reconciliation, review, codegen, closeout, and external-evidence surfaces behind explicit tiers.", reason = "#473/#474 came from dogfooding #438 closeout and directly affect whether agents can trust compact closeout/reconciliation without remembering hidden defaults.", promotion_signal = "continue current trust/cost friction before lower-priority capability extraction so package use stays cheaper than bypassing it.", suggested_first_slice = "#473 is active; use #474 next unless validation discovers a more severe closeout trust blocker." },
  { id = "command-generation-extraction-boundary", title = "Isolate generic command-generation machinery for future extraction", priority = "2", issues = ["#430", "#431", "#432", "#433", "#434", "#435"], outcome = "separate generic command-package generation from Agentic Workspace-specific contracts so later extraction is intentional rather than inertia.", reason = "still valuable, but lower than default-footprint and feature-tier work because generator extraction should not expand shipped or first-contact surfaces.", promotion_signal = "promote when generated-command work is selected or after #459 decides shipped-footprint boundaries.", suggested_first_slice = "#431, because package boundary creation should precede schema and loader decoupling." },
]
candidates = []
