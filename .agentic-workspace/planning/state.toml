[todo]
active_items = [
  { id = "low-trust-delegated-run-preflight-and-closeout-gating", status = "in-progress", surface = ".agentic-workspace/planning/state.toml", why_now = "The recent Gemini incident showed that weaker or undisciplined agents still have too much room to misread planning state, fabricate residue, and close lanes dishonestly. The package needs a first runtime-side guardrail that lets bounded implementation happen without trusting delegated agents to interpret or close planning correctly.", next_action = "Define the compact preflight answer and low-trust closeout rule set for delegated runs under the runtime-posture lane, then decide whether the slice should stay a direct task or promote into an execplan.", done_when = "Checked-in planning state names the low-trust delegated-run slice explicitly, and the next implementation pass can start from a bounded contract for preflight checks, required return residue, and lower-trust closeout gating." }
]
queued_items = []

[roadmap]
lanes = [
  { id = "machine-first-planning-chain", title = "Machine-first planning chain", priority = "first", issues = ["#261", "#280", "#282"], outcome = "Replace prose-first authored execplans with a genuinely machine-first canonical planning state, explicit structured references, and proportional review residue, with derived human-readable views.", reason = "The earlier first-slice archive explicitly kept the lane open, and the live follow-on issues still show that typed references, canonical review residue, and broader machine-first planning truth remain unfinished.", promotion_signal = "Keep active while planning still depends on Markdown-authored or Markdown-only surfaces for durable active or archived state.", suggested_first_slice = "Use the canonical execplan sidecar path on a real repo slice, then continue extracting remaining Markdown-authored planning truth, typed references, and proportional review residue into machine-first records and derived views." },
  { id = "runtime-posture-selective-handoff", title = "Runtime posture and selective handoff", priority = "second", issues = ["#285", "#286", "#289"], outcome = "Make local execution posture and selective strong-reasoning handoff cheap enough to use in ordinary work.", reason = "These issues form a single execution-policy loop from posture to preflight to compact escalation, and the latest weaker-agent failure shows the lane also needs a low-trust delegated-run guardrail before broader runtime optimization can be trusted.", promotion_signal = "Promote when one compact runtime-resolution, low-trust delegated-run guardrail, or strong-reasoning handoff artifact can guide real execution behavior.", suggested_first_slice = "Add low-trust delegated-run preflight and closeout gating so bounded implementation can be delegated without trusting weaker agents to interpret planning state or close lanes honestly." },
  { id = "local-only-integration-area", title = "Local-only integration area", priority = "third", issues = ["#287", "#288"], outcome = "Create a sanctioned local-only integration area for vendor/runtime-specific bridges without turning it into shared workflow state.", reason = "These are a narrow, coherent boundary-setting slice and should stay local and disposable.", promotion_signal = "Promote when a minimal root name and boundary convention are enough for safe local shortcuts.", suggested_first_slice = "Choose the local-only integration root and vendor/runtime subfolder convention." },
  { id = "pre-ingestion-refinement-templates", title = "Pre-ingestion refinement templates", priority = "fourth", issues = ["#290"], outcome = "Make strong general-purpose reasoning cheaper to apply before coding-agent ingestion through lightweight refinement templates.", reason = "The latest comment on this issue already shows a concrete starter set, making it ready for a bounded slice.", promotion_signal = "Promote when a few reusable templates reduce ambiguity before execution without adding a second workflow layer.", suggested_first_slice = "Land the pre-ingestion issue comment and boundary/proof clarification templates." },
  { id = "graceful-partial-compliance", title = "Graceful partial compliance and bypass trust", priority = "fifth", issues = ["#251", "#253", "#254", "#255", "#256", "#277"], outcome = "Make package startup and compact operating surfaces easier for generic agents to discover and cheaper to use, while making likely package bypass or missing planning residue visible as lower-trust review and closure state.", reason = "The startup/compliance cluster remains the main generic-agent adoption lane, and the newer preflight-first takeover issue now sits alongside the still-open cheaper-surface and lower-trust review work.", promotion_signal = "Promote when repeated generic-agent use still makes bypass locally cheaper than package use, startup gates still fail before package routing helps, or lower-trust closeout signals recur.", suggested_first_slice = "Use the intent-validation surface to audit startup discoverability, compact core-surface cost, preflight takeover robustness, and lower-trust closeout signals for the current graceful-compliance cluster." },
  { id = "declarative-portability-follow-through", title = "Declarative portability follow-through", priority = "sixth", issues = ["#258"], outcome = "Shift more stable lifecycle, reporting, and runtime truths into declarative contract artifacts with thinner Python adapters.", reason = "Portability improved directionally, but major stable truths still live in large Python implementations instead of more portable contract surfaces.", promotion_signal = "Promote when a bounded lifecycle or reporting truth is stable enough to extract or when another outside-facing integration still requires reverse-engineering large Python branches.", suggested_first_slice = "Identify one stable lifecycle or reporting contract currently trapped in Python and extract it into a smaller shared declarative artifact with adapter-backed validation." },
  { id = "system-intent-operational-follow-through", title = "System intent operational follow-through", priority = "seventh", issues = ["#276", "#279"], outcome = "Finish making mirrored system intent and planning-time consumption operational enough that slice shaping and larger-intent validation can use it explicitly without adding heavy ritual.", reason = "The mirrored system-intent substrate now exists, but open follow-on asks still remain around how planning should consume relevant system intent when shaping slices and validating broader lane advancement.", promotion_signal = "Promote when planning still has to infer relevant system intent manually during slice shaping or larger-intent validation, or when the current mirror/source-declaration contract still feels materially incomplete in ordinary use.", suggested_first_slice = "Audit which parts of the mirrored system-intent issue are already landed versus stale, then land the smallest planning-side hook that surfaces relevant system intent during slice shaping and larger-intent validation." },
  { id = "formal-workflow-substrate-follow-through", title = "Formal workflow substrate follow-through", priority = "eighth", issues = ["#278"], outcome = "Move more workflow authority from markdown-first contracts into a stricter, composable substrate without turning the product into a heavyweight workflow engine.", reason = "The structured config substrate landed, but the broader workflow-definition issue remains open and is still unrepresented in checked-in planning state.", promotion_signal = "Promote when ordinary work still depends on markdown workflow prose as the primary operational authority rather than as thin explanation or adaptation.", suggested_first_slice = "Identify one stable workflow component or obligation family that can move from markdown-first prose into a smaller formal substrate without widening into full workflow-engine design." },
  { id = "further-product-compression-on-repeated-pressure", title = "Further product compression on repeated pressure", priority = "ninth", issues = ["#230", "#275", "#281"], outcome = "Continue removing, demoting, or internalizing visible product residue only when repeated real use shows another surface still imposes first-line cognitive cost.", reason = "The bounded startup/front-door/doctrine/query compression tranche is complete, but the new static-routing-table issue shows there is still one open subtraction path around generated compatibility artefacts and visible adapter residue.", promotion_signal = "Promote when repeated dogfooding shows another visible surface still behaves like a first-line handbook, leaves avoidable startup ambiguity, or keeps unnecessary generated adapter residue in the default mental model.", suggested_first_slice = "Audit the remaining generated compatibility artefacts and prove which should disappear entirely, which should become static routing tables, and which still genuinely earn their keep." },
]
candidates = [
  { priority = "first", summary = "Machine-first planning chain" },
  { priority = "second", summary = "Runtime posture and selective handoff" },
  { priority = "third", summary = "Local-only integration area" },
  { priority = "fourth", summary = "Pre-ingestion refinement templates" },
  { priority = "fifth", summary = "Graceful partial compliance and bypass trust" },
  { priority = "sixth", summary = "Declarative portability follow-through" },
  { priority = "seventh", summary = "System intent operational follow-through" },
  { priority = "eighth", summary = "Formal workflow substrate follow-through" },
  { priority = "ninth", summary = "Further product compression on repeated pressure" },
]
