[todo]
active_items = []
queued_items = []

[roadmap]
lanes = [
  { id = "durable-residue-routing", title = "Route durable motivation and lessons out of planning archives", priority = "4", issues = ["#491", "#492", "#493", "#494", "#495"], outcome = "closeout routes durable learning to Memory/docs/contracts/checks/planning instead of relying on archives.", reason = "important but downstream of current product-shape and lifecycle boundaries.", promotion_signal = "promote after shipped surface and lifecycle issues close.", suggested_first_slice = "#492" },
  { id = "planning-state-maturity", title = "Make planning state explicit about maturity, roles, and promotion", priority = "5", issues = ["#496", "#497", "#498", "#499", "#500", "#501"], outcome = "planning state distinguishes directions, lanes, ready slices, active execplans, review, and closeout residue without acting like a vague TODO dump.", reason = "important for team-style handoff and future autopilot quality, but it should follow root/module boundary compression unless a planning-shape blocker appears.", promotion_signal = "promote when module-boundary work no longer blocks state-shape changes or when planning-summary friction becomes the active blocker.", suggested_first_slice = "#497" },
  { id = "structured-file-contract", title = "Schema or validate machine-readable files", priority = "6", issues = ["#502", "#503", "#504", "#505", "#506", "#508", "#509"], outcome = "machine-readable JSON/TOML/YAML files are schema-backed, typed-validator-backed, generated/derived, or explicitly classified diagnostics.", reason = "prevents freeform machine-readable state and supports generated/module-manifest work, but should be sliced after the first module-boundary decisions.", promotion_signal = "promote when component manifests or planning-state work starts adding new structured files.", suggested_first_slice = "#503" },
  { id = "workflow-friction-polish", title = "Remove small package-use friction discovered during dogfooding", priority = "7", issues = ["#507", "#515", "#522", "#523"], outcome = "common selector guesses, execplan scaffolding, archive cleanup, and historical/live tracking distinctions are easier to recover from without broad rereads or projection-noise closeout.", reason = "these are real dogfooding friction, but they are bounded polish unless they block the active lane.", promotion_signal = "promote opportunistically when touching report selectors, execplan authoring/checks, archive cleanup, or root CLI authority routing.", suggested_first_slice = "#507" },
  { id = "agent-created-aids-lifecycle", title = "Give agent-created reusable aids a lightweight lifecycle", priority = "8", issues = ["#516", "#517", "#518", "#519", "#520", "#521"], outcome = "agents can create, discover, validate, promote, and retire reusable aids without turning the repo into unowned scripts or runtime-specific residue.", reason = "supports long-run operating-cost reduction, but should follow the current root/module and structured-file contract lanes so aid manifests and discovery reuse those boundaries.", promotion_signal = "promote when repeated local workflow aids need a durable repo-native home or when structured-file/module-component work needs aid metadata.", suggested_first_slice = "#517" },
]
candidates = []
