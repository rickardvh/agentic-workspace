[todo]
active_items = []
queued_items = []

[roadmap]
lanes = [
  { id = "further-product-compression-on-repeated-pressure", title = "Further product compression on repeated pressure", priority = "fifth", issues = ["#230"], outcome = "Continue removing, demoting, or internalizing visible product residue only when repeated real use shows another surface still imposes first-line cognitive cost.", reason = "#281 compressed the generated helper docs into static routing adapters; #230 remains open for broader subtraction and product-shape compression that should not be closed by one cleanup.", promotion_signal = "Promote when repeated dogfooding shows another visible surface behaves like a first-line handbook, leaves startup ambiguity, keeps unnecessary adapter residue in the default mental model, leaves checked-in adapters stale, or lets memory runbooks accumulate rationale that belongs in a smaller decision surface.", suggested_first_slice = "Wait for a fresh repeated-use signal before choosing the next product-compression target under #230." },
]
candidates = [
  { priority = "fifth", summary = "Further product compression on repeated pressure" },
]
