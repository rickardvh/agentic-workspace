[todo]
active_items = [
  { id = "structured-reference-roles", status = "in-progress", title = "Add structured reference roles for closeout and review evidence", source = "github:#444", why_now = "After #443 suppressed consumed child continuations, #444 is the next issue in the high-priority dogfooding lane and should replace prose-sensitive issue reference interpretation with explicit roles.", next_action = "Plan and implement structured reference role handling for closeout and review evidence.", done_when = "Closeout/review artifacts can record issue refs with explicit roles and finished-work inspection consumes role data before fallback prose scanning." },
]
queued_items = []

[roadmap]
lanes = [
  { id = "long-session-dogfooding-intent-loop", title = "Address long-session dogfooding findings for intent continuity", priority = "1", issues = ["#442", "#443", "#444", "#445", "#446", "#447", "#448"], outcome = "make plan -> implement -> evaluate continuation behavior first-class enough that long issue lanes do not depend on chat memory or manual user detection.", reason = "live GitHub intake created #442-#448 from the checked-in long-session dogfooding report; #443 is active first because stale continuation pressure currently distorts execution readiness.", promotion_signal = "active now by maintainer request to ingest and address the new issues first.", suggested_first_slice = "#443, because stale child-continuation pressure is visible in compact summary and blocks clean autopilot routing for #442." },
  { id = "command-generation-extraction-boundary", title = "Isolate generic command-generation machinery for future extraction", priority = "4", issues = ["#430", "#431", "#432", "#433", "#434", "#435"], outcome = "separate generic command-package generation from Agentic Workspace-specific contracts so later extraction is intentional rather than inertia.", reason = "live GitHub intake shows #430 parent and child slices #431-#435 open; this lane depends on preserving workspace command truth while isolating generator mechanics.", promotion_signal = "promote after the immediate broad-ingestion guardrail or when generated-command work is selected.", suggested_first_slice = "#431, because package boundary creation should precede schema and loader decoupling." },
  { id = "operational-footprint-pressure", title = "Add operational footprint pressure for long-running package artifacts", priority = "5", issues = ["#436", "#437", "#438", "#439", "#440", "#441"], outcome = "make artifact growth visible across current memory, archives, reviews, and generated outputs without expanding ordinary startup.", reason = "live GitHub intake shows #436 parent and child slices #437-#441 open; these are accepted footprint-control issues from dogfooding.", promotion_signal = "promote after higher-risk workflow and architecture lanes or when footprint pressure blocks ordinary operation.", suggested_first_slice = "#437, because stale current-memory is both correctness and footprint risk." },
]
candidates = []
