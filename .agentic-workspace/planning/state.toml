# Agentic Workspace managed state.
# Do not edit by hand when the CLI is available.
# Inspect: uv run agentic-workspace summary --format json
# Mutate through the package command named by that output.

kind = "agentic-planning-state"
schema_version = "planning-state/v1"

work_items = []

[active]
execplans = []

[todo]
active_items = []
queued_items = []

[roadmap]
lanes = []
candidates = [
  { id = "github-planning-gates-dogfooding-p1", maturity = "candidate", status = "next", priority = "P1", refs = "GitHub #1000, #970, #971, #975", title = "Tighten planning gates and dogfooding regressions", outcome = "Planning safety tests, completion gates, and epic decomposition checks stop depending on ambient or self-reinforcing state.", reason = "High-priority non-codegen review and dogfooding findings should follow the P0 planning trust blockers.", promotion_signal = "Promote when touching implement safety, closeout gates, or epic decomposition.", suggested_first_slice = "Make #1000 independent of ambient active-execplan state or convert one review finding into a focused gate regression." },
  { id = "github-planning-workflow-observability-p2", maturity = "candidate", status = "deferred", priority = "P2", refs = "GitHub #974, #976, #981, #983, #984, #985, #986, #988", title = "Improve planning workflow observability and skill support", outcome = "Intent verification, Memory consultation, proof selection, SkillSpec gates, operating-loop slots, and next-safe-action packets reduce live context by answering what to read, what to skip, what proof is required, and whether an artifact is warranted.", reason = "Medium-priority planning workflow issues are accepted but should remain behind immediate trust and recoverability failures; when promoted, success should be measured by fewer raw-file reads and fewer human corrections, not by adding more workflow prose.", promotion_signal = "Promote after P0/P1 issues land or when a selected fix needs one of these surfaces as a prerequisite.", suggested_first_slice = "Define the smallest next-safe-action or intent-verification packet that can say answer-directly/no-artifact, use state.toml, or create an execplan, with tests for routine triage and durable intake." },
  { id = "github-low-priority-planning-lanes-p3", maturity = "candidate", status = "deferred", priority = "P3", refs = "GitHub #595, #627, #960, #961, #962, #963, #964, #989", title = "Defer broader planning, dogfooding, and SkillSpec lanes", outcome = "Lower-priority planning lanes remain visible while future SkillSpec and dogfooding work proves proportionality: compact guidance should prevent over-producing reviews, Memory notes, execplans, or raw context for routine tasks.", reason = "Issue intake found these are low-priority parent lanes, pilots, downstream SkillSpec work, or previously deferred friction lanes; when promoted, they should harden context-reduction behavior through real dogfooding rather than expanding visible methodology.", promotion_signal = "Promote only when the broader lane is intentionally selected.", suggested_first_slice = "For dogfooding, use #595 to measure context burden and artifact footprint on one real host-repo slice; for SkillSpec work, start with #961 and encode proportionality/no-artifact-by-default semantics before generated skill follow-ons." },
  { id = "github-codegen-deferred-p4", maturity = "candidate", status = "deferred", priority = "P4", refs = "GitHub #892, #929, #965, #972, #977, #978, #992, #994, #999, #1001, #1002, #1003, #1004, #1005, #1008", title = "Keep codegen-related issues deferred lowest priority", outcome = "Generated-command-package, generated-resource, Docker conformance, and generated text/runtime issues stay out of near-term selection.", reason = "User explicitly requested codegen-related work be deferred as low priority; these issues already carry codegen and lowest/deferred upstream labels.", promotion_signal = "Promote only after higher-priority non-codegen planning trust and recoverability issues are handled or maintainer direction changes.", suggested_first_slice = "If re-promoted, pick one bounded #892 prerequisite rather than reopening the full codegen epic." },
]
