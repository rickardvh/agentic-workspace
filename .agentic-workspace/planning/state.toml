[todo]
active_items = []
queued_items = []

[roadmap]
lanes = [
  { id = "architecture-boundary-remaining-slices", title = "Finish remaining architecture boundary checks", priority = "2", issues = ["#409", "#410", "#411"], outcome = "complete the remaining architecture-boundary lane by reducing root CLI interface authority and strengthening source/payload/root-install synchronization proof.", reason = "#412-#414 and #415-#418 are closed; #410 and #411 remain open under #409.", promotion_signal = "promote after the workflow guardrail lane is addressed or when architecture-boundary work is explicitly selected.", suggested_first_slice = "#410, because direct CLI authority remains the highest-risk source of future drift." },
  { id = "command-generation-extraction-boundary", title = "Isolate generic command-generation machinery for future extraction", priority = "4", issues = ["#430", "#431", "#432", "#433", "#434", "#435"], outcome = "separate generic command-package generation from Agentic Workspace-specific contracts so later extraction is intentional rather than inertia.", reason = "live GitHub intake shows #430 parent and child slices #431-#435 open; this lane depends on preserving workspace command truth while isolating generator mechanics.", promotion_signal = "promote after the immediate broad-ingestion guardrail or when generated-command work is selected.", suggested_first_slice = "#431, because package boundary creation should precede schema and loader decoupling." },
  { id = "operational-footprint-pressure", title = "Add operational footprint pressure for long-running package artifacts", priority = "5", issues = ["#436", "#437", "#438", "#439", "#440", "#441"], outcome = "make artifact growth visible across current memory, archives, reviews, and generated outputs without expanding ordinary startup.", reason = "live GitHub intake shows #436 parent and child slices #437-#441 open; these are accepted footprint-control issues from dogfooding.", promotion_signal = "promote after higher-risk workflow and architecture lanes or when footprint pressure blocks ordinary operation.", suggested_first_slice = "#437, because stale current-memory is both correctness and footprint risk." },
]
candidates = []
