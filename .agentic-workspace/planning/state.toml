[todo]
active_items = [
  { id = "capability-class-delegation-and-handoff-posture", surface = ".agentic-workspace/planning/execplans/capability-class-delegation-and-handoff-posture-2026-04-23.md", why_now = "The first roadmap lane is already archived, and the next open lane has a small high-leverage contract slice that should reduce delegation waste quickly." },
]
queued_items = []

[roadmap]
lanes = [
  { id = "repo-owned-structured-agent-configuration-system", title = "Repo-owned structured agent configuration system", priority = "first", issues = ["#269", "#270", "#271", "#272", "#273", "#274", "#275", "#276", "#278", "#279", "#281"], outcome = "Build a repo-owned agent configuration substrate that planning, memory, and future behavior modules can plug into while keeping structured contracts canonical, prose surfaces as adapters, startup bridges minimal, and weak-agent routing static.", reason = "The live #270 cluster remains the broadest parent lane and now clearly includes the mirrored system-intent contract, the slim root AGENTS entrypoint, the stricter workflow definition, and the move away from generated compatibility artifacts toward static routing tables.", promotion_signal = "Promote when the authority map, selective loading, prose adapter role, workflow definition, module attachment points, and static routing tables are all explicit enough that later slices can plug into one stable substrate instead of reopening the same boundary questions.", suggested_first_slice = "Define the structured configuration model and authority map (#271) before attaching the mirrored system-intent contract (#276), selective loading, workflow formalization, and prose adapters." },
  { id = "capability-class-delegation-and-handoff-posture", title = "Capability-class delegation and handoff posture", priority = "second", issues = ["#283", "#284"], outcome = "Make planning and workflow contracts carry soft execution-class guidance so bounded work can prefer weak, strong, local, or external execution without binding to one runtime.", reason = "The new #283/#284 cluster is a small but high-leverage execution-contract lane: it can reduce token waste, improve handoff decisions, and shape work toward the right execution strength before the runtime layer resolves concrete targets.", promotion_signal = "Promote when bounded work can express capability-class posture in a compact machine-readable way and the runtime/local layer can resolve those soft recommendations against available targets.", suggested_first_slice = "Define the minimal capability-class metadata shape in #284 before widening the work contract or binding it to any concrete runtime target profiles." },
  { id = "machine-first-planning-chain", title = "Machine-first planning chain", priority = "third", issues = ["#261", "#280", "#282"], outcome = "Replace prose-first authored execplans with a genuinely machine-first canonical planning state, explicit structured references, and proportional review residue, with derived human-readable views.", reason = "The planning-chain lane remains open, and the updated follow-on issues make it clear that typed references and review retention still need to be formalized before the chain is truly machine-first end to end.", promotion_signal = "Keep active while planning still depends on Markdown-authored or Markdown-only surfaces for durable active or archived state.", suggested_first_slice = "Use the canonical execplan sidecar path on a real repo slice, then continue extracting remaining Markdown-authored planning truth, typed references, and proportional review residue into machine-first records and derived views." },
  { id = "graceful-partial-compliance", title = "Graceful partial compliance and bypass trust", priority = "fourth", issues = ["#251", "#253", "#254", "#255", "#256", "#277"], outcome = "Make package startup and compact operating surfaces easier for generic agents to discover and cheaper to use, while making likely package bypass or missing planning residue visible as lower-trust review and closure state.", reason = "The startup/compliance cluster is still broad and now includes preflight-first takeover robustness, so the repo needs it ranked ahead of the older portability and residue-only cleanup lanes.", promotion_signal = "Promote when intent-validation surfaces show open-but-untracked external or repo-known follow-on, or when repeated generic-agent use still makes bypass locally cheaper than package use.", suggested_first_slice = "Use the intent-validation surface to audit startup discoverability, compact core-surface cost, and lower-trust closeout signals for the current graceful-compliance cluster." },
  { id = "declarative-portability-follow-through", title = "Declarative portability follow-through", priority = "fifth", issues = ["#258"], outcome = "Shift more stable lifecycle, reporting, and runtime truths into declarative contract artifacts with thinner Python adapters.", reason = "Portability improved directionally, but major stable truths still live in large Python implementations instead of more portable contract surfaces.", promotion_signal = "Promote when a bounded lifecycle/reporting truth is stable enough to extract or when another outside-facing integration still requires reverse-engineering large Python branches.", suggested_first_slice = "Identify one stable lifecycle or reporting contract currently trapped in Python and extract it into a smaller shared declarative artifact with adapter-backed validation." },
  { id = "further-product-compression-on-repeated-pressure", title = "Further product compression on repeated pressure", priority = "sixth", issues = ["#230"], outcome = "Continue removing, demoting, or internalizing visible product residue only when repeated real use shows another surface still imposes first-line cognitive cost.", reason = "The bounded startup/front-door/doctrine/query compression tranche is complete; future subtraction should reopen only on repeated evidence rather than on abstract appetite for simplification.", promotion_signal = "Promote when repeated dogfooding shows another visible surface still behaves like a first-line handbook, leaves avoidable startup ambiguity, or keeps unnecessary product residue in the default mental model.", suggested_first_slice = "Capture one repeated remaining residue class and prove whether it should disappear, merge, demote, or move behind a compact query surface." },
  { id = "memory-recurring-friction-improvement-pressure", title = "Memory recurring-friction improvement pressure", priority = "seventh", issues = ["#263"], outcome = "Preserve lightweight recurring-friction evidence in Memory or adjacent durable surfaces so repeated package failures accumulate visible improvement pressure without turning every annoyance into backlog.", reason = "The lane now has one real dogfooded recurring-friction entry plus a cheap dedicated checker, but the broader question remains whether this should stay repo-local validation or graduate into stronger shipped validation/reporting once more ordinary evidence accumulates.", promotion_signal = "Promote when repeated package friction still disappears, when the recurring-friction checker proves too narrow or too manual, or when repeated entries show the package should promote stronger remediation automatically.", suggested_first_slice = "Use the recurring-friction ledger on a second real repeated signal or promote the checker/reporting path into a stronger shipped validation surface if the repo keeps needing the same follow-through." },
]
candidates = [
  { priority = "first", summary = "Repo-owned structured agent configuration system" },
  { priority = "second", summary = "Capability-class delegation and handoff posture" },
  { priority = "third", summary = "Machine-first planning chain" },
  { priority = "fourth", summary = "Graceful partial compliance and bypass trust" },
  { priority = "fifth", summary = "Declarative portability follow-through" },
  { priority = "sixth", summary = "Further product compression on repeated pressure" },
  { priority = "seventh", summary = "Memory recurring-friction improvement pressure" },
]
