kind = "agentic-planning-state"
schema_version = "planning-state/v1"

work_items = []

[active]
execplans = []

[todo]
active_items = []
queued_items = []

[roadmap]
lanes = [
  { type = "lane", id = "memory-operating-loop", maturity = "candidate", status = "next", priority = "P0", refs = "GitHub #601, #602, #603, #604, #605, #606", title = "Make Memory consultation and learning capture part of the normal operating loop", outcome = "Expose compact Memory consult guidance before work, explicit durable-learning routing at closeout, existing-note-first capture, promotion pressure, and lightweight review evidence.", why_now = "The issue cluster is coherent, repo-local, and directly supports the package's normal operating loop without needing external host-repo execution.", promotion_signal = "Promote when ready to start the Memory lane; begin with startup/summary pre-work consult routing because it makes the rest of the loop visible.", suggested_first_slice = "#602: surface pre-work Memory consult recommendations in compact routing." },
  { type = "lane", id = "cli-version-drift-handshake", maturity = "candidate", status = "next", priority = "P1", refs = "GitHub #607, #608, #609, #610", title = "Detect invoked CLI version drift against repo expectations", reason = "Routes the remaining #607 parent intent through #610 after #608 and #609 landed.", outcome = "Expose invoked CLI identity, let host repos declare expected CLI posture, and report executable drift separately from managed payload drift.", why_now = "Host repos cannot trust startup/status output after package updates unless they can tell whether the executed CLI matches repo-owned expectations; this is package-local and directly actionable here.", promotion_signal = "Promote when command provenance or host-repo install trust remains the active priority; #608 and #609 landed, so executable drift reporting is now the remaining package-local slice.", suggested_first_slice = "#610: report executable drift separately from payload drift." },
  { type = "lane", id = "assurance-trust-and-evidence", maturity = "candidate", status = "deferred", priority = "P2", refs = "GitHub #596, #597, #598", title = "Tighten adaptive-assurance trust state, proof evidence, and onboarding", outcome = "Make assurance outputs distinguish declared, inferred, required, advisory, waived, blocking, selected, and executed proof states while giving host repos a small configuration path.", why_later = "This is package-local and actionable here, but it follows the current assurance primitives and should not outrank the Memory operating-loop or CLI provenance intake.", promotion_signal = "Promote after the Memory lane is either active or deliberately deferred, or when high-assurance review/closeout trust becomes the active maintainer priority.", suggested_first_slice = "#596: expose assurance trust state in compact outputs." },
  { type = "lane", id = "host-feedback-and-operating-cost", maturity = "candidate", status = "deferred", priority = "P3", refs = "GitHub #599, #600; excludes #595", title = "Prepare host feedback intake and review assurance operating cost", outcome = "Add a compact host-repo dogfooding report template and use resulting evidence to compare assurance safety gains against low-residue operating cost.", why_later = "#600 is actionable in this repo, but #599 depends on real-host evidence and #595 is intentionally excluded from this local intake because it must run in another repo.", promotion_signal = "Promote #600 when a structured host feedback template is needed before or after a real-host run; promote #599 only when host evidence exists.", suggested_first_slice = "#600: add a host-repo dogfooding report template." },
]
candidates = [
  { type = "candidate", id = "dogfooding-closeout-friction", maturity = "candidate", status = "deferred", priority = "P2", refs = "GitHub #611, #612, #613", title = "Reduce dogfooding closeout friction from tracker cache, routing, and schema gaps", reason = "Created from package-local dogfooding findings after the CLI compatibility lane; keep below active Memory and CLI drift lanes until those priorities are explicitly changed.", outcome = "Make closeout evidence freshness safer, archived continuation routing cheaper, and cli_compatibility output contracts more reliable.", suggested_first_slice = "#612: flag or refresh external evidence after tracker mutations." },
]
