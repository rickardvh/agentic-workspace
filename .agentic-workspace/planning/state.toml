[todo]
active_items = []
queued_items = []

[roadmap]
lanes = [
  { id = "generated-cli-adapter-migration", title = "Generated CLI adapter migration", priority = "first", issues = ["#230", "#356", "#358", "#359", "#360", "#361", "#362", "#363"], outcome = "Move shipped CLI adapter interface authority behind implementation-independent contracts while preserving runtime primitive ownership.", reason = "#357 completed the prerequisite audit; refreshed issue state splits the implementation lane into #358-#363.", promotion_signal = "Start with #358, then #359 using root `defaults` as the first generated adapter candidate.", suggested_first_slice = "Define the canonical command-adapter generation contract in #358 before generating the root `defaults` adapter in #359." },
]
candidates = []
