[todo]
active_items = [
  { id = "planning-default-core-install", status = "in-progress", title = "Shrink default planning install to core daily-operation surfaces", source = "github:#463", why_now = "#462 classified the planning payload and identified the smallest safe default install; #463 is the next #461 child that turns that classification into smaller default behavior.", surface = ".agentic-workspace/planning/execplans/planning-default-core-install.plan.json" },
]
queued_items = []

[roadmap]
lanes = [
  { id = "planning-payload-boundary", title = "Reclassify planning payload into core, optional, on-demand, and extraction candidates", priority = "1", issues = ["#461", "#463", "#464", "#465", "#466"], outcome = "make planning smaller by default and prevent planning from absorbing every useful agent-operation capability.", reason = "#462 classified the payload and routed the next child to #463 default shrink.", promotion_signal = "continue #463 until new planning installs contain only core daily-operation surfaces.", suggested_first_slice = "#463 is active; then sequence #464, #465, and #466 from the classification." },
  { id = "feature-tier-and-surface-merge", title = "Make advanced features opt-in through feature tiers and merged surfaces", priority = "2", issues = ["#449", "#450", "#451", "#452", "#453", "#454", "#455", "#456", "#457", "#458", "#459", "#460", "#467", "#469"], outcome = "make package use cheaper than bypassing it by shrinking default setup, capturing improvement signals cheaply, and merging advanced maintenance, reconciliation, review, and codegen surfaces behind explicit tiers.", reason = "#450 and children are the broad workspace compression lane that should absorb several overlapping surfaces; #469 is upstream signal capture and #467 turns those signals into total-operating-cost reduction, but #451 criteria should still precede broad behavior changes.", promotion_signal = "promote after the active #461/#462 payload-boundary lane or when payload classification needs repo-wide feature-tier decisions.", suggested_first_slice = "#451, unless #469 is needed first to prevent dogfooding signals from disappearing." },
  { id = "command-generation-extraction-boundary", title = "Isolate generic command-generation machinery for future extraction", priority = "3", issues = ["#430", "#431", "#432", "#433", "#434", "#435"], outcome = "separate generic command-package generation from Agentic Workspace-specific contracts so later extraction is intentional rather than inertia.", reason = "still valuable, but lower than default-footprint and feature-tier work because generator extraction should not expand shipped or first-contact surfaces.", promotion_signal = "promote when generated-command work is selected or after #459 decides shipped-footprint boundaries.", suggested_first_slice = "#431, because package boundary creation should precede schema and loader decoupling." },
  { id = "operational-footprint-pressure", title = "Add operational footprint pressure for long-running package artifacts", priority = "4", issues = ["#436", "#437", "#438", "#439", "#440", "#441"], outcome = "make artifact growth visible across current memory, archives, reviews, and generated outputs without expanding ordinary startup.", reason = "accepted footprint-control lane, but #455 may supersede part of #437 and #454 may merge some maintenance-pressure views.", promotion_signal = "promote after higher-risk workflow and architecture lanes or when footprint pressure blocks ordinary operation.", suggested_first_slice = "#437 unless #455 removes the stale current-memory surface first." },
]
candidates = []
