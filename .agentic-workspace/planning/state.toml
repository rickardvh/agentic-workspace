[todo]
active_items = []
queued_items = []

[roadmap]
lanes = [
  { id = "post-compression-adapter-followthrough", title = "Post-compression adapter and lifecycle follow-through", priority = "first", issues = ["#377", "#378"], outcome = "Cautiously extend generated adapter/lifecycle conformance work after the clean #376 baseline.", reason = "#376 found the baseline clean and selected #377 as the next safe implementation target.", promotion_signal = "Continue with #377 generated read-only package adapter.", suggested_first_slice = "Generate or mechanically assemble memory package status as the next read-only adapter if inspection confirms it is the lowest-risk candidate." },
  { id = "repo-local-dogfooding-skill", title = "Repo-local self-improvement dogfooding skill", priority = "second", issues = ["#379"], outcome = "Add a repo-local non-shipped skill for bounded system-intent-driven dogfooding.", reason = "#379 is independent but should follow the baseline/adapter safety pass.", promotion_signal = "Implement after the immediate post-compression checks/slices or if dogfooding friction blocks them.", suggested_first_slice = "Create the smallest repo-local self-improvement skill using existing package surfaces." },
]
candidates = []
