[todo]
active_items = []
queued_items = []

[roadmap]
lanes = [
  { id = "further-product-compression-on-repeated-pressure", title = "Further product compression on repeated pressure", priority = "fifth", issues = ["#230", "#281"], outcome = "Continue removing, demoting, or internalizing visible product residue only when repeated real use shows another surface still imposes first-line cognitive cost.", reason = "#319 and #326 landed as concrete product-compression fixes; #230 and #281 remain open for broader subtraction and generated-compatibility artifact decisions.", promotion_signal = "Promote when repeated dogfooding shows another visible surface behaves like a first-line handbook, leaves startup ambiguity, keeps unnecessary generated adapter residue in the default mental model, leaves checked-in adapters stale, or lets memory runbooks accumulate rationale that belongs in a smaller decision surface.", suggested_first_slice = "Audit the remaining generated compatibility artifacts under #281 and decide which should disappear, become static routing tables, or still earn their keep." },
  { id = "generated-tool-conformance", title = "Generated-tool conformance", priority = "sixth", issues = ["#306"], outcome = "Prepare a black-box conformance layer so generated CLIs, local MCP tools, static skills, or other adapters can be tested against shared operation contracts instead of trusted by implementation path.", reason = "The first conformance slice landed, but the lane should remain visible until #324 verifies whether the broader generated-tool intent is fully satisfied or only bounded-slice satisfied.", promotion_signal = "Promote if the #324 audit finds generated-tool conformance still lacks adapter coverage, negative cases, or parity checks for replaceable implementations.", suggested_first_slice = "After #324, decide whether the read-only conformance harness is enough or whether another generated-tool scenario should be added." },
  { id = "system-intent-operational-follow-through", title = "System intent operational follow-through", priority = "seventh", issues = ["#279"], outcome = "Finish making compiled system intent an explicit planning-time input so slice shaping and larger-intent validation can use it without adding heavy ritual.", reason = "The planning hook landed, but this lane should remain until #324 verifies that system-intent satisfaction was evidenced rather than assumed.", promotion_signal = "Promote if planning still has to infer relevant system intent manually during slice shaping or larger-intent validation after the closeout audit.", suggested_first_slice = "After #324, verify whether the system-intent alignment fields are sufficient or need a smaller follow-up." },
  { id = "pre-ingestion-refinement-templates", title = "Pre-ingestion refinement templates", priority = "eighth", issues = ["#290"], outcome = "Make strong general-purpose reasoning cheaper to apply before coding-agent ingestion through lightweight refinement templates.", reason = "The template slice landed, but this lane stays visible until #324 verifies whether the larger pre-ingestion intent is fully satisfied or needs more templates/proof.", promotion_signal = "Promote if issue refinement still enters coding-agent work with avoidable ambiguity after the closeout audit.", suggested_first_slice = "After #324, inspect whether the initial templates cover the recurring ambiguous intake cases or need one more bounded template." },
  { id = "local-only-integration-area", title = "Local-only integration area", priority = "ninth", issues = ["#287", "#288"], outcome = "Create a sanctioned local-only integration area for vendor/runtime-specific bridges without turning it into shared workflow state.", reason = "The root and boundary slice landed, but this lane should remain visible until #324 verifies the parent intent and any runtime-example follow-up. Keep it distinct from #328: integration shims are local execution aids; local-only memory is advisory machine-local knowledge.", promotion_signal = "Promote if local users still put disposable integrations into shared workflow surfaces or if runtime-specific examples are needed after closeout audit.", suggested_first_slice = "After #324, decide whether the local-only boundary is complete or needs one example/runtime bridge convention." },
  { id = "graceful-partial-compliance", title = "Graceful partial compliance and bypass trust", priority = "tenth", issues = ["#292", "#293", "#295", "#296", "#297", "#298", "#330"], outcome = "Make package startup, proof selection, boundary warnings, adapter authority, compact operating surfaces, and planning helpers easier for generic or bounded agents to discover and cheaper to use.", reason = "The visible follow-through slices landed, and #330 shows compact TOML planning state is still less dogfoodable than legacy Markdown TODO rows for `promote-to-plan`.", promotion_signal = "Promote if #324 or later dogfooding shows startup, compact-surface cost, proof selection, boundary confusion, lower-trust review, or package command support still fails materially.", suggested_first_slice = "After higher-priority lanes, fix #330 so `promote-to-plan` supports compact TOML active items or routes clearly to the supported compact-state promotion path." },
]
candidates = [
  { priority = "fifth", summary = "Further product compression on repeated pressure" },
  { priority = "sixth", summary = "Generated-tool conformance" },
  { priority = "seventh", summary = "System intent operational follow-through" },
  { priority = "eighth", summary = "Pre-ingestion refinement templates" },
  { priority = "ninth", summary = "Local-only integration area" },
  { priority = "tenth", summary = "Graceful partial compliance and bypass trust" },
]
