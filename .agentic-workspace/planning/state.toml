# Agentic Workspace managed state.
# Do not edit by hand when the CLI is available.
# Inspect: uv run agentic-workspace summary --format json
# Mutate through the package command named by that output.

kind = "agentic-planning-state"
schema_version = "planning-state/v1"

work_items = []

[active]
execplans = []

[todo]
active_items = []
queued_items = []

[roadmap]
lanes = []
candidates = [
  { id = "github-1076-dogfooding-build-duplicate-member-warnings-shoul", maturity = "candidate", status = "next", priority = "P3", refs = "GitHub #1076", title = "[Dogfooding]: Build duplicate-member warnings should be structured validation signals", outcome = "Route the upstream issue into a bounded Agentic Workspace slice before implementation.", reason = "Open prioritized upstream issue from refreshed external intent evidence.", promotion_signal = "Promote when this issue is selected for implementation or grouped into a bounded lane.", suggested_first_slice = "Inspect the issue body, choose the smallest workflow shape, and record exact proof before closeout." },
]
