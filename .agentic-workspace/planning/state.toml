# Agentic Workspace managed state.
# Do not edit by hand when the CLI is available.
# Inspect: uv run agentic-workspace summary --format json
# Mutate through the package command named by that output.

kind = "agentic-planning-state"
schema_version = "planning-state/v1"

work_items = []

[active]
execplans = []

[todo]
active_items = [
  { id = "tier-4-workspace-next", title = "Reduce Workspace runtime surface", maturity = "active", status = "active", surface = ".agentic-workspace/planning/execplans/tier-4-workspace-next.plan.json", why_now = "Promoted from .agentic-workspace/planning/decompositions/python-generated-cli.decomposition.json lane tier-4-workspace-next.", owner_role = "implementation", handoff_ready = true, refs = [".agentic-workspace/planning/decompositions/python-generated-cli.decomposition.json"] },
]
queued_items = []

[roadmap]
lanes = []
candidates = [
  { id = "github-codegen-deferred-p4", maturity = "candidate", status = "deferred", priority = "P4", refs = "GitHub #892, #929, #965, #972, #977, #978, #992, #994, #999, #1001, #1002, #1003, #1004, #1005, #1008", title = "Keep codegen-related issues deferred lowest priority", outcome = "Generated-command-package, generated-resource, Docker conformance, and generated text/runtime issues stay out of near-term selection.", reason = "User explicitly requested codegen-related work be deferred as low priority; these issues already carry codegen and lowest/deferred upstream labels.", promotion_signal = "Promote only after higher-priority non-codegen planning trust and recoverability issues are handled or maintainer direction changes.", suggested_first_slice = "If re-promoted, pick one bounded #892 prerequisite rather than reopening the full codegen epic." },
  { id = "github-1060-make-memory-improvement-captures-visible-to-prom", maturity = "candidate", status = "next", priority = "P2", refs = "GitHub #1060", title = "Make Memory improvement captures visible to promotion-report", outcome = "Route the upstream issue into a bounded Agentic Workspace slice before implementation.", reason = "Open prioritized upstream issue from refreshed external intent evidence.", promotion_signal = "Promote when this issue is selected for implementation or grouped into a bounded lane.", suggested_first_slice = "Inspect the issue body, choose the smallest workflow shape, and record exact proof before closeout." },
  { id = "github-1066-review-warn-before-closeout-archives-exceed-stru", maturity = "candidate", status = "next", priority = "P2", refs = "GitHub #1066", title = "[Review]: Warn before closeout archives exceed structured-file size guardrails", outcome = "Route the upstream issue into a bounded Agentic Workspace slice before implementation.", reason = "Open prioritized upstream issue from refreshed external intent evidence.", promotion_signal = "Promote when this issue is selected for implementation or grouped into a bounded lane.", suggested_first_slice = "Inspect the issue body, choose the smallest workflow shape, and record exact proof before closeout." },
  { id = "github-1071-dogfooding-github-refresh-should-reconcile-stale", maturity = "candidate", status = "next", priority = "P2", refs = "GitHub #1071", title = "[Dogfooding]: GitHub refresh should reconcile stale Planning candidates and preserve useful deltas", outcome = "Route the upstream issue into a bounded Agentic Workspace slice before implementation.", reason = "Open prioritized upstream issue from refreshed external intent evidence.", promotion_signal = "Promote when this issue is selected for implementation or grouped into a bounded lane.", suggested_first_slice = "Inspect the issue body, choose the smallest workflow shape, and record exact proof before closeout." },
]
