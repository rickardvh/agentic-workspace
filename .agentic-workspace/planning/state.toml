[todo]
active_items = []
queued_items = []

[roadmap]
lanes = [
  { id = "post-compression-adapter-followthrough", title = "Post-compression adapter and lifecycle follow-through", priority = "first", issues = ["#378"], outcome = "Cautiously extend generated adapter/lifecycle conformance work after the clean #376 baseline.", reason = "#377 is complete; #378 remains as the next post-compression follow-through slice.", promotion_signal = "Continue with #378 lifecycle dry-run conformance.", suggested_first_slice = "Make lifecycle dry-run plans conformance-backed without generating lifecycle mutation commands." },
  { id = "repo-local-dogfooding-skill", title = "Repo-local self-improvement dogfooding skill", priority = "second", issues = ["#379"], outcome = "Add a repo-local non-shipped skill for bounded system-intent-driven dogfooding.", reason = "#379 is independent but should follow the baseline/adapter safety pass.", promotion_signal = "Implement after the immediate post-compression checks/slices or if dogfooding friction blocks them.", suggested_first_slice = "Create the smallest repo-local self-improvement skill using existing package surfaces." },
]
candidates = []
