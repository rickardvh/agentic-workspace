# Agentic Workspace managed state.
# Do not edit by hand when the CLI is available.
# Inspect: uv run agentic-workspace summary --format json
# Mutate through the package command named by that output.

kind = "agentic-planning-state"
schema_version = "planning-state/v1"

work_items = []

[active]
execplans = []

[todo]
active_items = []
queued_items = []

[roadmap]
lanes = []
candidates = [
  { id = "github-929-complete-typescript-generated-json-resources-wit", maturity = "candidate", status = "next", priority = "P1", refs = "GitHub #929", title = "Complete TypeScript generated JSON resources without hiding runtime dependency", outcome = "Move TypeScript generated packages to generated JSON resources while keeping TypeScript completion blocked until operation contracts exist.", reason = "This is the next concrete #892 continuation now that Python completion is satisfied; it improves target parity without chasing low-ROI hand-owned Python reduction.", promotion_signal = "Promote before further extraction work unless a command-generation coupling blocks the TypeScript resource slice.", suggested_first_slice = "Generate TypeScript-side JSON resource files from command_package_ir, load them from generated adapters, and document any Python runtime bridge as transitional." },
  { id = "github-1100-codegen-prepare-command-generation-extraction-re", maturity = "candidate", status = "next", priority = "P2", refs = "GitHub #1100", title = "[Codegen]: Prepare command-generation extraction readiness", outcome = "Audit and narrow command-generation coupling so reusable generation machinery is separable after TypeScript resource parity.", reason = "Tier 7 extraction readiness is the remaining #892 continuation that was local-only before ingestion; do it after #929 unless it becomes a prerequisite.", promotion_signal = "Promote after #929 or when extraction-readiness coupling blocks the next generated target slice.", suggested_first_slice = "Run an extraction-readiness audit over command-generation imports, constants, generated-path assumptions, primitive ownership, and conformance fixture boundaries." },
  { id = "github-892-codegen-epic-implementation-independent-generate", maturity = "candidate", status = "next", priority = "P3", refs = "GitHub #892", title = "[Codegen Epic]: Implementation-independent generated command packages", outcome = "Coordinate the remaining codegen epic through bounded TypeScript-resource and extraction-readiness slices.", reason = "Parent issue remains the source of tier sequencing and completion rules, but should not outrank concrete child work.", promotion_signal = "Promote only as an umbrella lane when multiple child slices need parent-level coordination.", suggested_first_slice = "Promote #929 as the next concrete continuation; keep #892 open until TypeScript resource parity and extraction-readiness residue are explicitly routed or closed." },
  { id = "github-992-generated-cli-version-output-should-be-contract-", maturity = "candidate", status = "next", priority = "P2", refs = "GitHub #992", title = "Generated CLI version output should be contract-backed", outcome = "Derive generated CLI version output from contract or package metadata instead of a hardcoded placeholder.", reason = "User-visible generated behavior can drift from release metadata and creates false completion evidence.", promotion_signal = "Promote before final generated CLI completion or when touching generated parser/version behavior.", suggested_first_slice = "Replace the generated placeholder with metadata-backed version text and add static proof against fake generated literals." },
  { id = "github-1005-dogfooding-generated-force-includes-can-drift-fr", maturity = "candidate", status = "next", priority = "P2", refs = "GitHub #1005", title = "Dogfooding: generated force-includes can drift from payload surface classification", outcome = "Make generated force-include and payload-surface classification drift fail early with a targeted check.", reason = "Prevents packaging/resource omissions from surfacing only through late broad packaging proof.", promotion_signal = "Promote when adding generated package resources or force-included surfaces.", suggested_first_slice = "Derive expected classification stubs from generated force-includes or add a preflight that names missing generated surfaces." },
  { id = "github-1002-dogfooding-docker-conformance-can-fail-once-and-", maturity = "candidate", status = "next", priority = "P3", refs = "GitHub #1002", title = "Dogfooding: Docker conformance can fail once and pass unchanged on rerun", outcome = "Record failed-run-then-green Docker conformance evidence so strict closeout cannot overclaim proof quality.", reason = "Higher-risk proof reliability issue than recovered retry warnings, but not the next #892 implementation lane unless Docker proof flakiness recurs.", promotion_signal = "Promote before relying on Docker conformance as release proof or when a fresh failed-run-then-green event is observed.", suggested_first_slice = "Add structured flaky failure accounting with fixture/package/exception context and a strict mode that fails after observed adapter-contract failure." },
  { id = "github-977-review-python-docker-conformance-retry-masks-nat", maturity = "candidate", status = "next", priority = "P3", refs = "GitHub #977", title = "[Review]: Python Docker conformance retry masks native runtime crashes", outcome = "Separate clean generated Docker proof from pass-with-native-crash-retry proof.", reason = "Recovered native crashes are important closeout evidence but less direct than deterministic adapter-contract failures.", promotion_signal = "Promote with the Docker conformance reliability lane or merge into #1001/#978 if the implementation covers all retry cases.", suggested_first_slice = "Expose recovered native-crash counts and fixture context in machine-readable proof output." },
  { id = "github-1001-dogfooding-docker-conformance-can-pass-after-rec", maturity = "candidate", status = "next", priority = "P3", refs = "GitHub #1001", title = "Dogfooding: Docker conformance can pass after recovered native Python crashes", outcome = "Surface recovered native crashes as structured proof evidence with optional strict failure behavior.", reason = "Same conformance reliability lane as #977/#978; useful for proof trust but not a direct functional blocker.", promotion_signal = "Promote with the Docker conformance reliability lane or close as duplicate only after the shared behavior lands.", suggested_first_slice = "Count recovered native crashes, include fixture/entrypoint reproduction context, and support a strict non-zero-recovery mode." },
  { id = "github-978-track-intermittent-python-docker-conformance-nat", maturity = "candidate", status = "next", priority = "P3", refs = "GitHub #978", title = "Track intermittent Python Docker conformance native crash", outcome = "Bound or reproduce intermittent native Docker conformance crashes without weakening deterministic failure handling.", reason = "Tracking surface for the same native crash reliability problem; implementation can be consolidated with #977/#1001.", promotion_signal = "Promote only as part of a consolidated conformance reliability slice.", suggested_first_slice = "Classify the native -11 mode as reproducible defect or known environment residue with a bounded retry policy." },
  { id = "github-994-dogfood-generated-runtime-facades-need-explicit-", maturity = "candidate", status = "next", priority = "P4", refs = "GitHub #994", title = "Dogfood: generated runtime facades need explicit patch-forwarding semantics", outcome = "Document or test generated runtime facade export semantics so future targets do not rediscover patch-forwarding behavior.", reason = "Important portability contract, but current Python branch already fixed the immediate breakage; promote opportunistically with facade work.", promotion_signal = "Promote when adding another target runtime facade or tightening generated runtime compatibility tests.", suggested_first_slice = "Add a focused codegen test or contract note defining copied values, live aliases, or Python-only patch-forwarding hooks." }
]
