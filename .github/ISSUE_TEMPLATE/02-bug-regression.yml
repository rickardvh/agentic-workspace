name: Bug / Regression
description: Report a correctness, reliability, or regression problem in the repo or shipped product.
title: "[Bug]: "
labels:
  - bug
body:
  - type: markdown
    attributes:
      value: |
        Use this form for broken behavior, regressions, and reliability failures.

        Before filing, ask:
        “Could or should this have been found, prevented, or remediated by the product itself?”

  - type: checkboxes
    id: existing_issue
    attributes:
      label: Existing issue check
      options:
        - label: I searched the existing issues first
          required: true

  - type: dropdown
    id: affected_surface
    attributes:
      label: Affected surface
      options:
        - Workspace / orchestrator
        - Planning surfaces
        - Memory surfaces
        - Structured config
        - Workflow substrate
        - System intent
        - Startup / routing adapters
        - Proof / review / validation
        - Repo-local / unknown
    validations:
      required: true

  - type: textarea
    id: current_behavior
    attributes:
      label: Current behavior
      placeholder: What broke, failed, or produced the wrong result?
    validations:
      required: true

  - type: textarea
    id: expected_behavior
    attributes:
      label: Expected behavior
      placeholder: What should have happened instead?
    validations:
      required: true

  - type: textarea
    id: intended_outcome
    attributes:
      label: Intended outcome
      description: Describe the final corrected state that should be true after this issue is complete.
      placeholder: Describe the ordinary-path behavior after the bug is fixed.
    validations:
      required: true

  - type: textarea
    id: acceptance
    attributes:
      label: Acceptance criteria
      description: What observable final-state requirements must be true?
      placeholder: |
        - [ ] ...
        - [ ] ...
    validations:
      required: true

  - type: textarea
    id: non_solutions
    attributes:
      label: Non-solutions
      description: What does not close this issue unless explicitly listed in the acceptance criteria?
      value: |
        The following do not close this issue unless explicitly listed in the acceptance criteria:

        - documenting the problem without changing the affected behavior;
        - adding an inventory without completing the requested change;
        - reclassifying or renaming the current behavior while preserving the old dependency/path;
        - adding a follow-up issue instead of completing the stated outcome;
        - changing tests to accept the current behavior rather than making the intended behavior true.
    validations:
      required: true

  - type: textarea
    id: final_satisfaction
    attributes:
      label: Final satisfaction
      description: What must be true before this issue is complete, including the final fixed behavior and any blocking gaps?
      placeholder: Describe the final corrected state that closes this bug.
    validations:
      required: true

  - type: textarea
    id: evidence_required_for_final_completion
    attributes:
      label: Evidence required for final completion
      description: What proof, regression test, or review evidence is required before this bug can honestly close?
      placeholder: Name the evidence that proves the final expected behavior.
    validations:
      required: true

  - type: textarea
    id: completion_rule
    attributes:
      label: Completion rule
      description: What condition permits a PR to close this issue?
      value: A PR may close this issue only if the intended outcome is true in the ordinary path, not merely documented, inventoried, reclassified, or deferred.
    validations:
      required: true

  - type: textarea
    id: reproduction
    attributes:
      label: Steps to reproduce
      placeholder: |
        1. Start from...
        2. Run...
        3. Open...
        4. Observe...
      render: shell
    validations:
      required: true

  - type: textarea
    id: evidence
    attributes:
      label: Evidence
      description: Include logs, file paths, screenshots, issue links, or related commits.
      placeholder: Paste the smallest useful evidence set.
    validations:
      required: false

  - type: dropdown
    id: product_first
    attributes:
      label: Should this be addressed in the shipped product first?
      options:
        - "Yes: this looks like a package or contract defect"
        - "Maybe: repo symptom, but product changes may help"
        - "No: this appears repo-local"
    validations:
      required: true

  - type: textarea
    id: product_reasoning
    attributes:
      label: Product-first reasoning
      placeholder: Could or should this have been found, prevented, or remediated by the product itself?
    validations:
      required: true

  - type: dropdown
    id: urgency
    attributes:
      label: Urgency
      options:
        - Blocking current work
        - Should be scheduled soon
        - Useful follow-up, not urgent
    validations:
      required: true
