{
  "kind": "agentic-workspace/self-configuration-lifecycle-scenario/v1",
  "id": "minimal-bootstrap-through-capability-delta",
  "version": 1,
  "agent_profile": "generic-contract-following-agent",
  "user_actions": {
    "install_or_lifecycle_commands": 5,
    "genuine_semantic_answers": 3,
    "manual_setup_or_doctor_commands": 0,
    "raw_aw_state_edits": 0
  },
  "stages": [
    {"id": "minimal-bootstrap", "expected": "thin routing surfaces plus non-current readiness"},
    {"id": "ordinary-first-start", "expected": "non-setup task returns exact setup continuation"},
    {"id": "strong-inference", "expected": "system intent and proof routes are inferred without a question"},
    {"id": "semantic-choice", "expected": "plain-language assignment alternatives use config.policy-apply"},
    {"id": "defer", "expected": "local unresolved identity remains non-current and unrelated work proceeds"},
    {"id": "resume-new-session", "expected": "canonical compact state restores only the unresolved semantic question"},
    {"id": "generated-native-cleanup", "expected": "the generated TypeScript policy operation removes the complete setup table before Python startup resumes"},
    {"id": "quiet-current", "expected": "representative direct tasks contain no configuration readiness ceremony"},
    {"id": "explicit-capability-enable", "expected": "lifecycle owner enables an independent module; setup never does"},
    {"id": "no-semantic-delta", "expected": "unchanged capability contract produces no setup work"},
    {"id": "human-source-delta", "expected": "only the current module source obligation is surfaced"},
    {"id": "multi-release-convergence", "expected": "the current semantic revision is used without replaying skipped revisions"},
    {"id": "durable-disable", "expected": "explicit module disable retires its concern and returns startup to quiet"}
  ],
  "durable_residue": [
    ".agentic-workspace/config.toml",
    ".agentic-workspace/config.local.toml [setup only while deferred]",
    ".agentic-workspace/adoption-receipt.json current concern receipts",
    "canonical owner sources"
  ],
  "forbidden_residue": [
    "chat transcript",
    "questionnaire history",
    "setup generation ledger",
    "parallel source registry"
  ]
}
