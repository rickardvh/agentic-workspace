# Release v1.1.1

## Changes

- Export existing native diagnostic sessions with logging enabled without attempting registry publication.
- Admit exact-tree stack integration with mixed previously admitted changesets while preserving strict ordinary PR semver checks.
- Distinguish proposed source assessments from authorization to publish them in reconciliation decisions and retained evidence.
- Preserve composed dependencies when restricting scoped-instruction writes so unchanged exact actions remain executable.
- Allow ordinary coordinated release dispatch without a first-stable RC input while retaining the release owner's explicit v1.0.0 promotion gate.
