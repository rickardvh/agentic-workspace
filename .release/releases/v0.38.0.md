# Release v0.38.0

## Changes

- Require complete cross-transport and installed-footprint conformance evidence before external clients report readiness.
- Prove independent Python and TypeScript consumers against built artifacts, including exact mutation failure semantics and clean removal.
- Classify generated freshness metadata separately from semver-bearing package output.
- Require exact packed-artifact semantic conformance receipts for every supported generated command runtime.
- Collapse the duplicated workspace runtime mirror into one canonical owner with compatibility and review-scale ratchets.
- Make strict health fail on broken current state while keeping archive history non-blocking.
- Enforce lifecycle output budgets and report bounded progress for long-running validation commands.
- Add reversible Planning archive compaction with compact receipts, loss accounting, and host retention budgets.
- Prioritize tracked-source diagnostics and deduplicate evidence into explained improvement candidates.
- Require explicit trusted execution, blocking security scans, immutable workflows, SBOMs, and artifact attestations for support-bearing releases.
- Make missing-current-slice repair packets directly replayable with explicit absent-subject identity.
