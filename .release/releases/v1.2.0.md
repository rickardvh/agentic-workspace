# Release v1.2.0

## Changes

- Generate ownership read profiles through one Rust producer using Git's built-in path conversions, rejecting external filters and preserving distinct Git, semantic-text and raw-byte identities.
- Keep the managed AGENTS.md bootstrap as a minimal skill pointer and refresh arbitrary content inside a unique ordered fence.
- Build the native pair before Planning release checks and prove external-consumer payload conformance through public host adoption, preserving copied-seed rejection and actionable failure diagnostics.
- Evaluate named candidate standard Agent Skills through passive, bounded bundle validation and existing procedure currentness, without adding them to ordinary discovery.
