# Release v1.4.1

## Changes

- Isolate registry install storage from scored repositories and allow same-subject registry recovery with current maintainer tooling bound to immutable release assets.
- Keep Configuration setup and exact recovery reachable when a partial payload refresh leaves activation registries and procedure sources between versions.
- Run deterministic consumer validation on GitHub while retaining live-agent cases in the local harness with separate reporting scope.
