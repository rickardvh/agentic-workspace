# Release v0.36.1

## Changes

- Make release recovery status verify version, tag, and GitHub Release state before treating successful release runs as cleared.
- Avoid presenting an idle Planning surface as the current active work during setup.
- Make lifecycle upgrade detail selectors actionable and preserve dry-run diagnostics.
- Reduce broad implement changed-path first-decision latency by deferring expensive diagnostics behind explicit selectors.
