# Release v0.37.0

## Changes

- Expose fail-closed external operation readiness for supported client operations.
- Promote repeated corrections into compact agent guidance.
- Add a fail-closed separation-of-duty gate for independent high-assurance review.
- Expose collecting evaluation actions at startup only when their structured selectors match.
- Compile compact owner-facing evaluation reports only when explicitly requested or materially due.
- Allow revision-guarded overwrite to update one existing active execplan without recreating it.
- Declare architecture, instruction, and ownership context authorities for AW routing.
- Replace the runtime Memory starter with a routed source, generation, and install boundary note.
- Register durable Memory routes and freshness anchors so context drift is detected.
- Compile active context-improvement signals into explicit task-posture consequences.
- Make legacy review-worktree migration fail closed and transactional, preserving dirty, active, mismatched, and unpushed worktrees.
