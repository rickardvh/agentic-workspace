schema_version = "agentic-workspace/release-change/v1"
bump = "patch"
summary = "Reject missing installed startup skills explicitly in release consumer checks and align packed artifact rejection assertions."
