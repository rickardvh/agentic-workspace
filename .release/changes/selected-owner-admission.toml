schema_version = "agentic-workspace/release-change/v1"
bump = "patch"
summary = "Allow a selected Planning owner to continue within its declared scope while filtering no-op reconciliation residue."
issues = [2840]
