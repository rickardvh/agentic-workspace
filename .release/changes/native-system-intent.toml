schema_version = "agentic-workspace/release-change/v1"
bump = "patch"
summary = "Expose existing governing source and retained intent through exact native lazy reads, preserving source uncertainty without granting alignment or acceptance."
