schema_version = "agentic-workspace/release-change/v1"
bump = "patch"
summary = "Project accepted Planning targeted-write fields into handoff contracts and worker prompts instead of retaining stale scaffold values."
issues = [2959]
