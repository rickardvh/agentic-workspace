schema_version = "agentic-workspace/release-change/v1"
bump = "patch"
summary = "Require canonical workspace startup for repository work and make bundled Assignment requests discoverable through their owner references."
