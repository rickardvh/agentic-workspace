schema_version = "agentic-workspace/release-change/v1"
bump = "minor"
summary = "Replay packed npm artifact semantics locally through the same maintained validation authority used by CI and release workflows."
