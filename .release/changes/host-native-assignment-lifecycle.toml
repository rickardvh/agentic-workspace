schema_version = "agentic-workspace/release-change/v1"
bump = "patch"
summary = "Make commandless host-native assignment execution constructible, verify shared-worktree returns without duplicate patch application, normalize CRLF patch transport, enforce glob-scoped returns consistently, and restore the verification runtime in Docker conformance."
issues = [2947, 2960, 2961]
