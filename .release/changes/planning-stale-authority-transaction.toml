schema_version = "agentic-workspace/release-change/v1"
bump = "patch"
summary = "Compile mechanically stale Planning target-authority proposals into one bounded, atomic, and idempotent preview/apply transaction."
issues = [2804]
