schema_version = "agentic-workspace/release-change/v1"
bump = "patch"
summary = "Generate ownership read profiles through one Rust producer using Git's built-in path conversions, rejecting external filters and preserving distinct Git, semantic-text and raw-byte identities."
