schema_version = "agentic-workspace/release-change/v1"
bump = "patch"
summary = "Reject invalid public request envelopes and arguments with bounded value-free native diagnostics instead of echoing rejected content."
issues = [3498]
