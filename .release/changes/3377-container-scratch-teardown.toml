schema_version = "agentic-workspace/release-change/v1"
bump = "patch"
summary = "Remove authenticated disposable scratch containers without content snapshots while preserving deprecated bounded scratch-prune compatibility for 1.x callers and fail-closed removal without custody."
