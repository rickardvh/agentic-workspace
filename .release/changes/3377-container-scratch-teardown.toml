schema_version = "agentic-workspace/release-change/v1"
bump = "major"
summary = "Remove authenticated disposable scratch containers without content snapshots; retire scratch-prune and its selection argument, and preserve every container with missing custody."
