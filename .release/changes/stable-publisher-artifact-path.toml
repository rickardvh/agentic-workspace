schema_version = "agentic-workspace/release-change/v1"
bump = "patch"
summary = "Pass the absolute built-artifact directory to stable publisher smoke tests so isolated npm consumers install the intended archive."
