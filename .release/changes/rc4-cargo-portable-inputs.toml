schema_version = "agentic-workspace/release-change/v1"
bump = "patch"
summary = "Include declared portable materialization inputs in standalone Cargo source packages so the native pair builds from published archives."
