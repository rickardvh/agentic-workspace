schema_version = "agentic-workspace/release-change/v1"
bump = "patch"
summary = "Retain exact execution attempts and fail closed on uncertain effects instead of blindly retrying."
issues = [3000, 2987]
