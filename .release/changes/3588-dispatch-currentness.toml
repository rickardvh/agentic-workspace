schema_version = "agentic-workspace/release-change/v1"
bump = "patch"
summary = "Preserve startup source dependencies before sealing delegation handoffs and report bounded, actionable dispatch rejection reasons."
