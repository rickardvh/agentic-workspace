schema_version = "agentic-workspace/release-change/v1"
bump = "patch"
summary = "Discover setup assessment automatically after package updates, retain grounded Configuration decisions, and verify effective integration through its actual consumer."
