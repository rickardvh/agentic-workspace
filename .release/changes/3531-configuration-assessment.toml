schema_version = "agentic-workspace/release-change/v1"
bump = "patch"
summary = "Expose current setup assessment at native entry, bind reuse to setup and managed payload material, and retain grounded Configuration decisions with consumer verification."
