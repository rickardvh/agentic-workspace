schema_version = "agentic-workspace/release-change/v1"
bump = "patch"
summary = "Bridge installed dependency updates through product-owned triggers to Configuration assessment, bind reuse to setup and managed payload material, and retain grounded decisions with consumer verification."
