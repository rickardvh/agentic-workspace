schema_version = "agentic-workspace/release-change/v1"
bump = "patch"
summary = "Build the native pair before Planning release checks and prove external-consumer payload conformance through public host adoption, preserving copied-seed rejection and actionable failure diagnostics."
