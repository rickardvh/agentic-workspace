schema_version = "agentic-workspace/release-change/v1"
bump = "patch"
summary = "Make decision routes bounded, selector-complete, and explicit about task-level execute, delegate, or blocked disposition."
