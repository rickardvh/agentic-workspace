{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/external-evidence-operation-input.schema.json",
  "title": "External Evidence Operation Input",
  "description": "Public submit/query input that carries candidate JSON and an opaque trusted-host reference without custody assertions.",
  "type": "object",
  "properties": {
    "target": {"type": "string", "description": "Repository root containing policy and proof-subject inputs."},
    "format": {"type": "string", "enum": ["text", "json"], "description": "Requested command output format."},
    "candidate_json": {"type": "string", "minLength": 2, "description": "Serialized provider-neutral external evidence candidate."},
    "host_result_ref": {"type": "string", "pattern": "^external-evidence-host-result:[A-Za-z0-9._-]+$", "description": "Opaque reference resolved only by the package-trusted host boundary."}
  },
  "additionalProperties": false,
  "x-agentic-workspace-doc-role": "contract-reference"
}
