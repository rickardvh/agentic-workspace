{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/workflow-artifact-profiles.schema.json",
  "title": "Workflow Artifact Profiles",
  "description": "Profiles describing native workflow artifacts and their sync rules.",
  "type": "object",
  "required": [
    "schema_version",
    "default_profile",
    "profiles"
  ],
  "properties": {
    "schema_version": {
      "const": "agentic-workspace/workflow-artifact-profiles/v1",
      "description": "Contract version for this document."
    },
    "default_profile": {
      "type": "string",
      "description": "Default profile text value used by this contract."
    },
    "profiles": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "profile",
          "summary",
          "native_artifacts",
          "canonical_surfaces",
          "sync_rule",
          "handoff_rule"
        ],
        "properties": {
          "profile": {
            "type": "string",
            "description": "Profile text value used by this contract."
          },
          "summary": {
            "type": "string",
            "description": "Short human-readable summary for this entry."
          },
          "native_artifacts": {
            "type": "array",
            "items": {
              "type": "string",
              "description": "One entry in the native artifacts list."
            },
            "description": "Ordered native artifacts entries used by this contract."
          },
          "canonical_surfaces": {
            "type": "array",
            "minItems": 1,
            "items": {
              "type": "string",
              "description": "One entry in the canonical surfaces list."
            },
            "description": "Ordered canonical surfaces entries used by this contract."
          },
          "sync_rule": {
            "type": "string",
            "description": "Sync rule text value used by this contract."
          },
          "handoff_rule": {
            "type": "string",
            "description": "Handoff rule text value used by this contract."
          }
        },
        "additionalProperties": false,
        "description": "One entry in the profiles list."
      },
      "description": "Ordered profiles entries used by this contract."
    }
  },
  "additionalProperties": false,
  "x-agentic-workspace-doc-role": "contract-reference"
}
