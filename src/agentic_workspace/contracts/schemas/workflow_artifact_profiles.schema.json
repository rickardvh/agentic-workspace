{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/workflow-artifact-profiles.schema.json",
  "title": "Workflow Artifact Profiles",
  "description": "Schema for Workflow Artifact Profiles.",
  "type": "object",
  "required": [
    "schema_version",
    "default_profile",
    "profiles"
  ],
  "properties": {
    "schema_version": {
      "const": "agentic-workspace/workflow-artifact-profiles/v1",
      "description": "The schema version constant field in Workflow Artifact Profiles."
    },
    "default_profile": {
      "type": "string",
      "description": "The default profile string field in Workflow Artifact Profiles."
    },
    "profiles": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "profile",
          "summary",
          "native_artifacts",
          "canonical_surfaces",
          "sync_rule",
          "handoff_rule"
        ],
        "properties": {
          "profile": {
            "type": "string",
            "description": "The profile string field in Workflow Artifact Profiles."
          },
          "summary": {
            "type": "string",
            "description": "The summary string field in Workflow Artifact Profiles."
          },
          "native_artifacts": {
            "type": "array",
            "items": {
              "type": "string",
              "description": "Item string accepted by native artifacts in Workflow Artifact Profiles."
            },
            "description": "The native artifacts array field in Workflow Artifact Profiles."
          },
          "canonical_surfaces": {
            "type": "array",
            "minItems": 1,
            "items": {
              "type": "string",
              "description": "Item string accepted by canonical surfaces in Workflow Artifact Profiles."
            },
            "description": "The canonical surfaces array field in Workflow Artifact Profiles."
          },
          "sync_rule": {
            "type": "string",
            "description": "The sync rule string field in Workflow Artifact Profiles."
          },
          "handoff_rule": {
            "type": "string",
            "description": "The handoff rule string field in Workflow Artifact Profiles."
          }
        },
        "additionalProperties": false,
        "description": "Item object accepted by profiles in Workflow Artifact Profiles."
      },
      "description": "The profiles array field in Workflow Artifact Profiles."
    }
  },
  "additionalProperties": false,
  "x-agentic-workspace-doc-role": "contract-reference"
}
