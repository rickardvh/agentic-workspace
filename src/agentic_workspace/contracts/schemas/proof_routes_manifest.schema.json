{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/proof-routes-manifest.schema.json",
  "title": "Proof Routes Manifest",
  "description": "Schema for Proof Routes Manifest.",
  "type": "object",
  "required": [
    "schema_version",
    "canonical_doc",
    "command",
    "default_routes"
  ],
  "properties": {
    "schema_version": {
      "const": "proof-routes-manifest/v1",
      "description": "The schema version constant field in Proof Routes Manifest."
    },
    "canonical_doc": {
      "type": "string",
      "description": "The canonical doc string field in Proof Routes Manifest."
    },
    "command": {
      "type": "string",
      "description": "The command string field in Proof Routes Manifest."
    },
    "default_routes": {
      "type": "object",
      "minProperties": 1,
      "additionalProperties": {
        "type": "string",
        "description": "Additional named string entry for default routes in Proof Routes Manifest."
      },
      "description": "The default routes object field in Proof Routes Manifest."
    }
  },
  "additionalProperties": false,
  "x-agentic-workspace-doc-role": "contract-reference"
}
