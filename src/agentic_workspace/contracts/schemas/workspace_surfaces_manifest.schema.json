{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/workspace-surfaces-manifest.schema.json",
  "title": "Workspace Surfaces Manifest",
  "description": "Manifest of workspace-owned surfaces, generated artifacts, and ownership roles.",
  "type": "object",
  "required": [
    "schema_version",
    "payload_files",
    "necessary_surface_files",
    "necessary_surface_roots",
    "module_surface_files",
    "required_references",
    "reference_discovery",
    "reference_resolution",
    "no_cli_fallback",
    "system_intent_mirror_kind",
    "subsystem_intent_kind",
    "default_agents_path",
    "handoff_surfaces",
    "module_upgrade_source_paths",
    "proof_route_hints_path",
    "setup_findings_path",
    "mixed_agent_local_override_fields"
  ],
  "properties": {
    "schema_version": {
      "const": "workspace-surfaces-manifest/v1",
      "description": "Contract version for this document."
    },
    "payload_files": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "description": "One entry in the payload files list."
      },
      "description": "Ordered payload files entries used by this contract."
    },
    "necessary_surface_files": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {"type": "string"},
      "description": "Package-owned files that remain installed in the necessary-surface profile."
    },
    "necessary_surface_roots": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {"type": "string"},
      "description": "Directory roots retained and synchronized as units in the necessary-surface profile."
    },
    "module_surface_files": {
      "type": "object",
      "required": ["memory", "planning", "verification"],
      "properties": {
        "memory": {"$ref": "#/$defs/surfacePaths", "description": "Installed host paths contributed by the memory module."},
        "planning": {"$ref": "#/$defs/surfacePaths", "description": "Installed host paths contributed by the planning module."},
        "verification": {"$ref": "#/$defs/surfacePaths", "description": "Installed host paths contributed by the verification module."}
      },
      "additionalProperties": false,
      "description": "Installed host paths contributed by each selected module and added independently to closure cells."
    },
    "required_references": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["source", "target", "kind", "profiles", "modules"],
        "properties": {
          "source": {"type": "string"},
          "target": {"type": "string"},
          "kind": {"enum": ["installed-local", "package-resource", "optional"]},
          "profiles": {
            "type": "array",
            "minItems": 1,
            "uniqueItems": true,
            "items": {"enum": ["necessary-surfaces", "full-mirror"]}
          },
          "modules": {
            "type": "array",
            "uniqueItems": true,
            "items": {"enum": ["memory", "planning", "verification"]},
            "description": "All listed modules must be selected for this reference to apply; an empty list applies to every combination."
          },
          "degraded_behavior": {"type": "string", "minLength": 1}
        },
        "allOf": [
          {
            "if": {"properties": {"kind": {"const": "optional"}}, "required": ["kind"]},
            "then": {"required": ["degraded_behavior"]}
          }
        ],
        "additionalProperties": false
      },
      "description": "Required references that must resolve in every selected installed footprint."
    },
    "reference_discovery": {
      "type": "object",
      "required": ["source_globs"],
      "properties": {
        "source_globs": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {"type": "string"},
          "description": "Payload-relative globs whose matching text files are scanned for operational references."
        }
      },
      "additionalProperties": false,
      "description": "Installed operational text surfaces scanned mechanically for file-like Agentic Workspace references."
    },
    "reference_resolution": {
      "type": "object",
      "required": ["installed-local", "package-resource", "optional"],
      "properties": {
        "installed-local": {"type": "string", "description": "Resolution rule for paths installed into the host repository."},
        "package-resource": {"type": "string", "description": "Resolution rule for stable resources retained inside the installed package."},
        "optional": {"type": "string", "description": "Resolution rule for optional references with a declared degraded behavior."}
      },
      "additionalProperties": false,
      "description": "Canonical interpretation of installed reference kinds."
    },
    "no_cli_fallback": {
      "type": "object",
      "required": ["entrypoint", "module_map", "forbidden_action_markers", "module_headings", "next_safe_action"],
      "properties": {
        "entrypoint": {"type": "string", "description": "Installed startup skill used as the no-CLI fallback entrypoint."},
        "module_map": {"type": "string", "description": "Installed module map used to route selected modules without the CLI."},
        "forbidden_action_markers": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {"type": "string", "minLength": 1},
          "description": "Markers whose presence proves the entrypoint forbids unsafe fallback execution."
        },
        "module_headings": {
          "type": "object",
          "required": ["memory", "planning", "verification"],
          "properties": {
            "memory": {"type": "string", "minLength": 1, "description": "Heading that proves memory routing is installed."},
            "planning": {"type": "string", "minLength": 1, "description": "Heading that proves planning routing is installed."},
            "verification": {"type": "string", "minLength": 1, "description": "Heading that proves verification routing is installed."}
          },
          "additionalProperties": false,
          "description": "Expected module-map heading for every selectable module."
        },
        "next_safe_action": {"type": "string", "minLength": 1, "description": "Stable action returned when the installed CLI is unavailable."}
      },
      "additionalProperties": false,
      "description": "Content-level fallback boundary exercised from installed files with the CLI unavailable."
    },
    "system_intent_mirror_kind": {
      "type": "string",
      "description": "System intent mirror kind text value used by this contract."
    },
    "subsystem_intent_kind": {
      "type": "string",
      "description": "Subsystem intent set kind used by the editable scoped durable-intent store."
    },
    "default_agents_path": {
      "type": "string",
      "description": "Default agents path text value used by this contract."
    },
    "handoff_surfaces": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "description": "One entry in the handoff surfaces list."
      },
      "description": "Ordered handoff surfaces entries used by this contract."
    },
    "module_upgrade_source_paths": {
      "type": "object",
      "required": [
        "planning",
        "memory"
      ],
      "properties": {
        "planning": {
          "type": "string",
          "description": "Planning text value used by this contract."
        },
        "memory": {
          "type": "string",
          "description": "Memory text value used by this contract."
        }
      },
      "additionalProperties": {
        "type": "string",
        "description": "Named entry in the module upgrade source paths mapping."
      },
      "description": "Module upgrade source paths details used by this contract."
    },
    "setup_findings_path": {
      "type": "string",
      "description": "Setup findings path text value used by this contract."
    },
    "proof_route_hints_path": {
      "type": "string",
      "description": "Workspace-managed advisory proof route hints path."
    },
    "mixed_agent_local_override_fields": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "description": "One entry in the mixed agent local override fields list."
      },
      "description": "Ordered mixed agent local override fields entries used by this contract."
    }
  },
  "$defs": {
    "surfacePaths": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {"type": "string"}
    }
  },
  "additionalProperties": false,
  "x-agentic-workspace-doc-role": "contract-reference"
}
