{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/resolved-evidence-producer.schema.json",
  "title": "Resolved Evidence Producer",
  "description": "Host-authenticated producer and result custody supplied independently of an external evidence candidate.",
  "type": "object",
  "required": ["kind", "authenticated", "producer_id", "issuer_id", "result_contract", "result", "evidence_ref"],
  "properties": {
    "kind": {"const": "agentic-workspace/resolved-evidence-producer/v1", "description": "Contract discriminator."},
    "authenticated": {"const": true, "description": "Confirms the host resolved this custody record through its trusted integration boundary."},
    "producer_id": {"type": "string", "minLength": 1, "description": "Host-resolved producer identity."},
    "issuer_id": {"type": "string", "minLength": 1, "description": "Host-resolved issuer of the producer result."},
    "result_contract": {"type": "string", "minLength": 1, "description": "Authenticated result contract revision or identity."},
    "result": {"type": "string", "minLength": 1, "description": "Authenticated producer result, still distinct from admission status."},
    "evidence_ref": {"type": "string", "minLength": 1, "description": "Authenticated bounded reference to the external evidence."},
    "transport_id": {"type": "string", "description": "Transport used to obtain custody; it grants no producer authority."}
  },
  "additionalProperties": false,
  "x-agentic-workspace-doc-role": "contract-reference"
}
