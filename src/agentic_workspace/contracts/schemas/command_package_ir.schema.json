{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/command-package-ir.schema.json",
  "title": "Agentic Workspace Command Package IR",
  "description": "Intermediate representation for generating command package metadata and language/runtime adapters.",
  "type": "object",
  "required": [
    "schema_version",
    "summary",
    "schema",
    "source_contracts",
    "generation_policy",
    "packages"
  ],
  "properties": {
    "schema_version": {
      "const": "agentic-workspace/command-package-ir/v1",
      "description": "Command package IR contract version."
    },
    "summary": {
      "type": "string",
      "minLength": 1,
      "description": "Short summary of the generated command package boundary."
    },
    "schema": {
      "const": "schemas/command_package_ir.schema.json",
      "description": "Schema reference carried by the IR document."
    },
    "source_contracts": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1,
        "description": "One entry in the source contracts list."
      },
      "description": "Contract files that feed or constrain command package generation."
    },
    "generation_policy": {
      "type": "object",
      "required": [
        "authority_rule",
        "runtime_boundary",
        "ordinary_development_environment",
        "test_environment",
        "custom_codegen_boundary",
        "shell_adapter_policy",
        "direct_cli_edit_policy",
        "generated_package_maturity",
        "python_cli_completion",
        "non_python_runtime_binding"
      ],
      "properties": {
        "authority_rule": {
          "type": "string",
          "minLength": 1,
          "description": "Authority rule text value used by this contract."
        },
        "runtime_boundary": {
          "type": "string",
          "minLength": 1,
          "description": "Runtime boundary text value used by this contract."
        },
        "ordinary_development_environment": {
          "type": "string",
          "minLength": 1,
          "description": "Ordinary development environment text value used by this contract."
        },
        "test_environment": {
          "type": "string",
          "minLength": 1,
          "description": "Test environment text value used by this contract."
        },
        "custom_codegen_boundary": {
          "type": "string",
          "minLength": 1,
          "description": "Custom codegen boundary text value used by this contract."
        },
        "shell_adapter_policy": {
          "type": "string",
          "minLength": 1,
          "description": "Shell adapter policy text value used by this contract."
        },
        "direct_cli_edit_policy": {
          "type": "string",
          "minLength": 1,
          "description": "Direct cli edit policy text value used by this contract."
        },
        "generated_package_maturity": {
          "type": "object",
          "required": [
            "routing_rule",
            "levels"
          ],
          "properties": {
            "routing_rule": {
              "type": "string",
              "minLength": 1,
              "description": "Routing rule text value used by this contract."
            },
            "levels": {
              "type": "array",
              "minItems": 1,
              "items": {
                "$ref": "#/$defs/maturity_level",
                "description": "One entry in the levels list."
              },
              "description": "Ordered levels entries used by this contract."
            }
          },
          "additionalProperties": false,
          "description": "Generated package maturity details used by this contract."
        },
        "python_cli_completion": {
          "type": "object",
          "required": [
            "finish_line",
            "current_state",
            "allowed_hand_owned_cli_responsibilities",
            "must_move_behind_contracts_or_generation",
            "proof_requirements",
            "completion_gate"
          ],
          "properties": {
            "finish_line": {
              "type": "string",
              "minLength": 1,
              "description": "Finish line text value used by this contract."
            },
            "current_state": {
              "type": "string",
              "minLength": 1,
              "description": "Current state text value used by this contract."
            },
            "allowed_hand_owned_cli_responsibilities": {
              "$ref": "#/$defs/non_empty_strings",
              "description": "Python CLI responsibilities that may stay hand-owned."
            },
            "must_move_behind_contracts_or_generation": {
              "$ref": "#/$defs/non_empty_strings",
              "description": "Python CLI responsibilities that cannot be treated as complete while hand-owned."
            },
            "proof_requirements": {
              "$ref": "#/$defs/non_empty_strings",
              "description": "Proof requirements before Python generated CLI completion can be claimed."
            },
            "completion_gate": {
              "type": "object",
              "required": [
                "state",
                "scope",
                "satisfied_by"
              ],
              "properties": {
                "state": {
                  "enum": [
                    "pending",
                    "satisfied"
                  ],
                  "description": "Whether Python generated CLI completion prerequisites are satisfied."
                },
                "scope": {
                  "const": "python-only",
                  "description": "Completion scope; this gate must not claim TypeScript, Bash, PowerShell, or full codegen epic completion."
                },
                "satisfied_by": {
                  "type": "array",
                  "minItems": 1,
                  "items": {
                    "type": "object",
                    "required": [
                      "id",
                      "evidence",
                      "proof"
                    ],
                    "properties": {
                      "id": {
                        "type": "string",
                        "pattern": "^[a-z0-9][a-z0-9-]*$",
                        "description": "Stable completion prerequisite id."
                      },
                      "evidence": {
                        "type": "string",
                        "minLength": 1,
                        "description": "Evidence showing the prerequisite is satisfied."
                      },
                      "proof": {
                        "type": "string",
                        "minLength": 1,
                        "description": "Command, checker, or test surface that proves this prerequisite."
                      }
                    },
                    "additionalProperties": false,
                    "description": "One satisfied Python completion prerequisite."
                  },
                  "description": "Evidence items required before full Python generated CLI completion can be claimed."
                }
              },
              "additionalProperties": false,
              "description": "Schema-backed gate that separates full Python generated CLI completion from generated adapter maturity."
            }
          },
          "additionalProperties": false,
          "description": "Python generated CLI completion criteria distinct from adapter maturity."
        },
        "non_python_runtime_binding": {
          "type": "object",
          "required": [
            "selected_model",
            "default_runtime_command",
            "scope",
            "universal_contract_owns",
            "target_projection_owns",
            "runtime_owns",
            "error_mapping"
          ],
          "properties": {
            "selected_model": {
              "type": "string",
              "minLength": 1,
              "description": "Selected model text value used by this contract."
            },
            "default_runtime_command": {
              "type": "string",
              "minLength": 1,
              "description": "Default runtime command text value used by this contract."
            },
            "scope": {
              "type": "string",
              "minLength": 1,
              "description": "Scope text value used by this contract."
            },
            "universal_contract_owns": {
              "$ref": "#/$defs/non_empty_strings",
              "description": "Universal contract owns referenced contract shape used by this contract."
            },
            "target_projection_owns": {
              "$ref": "#/$defs/non_empty_strings",
              "description": "Target projection owns referenced contract shape used by this contract."
            },
            "runtime_owns": {
              "$ref": "#/$defs/non_empty_strings",
              "description": "Runtime owns referenced contract shape used by this contract."
            },
            "error_mapping": {
              "$ref": "#/$defs/non_empty_strings",
              "description": "Error mapping referenced contract shape used by this contract."
            }
          },
          "additionalProperties": false,
          "description": "Non python runtime binding details used by this contract."
        }
      },
      "additionalProperties": false,
      "description": "Policy for what generation owns, what runtime code owns, and how adapters stay safe."
    },
    "packages": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/package",
        "description": "One entry in the packages list."
      },
      "description": "Generated command packages and their command bindings."
    }
  },
  "additionalProperties": false,
  "$defs": {
    "package": {
      "type": "object",
      "required": [
        "id",
        "program",
        "package_role",
        "targets",
        "commands"
      ],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-z0-9][a-z0-9_.-]*$",
          "description": "Stable identifier used to reference this record."
        },
        "program": {
          "type": "string",
          "minLength": 1,
          "description": "Program text value used by this contract."
        },
        "package_role": {
          "enum": [
            "root-workspace-cli",
            "planning-module-cli",
            "memory-module-cli"
          ],
          "description": "Allowed package role value for routing or validation."
        },
        "python_runtime_binding": {
          "type": "object",
          "required": [
            "generated_package_module",
            "runtime_module"
          ],
          "properties": {
            "generated_package_module": {
              "type": "string",
              "pattern": "^[A-Za-z_][A-Za-z0-9_.]*$",
              "description": "Python module imported by generated process handoff for this package."
            },
            "runtime_module": {
              "type": "string",
              "pattern": "^[A-Za-z_][A-Za-z0-9_.]*$",
              "description": "Hand-owned Python runtime module imported by generated Python package bridge code."
            }
          },
          "additionalProperties": false,
          "description": "Python-specific runtime binding data supplied by IR rather than hard-coded in the generator."
        },
        "targets": {
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/target",
            "description": "One entry in the targets list."
          },
          "description": "Ordered targets entries used by this contract."
        },
        "commands": {
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/command",
            "description": "One entry in the commands list."
          },
          "description": "Commands associated with this check, proof, or workflow step."
        }
      },
      "additionalProperties": false,
      "description": "Package details used by this contract."
    },
    "target": {
      "type": "object",
      "required": [
        "kind",
        "package_name",
        "generated_root",
        "entrypoints",
        "test_environment",
        "maturity_level_ref"
      ],
      "properties": {
        "kind": {
          "enum": [
            "python",
            "typescript",
            "bash",
            "powershell"
          ],
          "description": "Discriminator identifying the payload or record shape."
        },
        "package_name": {
          "type": "string",
          "minLength": 1,
          "description": "Package name text value used by this contract."
        },
        "generated_root": {
          "type": "string",
          "minLength": 1,
          "description": "Generated root text value used by this contract."
        },
        "entrypoints": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "minLength": 1,
            "description": "One entry in the entrypoints list."
          },
          "description": "Ordered entrypoints entries used by this contract."
        },
        "test_environment": {
          "enum": [
            "python-dev",
            "docker"
          ],
          "description": "Allowed test environment value for routing or validation."
        },
        "maturity_level_ref": {
          "type": "string",
          "minLength": 1,
          "description": "Maturity level ref text value used by this contract."
        },
        "generation_status": {
          "enum": [
            "supported-now",
            "proof-fixture",
            "runnable-read-only-adapter",
            "runtime-backed-read-only-adapter",
            "weak-agent-safe-adapter",
            "deferred"
          ],
          "description": "Allowed generation status value for routing or validation."
        }
      },
      "additionalProperties": false,
      "description": "Destination file, surface, package, or record referenced by this entry."
    },
    "maturity_level": {
      "type": "object",
      "required": [
        "id",
        "summary",
        "runnable",
        "runtime_backed",
        "weak_agent_routing",
        "promotion_requires"
      ],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-z0-9][a-z0-9-]*$",
          "description": "Stable identifier used to reference this record."
        },
        "summary": {
          "type": "string",
          "minLength": 1,
          "description": "Short human-readable summary for this entry."
        },
        "runnable": {
          "type": "boolean",
          "description": "Runnable true/false policy flag used by this contract."
        },
        "runtime_backed": {
          "type": "boolean",
          "description": "Runtime backed true/false policy flag used by this contract."
        },
        "weak_agent_routing": {
          "enum": [
            "forbidden",
            "review-required",
            "allowed-read-only",
            "allowed-mutation-with-review"
          ],
          "description": "Allowed weak agent routing value for routing or validation."
        },
        "promotion_requires": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "Promotion requires referenced contract shape used by this contract."
        }
      },
      "additionalProperties": false,
      "description": "Maturity level details used by this contract."
    },
    "command": {
      "type": "object",
      "required": [
        "adapter_id",
        "status",
        "command",
        "operation_ref",
        "runtime_binding",
        "schemas",
        "effect_hints",
        "conformance_refs",
        "projection_boundary"
      ],
      "properties": {
        "adapter_id": {
          "type": "string",
          "pattern": "^[a-z0-9][a-z0-9_.-]*$",
          "description": "Adapter id text value used by this contract."
        },
        "status": {
          "enum": [
            "generated",
            "proof-fixture",
            "deferred"
          ],
          "description": "Current lifecycle, readiness, or health state."
        },
        "command": {
          "type": "object",
          "required": [
            "name"
          ],
          "properties": {
            "name": {
              "type": "string",
              "minLength": 1,
              "description": "Human-readable or lookup name for this entry."
            },
            "subcommand": {
              "type": "string",
              "minLength": 1,
              "description": "Subcommand text value used by this contract."
            },
            "manifest_ref": {
              "type": "string",
              "minLength": 1,
              "description": "Manifest ref text value used by this contract."
            }
          },
          "additionalProperties": false,
          "description": "Command string or command name used by this contract."
        },
        "interface": {
          "$ref": "#/$defs/interface",
          "description": "Generated parser-facing interface for migrated command surfaces."
        },
        "operation_ref": {
          "type": "object",
          "required": [
            "id",
            "path"
          ],
          "properties": {
            "id": {
              "type": "string",
              "minLength": 1,
              "description": "Stable identifier used to reference this record."
            },
            "path": {
              "type": "string",
              "pattern": "^operations/[A-Za-z0-9_.-]+\\.json$",
              "description": "Path segments or repository path used by this contract."
            }
          },
          "additionalProperties": false,
          "description": "Operation ref details used by this contract."
        },
        "runtime_binding": {
          "type": "object",
          "required": [
            "kind",
            "primitive_refs"
          ],
          "properties": {
            "kind": {
              "const": "operation-primitive-sequence",
              "description": "Discriminator identifying the payload or record shape."
            },
            "primitive_refs": {
              "type": "array",
              "minItems": 1,
              "items": {
                "type": "string",
                "pattern": "^[a-z0-9][a-z0-9_.-]*$",
                "description": "One entry in the primitive refs list."
              },
              "description": "Ordered primitive refs entries used by this contract."
            }
          },
          "additionalProperties": false,
          "description": "Runtime binding details used by this contract."
        },
        "schemas": {
          "type": "object",
          "required": [
            "input",
            "output"
          ],
          "properties": {
            "input": {
              "$ref": "#/$defs/schema_refs",
              "description": "Input referenced contract shape used by this contract."
            },
            "output": {
              "$ref": "#/$defs/schema_refs",
              "description": "Output contract for the operation or command."
            }
          },
          "additionalProperties": false,
          "description": "Schemas details used by this contract."
        },
        "effect_hints": {
          "type": "object",
          "required": [
            "read_only",
            "destructive",
            "idempotent",
            "writes_repo_state",
            "requires_preflight_gate"
          ],
          "properties": {
            "read_only": {
              "type": "boolean",
              "description": "Whether this operation or aid avoids writes."
            },
            "destructive": {
              "type": "boolean",
              "description": "Whether this action can delete, overwrite, or otherwise destroy state."
            },
            "idempotent": {
              "type": "boolean",
              "description": "Idempotent true/false policy flag used by this contract."
            },
            "writes_repo_state": {
              "type": "boolean",
              "description": "Writes repo state true/false policy flag used by this contract."
            },
            "requires_preflight_gate": {
              "type": "boolean",
              "description": "Requires preflight gate true/false policy flag used by this contract."
            }
          },
          "additionalProperties": false,
          "description": "Effect hints details used by this contract."
        },
        "conformance_refs": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "minLength": 1,
            "description": "One entry in the conformance refs list."
          },
          "description": "Ordered conformance refs entries used by this contract."
        },
        "projection_boundary": {
          "type": "object",
          "required": [
            "universal",
            "target_specific",
            "runtime_owned"
          ],
          "properties": {
            "universal": {
              "$ref": "#/$defs/non_empty_strings",
              "description": "Universal referenced contract shape used by this contract."
            },
            "target_specific": {
              "$ref": "#/$defs/non_empty_strings",
              "description": "Target specific referenced contract shape used by this contract."
            },
            "runtime_owned": {
              "$ref": "#/$defs/non_empty_strings",
              "description": "Runtime owned referenced contract shape used by this contract."
            }
          },
          "additionalProperties": false,
          "description": "Projection boundary details used by this contract."
        }
      },
      "additionalProperties": false,
      "description": "Command string or command name used by this contract."
    },
    "schema_refs": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[A-Za-z0-9_.-]+\\.schema\\.json$",
        "description": "One entry in the schema refs list."
      },
      "description": "Ordered schema refs entries used by this contract."
    },
    "interface": {
      "type": "object",
      "required": [
        "name",
        "help",
        "options"
      ],
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1,
          "description": "Visible command name parsed by generated adapters."
        },
        "help": {
          "type": "string",
          "minLength": 1,
          "description": "Command help text rendered by generated adapters."
        },
        "options": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/option",
            "description": "One generated option accepted by this command."
          },
          "description": "Option semantics for this generated command level."
        },
        "subcommand_dest": {
          "type": "string",
          "minLength": 1,
          "description": "Parser destination used for this command's nested subcommand."
        },
        "subcommands_required": {
          "type": "boolean",
          "description": "Whether the nested subcommand is required by the generated parser."
        },
        "operation_ref": {
          "type": "object",
          "required": [
            "id",
            "path"
          ],
          "properties": {
            "id": {
              "type": "string",
              "minLength": 1,
              "description": "Operation id selected when this nested interface is invoked."
            },
            "path": {
              "type": "string",
              "minLength": 1,
              "description": "Operation contract path selected when this nested interface is invoked."
            }
          },
          "additionalProperties": false,
          "description": "Optional operation identity for nested generated subcommands."
        },
        "subcommands": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/interface",
            "description": "Nested generated subcommand interface."
          },
          "description": "Nested command interfaces owned by the generated parser."
        }
      },
      "additionalProperties": false,
      "description": "Generated parser-facing interface for migrated command surfaces."
    },
    "option": {
      "type": "object",
      "required": [
        "name",
        "flags"
      ],
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1,
          "description": "Normalized option destination name."
        },
        "flags": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "minLength": 1,
            "description": "One command-line spelling for this option."
          },
          "description": "Command-line flags accepted by generated adapters."
        },
        "action": {
          "type": "string",
          "minLength": 1,
          "description": "Parser action such as store_true when the option is boolean."
        },
        "choices": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "minLength": 1,
            "description": "One accepted option value."
          },
          "description": "Allowed option values."
        },
        "default": {
          "description": "Default value applied by the generated parser when the option is omitted."
        },
        "nargs": {
          "description": "Argparse-style arity marker for list-like options."
        },
        "type": {
          "enum": [
            "integer"
          ],
          "description": "Primitive parser coercion used by generated adapters."
        },
        "required": {
          "type": "boolean",
          "description": "Whether the option must be provided."
        },
        "help": {
          "type": "string",
          "minLength": 1,
          "description": "Option help text rendered by generated adapters."
        }
      },
      "additionalProperties": false,
      "description": "One generated parser option."
    },
    "non_empty_strings": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1,
        "description": "One entry in the non empty strings list."
      },
      "description": "Ordered non empty strings entries used by this contract."
    }
  },
  "x-agentic-workspace-doc-role": "contract-reference"
}
