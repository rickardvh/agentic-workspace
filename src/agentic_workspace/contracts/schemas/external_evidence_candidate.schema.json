{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/external-evidence-candidate.schema.json",
  "title": "External Evidence Candidate",
  "description": "Provider-neutral candidate reference that remains untrusted until repository proof policy admits it.",
  "type": "object",
  "required": ["kind", "proof_route", "evidence_class", "proof_subject"],
  "properties": {
    "kind": {"const": "agentic-workspace/external-evidence-candidate/v1", "description": "Contract discriminator."},
    "producer_id": {"type": "string", "minLength": 1, "description": "Untrusted producer assertion retained only for mismatch diagnostics; host custody supplies admitted identity."},
    "transport_id": {"type": "string", "description": "Transport identity, which grants no producer authority."},
    "proof_route": {"type": "string", "minLength": 1, "description": "Claimed proof route."},
    "evidence_class": {"type": "string", "minLength": 1, "description": "Claimed evidence class."},
    "result_contract": {"type": "string", "minLength": 1, "description": "Untrusted result-contract assertion used only for mismatch diagnostics."},
    "result": {"type": "string", "minLength": 1, "description": "Untrusted result assertion used only for mismatch diagnostics."},
    "evidence_ref": {"type": "string", "minLength": 1, "description": "Untrusted reference assertion used only for mismatch diagnostics."},
    "application_id": {"type": "string", "minLength": 1, "description": "Optional source-bound assurance application identity required by repository evidence policy."},
    "proof_subject": {"type": "object", "description": "Existing dependency-scoped proof subject."}
  },
  "additionalProperties": false,
  "x-agentic-workspace-doc-role": "contract-reference"
}
