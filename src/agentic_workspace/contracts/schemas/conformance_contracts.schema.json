{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/conformance-contracts.schema.json",
  "title": "Agentic Workspace Conformance Contracts Registry",
  "description": "Schema for Agentic Workspace Conformance Contracts Registry.",
  "type": "object",
  "required": [
    "schema_version",
    "summary",
    "schema",
    "contracts"
  ],
  "properties": {
    "schema_version": {
      "const": "agentic-workspace/conformance-contracts/v1",
      "description": "The schema version constant field in Agentic Workspace Conformance Contracts Registry."
    },
    "summary": {
      "type": "string",
      "minLength": 1,
      "description": "The summary string field in Agentic Workspace Conformance Contracts Registry."
    },
    "schema": {
      "const": "schemas/conformance.schema.json",
      "description": "The schema constant field in Agentic Workspace Conformance Contracts Registry."
    },
    "contracts": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "id",
          "operation_id",
          "path",
          "adapter_kind"
        ],
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9_.-]*$",
            "description": "The id string field in Agentic Workspace Conformance Contracts Registry."
          },
          "operation_id": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9_.-]*$",
            "description": "The operation id string field in Agentic Workspace Conformance Contracts Registry."
          },
          "path": {
            "type": "string",
            "pattern": "^conformance/[a-z0-9_.-]+\\.json$",
            "description": "The path string field in Agentic Workspace Conformance Contracts Registry."
          },
          "adapter_kind": {
            "type": "string",
            "enum": [
              "process"
            ],
            "description": "The adapter kind enumerated value field in Agentic Workspace Conformance Contracts Registry."
          }
        },
        "additionalProperties": false,
        "description": "Item object accepted by contracts in Agentic Workspace Conformance Contracts Registry."
      },
      "description": "The contracts array field in Agentic Workspace Conformance Contracts Registry."
    }
  },
  "additionalProperties": false,
  "x-agentic-workspace-doc-role": "contract-reference"
}
