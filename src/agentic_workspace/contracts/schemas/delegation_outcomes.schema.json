{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/delegation-outcomes.schema.json",
  "title": "Agentic Workspace Local Delegation Outcomes",
  "description": "Schema for Agentic Workspace Local Delegation Outcomes.",
  "type": "object",
  "required": [
    "kind",
    "records"
  ],
  "properties": {
    "kind": {
      "const": "agentic-workspace/delegation-outcomes/v1",
      "description": "The kind constant field in Agentic Workspace Local Delegation Outcomes."
    },
    "records": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "recorded_at",
          "delegation_target",
          "task_class",
          "outcome",
          "handoff_sufficiency",
          "review_burden",
          "escalation_required"
        ],
        "properties": {
          "recorded_at": {
            "type": "string",
            "minLength": 1,
            "description": "The recorded at string field in Agentic Workspace Local Delegation Outcomes."
          },
          "delegation_target": {
            "type": "string",
            "minLength": 1,
            "description": "The delegation target string field in Agentic Workspace Local Delegation Outcomes."
          },
          "task_class": {
            "type": "string",
            "minLength": 1,
            "description": "The task class string field in Agentic Workspace Local Delegation Outcomes."
          },
          "outcome": {
            "type": "string",
            "enum": [
              "success",
              "mixed",
              "failed"
            ],
            "description": "The outcome enumerated value field in Agentic Workspace Local Delegation Outcomes."
          },
          "handoff_sufficiency": {
            "type": "string",
            "enum": [
              "sufficient",
              "borderline",
              "insufficient"
            ],
            "description": "The handoff sufficiency enumerated value field in Agentic Workspace Local Delegation Outcomes."
          },
          "review_burden": {
            "type": "string",
            "enum": [
              "light",
              "normal",
              "high"
            ],
            "description": "The review burden enumerated value field in Agentic Workspace Local Delegation Outcomes."
          },
          "escalation_required": {
            "type": "boolean",
            "description": "The escalation required boolean field in Agentic Workspace Local Delegation Outcomes."
          }
        },
        "additionalProperties": false,
        "description": "Item object accepted by records in Agentic Workspace Local Delegation Outcomes."
      },
      "description": "The records array field in Agentic Workspace Local Delegation Outcomes."
    }
  },
  "additionalProperties": false,
  "x-agentic-workspace-doc-role": "contract-reference"
}
