{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/cli-option-groups.schema.json",
  "title": "Cli Option Groups",
  "description": "Schema for Cli Option Groups.",
  "type": "object",
  "required": [
    "schema_version",
    "option_groups"
  ],
  "properties": {
    "schema_version": {
      "const": "agentic-workspace/cli-option-groups/v1",
      "description": "The schema version constant field in Cli Option Groups."
    },
    "option_groups": {
      "type": "object",
      "minProperties": 1,
      "additionalProperties": {
        "type": "object",
        "properties": {
          "uses": {
            "type": "array",
            "items": {
              "type": "string",
              "description": "Item string accepted by uses in Cli Option Groups."
            },
            "description": "The uses array field in Cli Option Groups."
          },
          "options": {
            "type": "array",
            "items": {
              "$ref": "#/$defs/option",
              "description": "Item reference accepted by options in Cli Option Groups."
            },
            "description": "The options array field in Cli Option Groups."
          }
        },
        "additionalProperties": false,
        "description": "Additional named object entry for option groups in Cli Option Groups."
      },
      "description": "The option groups object field in Cli Option Groups."
    }
  },
  "$defs": {
    "option": {
      "type": "object",
      "required": [
        "name",
        "flags"
      ],
      "properties": {
        "name": {
          "type": "string",
          "description": "The name string field in Cli Option Groups."
        },
        "flags": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "description": "Item string accepted by flags in Cli Option Groups."
          },
          "description": "The flags array field in Cli Option Groups."
        },
        "action": {
          "type": "string",
          "description": "The action string field in Cli Option Groups."
        },
        "choices": {
          "type": "array",
          "items": {
            "type": [
              "string",
              "integer",
              "number",
              "boolean"
            ],
            "description": "Item string or integer or number or boolean accepted by choices in Cli Option Groups."
          },
          "description": "The choices array field in Cli Option Groups."
        },
        "choices_ref": {
          "type": "string",
          "description": "The choices ref string field in Cli Option Groups."
        },
        "default": {},
        "default_ref": {
          "type": "string",
          "description": "The default ref string field in Cli Option Groups."
        },
        "type": {
          "type": "string",
          "description": "The type string field in Cli Option Groups."
        },
        "required": {
          "type": "boolean",
          "description": "The required boolean field in Cli Option Groups."
        },
        "help": {
          "type": "string",
          "description": "The help string field in Cli Option Groups."
        },
        "help_template": {
          "type": "string",
          "description": "The help template string field in Cli Option Groups."
        }
      },
      "additionalProperties": false,
      "description": "Reusable option object definition for Cli Option Groups."
    }
  },
  "additionalProperties": false,
  "x-agentic-workspace-doc-role": "contract-reference"
}
