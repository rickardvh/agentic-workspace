{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/improvement-signal-contract.schema.json",
  "title": "Agentic Workspace Improvement Signal Contract",
  "description": "Contract for classifying improvement signals and routing them to the right owner surface.",
  "type": "object",
  "required": [
    "schema_version",
    "kind",
    "role",
    "rule",
    "candidate_kind",
    "required_fields",
    "kinds",
    "likely_remediations",
    "validation_failure_classes",
    "validation_remedy_order",
    "correct_by_design_review",
    "confidence",
    "recurrence",
    "lifecycle_states",
    "lifecycle_rule",
    "retirement_criteria_fields",
    "immediate_actions",
    "retention",
    "destinations",
    "closeout_statuses",
    "guardrails"
  ],
  "properties": {
    "schema_version": {
      "const": "agentic-workspace/improvement-signal-contract/v1",
      "description": "Contract version for this document."
    },
    "kind": {
      "const": "workspace-improvement-signal-contract/v1",
      "description": "Discriminator identifying the payload or record shape."
    },
    "role": {
      "const": "candidate-shape-not-backlog",
      "description": "Fixed role value required by this contract."
    },
    "rule": {
      "type": "string",
      "minLength": 1,
      "description": "Policy rule that explains this contract behavior."
    },
    "candidate_kind": {
      "const": "workspace-improvement-signal-candidate/v1",
      "description": "Fixed candidate kind value required by this contract."
    },
    "required_fields": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1,
        "description": "One entry in the required fields list."
      },
      "description": "Ordered required fields entries used by this contract."
    },
    "kinds": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1,
        "description": "One entry in the kinds list."
      },
      "description": "Ordered kinds entries used by this contract."
    },
    "likely_remediations": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1,
        "description": "One entry in the likely remediations list."
      },
      "description": "Ordered likely remediations entries used by this contract."
    },
    "validation_failure_classes": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "class",
          "signal_rule",
          "route",
          "preferred_remediations"
        ],
        "properties": {
          "class": {
            "enum": [
              "user_or_content_error",
              "environment_or_dependency_error",
              "interface_design_error",
              "unclear_proof_contract"
            ],
            "description": "Classification used for routing, filtering, or policy selection."
          },
          "signal_rule": {
            "type": "string",
            "minLength": 1,
            "description": "Signal rule text value used by this contract."
          },
          "route": {
            "type": "string",
            "minLength": 1,
            "description": "Route text value used by this contract."
          },
          "preferred_remediations": {
            "type": "array",
            "minItems": 1,
            "items": {
              "type": "string",
              "minLength": 1,
              "description": "One entry in the preferred remediations list."
            },
            "description": "Ordered preferred remediations entries used by this contract."
          }
        },
        "additionalProperties": false,
        "description": "One entry in the validation failure classes list."
      },
      "description": "Ordered validation failure classes entries used by this contract."
    },
    "validation_remedy_order": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1,
        "description": "One entry in the validation remedy order list."
      },
      "description": "Ordered validation remedy order entries used by this contract."
    },
    "correct_by_design_review": {
      "type": "object",
      "required": [
        "status",
        "rule",
        "ask_when",
        "questions",
        "preferred_remediation_order",
        "closeout_field"
      ],
      "properties": {
        "status": {
          "type": "string",
          "minLength": 1,
          "description": "Current lifecycle, readiness, or health state."
        },
        "rule": {
          "type": "string",
          "minLength": 1,
          "description": "Policy rule that explains this contract behavior."
        },
        "ask_when": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "minLength": 1,
            "description": "One entry in the ask when list."
          },
          "description": "Ordered ask when entries used by this contract."
        },
        "questions": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "minLength": 1,
            "description": "One entry in the questions list."
          },
          "description": "Ordered questions entries used by this contract."
        },
        "preferred_remediation_order": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "minLength": 1,
            "description": "One entry in the preferred remediation order list."
          },
          "description": "Ordered preferred remediation order entries used by this contract."
        },
        "closeout_field": {
          "type": "string",
          "minLength": 1,
          "description": "Closeout field text value used by this contract."
        }
      },
      "additionalProperties": false,
      "description": "Correct by design review details used by this contract."
    },
    "confidence": {
      "type": "array",
      "minItems": 1,
      "items": {
        "enum": [
          "low",
          "medium",
          "high"
        ],
        "description": "One entry in the confidence list."
      },
      "description": "Ordered confidence entries used by this contract."
    },
    "recurrence": {
      "type": "array",
      "minItems": 1,
      "items": {
        "enum": [
          "first_seen",
          "repeated",
          "human_confirmed"
        ],
        "description": "One entry in the recurrence list."
      },
      "description": "Ordered recurrence entries used by this contract."
    },
    "lifecycle_states": {
      "type": "array",
      "minItems": 1,
      "items": {
        "enum": [
          "active",
          "mitigated",
          "accepted-risk",
          "promoted-to-issue",
          "obsolete"
        ],
        "description": "One entry in the lifecycle states list."
      },
      "description": "Ordered lifecycle states entries used by this contract."
    },
    "lifecycle_rule": {
      "type": "string",
      "minLength": 1,
      "description": "Policy rule that explains how improvement pressure lifecycle state affects routing."
    },
    "retirement_criteria_fields": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1,
        "description": "One entry in the retirement criteria fields list."
      },
      "description": "Fields that explain what retires or promotes an improvement pressure record."
    },
    "immediate_actions": {
      "type": "array",
      "minItems": 1,
      "items": {
        "enum": [
          "fix_now",
          "route",
          "review",
          "remember",
          "dismiss"
        ],
        "description": "One entry in the immediate actions list."
      },
      "description": "Ordered immediate actions entries used by this contract."
    },
    "retention": {
      "type": "array",
      "minItems": 1,
      "items": {
        "enum": [
          "shrink_after_fix",
          "delete_after_fix",
          "keep_with_justification"
        ],
        "description": "One entry in the retention list."
      },
      "description": "Ordered retention entries used by this contract."
    },
    "destinations": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1,
        "description": "One entry in the destinations list."
      },
      "description": "Ordered destinations entries used by this contract."
    },
    "closeout_statuses": {
      "type": "array",
      "minItems": 1,
      "items": {
        "enum": [
          "found",
          "fixed",
          "routed",
          "dismissed",
          "none"
        ],
        "description": "One entry in the closeout statuses list."
      },
      "description": "Ordered closeout statuses entries used by this contract."
    },
    "guardrails": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1,
        "description": "One entry in the guardrails list."
      },
      "description": "Ordered guardrails entries used by this contract."
    }
  },
  "additionalProperties": false,
  "x-agentic-workspace-doc-role": "contract-reference"
}
