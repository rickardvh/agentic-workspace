{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/skill-spec.schema.json",
  "title": "SkillSpec Contract",
  "description": "Package-owned semantic contract for skills that steer agent behavior while the CLI performs robust state inspection, mutation, validation, and reporting.",
  "type": "object",
  "required": [
    "schema_version",
    "rule",
    "specs",
    "transition_gates",
    "module_slots",
    "generated_plugin_targets"
  ],
  "properties": {
    "schema_version": {
      "const": "agentic-workspace/skill-spec/v1",
      "description": "Version marker for SkillSpec manifests."
    },
    "rule": {
      "type": "string",
      "minLength": 1,
      "description": "Operating rule that explains how SkillSpec relates skills, CLI commands, fallback guidance, and generated targets."
    },
    "specs": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/spec"
      },
      "description": "Ordered skill contracts that can be consumed by hand-authored skills, references, or later generators."
    },
    "transition_gates": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/transition_gate"
      },
      "description": "Structured SkillSpec-compatible transition gates that can be consumed without scraping skill prose."
    },
    "module_slots": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/module_slot"
      },
      "description": "Structured SkillSpec-compatible module ownership slots used by next-safe-action packets and operating-loop skills."
    },
    "generated_plugin_targets": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/generated_plugin_target"
      },
      "description": "Framework-native plugin/adaptor targets generated from SkillSpec without owning product behavior."
    }
  },
  "$defs": {
    "non_empty_string": {
      "type": "string",
      "minLength": 1,
      "description": "Non-empty text used in SkillSpec fields."
    },
    "non_empty_strings": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/non_empty_string"
      },
      "description": "Ordered non-empty text entries."
    },
    "field_ref": {
      "type": "object",
      "required": [
        "path",
        "decision"
      ],
      "properties": {
        "path": {
          "$ref": "#/$defs/non_empty_string",
          "description": "Dot-path or selector path in a CLI JSON output that a skill is expected to inspect."
        },
        "decision": {
          "$ref": "#/$defs/non_empty_string",
          "description": "Agent decision answered by this field."
        },
        "fallback_when_missing": {
          "$ref": "#/$defs/non_empty_string",
          "description": "Conservative behavior when the field is absent or the output is unavailable."
        }
      },
      "additionalProperties": false,
      "description": "CLI output field interpreted by a skill."
    },
    "surface": {
      "type": "object",
      "required": [
        "path",
        "authority"
      ],
      "properties": {
        "path": {
          "$ref": "#/$defs/non_empty_string",
          "description": "Repo-relative surface owned, read, or mutated under this skill contract."
        },
        "authority": {
          "enum": [
            "read-only-router",
            "cli-mutated-state",
            "package-payload",
            "reference-only"
          ],
          "description": "Authority role the skill assigns to the surface."
        },
        "edit_rule": {
          "$ref": "#/$defs/non_empty_string",
          "description": "How an agent should change or avoid changing this surface."
        }
      },
      "additionalProperties": false,
      "description": "Workspace surface governed by the skill."
    },
    "cli_command": {
      "type": "object",
      "required": [
        "id",
        "command_ref",
        "command",
        "preferred_invocation",
        "purpose",
        "mutates_state",
        "key_output_fields",
        "decisions_answered",
        "failure_fallback"
      ],
      "properties": {
        "id": {
          "$ref": "#/$defs/non_empty_string",
          "description": "Stable command affordance identifier."
        },
        "command_ref": {
          "$ref": "#/$defs/non_empty_string",
          "description": "Dot-separated reference to the canonical command metadata entry in cli_commands.json."
        },
        "command": {
          "$ref": "#/$defs/non_empty_string",
          "description": "Skill invocation command path; command_ref is the canonical command surface fact."
        },
        "preferred_invocation": {
          "$ref": "#/$defs/non_empty_string",
          "description": "Preferred invocation shape, including format expectations when relevant."
        },
        "purpose": {
          "$ref": "#/$defs/non_empty_string",
          "description": "Situation where the skill should prefer this command over raw file inspection or manual mutation."
        },
        "mutates_state": {
          "type": "boolean",
          "description": "True when the command is expected to write workspace state."
        },
        "key_output_fields": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "CLI output fields the skill should interpret."
        },
        "decisions_answered": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "Agent decisions this command is expected to make cheaper or safer."
        },
        "failure_fallback": {
          "$ref": "#/$defs/non_empty_string",
          "description": "Conservative fallback if the command is unavailable or fails before producing usable output."
        }
      },
      "additionalProperties": false,
      "description": "CLI affordance used by a skill."
    },
    "proportionality": {
      "type": "object",
      "required": [
        "no_artifact_by_default",
        "artifact_creation_allowed_when",
        "ordinary_work_rule"
      ],
      "properties": {
        "no_artifact_by_default": {
          "type": "boolean",
          "description": "Whether routine use of this skill should avoid creating review, planning, memory, or handoff artifacts."
        },
        "artifact_creation_allowed_when": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "Conditions that justify durable artifact creation."
        },
        "ordinary_work_rule": {
          "$ref": "#/$defs/non_empty_string",
          "description": "Rule for keeping low-risk or direct work lightweight."
        }
      },
      "additionalProperties": false,
      "description": "Proportionality and artifact policy for the skill."
    },
    "generated_target_requirements": {
      "type": "object",
      "required": [
        "status",
        "must_preserve"
      ],
      "properties": {
        "status": {
          "enum": [
            "contract-only",
            "ready-for-renderer",
            "deferred"
          ],
          "description": "Current readiness of this skill contract for generated target output."
        },
        "must_preserve": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "Semantics that any generated or adapter-specific surface must preserve."
        },
        "deferred_until": {
          "$ref": "#/$defs/non_empty_string",
          "description": "Condition that must be satisfied before renderer or target-adapter work begins."
        }
      },
      "additionalProperties": false,
      "description": "Constraints for future generated skill or adapter surfaces."
    },
    "generated_plugin_target": {
      "type": "object",
      "required": [
        "id",
        "framework",
        "title",
        "status",
        "output_path",
        "plugin_name",
        "source_skill_ids",
        "generated_skill_paths",
        "preferred_cli_commands",
        "interpreted_fields",
        "forbidden_actions",
        "fallback_when_cli_unavailable",
        "must_preserve",
        "cli_dependency",
        "next_safe_action_semantics"
      ],
      "properties": {
        "id": {
          "$ref": "#/$defs/non_empty_string",
          "description": "Stable generated plugin target identifier."
        },
        "framework": {
          "$ref": "#/$defs/non_empty_string",
          "description": "Host framework or adapter family for the generated target."
        },
        "title": {
          "$ref": "#/$defs/non_empty_string",
          "description": "Human-readable target title."
        },
        "status": {
          "enum": [
            "ready-for-renderer",
            "generated",
            "deferred"
          ],
          "description": "Generation readiness for this plugin target."
        },
        "output_path": {
          "$ref": "#/$defs/non_empty_string",
          "description": "Repo-relative generated plugin manifest path."
        },
        "plugin_name": {
          "$ref": "#/$defs/non_empty_string",
          "description": "Framework-native plugin identifier."
        },
        "source_skill_ids": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "SkillSpec ids projected into this plugin target."
        },
        "generated_skill_paths": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "Generated skill paths this plugin target exposes or links to."
        },
        "preferred_cli_commands": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "CLI invocations the plugin target should prefer when available."
        },
        "interpreted_fields": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "Output fields the plugin target must preserve for agent decisions."
        },
        "forbidden_actions": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "Actions the generated plugin must preserve as forbidden."
        },
        "fallback_when_cli_unavailable": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "No-CLI fallback behavior preserved by the generated plugin."
        },
        "must_preserve": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "Semantics required for target equivalence."
        },
        "cli_dependency": {
          "enum": [
            "preferred-when-available",
            "required",
            "none"
          ],
          "description": "Whether the target depends on the CLI or only prefers it."
        },
        "next_safe_action_semantics": {
          "type": "boolean",
          "description": "Whether the target preserves next-safe-action packet semantics."
        }
      },
      "additionalProperties": false,
      "description": "A framework-native plugin/adaptor target generated from SkillSpec."
    },
    "transition_gate": {
      "type": "object",
      "required": [
        "id",
        "title",
        "trigger",
        "module_slot",
        "preferred_cli_or_report",
        "interpreted_fields",
        "allowed_actions",
        "forbidden_actions",
        "proof_obligations",
        "closeout_obligations",
        "fallback_when_cli_unavailable",
        "generated_target_requirements"
      ],
      "properties": {
        "id": {
          "$ref": "#/$defs/non_empty_string",
          "description": "Stable transition gate identifier."
        },
        "title": {
          "$ref": "#/$defs/non_empty_string",
          "description": "Human-readable transition gate title."
        },
        "trigger": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "Conditions that activate this transition gate."
        },
        "module_slot": {
          "$ref": "#/$defs/non_empty_string",
          "description": "Module slot that owns the transition decision."
        },
        "preferred_cli_or_report": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "Preferred CLI command or report surface for this transition."
        },
        "interpreted_fields": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "Output fields consumed by this transition gate."
        },
        "allowed_actions": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "Actions permitted by this transition gate."
        },
        "forbidden_actions": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "Actions forbidden until a newer transition decision supersedes this gate."
        },
        "proof_obligations": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "Proof obligations that must be satisfied for this transition."
        },
        "closeout_obligations": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "Closeout obligations carried by this transition."
        },
        "fallback_when_cli_unavailable": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "No-CLI fallback that preserves the transition's forbidden actions and ownership."
        },
        "generated_target_requirements": {
          "$ref": "#/$defs/generated_target_requirements",
          "description": "Requirements future generated skills or adapters must preserve for this transition."
        }
      },
      "additionalProperties": false,
      "description": "A structured workflow transition gate carried by the SkillSpec contract."
    },
    "module_slot": {
      "type": "object",
      "required": [
        "id",
        "title",
        "owns",
        "preferred_cli_or_report",
        "interpreted_fields",
        "allowed_actions",
        "forbidden_actions",
        "proof_obligations",
        "closeout_obligations",
        "fallback_when_cli_unavailable",
        "generated_target_requirements"
      ],
      "properties": {
        "id": {
          "$ref": "#/$defs/non_empty_string",
          "description": "Stable module slot identifier."
        },
        "title": {
          "$ref": "#/$defs/non_empty_string",
          "description": "Human-readable module slot title."
        },
        "owns": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "Responsibilities owned by this module slot."
        },
        "preferred_cli_or_report": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "Preferred CLI command or report surface for this slot."
        },
        "interpreted_fields": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "Output fields this slot is expected to preserve or inspect."
        },
        "allowed_actions": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "Actions permitted inside this module slot."
        },
        "forbidden_actions": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "Actions this module slot must not absorb from another owner."
        },
        "proof_obligations": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "Proof obligations owned or preserved by this module slot."
        },
        "closeout_obligations": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "Closeout obligations owned or preserved by this module slot."
        },
        "fallback_when_cli_unavailable": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "No-CLI fallback that preserves this slot's ownership boundary."
        },
        "generated_target_requirements": {
          "$ref": "#/$defs/generated_target_requirements",
          "description": "Requirements future generated skills or adapters must preserve for this module slot."
        }
      },
      "additionalProperties": false,
      "description": "A structured ownership slot carried by the SkillSpec contract."
    },
    "spec": {
      "type": "object",
      "required": [
        "id",
        "title",
        "status",
        "applies_when",
        "proportionality",
        "preferred_cli_commands",
        "interpreted_output_fields",
        "owned_surfaces",
        "allowed_actions",
        "forbidden_actions",
        "fallback_when_cli_unavailable",
        "proof_obligations",
        "closeout_obligations",
        "generated_target_requirements"
      ],
      "properties": {
        "id": {
          "$ref": "#/$defs/non_empty_string",
          "description": "Stable SkillSpec identifier."
        },
        "title": {
          "$ref": "#/$defs/non_empty_string",
          "description": "Human-readable skill contract title."
        },
        "status": {
          "enum": [
            "active",
            "draft",
            "deferred"
          ],
          "description": "Lifecycle status for this skill contract."
        },
        "applies_when": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "Conditions that should route an agent to this skill behavior."
        },
        "proportionality": {
          "$ref": "#/$defs/proportionality",
          "description": "Artifact and proportionality policy for this skill."
        },
        "preferred_cli_commands": {
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/cli_command"
          },
          "description": "CLI commands the skill should prefer when available."
        },
        "interpreted_output_fields": {
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/field_ref"
          },
          "description": "Output fields that drive skill decisions."
        },
        "owned_surfaces": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/surface"
          },
          "description": "Repo surfaces this skill may read, route to, or ask the CLI to mutate."
        },
        "allowed_actions": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "Actions permitted under this skill contract."
        },
        "forbidden_actions": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "Actions this skill must not instruct or normalize."
        },
        "fallback_when_cli_unavailable": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "Conservative behavior when the preferred CLI cannot be used."
        },
        "proof_obligations": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "Validation or inspection obligations before claiming the skill-guided work is correct."
        },
        "closeout_obligations": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "Continuation and completion obligations before the agent reports done."
        },
        "generated_target_requirements": {
          "$ref": "#/$defs/generated_target_requirements",
          "description": "Requirements for future generated projections of the skill."
        }
      },
      "additionalProperties": false,
      "description": "A single skill behavior contract."
    }
  },
  "additionalProperties": false,
  "x-agentic-workspace-doc-role": "contract-reference",
  "x-agentic-workspace-see-also": [
    "docs/reference/startup-context.md",
    "docs/reference/cli-commands.md",
    "docs/reference/proof-selection-rules.md"
  ]
}
