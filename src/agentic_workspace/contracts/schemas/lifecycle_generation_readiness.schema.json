{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/lifecycle-generation-readiness.schema.json",
  "title": "Lifecycle Generation Readiness Contract",
  "description": "Readiness contract for deciding whether lifecycle surfaces can be generated safely.",
  "type": "object",
  "required": [
    "schema_version",
    "summary",
    "phase_model",
    "commands",
    "primitive_review",
    "output_contract",
    "conformance_fixture_plan",
    "generation_decision"
  ],
  "properties": {
    "schema_version": {
      "const": "agentic-workspace/lifecycle-generation-readiness/v1",
      "description": "Contract version for this document."
    },
    "summary": {
      "type": "string",
      "minLength": 1,
      "description": "Short human-readable summary for this entry."
    },
    "phase_model": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1,
        "description": "One entry in the phase model list."
      },
      "description": "Ordered phase model entries used by this contract."
    },
    "commands": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "surface",
          "command",
          "phases",
          "adapter_owned_phases",
          "runtime_owned_phases",
          "effects",
          "generation_eligibility"
        ],
        "properties": {
          "surface": {
            "enum": [
              "root",
              "planning-package",
              "memory-package"
            ],
            "description": "Allowed surface value for routing or validation."
          },
          "command": {
            "enum": [
              "install",
              "init",
              "adopt",
              "upgrade",
              "uninstall",
              "doctor",
              "status"
            ],
            "description": "Command string or command name used by this contract."
          },
          "phases": {
            "$ref": "#/$defs/non_empty_strings",
            "description": "Phases referenced contract shape used by this contract."
          },
          "adapter_owned_phases": {
            "$ref": "#/$defs/non_empty_strings",
            "description": "Adapter owned phases referenced contract shape used by this contract."
          },
          "runtime_owned_phases": {
            "$ref": "#/$defs/non_empty_strings",
            "description": "Runtime owned phases referenced contract shape used by this contract."
          },
          "effects": {
            "type": "object",
            "required": [
              "read_only",
              "writes_repo_state",
              "destructive_potential",
              "idempotency",
              "preflight_required",
              "local_only_interaction",
              "branch_sensitive",
              "review_required_when_ambiguous"
            ],
            "properties": {
              "read_only": {
                "type": "boolean",
                "description": "Whether this operation or aid avoids writes."
              },
              "writes_repo_state": {
                "type": "boolean",
                "description": "Writes repo state true/false policy flag used by this contract."
              },
              "destructive_potential": {
                "type": "boolean",
                "description": "Destructive potential true/false policy flag used by this contract."
              },
              "idempotency": {
                "enum": [
                  "idempotent",
                  "planned-idempotent",
                  "context-dependent"
                ],
                "description": "Expectations for repeated execution against the same fixture."
              },
              "preflight_required": {
                "type": "boolean",
                "description": "Preflight required true/false policy flag used by this contract."
              },
              "local_only_interaction": {
                "type": "boolean",
                "description": "Local only interaction true/false policy flag used by this contract."
              },
              "branch_sensitive": {
                "type": "boolean",
                "description": "Branch sensitive true/false policy flag used by this contract."
              },
              "review_required_when_ambiguous": {
                "type": "boolean",
                "description": "Review required when ambiguous true/false policy flag used by this contract."
              }
            },
            "additionalProperties": false,
            "description": "Side-effect model for safe execution."
          },
          "generation_eligibility": {
            "enum": [
              "eligible-read-only",
              "deferred-mutation",
              "deferred-destructive"
            ],
            "description": "Allowed generation eligibility value for routing or validation."
          },
          "notes": {
            "type": "string",
            "description": "Additional notes that qualify this record."
          }
        },
        "additionalProperties": false,
        "description": "One entry in the commands list."
      },
      "description": "Commands associated with this check, proof, or workflow step."
    },
    "primitive_review": {
      "$ref": "#/$defs/non_empty_strings",
      "description": "Primitive review referenced contract shape used by this contract."
    },
    "output_contract": {
      "$ref": "#/$defs/non_empty_strings",
      "description": "Output contract referenced contract shape used by this contract."
    },
    "conformance_fixture_plan": {
      "$ref": "#/$defs/non_empty_strings",
      "description": "Conformance fixture plan referenced contract shape used by this contract."
    },
    "generation_decision": {
      "$ref": "#/$defs/non_empty_strings",
      "description": "Generation decision referenced contract shape used by this contract."
    }
  },
  "additionalProperties": false,
  "$defs": {
    "non_empty_strings": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1,
        "description": "One entry in the non empty strings list."
      },
      "description": "Ordered non empty strings entries used by this contract."
    }
  },
  "x-agentic-workspace-doc-role": "contract-reference"
}
