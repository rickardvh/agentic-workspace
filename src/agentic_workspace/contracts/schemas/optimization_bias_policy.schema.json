{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/optimization-bias-policy.schema.json",
  "title": "Optimization Bias Policy",
  "description": "Policy describing output tradeoffs for agent efficiency, balance, and human legibility.",
  "type": "object",
  "required": [
    "schema_version",
    "default_mode",
    "owner_surface",
    "must_not_change",
    "surface_boundary",
    "modes"
  ],
  "properties": {
    "schema_version": {
      "const": "agentic-workspace/optimization-bias-policy/v1",
      "description": "Contract version for this document."
    },
    "default_mode": {
      "type": "string",
      "description": "Default mode text value used by this contract."
    },
    "owner_surface": {
      "type": "string",
      "description": "Owner surface text value used by this contract."
    },
    "must_not_change": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "description": "One entry in the must not change list."
      },
      "description": "Ordered must not change entries used by this contract."
    },
    "surface_boundary": {
      "$ref": "#/$defs/surfaceBoundary",
      "description": "Surface boundary referenced contract shape used by this contract."
    },
    "modes": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "mode",
          "summary",
          "report_density",
          "residue_density",
          "rendered_view_style",
          "allows",
          "does_not_affect",
          "surface_boundary"
        ],
        "properties": {
          "mode": {
            "type": "string",
            "description": "Mode text value used by this contract."
          },
          "summary": {
            "type": "string",
            "description": "Short human-readable summary for this entry."
          },
          "report_density": {
            "type": "string",
            "description": "Report density text value used by this contract."
          },
          "residue_density": {
            "type": "string",
            "description": "Residue density text value used by this contract."
          },
          "rendered_view_style": {
            "type": "string",
            "description": "Rendered view style text value used by this contract."
          },
          "allows": {
            "type": "array",
            "minItems": 1,
            "items": {
              "type": "string",
              "description": "One entry in the allows list."
            },
            "description": "Ordered allows entries used by this contract."
          },
          "does_not_affect": {
            "type": "array",
            "minItems": 1,
            "items": {
              "type": "string",
              "description": "One entry in the does not affect list."
            },
            "description": "Ordered does not affect entries used by this contract."
          },
          "surface_boundary": {
            "$ref": "#/$defs/surfaceBoundary",
            "description": "Surface boundary referenced contract shape used by this contract."
          }
        },
        "additionalProperties": false,
        "description": "One entry in the modes list."
      },
      "description": "Ordered modes entries used by this contract."
    }
  },
  "$defs": {
    "surfaceBoundary": {
      "type": "object",
      "required": [
        "honors_bias",
        "stays_invariant"
      ],
      "properties": {
        "honors_bias": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "description": "One entry in the honors bias list."
          },
          "description": "Ordered honors bias entries used by this contract."
        },
        "stays_invariant": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "description": "One entry in the stays invariant list."
          },
          "description": "Ordered stays invariant entries used by this contract."
        }
      },
      "additionalProperties": false,
      "description": "Surfaceboundary details used by this contract."
    }
  },
  "additionalProperties": false,
  "x-agentic-workspace-doc-role": "contract-reference"
}
