{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/module-capability.schema.json",
  "title": "Agentic Workspace Module Capability Contract",
  "description": "Module capability contract for describing package-owned features and execution posture.",
  "type": "object",
  "required": [
    "name",
    "description",
    "capabilities",
    "commands",
    "install_signals",
    "workflow_surfaces",
    "result_contract"
  ],
  "properties": {
    "name": {
      "type": "string",
      "description": "Durable name of the module."
    },
    "description": {
      "type": "string",
      "description": "Human-readable description of the module's purpose."
    },
    "selection_rank": {
      "type": "integer",
      "description": "Rank used to order modules during selection and execution."
    },
    "include_in_full_preset": {
      "type": "boolean",
      "description": "Whether this module is included in the 'full' preset by default."
    },
    "capabilities": {
      "type": "array",
      "items": {
        "type": "string",
        "description": "One entry in the capabilities list."
      },
      "minItems": 1,
      "description": "Durable product capabilities the module owns."
    },
    "commands": {
      "type": "array",
      "items": {
        "type": "string",
        "description": "One entry in the commands list."
      },
      "description": "Lifecycle hooks the workspace layer may orchestrate."
    },
    "command_args": {
      "type": "object",
      "patternProperties": {
        "^.+$": {
          "type": "array",
          "items": {
            "enum": [
              "target",
              "dry_run",
              "force"
            ],
            "description": "One entry in the +$ list."
          },
          "description": "Ordered +$ entries used by this contract."
        }
      },
      "description": "Arguments accepted by each lifecycle command."
    },
    "install_signals": {
      "type": "array",
      "items": {
        "type": "string",
        "description": "One entry in the install signals list."
      },
      "minItems": 1,
      "description": "Repo surfaces that indicate the module is materially installed."
    },
    "workflow_surfaces": {
      "type": "array",
      "items": {
        "type": "string",
        "description": "One entry in the workflow surfaces list."
      },
      "minItems": 1,
      "description": "Repo surfaces that belong to the module's working contract."
    },
    "generated_artifacts": {
      "type": "array",
      "items": {
        "type": "string",
        "description": "One entry in the generated artifacts list."
      },
      "description": "Generated outputs whose drift should be reported instead of hand-edited."
    },
    "dependencies": {
      "type": "array",
      "items": {
        "type": "string",
        "description": "One entry in the dependencies list."
      },
      "description": "Other modules that must also be selected when this module is selected."
    },
    "conflicts": {
      "type": "array",
      "items": {
        "type": "string",
        "description": "One entry in the conflicts list."
      },
      "description": "Other modules that may not be selected at the same time."
    },
    "startup_steps": {
      "type": "array",
      "items": {
        "type": "string",
        "description": "One entry in the startup steps list."
      },
      "description": "Ordered steps recommended for fresh agents during startup."
    },
    "sources_of_truth": {
      "type": "array",
      "items": {
        "type": "string",
        "description": "One entry in the sources of truth list."
      },
      "description": "Canonical sources of truth for the module's domain."
    },
    "result_contract": {
      "type": "object",
      "required": [
        "schema_version",
        "guaranteed_fields",
        "action_fields",
        "warning_fields"
      ],
      "properties": {
        "schema_version": {
          "type": "string",
          "description": "Contract version for this document."
        },
        "guaranteed_fields": {
          "type": "array",
          "items": {
            "type": "string",
            "description": "One entry in the guaranteed fields list."
          },
          "description": "Ordered guaranteed fields entries used by this contract."
        },
        "action_fields": {
          "type": "array",
          "items": {
            "type": "string",
            "description": "One entry in the action fields list."
          },
          "description": "Ordered action fields entries used by this contract."
        },
        "warning_fields": {
          "type": "array",
          "items": {
            "type": "string",
            "description": "One entry in the warning fields list."
          },
          "description": "Ordered warning fields entries used by this contract."
        }
      },
      "description": "The schema version and required fields the workspace adapter guarantees to preserve."
    }
  },
  "additionalProperties": false,
  "x-agentic-workspace-doc-role": "contract-reference"
}
