{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/module-capability.schema.json",
  "title": "Agentic Workspace Module Capability Contract",
  "type": "object",
  "required": [
    "name",
    "description",
    "capabilities",
    "commands",
    "install_signals",
    "workflow_surfaces",
    "result_contract"
  ],
  "properties": {
    "name": {
      "type": "string",
      "description": "Durable name of the module."
    },
    "description": {
      "type": "string",
      "description": "Human-readable description of the module's purpose."
    },
    "selection_rank": {
      "type": "integer",
      "description": "Rank used to order modules during selection and execution."
    },
    "include_in_full_preset": {
      "type": "boolean",
      "description": "Whether this module is included in the 'full' preset by default."
    },
    "capabilities": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 1,
      "description": "Durable product capabilities the module owns."
    },
    "commands": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Lifecycle hooks the workspace layer may orchestrate."
    },
    "command_args": {
      "type": "object",
      "patternProperties": {
        "^.+$": {
          "type": "array",
          "items": {
            "enum": ["target", "dry_run", "force"]
          }
        }
      },
      "description": "Arguments accepted by each lifecycle command."
    },
    "install_signals": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 1,
      "description": "Repo surfaces that indicate the module is materially installed."
    },
    "workflow_surfaces": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 1,
      "description": "Repo surfaces that belong to the module's working contract."
    },
    "generated_artifacts": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Generated outputs whose drift should be reported instead of hand-edited."
    },
    "dependencies": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Other modules that must also be selected when this module is selected."
    },
    "conflicts": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Other modules that may not be selected at the same time."
    },
    "startup_steps": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Ordered steps recommended for fresh agents during startup."
    },
    "sources_of_truth": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Canonical sources of truth for the module's domain."
    },
    "result_contract": {
      "type": "object",
      "required": ["schema_version", "guaranteed_fields", "action_fields", "warning_fields"],
      "properties": {
        "schema_version": { "type": "string" },
        "guaranteed_fields": {
          "type": "array",
          "items": { "type": "string" }
        },
        "action_fields": {
          "type": "array",
          "items": { "type": "string" }
        },
        "warning_fields": {
          "type": "array",
          "items": { "type": "string" }
        }
      },
      "description": "The schema version and required fields the workspace adapter guarantees to preserve."
    }
  },
  "additionalProperties": false
}
