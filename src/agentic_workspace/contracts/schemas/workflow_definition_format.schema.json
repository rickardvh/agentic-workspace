{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/workflow-definition-format.schema.json",
  "title": "Workflow Definition Format",
  "description": "Format contract for repo-custom workflow obligation declarations.",
  "type": "object",
  "required": [
    "schema_version",
    "canonical_doc",
    "rule",
    "primary_component_family",
    "composition",
    "flexibility_boundary",
    "migration_posture"
  ],
  "properties": {
    "schema_version": {
      "const": "agentic-workspace/workflow-definition-format/v1",
      "description": "Contract version for this document."
    },
    "canonical_doc": {
      "type": "string",
      "minLength": 1,
      "description": "Canonical doc text value used by this contract."
    },
    "rule": {
      "type": "string",
      "minLength": 1,
      "description": "Policy rule that explains this contract behavior."
    },
    "primary_component_family": {
      "type": "object",
      "required": [
        "id",
        "owner_surface",
        "attachment_points",
        "fields"
      ],
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "description": "Stable identifier used to reference this record."
        },
        "owner_surface": {
          "type": "string",
          "minLength": 1,
          "description": "Owner surface text value used by this contract."
        },
        "attachment_points": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "minLength": 1,
            "description": "One entry in the attachment points list."
          },
          "description": "Ordered attachment points entries used by this contract."
        },
        "fields": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "object",
            "required": [
              "field",
              "required",
              "purpose"
            ],
            "properties": {
              "field": {
                "type": "string",
                "minLength": 1,
                "description": "Field text value used by this contract."
              },
              "required": {
                "type": "boolean",
                "description": "Required true/false policy flag used by this contract."
              },
              "purpose": {
                "type": "string",
                "minLength": 1,
                "description": "Purpose text value used by this contract."
              }
            },
            "additionalProperties": false,
            "description": "One entry in the fields list."
          },
          "description": "Ordered fields entries used by this contract."
        }
      },
      "additionalProperties": false,
      "description": "Primary component family details used by this contract."
    },
    "composition": {
      "$ref": "#/$defs/string_map",
      "description": "Composition referenced contract shape used by this contract."
    },
    "flexibility_boundary": {
      "type": "object",
      "required": [
        "formalize",
        "leave_flexible",
        "must_not"
      ],
      "properties": {
        "formalize": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "Formalize referenced contract shape used by this contract."
        },
        "leave_flexible": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "Leave flexible referenced contract shape used by this contract."
        },
        "must_not": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "Must not referenced contract shape used by this contract."
        }
      },
      "additionalProperties": false,
      "description": "Flexibility boundary details used by this contract."
    },
    "migration_posture": {
      "$ref": "#/$defs/string_map",
      "description": "Migration posture referenced contract shape used by this contract."
    }
  },
  "additionalProperties": false,
  "$defs": {
    "non_empty_strings": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1,
        "description": "One entry in the non empty strings list."
      },
      "description": "Ordered non empty strings entries used by this contract."
    },
    "string_map": {
      "type": "object",
      "minProperties": 1,
      "additionalProperties": {
        "type": "string",
        "minLength": 1,
        "description": "Named entry in the string map mapping."
      },
      "description": "String map details used by this contract."
    }
  },
  "x-agentic-workspace-doc-role": "contract-reference"
}
