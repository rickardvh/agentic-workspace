{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/preflight-policy.schema.json",
  "title": "Preflight Policy",
  "description": "Policy for preflight routing, takeover context, and strict preflight gates.",
  "type": "object",
  "required": [
    "schema_version",
    "summary",
    "high_risk_commands",
    "token",
    "default_max_age_seconds",
    "strict_gate"
  ],
  "properties": {
    "schema_version": {
      "const": "agentic-workspace/preflight-policy/v1",
      "description": "Contract version for this document."
    },
    "summary": {
      "type": "string",
      "minLength": 1,
      "description": "Short human-readable summary for this entry."
    },
    "high_risk_commands": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "minLength": 1,
        "description": "One entry in the high risk commands list."
      },
      "description": "Ordered high risk commands entries used by this contract."
    },
    "token": {
      "type": "object",
      "required": [
        "prefix",
        "issued_at_epoch_encoding"
      ],
      "properties": {
        "prefix": {
          "type": "string",
          "minLength": 1,
          "description": "Prefix text value used by this contract."
        },
        "issued_at_epoch_encoding": {
          "type": "string",
          "enum": [
            "decimal_unix_epoch_seconds"
          ],
          "description": "Allowed issued at epoch encoding value for routing or validation."
        }
      },
      "additionalProperties": false,
      "description": "Token details used by this contract."
    },
    "default_max_age_seconds": {
      "type": "integer",
      "minimum": 1,
      "description": "Default max age seconds numeric value used by this contract."
    },
    "strict_gate": {
      "type": "object",
      "required": [
        "enabled_by_option",
        "token_option",
        "max_age_option",
        "missing_token_error",
        "invalid_token_error",
        "non_positive_max_age_error",
        "future_token_error",
        "stale_token_error_template"
      ],
      "properties": {
        "enabled_by_option": {
          "type": "string",
          "minLength": 1,
          "description": "Enabled by option text value used by this contract."
        },
        "token_option": {
          "type": "string",
          "minLength": 1,
          "description": "Token option text value used by this contract."
        },
        "max_age_option": {
          "type": "string",
          "minLength": 1,
          "description": "Max age option text value used by this contract."
        },
        "missing_token_error": {
          "type": "string",
          "minLength": 1,
          "description": "Missing token error text value used by this contract."
        },
        "invalid_token_error": {
          "type": "string",
          "minLength": 1,
          "description": "Invalid token error text value used by this contract."
        },
        "non_positive_max_age_error": {
          "type": "string",
          "minLength": 1,
          "description": "Non positive max age error text value used by this contract."
        },
        "future_token_error": {
          "type": "string",
          "minLength": 1,
          "description": "Future token error text value used by this contract."
        },
        "stale_token_error_template": {
          "type": "string",
          "minLength": 1,
          "description": "Stale token error template text value used by this contract."
        }
      },
      "additionalProperties": false,
      "description": "Strict gate details used by this contract."
    }
  },
  "additionalProperties": false,
  "x-agentic-workspace-doc-role": "contract-reference"
}
