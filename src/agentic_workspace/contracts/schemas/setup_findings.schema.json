{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/setup-findings.schema.json",
  "title": "Agentic Workspace Setup Findings",
  "description": "Setup findings payload for post-bootstrap issues that may need promotion into planning, docs, config, or follow-up work.",
  "type": "object",
  "required": [
    "kind",
    "findings"
  ],
  "properties": {
    "kind": {
      "const": "workspace-setup-findings/v1",
      "description": "Discriminator for setup findings payloads."
    },
    "findings": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "class",
          "summary"
        ],
        "properties": {
          "class": {
            "type": "string",
            "enum": [
              "repo_friction_evidence",
              "planning_candidate"
            ],
            "description": "The class enumerated value field in Agentic Workspace Setup Findings."
          },
          "summary": {
            "type": "string",
            "minLength": 1,
            "description": "The summary string field in Agentic Workspace Setup Findings."
          },
          "confidence": {
            "type": "number",
            "minimum": 0.0,
            "maximum": 1.0,
            "description": "The confidence number field in Agentic Workspace Setup Findings."
          },
          "path": {
            "type": "string",
            "minLength": 1,
            "description": "The path string field in Agentic Workspace Setup Findings."
          },
          "refs": {
            "type": "array",
            "maxItems": 5,
            "items": {
              "type": "string",
              "minLength": 1,
              "description": "Item string accepted by refs in Agentic Workspace Setup Findings."
            },
            "description": "The refs array field in Agentic Workspace Setup Findings."
          },
          "next_action": {
            "type": "string",
            "minLength": 1,
            "description": "The next action string field in Agentic Workspace Setup Findings."
          },
          "why": {
            "type": "string",
            "minLength": 1,
            "description": "The why string field in Agentic Workspace Setup Findings."
          },
          "observed_during": {
            "type": "string",
            "minLength": 1,
            "description": "The observed during string field in Agentic Workspace Setup Findings."
          },
          "signal_kind": {
            "type": "string",
            "enum": [
              "workflow_cost",
              "architecture_cost",
              "footprint_cost",
              "validation_friction",
              "docs_routing",
              "module_boundary",
              "repeated_human_steering",
              "weak_agent_failure",
              "shipped_footprint",
              "default_feature_creep"
            ],
            "description": "The signal kind enumerated value field in Agentic Workspace Setup Findings."
          },
          "cost": {
            "type": "string",
            "minLength": 1,
            "description": "The cost string field in Agentic Workspace Setup Findings."
          },
          "suspected_owner": {
            "type": "string",
            "minLength": 1,
            "description": "The suspected owner string field in Agentic Workspace Setup Findings."
          },
          "likely_remediation": {
            "type": "string",
            "enum": [
              "docs",
              "tests",
              "validation",
              "command",
              "scaffold",
              "writer_helper",
              "alias",
              "lifecycle_command",
              "agent_aid",
              "skill",
              "refactor",
              "module_split",
              "remove",
              "make_optional",
              "unship",
              "unknown"
            ],
            "description": "The likely remediation enumerated value field in Agentic Workspace Setup Findings."
          },
          "recurrence": {
            "type": "string",
            "enum": [
              "first_seen",
              "repeated",
              "human_confirmed"
            ],
            "description": "The recurrence enumerated value field in Agentic Workspace Setup Findings."
          },
          "validation_failure_class": {
            "type": "string",
            "enum": [
              "user_or_content_error",
              "environment_or_dependency_error",
              "interface_design_error",
              "unclear_proof_contract"
            ],
            "description": "The validation failure class enumerated value field in Agentic Workspace Setup Findings."
          }
        },
        "additionalProperties": false,
        "description": "Item object accepted by findings in Agentic Workspace Setup Findings."
      },
      "description": "Setup findings discovered during install, doctor, or setup review."
    }
  },
  "additionalProperties": false,
  "x-agentic-workspace-doc-role": "contract-reference"
}
