{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/contract-inventory.schema.json",
  "title": "Workspace Contract Inventory",
  "description": "Schema for Workspace Contract Inventory.",
  "type": "object",
  "required": [
    "schema_version",
    "canonical_doc",
    "owner_choice_model",
    "areas"
  ],
  "properties": {
    "schema_version": {
      "const": "workspace-contract-inventory/v1",
      "description": "The schema version constant field in Workspace Contract Inventory."
    },
    "canonical_doc": {
      "type": "string",
      "description": "The canonical doc string field in Workspace Contract Inventory."
    },
    "owner_choice_model": {
      "type": "object",
      "required": [
        "purpose",
        "review_questions",
        "concern_classes"
      ],
      "properties": {
        "purpose": {
          "type": "string",
          "minLength": 1,
          "description": "The purpose string field in Workspace Contract Inventory."
        },
        "review_questions": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "minLength": 1,
            "description": "Item string accepted by review questions in Workspace Contract Inventory."
          },
          "description": "The review questions array field in Workspace Contract Inventory."
        },
        "concern_classes": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "object",
            "required": [
              "id",
              "owner_surface",
              "authority_class",
              "route_when",
              "avoid_when"
            ],
            "properties": {
              "id": {
                "type": "string",
                "minLength": 1,
                "description": "The id string field in Workspace Contract Inventory."
              },
              "owner_surface": {
                "type": "string",
                "minLength": 1,
                "description": "The owner surface string field in Workspace Contract Inventory."
              },
              "authority_class": {
                "type": "string",
                "minLength": 1,
                "description": "The authority class string field in Workspace Contract Inventory."
              },
              "route_when": {
                "type": "string",
                "minLength": 1,
                "description": "The route when string field in Workspace Contract Inventory."
              },
              "avoid_when": {
                "type": "string",
                "minLength": 1,
                "description": "The avoid when string field in Workspace Contract Inventory."
              }
            },
            "additionalProperties": false,
            "description": "Item object accepted by concern classes in Workspace Contract Inventory."
          },
          "description": "The concern classes array field in Workspace Contract Inventory."
        }
      },
      "additionalProperties": false,
      "description": "The owner choice model object field in Workspace Contract Inventory."
    },
    "areas": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "area",
          "classification",
          "owner_surface",
          "consumers",
          "notes"
        ],
        "properties": {
          "area": {
            "type": "string",
            "description": "The area string field in Workspace Contract Inventory."
          },
          "classification": {
            "enum": [
              "declarative",
              "procedural",
              "procedural-contract",
              "derived"
            ],
            "description": "The classification enumerated value field in Workspace Contract Inventory."
          },
          "owner_surface": {
            "type": "string",
            "description": "The owner surface string field in Workspace Contract Inventory."
          },
          "consumers": {
            "type": "array",
            "items": {
              "type": "string",
              "description": "Item string accepted by consumers in Workspace Contract Inventory."
            },
            "description": "The consumers array field in Workspace Contract Inventory."
          },
          "notes": {
            "type": "string",
            "description": "The notes string field in Workspace Contract Inventory."
          }
        },
        "additionalProperties": false,
        "description": "Item object accepted by areas in Workspace Contract Inventory."
      },
      "description": "The areas array field in Workspace Contract Inventory."
    }
  },
  "additionalProperties": false,
  "x-agentic-workspace-doc-role": "contract-reference"
}
