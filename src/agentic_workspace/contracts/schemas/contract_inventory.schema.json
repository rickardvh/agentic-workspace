{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/contract-inventory.schema.json",
  "title": "Workspace Contract Inventory",
  "description": "Inventory of contract surfaces, owners, validators, and freshness checks.",
  "type": "object",
  "required": [
    "schema_version",
    "canonical_doc",
    "owner_choice_model",
    "areas"
  ],
  "properties": {
    "schema_version": {
      "const": "workspace-contract-inventory/v1",
      "description": "Contract version for this document."
    },
    "canonical_doc": {
      "type": "string",
      "description": "Canonical doc text value used by this contract."
    },
    "owner_choice_model": {
      "type": "object",
      "required": [
        "purpose",
        "review_questions",
        "concern_classes"
      ],
      "properties": {
        "purpose": {
          "type": "string",
          "minLength": 1,
          "description": "Purpose text value used by this contract."
        },
        "review_questions": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "minLength": 1,
            "description": "One entry in the review questions list."
          },
          "description": "Ordered review questions entries used by this contract."
        },
        "concern_classes": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "object",
            "required": [
              "id",
              "owner_surface",
              "authority_class",
              "route_when",
              "avoid_when"
            ],
            "properties": {
              "id": {
                "type": "string",
                "minLength": 1,
                "description": "Stable identifier used to reference this record."
              },
              "owner_surface": {
                "type": "string",
                "minLength": 1,
                "description": "Owner surface text value used by this contract."
              },
              "authority_class": {
                "type": "string",
                "minLength": 1,
                "description": "Authority class text value used by this contract."
              },
              "route_when": {
                "type": "string",
                "minLength": 1,
                "description": "Route when text value used by this contract."
              },
              "avoid_when": {
                "type": "string",
                "minLength": 1,
                "description": "Avoid when text value used by this contract."
              }
            },
            "additionalProperties": false,
            "description": "One entry in the concern classes list."
          },
          "description": "Ordered concern classes entries used by this contract."
        }
      },
      "additionalProperties": false,
      "description": "Owner choice model details used by this contract."
    },
    "areas": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "area",
          "classification",
          "owner_surface",
          "consumers",
          "notes"
        ],
        "properties": {
          "area": {
            "type": "string",
            "description": "Area text value used by this contract."
          },
          "classification": {
            "enum": [
              "declarative",
              "procedural",
              "procedural-contract",
              "derived"
            ],
            "description": "Classification used for routing, filtering, or policy selection."
          },
          "owner_surface": {
            "type": "string",
            "description": "Owner surface text value used by this contract."
          },
          "consumers": {
            "type": "array",
            "items": {
              "type": "string",
              "description": "One entry in the consumers list."
            },
            "description": "Ordered consumers entries used by this contract."
          },
          "notes": {
            "type": "string",
            "description": "Additional notes that qualify this record."
          }
        },
        "additionalProperties": false,
        "description": "One entry in the areas list."
      },
      "description": "Ordered areas entries used by this contract."
    }
  },
  "additionalProperties": false,
  "x-agentic-workspace-doc-role": "contract-reference"
}
