{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/operation.schema.json",
  "title": "Agentic Workspace Operation Contract",
  "description": "Contract for one Agentic Workspace operation, including command shape, effects, IO, guards, and proof.",
  "type": "object",
  "required": [
    "schema_version",
    "id",
    "summary",
    "classification",
    "inputs",
    "output",
    "effects",
    "locality",
    "steps",
    "guards",
    "proof"
  ],
  "properties": {
    "schema_version": {
      "const": "agentic-workspace/operation/v1",
      "description": "Operation contract version."
    },
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9_.-]*$",
      "description": "Stable operation identifier."
    },
    "command_surface": {
      "type": "object",
      "required": [
        "command"
      ],
      "properties": {
        "program": {
          "type": "string",
          "minLength": 1,
          "description": "The program string field in Agentic Workspace Operation Contract."
        },
        "command": {
          "type": "string",
          "minLength": 1,
          "description": "The command string field in Agentic Workspace Operation Contract."
        },
        "subcommand": {
          "type": "string",
          "minLength": 1,
          "description": "The subcommand string field in Agentic Workspace Operation Contract."
        },
        "format_option": {
          "type": "string",
          "minLength": 1,
          "description": "The format option string field in Agentic Workspace Operation Contract."
        }
      },
      "additionalProperties": false,
      "description": "CLI command or subcommand shape that exposes the operation."
    },
    "summary": {
      "type": "string",
      "minLength": 1,
      "description": "Short human-readable operation summary."
    },
    "intent": {
      "type": "string",
      "description": "User or system intent the operation is designed to satisfy."
    },
    "classification": {
      "type": "string",
      "enum": [
        "read-only-report",
        "metadata-refresh",
        "lifecycle-mutation",
        "local-record-append",
        "derived-output"
      ],
      "description": "Output and authority classification for the operation."
    },
    "inputs": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "name"
        ],
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1,
            "description": "The name string field in Agentic Workspace Operation Contract."
          },
          "source": {
            "type": "string",
            "enum": [
              "cli-option",
              "default",
              "derived"
            ],
            "description": "The source enumerated value field in Agentic Workspace Operation Contract."
          },
          "required": {
            "type": "boolean",
            "description": "The required boolean field in Agentic Workspace Operation Contract."
          },
          "schema_ref": {
            "type": "string",
            "description": "The schema ref string field in Agentic Workspace Operation Contract."
          },
          "description": {
            "type": "string",
            "description": "The description string field in Agentic Workspace Operation Contract."
          }
        },
        "additionalProperties": false,
        "description": "Item object accepted by inputs in Agentic Workspace Operation Contract."
      },
      "description": "Inputs the operation accepts and where they come from."
    },
    "output": {
      "type": "object",
      "required": [
        "kind"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "enum": [
            "text-or-json",
            "json",
            "text",
            "module-result"
          ],
          "description": "The kind enumerated value field in Agentic Workspace Operation Contract."
        },
        "schema_ref": {
          "type": "string",
          "description": "The schema ref string field in Agentic Workspace Operation Contract."
        },
        "description": {
          "type": "string",
          "description": "The description string field in Agentic Workspace Operation Contract."
        }
      },
      "additionalProperties": false,
      "description": "Output kind and schema produced by the operation."
    },
    "effects": {
      "type": "object",
      "required": [
        "read_only",
        "destructive",
        "idempotent",
        "writes_repo_state"
      ],
      "properties": {
        "read_only": {
          "type": "boolean",
          "description": "The read only boolean field in Agentic Workspace Operation Contract."
        },
        "destructive": {
          "type": "boolean",
          "description": "The destructive boolean field in Agentic Workspace Operation Contract."
        },
        "idempotent": {
          "type": "boolean",
          "description": "The idempotent boolean field in Agentic Workspace Operation Contract."
        },
        "writes_repo_state": {
          "type": "boolean",
          "description": "The writes repo state boolean field in Agentic Workspace Operation Contract."
        },
        "requires_preflight_gate": {
          "type": "boolean",
          "description": "The requires preflight gate boolean field in Agentic Workspace Operation Contract."
        }
      },
      "additionalProperties": false,
      "description": "Side-effect model for safe execution and automation."
    },
    "locality": {
      "type": "object",
      "required": [
        "requires_repo",
        "network"
      ],
      "properties": {
        "requires_repo": {
          "type": "boolean",
          "description": "The requires repo boolean field in Agentic Workspace Operation Contract."
        },
        "network": {
          "type": "string",
          "enum": [
            "forbidden",
            "not-required",
            "allowed"
          ],
          "description": "The network enumerated value field in Agentic Workspace Operation Contract."
        },
        "outside_repo_writes": {
          "type": "string",
          "enum": [
            "forbidden",
            "allowed"
          ],
          "description": "The outside repo writes enumerated value field in Agentic Workspace Operation Contract."
        }
      },
      "additionalProperties": false,
      "description": "Repo, network, and outside-repo locality requirements."
    },
    "reads": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/surface_access",
        "description": "Item reference accepted by reads in Agentic Workspace Operation Contract."
      },
      "description": "Surfaces or state the operation reads."
    },
    "writes": {
      "type": "array",
      "items": {
        "allOf": [
          {
            "$ref": "#/$defs/surface_access",
            "description": "Allowed reference variant for writes[] in Agentic Workspace Operation Contract."
          },
          {
            "type": "object",
            "properties": {
              "when": {
                "type": "string",
                "description": "The when string field in Agentic Workspace Operation Contract."
              }
            },
            "description": "Allowed object variant for writes[] in Agentic Workspace Operation Contract."
          }
        ],
        "description": "Item composed value accepted by writes in Agentic Workspace Operation Contract."
      },
      "description": "Surfaces or state the operation writes."
    },
    "steps": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "id",
          "uses",
          "description"
        ],
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9_-]*$",
            "description": "The id string field in Agentic Workspace Operation Contract."
          },
          "uses": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9_.-]*$",
            "description": "The uses string field in Agentic Workspace Operation Contract."
          },
          "description": {
            "type": "string",
            "minLength": 1,
            "description": "The description string field in Agentic Workspace Operation Contract."
          },
          "when": {
            "type": "string",
            "description": "The when string field in Agentic Workspace Operation Contract."
          },
          "inputs": {
            "type": "array",
            "items": {
              "type": "string",
              "description": "Item string accepted by inputs in Agentic Workspace Operation Contract."
            },
            "description": "The inputs array field in Agentic Workspace Operation Contract."
          },
          "outputs": {
            "type": "array",
            "items": {
              "type": "string",
              "description": "Item string accepted by outputs in Agentic Workspace Operation Contract."
            },
            "description": "The outputs array field in Agentic Workspace Operation Contract."
          }
        },
        "additionalProperties": false,
        "description": "Item object accepted by steps in Agentic Workspace Operation Contract."
      },
      "description": "Primitive implementation steps that define operation behavior."
    },
    "guards": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1,
        "description": "Item string accepted by guards in Agentic Workspace Operation Contract."
      },
      "description": "Safety or escalation guards for using the operation."
    },
    "proof": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1,
        "description": "Item string accepted by proof in Agentic Workspace Operation Contract."
      },
      "description": "Validation expectations for the operation contract."
    },
    "projections": {
      "type": "object",
      "properties": {
        "cli": {
          "type": "object",
          "properties": {
            "command_ref": {
              "type": "string",
              "description": "The command ref string field in Agentic Workspace Operation Contract."
            }
          },
          "additionalProperties": false,
          "description": "The cli object field in Agentic Workspace Operation Contract."
        },
        "mcp": {
          "type": "object",
          "properties": {
            "tool_name": {
              "type": "string",
              "description": "The tool name string field in Agentic Workspace Operation Contract."
            },
            "read_only_hint": {
              "type": "boolean",
              "description": "The read only hint boolean field in Agentic Workspace Operation Contract."
            },
            "destructive_hint": {
              "type": "boolean",
              "description": "The destructive hint boolean field in Agentic Workspace Operation Contract."
            },
            "idempotent_hint": {
              "type": "boolean",
              "description": "The idempotent hint boolean field in Agentic Workspace Operation Contract."
            }
          },
          "additionalProperties": false,
          "description": "The mcp object field in Agentic Workspace Operation Contract."
        },
        "skill": {
          "type": "object",
          "properties": {
            "activation": {
              "type": "array",
              "items": {
                "type": "string",
                "description": "Item string accepted by activation in Agentic Workspace Operation Contract."
              },
              "description": "The activation array field in Agentic Workspace Operation Contract."
            },
            "instruction_summary": {
              "type": "string",
              "description": "The instruction summary string field in Agentic Workspace Operation Contract."
            }
          },
          "additionalProperties": false,
          "description": "The skill object field in Agentic Workspace Operation Contract."
        }
      },
      "additionalProperties": false,
      "description": "Adapter projections such as CLI, MCP, or skill bindings."
    },
    "migration_status": {
      "type": "string",
      "enum": [
        "draft-contract-only",
        "validated-contract",
        "runtime-consumed"
      ],
      "description": "Maturity of the operation contract relative to implementation."
    }
  },
  "additionalProperties": false,
  "$defs": {
    "surface_access": {
      "type": "object",
      "required": [
        "surface"
      ],
      "properties": {
        "surface": {
          "type": "string",
          "minLength": 1,
          "description": "The surface string field in Agentic Workspace Operation Contract."
        },
        "required": {
          "type": "boolean",
          "description": "The required boolean field in Agentic Workspace Operation Contract."
        },
        "description": {
          "type": "string",
          "description": "The description string field in Agentic Workspace Operation Contract."
        },
        "when": {
          "type": "string",
          "description": "The when string field in Agentic Workspace Operation Contract."
        }
      },
      "additionalProperties": false,
      "description": "Reusable surface access object definition for Agentic Workspace Operation Contract."
    }
  },
  "x-agentic-workspace-doc-role": "contract-reference"
}
