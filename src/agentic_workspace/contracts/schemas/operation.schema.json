{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/operation.schema.json",
  "title": "Agentic Workspace Operation Contract",
  "description": "Contract for one Agentic Workspace operation, including command shape, effects, IO, guards, and proof.",
  "type": "object",
  "required": [
    "schema_version",
    "id",
    "summary",
    "classification",
    "inputs",
    "output",
    "effects",
    "locality",
    "steps",
    "guards",
    "proof"
  ],
  "properties": {
    "schema_version": {
      "const": "agentic-workspace/operation/v1",
      "description": "Operation contract version."
    },
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9_.-]*$",
      "description": "Stable operation identifier."
    },
    "command_surface": {
      "type": "object",
      "required": [
        "command"
      ],
      "properties": {
        "program": {
          "type": "string",
          "minLength": 1,
          "description": "Program text value used by this contract."
        },
        "command": {
          "type": "string",
          "minLength": 1,
          "description": "Command string or command name used by this contract."
        },
        "subcommand": {
          "type": "string",
          "minLength": 1,
          "description": "Subcommand text value used by this contract."
        },
        "format_option": {
          "type": "string",
          "minLength": 1,
          "description": "Format option text value used by this contract."
        }
      },
      "additionalProperties": false,
      "description": "CLI command or subcommand shape that exposes the operation."
    },
    "summary": {
      "type": "string",
      "minLength": 1,
      "description": "Short human-readable operation summary."
    },
    "intent": {
      "type": "string",
      "description": "User or system intent the operation is designed to satisfy."
    },
    "classification": {
      "type": "string",
      "enum": [
        "read-only-report",
        "metadata-refresh",
        "lifecycle-mutation",
        "local-record-append",
        "derived-output"
      ],
      "description": "Output and authority classification for the operation."
    },
    "inputs": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "name"
        ],
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1,
            "description": "Human-readable or lookup name for this entry."
          },
          "source": {
            "type": "string",
            "enum": [
              "cli-option",
              "default",
              "derived"
            ],
            "description": "Origin or authority source for this value."
          },
          "required": {
            "type": "boolean",
            "description": "Required true/false policy flag used by this contract."
          },
          "schema_ref": {
            "type": "string",
            "description": "Schema reference used to validate the output payload."
          },
          "description": {
            "type": "string",
            "description": "Longer explanation or display text for this entry."
          }
        },
        "additionalProperties": false,
        "description": "One entry in the inputs list."
      },
      "description": "Inputs the operation accepts and where they come from."
    },
    "output": {
      "type": "object",
      "required": [
        "kind"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "enum": [
            "text-or-json",
            "json",
            "text",
            "module-result"
          ],
          "description": "Discriminator identifying the payload or record shape."
        },
        "schema_ref": {
          "type": "string",
          "description": "Schema reference used to validate the output payload."
        },
        "description": {
          "type": "string",
          "description": "Longer explanation or display text for this entry."
        }
      },
      "additionalProperties": false,
      "description": "Output kind and schema produced by the operation."
    },
    "effects": {
      "type": "object",
      "required": [
        "read_only",
        "destructive",
        "idempotent",
        "writes_repo_state"
      ],
      "properties": {
        "read_only": {
          "type": "boolean",
          "description": "Whether this operation or aid avoids writes."
        },
        "destructive": {
          "type": "boolean",
          "description": "Whether this action can delete, overwrite, or otherwise destroy state."
        },
        "idempotent": {
          "type": "boolean",
          "description": "Idempotent true/false policy flag used by this contract."
        },
        "writes_repo_state": {
          "type": "boolean",
          "description": "Writes repo state true/false policy flag used by this contract."
        },
        "requires_preflight_gate": {
          "type": "boolean",
          "description": "Requires preflight gate true/false policy flag used by this contract."
        }
      },
      "additionalProperties": false,
      "description": "Side-effect model for safe execution and automation."
    },
    "locality": {
      "type": "object",
      "required": [
        "requires_repo",
        "network"
      ],
      "properties": {
        "requires_repo": {
          "type": "boolean",
          "description": "Requires repo true/false policy flag used by this contract."
        },
        "network": {
          "type": "string",
          "enum": [
            "forbidden",
            "not-required",
            "allowed"
          ],
          "description": "Network access requirement or allowance."
        },
        "outside_repo_writes": {
          "type": "string",
          "enum": [
            "forbidden",
            "allowed"
          ],
          "description": "Allowed outside repo writes value for routing or validation."
        }
      },
      "additionalProperties": false,
      "description": "Repo, network, and outside-repo locality requirements."
    },
    "reads": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/surface_access",
        "description": "One entry in the reads list."
      },
      "description": "Surfaces or state the operation reads."
    },
    "writes": {
      "type": "array",
      "items": {
        "allOf": [
          {
            "$ref": "#/$defs/surface_access",
            "description": "Allof[0] referenced contract shape used by this contract."
          },
          {
            "type": "object",
            "properties": {
              "when": {
                "type": "string",
                "description": "When text value used by this contract."
              }
            },
            "description": "Allof[1] details used by this contract."
          }
        ],
        "description": "One entry in the writes list."
      },
      "description": "Surfaces or state the operation writes."
    },
    "steps": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "id",
          "uses",
          "description"
        ],
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9_-]*$",
            "description": "Stable identifier used to reference this record."
          },
          "uses": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9_.-]*$",
            "description": "Uses text value used by this contract."
          },
          "description": {
            "type": "string",
            "minLength": 1,
            "description": "Longer explanation or display text for this entry."
          },
          "when": {
            "type": "string",
            "description": "When text value used by this contract."
          },
          "inputs": {
            "type": "array",
            "items": {
              "type": "string",
              "description": "One entry in the inputs list."
            },
            "description": "Inputs accepted by the operation or command."
          },
          "outputs": {
            "type": "array",
            "items": {
              "type": "string",
              "description": "One entry in the outputs list."
            },
            "description": "Ordered outputs entries used by this contract."
          }
        },
        "additionalProperties": false,
        "description": "One entry in the steps list."
      },
      "description": "Primitive implementation steps that define operation behavior."
    },
    "guards": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1,
        "description": "One entry in the guards list."
      },
      "description": "Safety or escalation guards for using the operation."
    },
    "proof": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1,
        "description": "One entry in the proof list."
      },
      "description": "Validation expectations for the operation contract."
    },
    "projections": {
      "type": "object",
      "properties": {
        "cli": {
          "type": "object",
          "properties": {
            "command_ref": {
              "type": "string",
              "description": "Command ref text value used by this contract."
            }
          },
          "additionalProperties": false,
          "description": "Cli details used by this contract."
        },
        "mcp": {
          "type": "object",
          "properties": {
            "tool_name": {
              "type": "string",
              "description": "Tool name text value used by this contract."
            },
            "read_only_hint": {
              "type": "boolean",
              "description": "Read only hint true/false policy flag used by this contract."
            },
            "destructive_hint": {
              "type": "boolean",
              "description": "Destructive hint true/false policy flag used by this contract."
            },
            "idempotent_hint": {
              "type": "boolean",
              "description": "Idempotent hint true/false policy flag used by this contract."
            }
          },
          "additionalProperties": false,
          "description": "Mcp details used by this contract."
        },
        "skill": {
          "type": "object",
          "properties": {
            "activation": {
              "type": "array",
              "items": {
                "type": "string",
                "description": "One entry in the activation list."
              },
              "description": "Ordered activation entries used by this contract."
            },
            "instruction_summary": {
              "type": "string",
              "description": "Instruction summary text value used by this contract."
            }
          },
          "additionalProperties": false,
          "description": "Skill details used by this contract."
        }
      },
      "additionalProperties": false,
      "description": "Adapter projections such as CLI, MCP, or skill bindings."
    },
    "migration_status": {
      "type": "string",
      "enum": [
        "draft-contract-only",
        "validated-contract",
        "runtime-consumed"
      ],
      "description": "Maturity of the operation contract relative to implementation."
    }
  },
  "additionalProperties": false,
  "$defs": {
    "surface_access": {
      "type": "object",
      "required": [
        "surface"
      ],
      "properties": {
        "surface": {
          "type": "string",
          "minLength": 1,
          "description": "Surface text value used by this contract."
        },
        "required": {
          "type": "boolean",
          "description": "Required true/false policy flag used by this contract."
        },
        "description": {
          "type": "string",
          "description": "Longer explanation or display text for this entry."
        },
        "when": {
          "type": "string",
          "description": "When text value used by this contract."
        }
      },
      "additionalProperties": false,
      "description": "Surface access details used by this contract."
    }
  },
  "x-agentic-workspace-doc-role": "contract-reference"
}
