{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/setup-findings-policy.schema.json",
  "title": "Setup Findings Promotion Policy",
  "description": "Policy for promoting setup findings into docs, planning, config, checks, or issues.",
  "type": "object",
  "required": [
    "schema_version",
    "artifact_path",
    "accepted_kind",
    "promotion_confidence_threshold",
    "accepted_classes",
    "secondary"
  ],
  "properties": {
    "schema_version": {
      "const": "agentic-workspace/setup-findings-policy/v1",
      "description": "Contract version for this document."
    },
    "artifact_path": {
      "type": "string",
      "description": "Artifact path text value used by this contract."
    },
    "accepted_kind": {
      "type": "string",
      "description": "Accepted kind text value used by this contract."
    },
    "promotion_confidence_threshold": {
      "type": "number",
      "minimum": 0.0,
      "maximum": 1.0,
      "description": "Promotion confidence threshold contract value used by this contract."
    },
    "accepted_classes": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "class",
          "summary",
          "promote_to",
          "preserve_when",
          "transient_when",
          "required_for_promotion"
        ],
        "properties": {
          "class": {
            "type": "string",
            "description": "Classification used for routing, filtering, or policy selection."
          },
          "summary": {
            "type": "string",
            "description": "Short human-readable summary for this entry."
          },
          "promote_to": {
            "type": "string",
            "description": "Promote to text value used by this contract."
          },
          "preserve_when": {
            "type": "array",
            "items": {
              "type": "string",
              "description": "One entry in the preserve when list."
            },
            "description": "Ordered preserve when entries used by this contract."
          },
          "transient_when": {
            "type": "array",
            "items": {
              "type": "string",
              "description": "One entry in the transient when list."
            },
            "description": "Ordered transient when entries used by this contract."
          },
          "required_for_promotion": {
            "type": "array",
            "items": {
              "type": "string",
              "enum": [
                "path_or_refs",
                "next_action"
              ],
              "description": "One entry in the required for promotion list."
            },
            "description": "Ordered required for promotion entries used by this contract."
          }
        },
        "additionalProperties": false,
        "description": "One entry in the accepted classes list."
      },
      "description": "Ordered accepted classes entries used by this contract."
    },
    "secondary": {
      "type": "array",
      "items": {
        "type": "string",
        "description": "One entry in the secondary list."
      },
      "description": "Ordered secondary entries used by this contract."
    }
  },
  "additionalProperties": false,
  "x-agentic-workspace-doc-role": "contract-reference"
}
