{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/workspace-config.schema.json",
  "title": "Agentic Workspace Repo Config",
  "description": "Human configuration. Unknown fields are rejected; absence uses owner defaults.",
  "type": "object",
  "properties": {
    "execution_posture": {
      "type": "object",
      "maxProperties": 256,
      "description": "Provider-neutral execution posture keyed by current repository semantic route. Requirements narrow eligibility; preferences inform comparison only.",
      "additionalProperties": {
        "description": "Posture for one exact current semantic route, without a built-in task taxonomy.",
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "independent_context": {
            "type": "boolean",
            "description": "Require a current work-relative independent context; a static target tag cannot establish independence."
          },
          "required_execution_guarantees": {
            "description": "Hard provider-neutral execution requirements applied before comparison.",
            "type": "array",
            "maxItems": 32,
            "uniqueItems": true,
            "items": {
              "type": "string",
              "minLength": 1,
              "maxLength": 128
            }
          },
          "preferred_execution_guarantees": {
            "description": "Soft comparison preferences among already eligible alternatives.",
            "type": "array",
            "maxItems": 32,
            "uniqueItems": true,
            "items": {
              "type": "string",
              "minLength": 1,
              "maxLength": 128
            }
          }
        }
      }
    },
    "modules": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "independent": {
          "description": "Durable admission of independently linked native owners. Installation alone grants no effects, claims, reads or restrictions. Operational state remains with the owner.",
          "type": "object",
          "maxProperties": 256,
          "propertyNames": {
            "pattern": "^[a-z][a-z0-9-]{0,63}$"
          },
          "additionalProperties": {
            "description": "Exact repository grant for one separately linked native owner.",
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "effects": {
                "description": "Effect identifiers granted only within this owner domain. Omission grants nothing; an empty list may be omitted.",
                "type": "array",
                "items": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 1024
                },
                "maxItems": 16,
                "uniqueItems": true
              },
              "claims": {
                "description": "Exclusive claim identifiers separately granted to this owner. Omission grants nothing; an empty list may be omitted.",
                "type": "array",
                "items": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 1024
                },
                "maxItems": 16,
                "uniqueItems": true
              },
              "restrictions": {
                "description": "Exact task, effect or claim scopes this owner may restrict. Omission grants nothing; an empty list may be omitted.",
                "type": "array",
                "items": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 1024
                },
                "maxItems": 32,
                "uniqueItems": true
              },
              "reads": {
                "description": "Bounded exact repository-relative sources the owner may observe. Omission grants nothing; an empty list may be omitted.",
                "type": "array",
                "items": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 1024
                },
                "maxItems": 32,
                "uniqueItems": true
              },
              "scope": {
                "description": "Repository path prefixes that select this owner for current changed work. Omission grants nothing; an empty list may be omitted.",
                "type": "array",
                "items": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 1024
                },
                "maxItems": 32,
                "uniqueItems": true
              },
              "settings": {
                "description": "Durable owner settings validated against its declared configuration schema.",
                "type": "object"
              },
              "binding": {
                "description": "Exact repository-approved implementation and descriptor binding prepared by the selected linked owner. Never auto-follows a descriptor update.",
                "type": "string",
                "pattern": "^sha256:[0-9a-f]{64}$"
              }
            },
            "required": [
              "binding"
            ]
          }
        },
        "enabled": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": [
              "planning",
              "memory",
              "verification"
            ]
          },
          "uniqueItems": true,
          "description": "Repo-owned enabled Agentic Workspace modules. Use [] for routing/config-only installs.",
          "default": [
            "planning",
            "memory"
          ]
        }
      },
      "required": [
        "enabled"
      ],
      "description": "Authoritative module enablement for this repository."
    },
    "workspace": {
      "description": "Repo-owned workspace policy for startup adapter shape, output posture, and advanced feature opt-ins. Module selection lives in top-level modules.enabled.",
      "type": "object",
      "default": {},
      "x-agentic-workspace-doc-role": "public",
      "x-agentic-workspace-unknown-properties": "warn",
      "properties": {
        "enabled": {
          "description": "Effective Agentic Workspace operation switch. Defaults to true; when false, ordinary AW workflow commands return a disabled-state packet while diagnostic and recovery commands remain available. Machine-local config.local.toml may override this value.",
          "type": "boolean",
          "default": true,
          "x-agentic-workspace-effective-default-source": "product-default-or-repo-config-or-local-override"
        },
        "agent_instructions_file": {
          "description": "Repo startup adapter text file agents should read first.",
          "type": "string",
          "minLength": 1,
          "default": "AGENTS.md",
          "examples": [
            "AGENTS.md",
            "CLAUDE.md",
            ".cursorrules",
            ".cursor/rules/project.mdc",
            "docs/agent-instructions.md"
          ],
          "x-agentic-workspace-effective-default-source": "product-default-or-autodetected-existing"
        },
        "workflow_artifact_profile": {
          "description": "Shape of generated workflow and handoff artefacts.",
          "enum": [
            "repo-owned",
            "gemini"
          ],
          "default": "repo-owned"
        },
        "improvement_latitude": {
          "description": "How much repo-directed improvement initiative Agentic Workspace may surface by default.",
          "enum": [
            "none",
            "reporting",
            "conservative",
            "proactive"
          ],
          "default": "conservative"
        },
        "cli_invoke": {
          "description": "Repo-owned command prefix agents should use to invoke Agentic Workspace in this repository, such as a source-checkout or dev-dependency invocation. User-specific installs can still override this with .agentic-workspace/config.local.toml.",
          "type": "string",
          "minLength": 1,
          "default": "agentic-workspace",
          "examples": [
            "agentic-workspace",
            "uv run agentic-workspace",
            "npm exec agentic-workspace"
          ],
          "x-agentic-workspace-effective-default-source": "product-default-or-repo-config-or-local-override"
        }
      },
      "additionalProperties": false
    },
    "system_intent": {
      "description": "Repo-owned sources for durable product or project intent.",
      "type": "object",
      "default": {},
      "x-agentic-workspace-doc-role": "public",
      "x-agentic-workspace-unknown-properties": "warn",
      "properties": {
        "sources": {
          "description": "Ordered candidate files that describe durable repo intent.",
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1,
            "description": "One entry in the sources list."
          },
          "uniqueItems": true,
          "examples": [
            [
              "README.md",
              "docs/product.md"
            ]
          ],
          "x-agentic-workspace-effective-default-source": "autodetected-existing-or-product-default"
        },
        "preferred_source": {
          "description": "Preferred intent source, usually one of system_intent.sources.",
          "type": "string",
          "minLength": 1,
          "examples": [
            "README.md"
          ],
          "x-agentic-workspace-effective-default-source": "autodetected-existing-or-product-default"
        }
      },
      "additionalProperties": false
    },
    "assurance": {
      "description": "Default trust and proof posture for work when active planning does not override it.",
      "type": "object",
      "default": {},
      "x-agentic-workspace-doc-role": "advanced",
      "x-agentic-workspace-unknown-properties": "warn",
      "properties": {
        "default_level": {
          "description": "Baseline assurance level for proof and trust guidance.",
          "enum": [
            "low",
            "medium",
            "high",
            "critical"
          ],
          "default": "low"
        },
        "agent_may_escalate": {
          "description": "Whether agents may raise assurance level when risk or scope warrants it.",
          "type": "boolean",
          "default": true
        },
        "agent_may_deescalate": {
          "description": "Whether agents may lower assurance level below the configured default.",
          "type": "boolean",
          "default": false
        },
        "strict_closeout": {
          "description": "Require a current task-bound Verification claim judgement before completion, even when no protocol matches. False does not waive independently binding proof or review obligations.",
          "type": "boolean",
          "default": false
        },
        "decision_record_target": {
          "description": "Confined repository-relative destination for future durable decision records, not admission of existing decision history. Without decision_record_revision, an absent directory or ordinary Markdown with no relevant typed AW decisions requires no archive admission. Unadmitted repository bytes never contribute authority. When current work depends on repository-source admission and no already-admitted fallback safely preserves the decision, missing admission fails closed with assurance.decision_record_revision, the affected source and the required repository/source-owner action. An already-admitted fallback instead keeps its consequence and pending stronger-owner reconciliation; missing stronger-owner admission alone adds no task-wide blocker. One optional trailing slash is accepted and removed for archive identity; absolute paths, backslashes, colons, empty interior segments, dot/traversal segments and .git segments are rejected. Exact source-file and changed-path references do not accept a trailing slash.",
          "type": "string",
          "minLength": 1,
          "examples": [
            "docs/decisions/"
          ]
        },
        "decision_record_fallback": {
          "description": "Independent Memory source-owner admission of a bounded decision archive snapshot and its semantic provenance; never a public request argument.",
          "type": "object",
          "required": [
            "archive",
            "admitted_revision"
          ],
          "additionalProperties": false,
          "properties": {
            "archive": {
              "description": "Confined repository-relative Memory archive directory admitted by its host owner. Uses the same directory path contract as assurance.decision_record_target, including one optional trailing slash; native capture requires the existing .agentic-workspace/memory/repo/ owner.",
              "type": "string",
              "minLength": 1
            },
            "admitted_revision": {
              "description": "Immutable Git commit explicitly admitting this Memory snapshot and semantic provenance.",
              "type": "string",
              "pattern": "^[0-9a-fA-F]{40}$"
            }
          }
        },
        "decision_delegations": {
          "description": "Standing repository policy delegating future semantic decisions to the acting agent only for an exact durable owner and path set. Each decision separately binds current material, work, dependencies and destination. Shared policy only; no actor identity, execution/proof/claim authority or local override is granted.",
          "type": "array",
          "maxItems": 64,
          "uniqueItems": true,
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "owner",
              "scope"
            ],
            "properties": {
              "owner": {
                "description": "Existing durable decision owner receiving the delegated decision.",
                "enum": [
                  "memory",
                  "repository",
                  "verification",
                  "configuration",
                  "scoped-instructions"
                ]
              },
              "scope": {
                "description": "Exact set of path:repository-relative-file scopes. No wildcard, subset, task-class or prose matching.",
                "type": "array",
                "minItems": 1,
                "maxItems": 32,
                "uniqueItems": true,
                "items": {
                  "type": "string",
                  "pattern": "^path:[^*?\\[\\]]+$",
                  "maxLength": 8192
                }
              }
            }
          }
        },
        "instruction_revision": {
          "type": "string",
          "pattern": "^[0-9a-f]{40}$",
          "description": "Trusted repository-owner admission of binding checks/protect in the exact scoped Markdown snapshot. Never inferred from HEAD, tracking, or instruction contents."
        },
        "decision_record_revision": {
          "description": "Optional trusted source-owner admission of this exact archive Git commit, including semantic provenance. Required to use historical typed repository decisions as current context, not merely to configure a future decision destination. Native publications retain their independently checked owner custody. Only the repository/source owner can admit the snapshot; never infer from HEAD, tracking, installation, caller input or record actor strings.",
          "type": "string",
          "pattern": "^[0-9a-fA-F]{40}$"
        }
      },
      "additionalProperties": false
    },
    "payload": {
      "description": "Repo-owned installed payload target policy. Use this when the checked-in payload must be kept aligned with a release or source-checkout capability target independent of the CLI install location.",
      "type": "object",
      "default": {},
      "x-agentic-workspace-doc-role": "maintainer",
      "x-agentic-workspace-unknown-properties": "warn",
      "not": {
        "required": [
          "target_release",
          "dogfood_latest"
        ]
      },
      "properties": {
        "target_release": {
          "description": "Desired checked-in Agentic Workspace payload release. Use source-current for source-checkout dogfooding.",
          "type": "string",
          "minLength": 1,
          "examples": [
            "0.24.0",
            "source-current"
          ]
        },
        "minimum_capabilities": {
          "description": "Payload-owned install-target capabilities that must be present in checked-in payload provenance before the target is satisfied.",
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1,
            "description": "One required payload capability."
          },
          "uniqueItems": true,
          "default": [],
          "examples": [
            [
              "installed-state-sync-v2"
            ]
          ]
        },
        "policy": {
          "description": "How strongly startup should gate work when the installed payload does not satisfy the repo-declared target.",
          "enum": [
            "advisory",
            "required-before-claim",
            "required-before-work"
          ],
          "default": "advisory"
        }
      },
      "additionalProperties": false
    }
  },
  "x-agentic-workspace-doc-role": "public-reference",
  "x-agentic-workspace-see-also": [
    ".agentic-workspace/docs/workspace-config-contract.md",
    "docs/reference",
    "docs/generated"
  ],
  "x-agentic-workspace-unknown-properties": "reject",
  "additionalProperties": false
}
