{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/cli-commands.schema.json",
  "title": "Cli Commands",
  "description": "CLI command manifest used to validate command metadata and parser parity.",
  "type": "object",
  "required": [
    "schema_version",
    "program",
    "description",
    "commands"
  ],
  "properties": {
    "schema_version": {
      "const": "agentic-workspace/cli-commands/v1",
      "description": "Contract version for this document."
    },
    "program": {
      "type": "string",
      "description": "Program text value used by this contract."
    },
    "description": {
      "type": "string",
      "description": "Longer explanation or display text for this entry."
    },
    "commands": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/command",
        "description": "One entry in the commands list."
      },
      "description": "Commands associated with this check, proof, or workflow step."
    }
  },
  "$defs": {
    "command": {
      "type": "object",
      "required": [
        "name",
        "help",
        "role",
        "audience"
      ],
      "properties": {
        "name": {
          "type": "string",
          "description": "Human-readable or lookup name for this entry."
        },
        "help": {
          "type": "string",
          "description": "Help text value used by this contract."
        },
        "role": {
          "enum": [
            "core_lifecycle",
            "core_context_router",
            "reusable_host_repo_diagnostics",
            "module_delegation_front_door",
            "candidate_for_module_ownership",
            "source_checkout_only_maintainer_development",
            "remove_or_hide"
          ],
          "description": "Allowed role value for routing or validation."
        },
        "audience": {
          "enum": [
            "ordinary_host_repo",
            "advanced_host_repo",
            "local_only",
            "source_checkout_only"
          ],
          "description": "Allowed audience value for routing or validation."
        },
        "classification_note": {
          "type": "string",
          "description": "Classification note text value used by this contract."
        },
        "uses_option_groups": {
          "type": "array",
          "items": {
            "type": "string",
            "description": "One entry in the uses option groups list."
          },
          "description": "Ordered uses option groups entries used by this contract."
        },
        "options": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/option",
            "description": "One entry in the options list."
          },
          "description": "Ordered options entries used by this contract."
        },
        "subcommand_dest": {
          "type": "string",
          "description": "Subcommand dest text value used by this contract."
        },
        "subcommands_required": {
          "type": "boolean",
          "description": "Whether a subcommand is required when subcommands are declared."
        },
        "subcommands": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/command",
            "description": "One entry in the subcommands list."
          },
          "description": "Ordered subcommands entries used by this contract."
        }
      },
      "additionalProperties": false,
      "description": "Command string or command name used by this contract."
    },
    "option": {
      "type": "object",
      "required": [
        "name",
        "flags"
      ],
      "properties": {
        "name": {
          "type": "string",
          "description": "Human-readable or lookup name for this entry."
        },
        "flags": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "description": "One entry in the flags list."
          },
          "description": "Ordered flags entries used by this contract."
        },
        "action": {
          "type": "string",
          "description": "Action text value used by this contract."
        },
        "nargs": {
          "type": [
            "string",
            "integer"
          ],
          "description": "Nargs contract value used by this contract."
        },
        "choices": {
          "type": "array",
          "items": {
            "type": [
              "string",
              "integer",
              "number",
              "boolean"
            ],
            "description": "One entry in the choices list."
          },
          "description": "Ordered choices entries used by this contract."
        },
        "choices_ref": {
          "type": "string",
          "description": "Choices ref text value used by this contract."
        },
        "default": {},
        "default_ref": {
          "type": "string",
          "description": "Default ref text value used by this contract."
        },
        "type": {
          "type": "string",
          "description": "Category used to choose validation, rendering, or routing behavior."
        },
        "required": {
          "type": "boolean",
          "description": "Required true/false policy flag used by this contract."
        },
        "help": {
          "type": "string",
          "description": "Help text value used by this contract."
        },
        "help_template": {
          "type": "string",
          "description": "Help template text value used by this contract."
        }
      },
      "additionalProperties": false,
      "description": "Option details used by this contract."
    }
  },
  "additionalProperties": false,
  "x-agentic-workspace-doc-role": "contract-reference"
}
