{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/command-adapter-generation.schema.json",
  "title": "Agentic Workspace Command Adapter Generation Contract",
  "description": "Schema for Agentic Workspace Command Adapter Generation Contract.",
  "type": "object",
  "required": [
    "schema_version",
    "summary",
    "schema",
    "projection_requirements",
    "adapters"
  ],
  "properties": {
    "schema_version": {
      "const": "agentic-workspace/command-adapter-generation/v1",
      "description": "The schema version constant field in Agentic Workspace Command Adapter Generation Contract."
    },
    "summary": {
      "type": "string",
      "minLength": 1,
      "description": "The summary string field in Agentic Workspace Command Adapter Generation Contract."
    },
    "schema": {
      "const": "schemas/command_adapter_generation.schema.json",
      "description": "The schema constant field in Agentic Workspace Command Adapter Generation Contract."
    },
    "generated_outputs": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "program",
          "path"
        ],
        "properties": {
          "program": {
            "type": "string",
            "minLength": 1,
            "description": "The program string field in Agentic Workspace Command Adapter Generation Contract."
          },
          "path": {
            "type": "string",
            "pattern": "^[A-Za-z0-9_./-]+\\.py$",
            "description": "The path string field in Agentic Workspace Command Adapter Generation Contract."
          }
        },
        "additionalProperties": false,
        "description": "Item object accepted by generated outputs in Agentic Workspace Command Adapter Generation Contract."
      },
      "description": "The generated outputs array field in Agentic Workspace Command Adapter Generation Contract."
    },
    "package_surface_migration": {
      "type": "object",
      "required": [
        "planning",
        "memory"
      ],
      "properties": {
        "planning": {
          "$ref": "#/$defs/package_migration",
          "description": "The planning reference field in Agentic Workspace Command Adapter Generation Contract."
        },
        "memory": {
          "$ref": "#/$defs/package_migration",
          "description": "The memory reference field in Agentic Workspace Command Adapter Generation Contract."
        }
      },
      "additionalProperties": false,
      "description": "The package surface migration object field in Agentic Workspace Command Adapter Generation Contract."
    },
    "projection_requirements": {
      "type": "object",
      "required": [
        "core_contract_rule",
        "universal_command_truth",
        "adapter_specific_rendering",
        "future_target_kinds",
        "pollution_guardrails"
      ],
      "properties": {
        "core_contract_rule": {
          "type": "string",
          "minLength": 1,
          "description": "The core contract rule string field in Agentic Workspace Command Adapter Generation Contract."
        },
        "universal_command_truth": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "The universal command truth reference field in Agentic Workspace Command Adapter Generation Contract."
        },
        "adapter_specific_rendering": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "The adapter specific rendering reference field in Agentic Workspace Command Adapter Generation Contract."
        },
        "future_target_kinds": {
          "type": "array",
          "minItems": 3,
          "items": {
            "$ref": "#/$defs/future_target_kind",
            "description": "Item reference accepted by future target kinds in Agentic Workspace Command Adapter Generation Contract."
          },
          "description": "The future target kinds array field in Agentic Workspace Command Adapter Generation Contract."
        },
        "pollution_guardrails": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "The pollution guardrails reference field in Agentic Workspace Command Adapter Generation Contract."
        }
      },
      "additionalProperties": false,
      "description": "The projection requirements object field in Agentic Workspace Command Adapter Generation Contract."
    },
    "adapters": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/adapter",
        "description": "Item reference accepted by adapters in Agentic Workspace Command Adapter Generation Contract."
      },
      "description": "The adapters array field in Agentic Workspace Command Adapter Generation Contract."
    }
  },
  "additionalProperties": false,
  "$defs": {
    "adapter": {
      "type": "object",
      "required": [
        "id",
        "status",
        "command",
        "operation_ref",
        "runtime_binding",
        "schemas",
        "effect_hints",
        "projection_hints",
        "generation_boundary",
        "conformance_refs"
      ],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-z0-9][a-z0-9_.-]*$",
          "description": "The id string field in Agentic Workspace Command Adapter Generation Contract."
        },
        "status": {
          "enum": [
            "draft",
            "ready-for-first-generation",
            "generated",
            "deferred"
          ],
          "description": "The status enumerated value field in Agentic Workspace Command Adapter Generation Contract."
        },
        "command": {
          "type": "object",
          "required": [
            "program",
            "name",
            "command_manifest",
            "option_group_manifest"
          ],
          "properties": {
            "program": {
              "type": "string",
              "minLength": 1,
              "description": "The program string field in Agentic Workspace Command Adapter Generation Contract."
            },
            "name": {
              "type": "string",
              "minLength": 1,
              "description": "The name string field in Agentic Workspace Command Adapter Generation Contract."
            },
            "subcommand": {
              "type": "string",
              "minLength": 1,
              "description": "The subcommand string field in Agentic Workspace Command Adapter Generation Contract."
            },
            "command_manifest": {
              "enum": [
                "cli_commands.json",
                "package:planning:cli",
                "package:memory:cli"
              ],
              "description": "The command manifest enumerated value field in Agentic Workspace Command Adapter Generation Contract."
            },
            "option_group_manifest": {
              "enum": [
                "cli_option_groups.json",
                "package:planning:cli",
                "package:memory:cli"
              ],
              "description": "The option group manifest enumerated value field in Agentic Workspace Command Adapter Generation Contract."
            }
          },
          "additionalProperties": false,
          "description": "The command object field in Agentic Workspace Command Adapter Generation Contract."
        },
        "operation_ref": {
          "type": "object",
          "required": [
            "id",
            "registry",
            "path"
          ],
          "properties": {
            "id": {
              "type": "string",
              "minLength": 1,
              "description": "The id string field in Agentic Workspace Command Adapter Generation Contract."
            },
            "registry": {
              "const": "operation_contracts.json",
              "description": "The registry constant field in Agentic Workspace Command Adapter Generation Contract."
            },
            "path": {
              "type": "string",
              "pattern": "^operations/[A-Za-z0-9_.-]+\\.json$",
              "description": "The path string field in Agentic Workspace Command Adapter Generation Contract."
            }
          },
          "additionalProperties": false,
          "description": "The operation ref object field in Agentic Workspace Command Adapter Generation Contract."
        },
        "runtime_binding": {
          "type": "object",
          "required": [
            "kind",
            "primitive_refs"
          ],
          "properties": {
            "kind": {
              "const": "operation-primitive-sequence",
              "description": "The kind constant field in Agentic Workspace Command Adapter Generation Contract."
            },
            "primitive_refs": {
              "type": "array",
              "minItems": 1,
              "items": {
                "type": "string",
                "pattern": "^[a-z0-9][a-z0-9_.-]*$",
                "description": "Item string accepted by primitive refs in Agentic Workspace Command Adapter Generation Contract."
              },
              "description": "The primitive refs array field in Agentic Workspace Command Adapter Generation Contract."
            }
          },
          "additionalProperties": false,
          "description": "The runtime binding object field in Agentic Workspace Command Adapter Generation Contract."
        },
        "schemas": {
          "type": "object",
          "required": [
            "input",
            "output"
          ],
          "properties": {
            "input": {
              "$ref": "#/$defs/schema_refs",
              "description": "The input reference field in Agentic Workspace Command Adapter Generation Contract."
            },
            "output": {
              "$ref": "#/$defs/schema_refs",
              "description": "The output reference field in Agentic Workspace Command Adapter Generation Contract."
            }
          },
          "additionalProperties": false,
          "description": "The schemas object field in Agentic Workspace Command Adapter Generation Contract."
        },
        "effect_hints": {
          "type": "object",
          "required": [
            "read_only",
            "destructive",
            "idempotent",
            "writes_repo_state",
            "requires_preflight_gate"
          ],
          "properties": {
            "read_only": {
              "type": "boolean",
              "description": "The read only boolean field in Agentic Workspace Command Adapter Generation Contract."
            },
            "destructive": {
              "type": "boolean",
              "description": "The destructive boolean field in Agentic Workspace Command Adapter Generation Contract."
            },
            "idempotent": {
              "type": "boolean",
              "description": "The idempotent boolean field in Agentic Workspace Command Adapter Generation Contract."
            },
            "writes_repo_state": {
              "type": "boolean",
              "description": "The writes repo state boolean field in Agentic Workspace Command Adapter Generation Contract."
            },
            "requires_preflight_gate": {
              "type": "boolean",
              "description": "The requires preflight gate boolean field in Agentic Workspace Command Adapter Generation Contract."
            }
          },
          "additionalProperties": false,
          "description": "The effect hints object field in Agentic Workspace Command Adapter Generation Contract."
        },
        "projection_hints": {
          "type": "object",
          "required": [
            "universal_fields",
            "adapter_specific_fields"
          ],
          "properties": {
            "universal_fields": {
              "type": "array",
              "minItems": 1,
              "items": {
                "type": "string",
                "minLength": 1,
                "description": "Item string accepted by universal fields in Agentic Workspace Command Adapter Generation Contract."
              },
              "description": "The universal fields array field in Agentic Workspace Command Adapter Generation Contract."
            },
            "adapter_specific_fields": {
              "type": "array",
              "items": {
                "type": "object",
                "required": [
                  "kind",
                  "fields"
                ],
                "properties": {
                  "kind": {
                    "type": "string",
                    "minLength": 1,
                    "description": "The kind string field in Agentic Workspace Command Adapter Generation Contract."
                  },
                  "fields": {
                    "type": "array",
                    "minItems": 1,
                    "items": {
                      "type": "string",
                      "minLength": 1,
                      "description": "Item string accepted by fields in Agentic Workspace Command Adapter Generation Contract."
                    },
                    "description": "The fields array field in Agentic Workspace Command Adapter Generation Contract."
                  }
                },
                "additionalProperties": false,
                "description": "Item object accepted by adapter specific fields in Agentic Workspace Command Adapter Generation Contract."
              },
              "description": "The adapter specific fields array field in Agentic Workspace Command Adapter Generation Contract."
            }
          },
          "additionalProperties": false,
          "description": "The projection hints object field in Agentic Workspace Command Adapter Generation Contract."
        },
        "generation_boundary": {
          "type": "object",
          "required": [
            "contract_owns",
            "generated_adapter_owns",
            "runtime_owns"
          ],
          "properties": {
            "contract_owns": {
              "$ref": "#/$defs/non_empty_strings",
              "description": "The contract owns reference field in Agentic Workspace Command Adapter Generation Contract."
            },
            "generated_adapter_owns": {
              "$ref": "#/$defs/non_empty_strings",
              "description": "The generated adapter owns reference field in Agentic Workspace Command Adapter Generation Contract."
            },
            "runtime_owns": {
              "$ref": "#/$defs/non_empty_strings",
              "description": "The runtime owns reference field in Agentic Workspace Command Adapter Generation Contract."
            }
          },
          "additionalProperties": false,
          "description": "The generation boundary object field in Agentic Workspace Command Adapter Generation Contract."
        },
        "conformance_refs": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "minLength": 1,
            "description": "Item string accepted by conformance refs in Agentic Workspace Command Adapter Generation Contract."
          },
          "description": "The conformance refs array field in Agentic Workspace Command Adapter Generation Contract."
        }
      },
      "additionalProperties": false,
      "description": "Reusable adapter object definition for Agentic Workspace Command Adapter Generation Contract."
    },
    "schema_refs": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[A-Za-z0-9_.-]+\\.schema\\.json$",
        "description": "Item string accepted by schema refs in Agentic Workspace Command Adapter Generation Contract."
      },
      "description": "Reusable schema refs array definition for Agentic Workspace Command Adapter Generation Contract."
    },
    "non_empty_strings": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1,
        "description": "Item string accepted by non empty strings in Agentic Workspace Command Adapter Generation Contract."
      },
      "description": "Reusable non empty strings array definition for Agentic Workspace Command Adapter Generation Contract."
    },
    "package_migration": {
      "type": "object",
      "required": [
        "program",
        "first_read_only_candidate",
        "status",
        "next_step"
      ],
      "properties": {
        "program": {
          "type": "string",
          "minLength": 1,
          "description": "The program string field in Agentic Workspace Command Adapter Generation Contract."
        },
        "first_read_only_candidate": {
          "type": "string",
          "minLength": 1,
          "description": "The first read only candidate string field in Agentic Workspace Command Adapter Generation Contract."
        },
        "status": {
          "type": "string",
          "minLength": 1,
          "description": "The status string field in Agentic Workspace Command Adapter Generation Contract."
        },
        "next_step": {
          "type": "string",
          "minLength": 1,
          "description": "The next step string field in Agentic Workspace Command Adapter Generation Contract."
        }
      },
      "additionalProperties": false,
      "description": "Reusable package migration object definition for Agentic Workspace Command Adapter Generation Contract."
    },
    "future_target_kind": {
      "type": "object",
      "required": [
        "kind",
        "status",
        "requirements"
      ],
      "properties": {
        "kind": {
          "enum": [
            "process-cli",
            "npm-cli",
            "posix-shell",
            "powershell",
            "binary",
            "local-mcp-tool",
            "generated-skill"
          ],
          "description": "The kind enumerated value field in Agentic Workspace Command Adapter Generation Contract."
        },
        "status": {
          "enum": [
            "supported-now",
            "requirements-baseline"
          ],
          "description": "The status enumerated value field in Agentic Workspace Command Adapter Generation Contract."
        },
        "requirements": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "The requirements reference field in Agentic Workspace Command Adapter Generation Contract."
        }
      },
      "additionalProperties": false,
      "description": "Reusable future target kind object definition for Agentic Workspace Command Adapter Generation Contract."
    }
  },
  "x-agentic-workspace-doc-role": "contract-reference"
}
