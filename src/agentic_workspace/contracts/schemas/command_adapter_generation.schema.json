{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/command-adapter-generation.schema.json",
  "title": "Agentic Workspace Command Adapter Generation Contract",
  "description": "Contract that connects command shapes, operation contracts, primitives, schemas, and adapter projections.",
  "type": "object",
  "required": [
    "schema_version",
    "summary",
    "schema",
    "projection_requirements",
    "adapters"
  ],
  "properties": {
    "schema_version": {
      "const": "agentic-workspace/command-adapter-generation/v1",
      "description": "Contract version for this document."
    },
    "summary": {
      "type": "string",
      "minLength": 1,
      "description": "Short human-readable summary for this entry."
    },
    "schema": {
      "const": "schemas/command_adapter_generation.schema.json",
      "description": "Schema reference or schema metadata for this payload."
    },
    "generated_outputs": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "program",
          "path"
        ],
        "properties": {
          "program": {
            "type": "string",
            "minLength": 1,
            "description": "Program text value used by this contract."
          },
          "path": {
            "type": "string",
            "pattern": "^generated/[A-Za-z0-9_./-]+\\.json$",
            "description": "Repository path for generated adapter metadata JSON."
          }
        },
        "additionalProperties": false,
        "description": "One entry in the generated outputs list."
      },
      "description": "Ordered generated outputs entries used by this contract."
    },
    "package_surface_migration": {
      "type": "object",
      "required": [
        "planning",
        "memory"
      ],
      "properties": {
        "planning": {
          "$ref": "#/$defs/package_migration",
          "description": "Planning referenced contract shape used by this contract."
        },
        "memory": {
          "$ref": "#/$defs/package_migration",
          "description": "Memory referenced contract shape used by this contract."
        }
      },
      "additionalProperties": false,
      "description": "Package surface migration details used by this contract."
    },
    "projection_requirements": {
      "type": "object",
      "required": [
        "core_contract_rule",
        "universal_command_truth",
        "adapter_specific_rendering",
        "future_target_kinds",
        "pollution_guardrails"
      ],
      "properties": {
        "core_contract_rule": {
          "type": "string",
          "minLength": 1,
          "description": "Core contract rule text value used by this contract."
        },
        "universal_command_truth": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "Universal command truth referenced contract shape used by this contract."
        },
        "adapter_specific_rendering": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "Adapter specific rendering referenced contract shape used by this contract."
        },
        "future_target_kinds": {
          "type": "array",
          "minItems": 3,
          "items": {
            "$ref": "#/$defs/future_target_kind",
            "description": "One entry in the future target kinds list."
          },
          "description": "Ordered future target kinds entries used by this contract."
        },
        "pollution_guardrails": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "Pollution guardrails referenced contract shape used by this contract."
        }
      },
      "additionalProperties": false,
      "description": "Projection requirements details used by this contract."
    },
    "adapters": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/adapter",
        "description": "One entry in the adapters list."
      },
      "description": "Ordered adapters entries used by this contract."
    }
  },
  "additionalProperties": false,
  "$defs": {
    "adapter": {
      "type": "object",
      "required": [
        "id",
        "status",
        "command",
        "operation_ref",
        "runtime_binding",
        "schemas",
        "effect_hints",
        "projection_hints",
        "generation_boundary",
        "conformance_refs"
      ],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-z0-9][a-z0-9_.-]*$",
          "description": "Stable identifier used to reference this record."
        },
        "status": {
          "enum": [
            "draft",
            "ready-for-first-generation",
            "generated",
            "deferred"
          ],
          "description": "Current lifecycle, readiness, or health state."
        },
        "command": {
          "type": "object",
          "required": [
            "program",
            "name",
            "command_manifest",
            "option_group_manifest"
          ],
          "properties": {
            "program": {
              "type": "string",
              "minLength": 1,
              "description": "Program text value used by this contract."
            },
            "name": {
              "type": "string",
              "minLength": 1,
              "description": "Human-readable or lookup name for this entry."
            },
            "subcommand": {
              "type": "string",
              "minLength": 1,
              "description": "Subcommand text value used by this contract."
            },
            "subcommands": {
              "type": "array",
              "items": {
                "type": "string",
                "minLength": 1
              },
              "minItems": 1,
              "uniqueItems": true,
              "description": "Set of subcommand text values exposed by a generated command front door."
            },
            "command_manifest": {
              "enum": [
                "cli_commands.json",
                "package:planning:cli",
                "package:memory:cli"
              ],
              "description": "Allowed command manifest value for routing or validation."
            },
            "option_group_manifest": {
              "enum": [
                "cli_option_groups.json",
                "package:planning:cli",
                "package:memory:cli"
              ],
              "description": "Allowed option group manifest value for routing or validation."
            }
          },
          "additionalProperties": false,
          "description": "Command string or command name used by this contract."
        },
        "operation_ref": {
          "type": "object",
          "required": [
            "id",
            "registry",
            "path"
          ],
          "properties": {
            "id": {
              "type": "string",
              "minLength": 1,
              "description": "Stable identifier used to reference this record."
            },
            "registry": {
              "const": "operation_contracts.json",
              "description": "Fixed registry value required by this contract."
            },
            "path": {
              "type": "string",
              "pattern": "^operations/[A-Za-z0-9_.-]+\\.json$",
              "description": "Path segments or repository path used by this contract."
            }
          },
          "additionalProperties": false,
          "description": "Operation ref details used by this contract."
        },
        "runtime_binding": {
          "type": "object",
          "required": [
            "kind",
            "primitive_refs"
          ],
          "properties": {
            "kind": {
              "const": "operation-primitive-sequence",
              "description": "Discriminator identifying the payload or record shape."
            },
            "primitive_refs": {
              "type": "array",
              "minItems": 1,
              "items": {
                "type": "string",
                "pattern": "^[a-z0-9][a-z0-9_.-]*$",
                "description": "One entry in the primitive refs list."
              },
              "description": "Ordered primitive refs entries used by this contract."
            }
          },
          "additionalProperties": false,
          "description": "Runtime binding details used by this contract."
        },
        "schemas": {
          "type": "object",
          "required": [
            "input",
            "output"
          ],
          "properties": {
            "input": {
              "$ref": "#/$defs/schema_refs",
              "description": "Input referenced contract shape used by this contract."
            },
            "output": {
              "$ref": "#/$defs/schema_refs",
              "description": "Output contract for the operation or command."
            }
          },
          "additionalProperties": false,
          "description": "Schemas details used by this contract."
        },
        "effect_hints": {
          "type": "object",
          "required": [
            "read_only",
            "destructive",
            "idempotent",
            "writes_repo_state",
            "requires_preflight_gate"
          ],
          "properties": {
            "read_only": {
              "type": "boolean",
              "description": "Whether this operation or aid avoids writes."
            },
            "destructive": {
              "type": "boolean",
              "description": "Whether this action can delete, overwrite, or otherwise destroy state."
            },
            "idempotent": {
              "type": "boolean",
              "description": "Idempotent true/false policy flag used by this contract."
            },
            "writes_repo_state": {
              "type": "boolean",
              "description": "Writes repo state true/false policy flag used by this contract."
            },
            "requires_preflight_gate": {
              "type": "boolean",
              "description": "Requires preflight gate true/false policy flag used by this contract."
            }
          },
          "additionalProperties": false,
          "description": "Effect hints details used by this contract."
        },
        "projection_hints": {
          "type": "object",
          "required": [
            "universal_fields",
            "adapter_specific_fields"
          ],
          "properties": {
            "universal_fields": {
              "type": "array",
              "minItems": 1,
              "items": {
                "type": "string",
                "minLength": 1,
                "description": "One entry in the universal fields list."
              },
              "description": "Ordered universal fields entries used by this contract."
            },
            "adapter_specific_fields": {
              "type": "array",
              "items": {
                "type": "object",
                "required": [
                  "kind",
                  "fields"
                ],
                "properties": {
                  "kind": {
                    "type": "string",
                    "minLength": 1,
                    "description": "Discriminator identifying the payload or record shape."
                  },
                  "fields": {
                    "type": "array",
                    "minItems": 1,
                    "items": {
                      "type": "string",
                      "minLength": 1,
                      "description": "One entry in the fields list."
                    },
                    "description": "Ordered fields entries used by this contract."
                  }
                },
                "additionalProperties": false,
                "description": "One entry in the adapter specific fields list."
              },
              "description": "Ordered adapter specific fields entries used by this contract."
            }
          },
          "additionalProperties": false,
          "description": "Projection hints details used by this contract."
        },
        "generation_boundary": {
          "type": "object",
          "required": [
            "contract_owns",
            "generated_adapter_owns",
            "runtime_owns"
          ],
          "properties": {
            "contract_owns": {
              "$ref": "#/$defs/non_empty_strings",
              "description": "Contract owns referenced contract shape used by this contract."
            },
            "generated_adapter_owns": {
              "$ref": "#/$defs/non_empty_strings",
              "description": "Generated adapter owns referenced contract shape used by this contract."
            },
            "runtime_owns": {
              "$ref": "#/$defs/non_empty_strings",
              "description": "Runtime owns referenced contract shape used by this contract."
            }
          },
          "additionalProperties": false,
          "description": "Generation boundary details used by this contract."
        },
        "conformance_refs": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "minLength": 1,
            "description": "One entry in the conformance refs list."
          },
          "description": "Ordered conformance refs entries used by this contract."
        }
      },
      "additionalProperties": false,
      "description": "Process adapter used to execute the conformance fixture."
    },
    "schema_refs": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[A-Za-z0-9_.-]+\\.schema\\.json$",
        "description": "One entry in the schema refs list."
      },
      "description": "Ordered schema refs entries used by this contract."
    },
    "non_empty_strings": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1,
        "description": "One entry in the non empty strings list."
      },
      "description": "Ordered non empty strings entries used by this contract."
    },
    "package_migration": {
      "type": "object",
      "required": [
        "program",
        "first_read_only_candidate",
        "status",
        "next_step"
      ],
      "properties": {
        "program": {
          "type": "string",
          "minLength": 1,
          "description": "Program text value used by this contract."
        },
        "first_read_only_candidate": {
          "type": "string",
          "minLength": 1,
          "description": "First read only candidate text value used by this contract."
        },
        "status": {
          "type": "string",
          "minLength": 1,
          "description": "Current lifecycle, readiness, or health state."
        },
        "next_step": {
          "type": "string",
          "minLength": 1,
          "description": "Next step text value used by this contract."
        }
      },
      "additionalProperties": false,
      "description": "Package migration details used by this contract."
    },
    "future_target_kind": {
      "type": "object",
      "required": [
        "kind",
        "status",
        "requirements"
      ],
      "properties": {
        "kind": {
          "enum": [
            "process-cli",
            "npm-cli",
            "posix-shell",
            "powershell",
            "binary",
            "local-mcp-tool",
            "generated-skill"
          ],
          "description": "Discriminator identifying the payload or record shape."
        },
        "status": {
          "enum": [
            "supported-now",
            "requirements-baseline"
          ],
          "description": "Current lifecycle, readiness, or health state."
        },
        "requirements": {
          "$ref": "#/$defs/non_empty_strings",
          "description": "Requirements referenced contract shape used by this contract."
        }
      },
      "additionalProperties": false,
      "description": "Future target kind details used by this contract."
    }
  },
  "x-agentic-workspace-doc-role": "contract-reference"
}
