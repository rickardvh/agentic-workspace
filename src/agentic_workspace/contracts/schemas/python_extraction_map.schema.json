{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/python-extraction-map.schema.json",
  "title": "Python-Owned Extraction Map",
  "description": "Schema for Python-Owned Extraction Map.",
  "type": "object",
  "required": [
    "schema_version",
    "parent_issue",
    "purpose",
    "authority_statuses",
    "extraction_readiness",
    "candidates",
    "next_slices"
  ],
  "properties": {
    "schema_version": {
      "const": "agentic-workspace/python-extraction-map/v1",
      "description": "The schema version constant field in Python-Owned Extraction Map."
    },
    "parent_issue": {
      "type": "string",
      "description": "The parent issue string field in Python-Owned Extraction Map."
    },
    "purpose": {
      "type": "string",
      "description": "The purpose string field in Python-Owned Extraction Map."
    },
    "authority_statuses": {
      "type": "array",
      "items": {
        "enum": [
          "authoritative",
          "derived",
          "adapter",
          "procedural-owned"
        ],
        "description": "Item enumerated value accepted by authority statuses in Python-Owned Extraction Map."
      },
      "minItems": 4,
      "description": "The authority statuses array field in Python-Owned Extraction Map."
    },
    "extraction_readiness": {
      "type": "array",
      "items": {
        "enum": [
          "extracted",
          "extract now",
          "extract after schema exists",
          "operation-contract candidate",
          "template candidate",
          "procedural-owned for now",
          "live-inspection-owned for now",
          "defer with reason"
        ],
        "description": "Item enumerated value accepted by extraction readiness in Python-Owned Extraction Map."
      },
      "minItems": 7,
      "description": "The extraction readiness array field in Python-Owned Extraction Map."
    },
    "candidates": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "id",
          "source",
          "candidate_content",
          "proposed_owner_surface",
          "future_owner",
          "authority_status",
          "extraction_readiness",
          "rationale"
        ],
        "properties": {
          "id": {
            "type": "string",
            "description": "The id string field in Python-Owned Extraction Map."
          },
          "source": {
            "type": "string",
            "description": "The source string field in Python-Owned Extraction Map."
          },
          "candidate_content": {
            "type": "array",
            "items": {
              "type": "string",
              "description": "Item string accepted by candidate content in Python-Owned Extraction Map."
            },
            "minItems": 1,
            "description": "The candidate content array field in Python-Owned Extraction Map."
          },
          "proposed_owner_surface": {
            "type": "string",
            "description": "The proposed owner surface string field in Python-Owned Extraction Map."
          },
          "future_owner": {
            "enum": [
              "JSON contract data",
              "JSON Schema",
              "markdown contract/doc text",
              "render templates",
              "generated or schema-derived Python model",
              "procedural execution",
              "live repo inspection"
            ],
            "description": "The future owner enumerated value field in Python-Owned Extraction Map."
          },
          "authority_status": {
            "enum": [
              "authoritative",
              "derived",
              "adapter",
              "procedural-owned"
            ],
            "description": "The authority status enumerated value field in Python-Owned Extraction Map."
          },
          "extraction_readiness": {
            "enum": [
              "extracted",
              "extract now",
              "extract after schema exists",
              "operation-contract candidate",
              "template candidate",
              "procedural-owned for now",
              "live-inspection-owned for now",
              "defer with reason"
            ],
            "description": "The extraction readiness enumerated value field in Python-Owned Extraction Map."
          },
          "rationale": {
            "type": "string",
            "description": "The rationale string field in Python-Owned Extraction Map."
          },
          "issue_hint": {
            "type": "string",
            "description": "The issue hint string field in Python-Owned Extraction Map."
          }
        },
        "additionalProperties": false,
        "description": "Item object accepted by candidates in Python-Owned Extraction Map."
      },
      "description": "The candidates array field in Python-Owned Extraction Map."
    },
    "next_slices": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "issue",
          "summary",
          "candidate_ids"
        ],
        "properties": {
          "issue": {
            "type": "string",
            "description": "The issue string field in Python-Owned Extraction Map."
          },
          "summary": {
            "type": "string",
            "description": "The summary string field in Python-Owned Extraction Map."
          },
          "candidate_ids": {
            "type": "array",
            "items": {
              "type": "string",
              "description": "Item string accepted by candidate ids in Python-Owned Extraction Map."
            },
            "minItems": 1,
            "description": "The candidate ids array field in Python-Owned Extraction Map."
          }
        },
        "additionalProperties": false,
        "description": "Item object accepted by next slices in Python-Owned Extraction Map."
      },
      "description": "The next slices array field in Python-Owned Extraction Map."
    }
  },
  "additionalProperties": false,
  "x-agentic-workspace-doc-role": "contract-reference"
}
