{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/python-extraction-map.schema.json",
  "title": "Python-Owned Extraction Map",
  "description": "Map of Python-owned behavior that can or cannot move into contracts.",
  "type": "object",
  "required": [
    "schema_version",
    "parent_issue",
    "purpose",
    "authority_statuses",
    "extraction_readiness",
    "candidates",
    "next_slices"
  ],
  "properties": {
    "schema_version": {
      "const": "agentic-workspace/python-extraction-map/v1",
      "description": "Contract version for this document."
    },
    "parent_issue": {
      "type": "string",
      "description": "Parent issue text value used by this contract."
    },
    "purpose": {
      "type": "string",
      "description": "Purpose text value used by this contract."
    },
    "authority_statuses": {
      "type": "array",
      "items": {
        "enum": [
          "authoritative",
          "derived",
          "adapter",
          "procedural-owned"
        ],
        "description": "One entry in the authority statuses list."
      },
      "minItems": 4,
      "description": "Ordered authority statuses entries used by this contract."
    },
    "extraction_readiness": {
      "type": "array",
      "items": {
        "enum": [
          "extracted",
          "extract now",
          "extract after schema exists",
          "operation-contract candidate",
          "template candidate",
          "procedural-owned for now",
          "live-inspection-owned for now",
          "defer with reason"
        ],
        "description": "One entry in the extraction readiness list."
      },
      "minItems": 7,
      "description": "Ordered extraction readiness entries used by this contract."
    },
    "candidates": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "id",
          "source",
          "candidate_content",
          "proposed_owner_surface",
          "future_owner",
          "authority_status",
          "extraction_readiness",
          "rationale"
        ],
        "properties": {
          "id": {
            "type": "string",
            "description": "Stable identifier used to reference this record."
          },
          "source": {
            "type": "string",
            "description": "Origin or authority source for this value."
          },
          "candidate_content": {
            "type": "array",
            "items": {
              "type": "string",
              "description": "One entry in the candidate content list."
            },
            "minItems": 1,
            "description": "Ordered candidate content entries used by this contract."
          },
          "proposed_owner_surface": {
            "type": "string",
            "description": "Proposed owner surface text value used by this contract."
          },
          "future_owner": {
            "enum": [
              "JSON contract data",
              "JSON Schema",
              "markdown contract/doc text",
              "render templates",
              "generated or schema-derived Python model",
              "procedural execution",
              "live repo inspection"
            ],
            "description": "Allowed future owner value for routing or validation."
          },
          "authority_status": {
            "enum": [
              "authoritative",
              "derived",
              "adapter",
              "procedural-owned"
            ],
            "description": "Allowed authority status value for routing or validation."
          },
          "extraction_readiness": {
            "enum": [
              "extracted",
              "extract now",
              "extract after schema exists",
              "operation-contract candidate",
              "template candidate",
              "procedural-owned for now",
              "live-inspection-owned for now",
              "defer with reason"
            ],
            "description": "Allowed extraction readiness value for routing or validation."
          },
          "rationale": {
            "type": "string",
            "description": "Rationale text value used by this contract."
          },
          "issue_hint": {
            "type": "string",
            "description": "Issue hint text value used by this contract."
          }
        },
        "additionalProperties": false,
        "description": "One entry in the candidates list."
      },
      "description": "Ordered candidates entries used by this contract."
    },
    "next_slices": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "issue",
          "summary",
          "candidate_ids"
        ],
        "properties": {
          "issue": {
            "type": "string",
            "description": "Issue text value used by this contract."
          },
          "summary": {
            "type": "string",
            "description": "Short human-readable summary for this entry."
          },
          "candidate_ids": {
            "type": "array",
            "items": {
              "type": "string",
              "description": "One entry in the candidate ids list."
            },
            "minItems": 1,
            "description": "Ordered candidate ids entries used by this contract."
          }
        },
        "additionalProperties": false,
        "description": "One entry in the next slices list."
      },
      "description": "Ordered next slices entries used by this contract."
    }
  },
  "additionalProperties": false,
  "x-agentic-workspace-doc-role": "contract-reference"
}
