{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/operation-contracts.schema.json",
  "title": "Agentic Workspace Operation Contracts Registry",
  "description": "Schema for Agentic Workspace Operation Contracts Registry.",
  "type": "object",
  "required": [
    "schema_version",
    "summary",
    "primitive_registry",
    "schema",
    "operations"
  ],
  "properties": {
    "schema_version": {
      "const": "agentic-workspace/operation-contracts/v1",
      "description": "The schema version constant field in Agentic Workspace Operation Contracts Registry."
    },
    "summary": {
      "type": "string",
      "minLength": 1,
      "description": "The summary string field in Agentic Workspace Operation Contracts Registry."
    },
    "primitive_registry": {
      "const": "operation_primitives.json",
      "description": "The primitive registry constant field in Agentic Workspace Operation Contracts Registry."
    },
    "schema": {
      "const": "schemas/operation.schema.json",
      "description": "The schema constant field in Agentic Workspace Operation Contracts Registry."
    },
    "operations": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "id",
          "path",
          "command",
          "migration_status"
        ],
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9_.-]*$",
            "description": "The id string field in Agentic Workspace Operation Contracts Registry."
          },
          "path": {
            "type": "string",
            "pattern": "^operations/[a-z0-9_.-]+\\.json$",
            "description": "The path string field in Agentic Workspace Operation Contracts Registry."
          },
          "command": {
            "type": "string",
            "minLength": 1,
            "description": "The command string field in Agentic Workspace Operation Contracts Registry."
          },
          "migration_status": {
            "type": "string",
            "enum": [
              "draft-contract-only",
              "validated-contract",
              "runtime-consumed"
            ],
            "description": "The migration status enumerated value field in Agentic Workspace Operation Contracts Registry."
          }
        },
        "additionalProperties": false,
        "description": "Item object accepted by operations in Agentic Workspace Operation Contracts Registry."
      },
      "description": "The operations array field in Agentic Workspace Operation Contracts Registry."
    }
  },
  "additionalProperties": false,
  "x-agentic-workspace-doc-role": "contract-reference"
}
