{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/python-contract-consumption.schema.json",
  "title": "Python Contract Consumption Policy",
  "description": "Policy describing how Python runtime code consumes contract files.",
  "type": "object",
  "required": [
    "schema_version",
    "loader_boundary",
    "dynamic_validated_loader_boundary",
    "validated_at_consumption",
    "model_derivation_policy"
  ],
  "properties": {
    "schema_version": {
      "const": "agentic-workspace/python-contract-consumption/v1",
      "description": "Contract version for this document."
    },
    "loader_boundary": {
      "type": "object",
      "required": [
        "module",
        "generic_loader",
        "validated_loader",
        "error_type"
      ],
      "properties": {
        "module": {
          "type": "string",
          "description": "Module that owns or consumes this entry."
        },
        "generic_loader": {
          "type": "string",
          "description": "Generic loader text value used by this contract."
        },
        "validated_loader": {
          "type": "string",
          "description": "Validated loader text value used by this contract."
        },
        "error_type": {
          "type": "string",
          "description": "Error type text value used by this contract."
        }
      },
      "additionalProperties": false,
      "description": "Loader boundary details used by this contract."
    },
    "validated_at_consumption": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "contract",
          "schema",
          "loader",
          "consumer"
        ],
        "properties": {
          "contract": {
            "type": "string",
            "description": "Contract text value used by this contract."
          },
          "schema": {
            "type": "string",
            "description": "Schema reference or schema metadata for this payload."
          },
          "loader": {
            "type": "string",
            "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
            "description": "Loader text value used by this contract."
          },
          "consumer": {
            "type": "string",
            "description": "Consumer text value used by this contract."
          }
        },
        "additionalProperties": false,
        "description": "One entry in the validated at consumption list."
      },
      "description": "Ordered validated at consumption entries used by this contract."
    },
    "dynamic_validated_loader_boundary": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "loader",
          "schema",
          "reason"
        ],
        "properties": {
          "loader": {
            "type": "string",
            "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
            "description": "Loader text value used by this contract."
          },
          "schema": {
            "type": "string",
            "description": "Schema reference or schema metadata for this payload."
          },
          "reason": {
            "type": "string",
            "description": "Reason text value used by this contract."
          }
        },
        "additionalProperties": false,
        "description": "One entry in the dynamic validated loader boundary list."
      },
      "description": "Ordered dynamic validated loader boundary entries used by this contract."
    },
    "model_derivation_policy": {
      "type": "object",
      "required": [
        "authority_rule",
        "first_schema_derived_target",
        "handwritten_model_boundary"
      ],
      "properties": {
        "authority_rule": {
          "type": "string",
          "description": "Authority rule text value used by this contract."
        },
        "first_schema_derived_target": {
          "type": "string",
          "description": "First schema derived target text value used by this contract."
        },
        "handwritten_model_boundary": {
          "type": "string",
          "description": "Handwritten model boundary text value used by this contract."
        }
      },
      "additionalProperties": false,
      "description": "Model derivation policy details used by this contract."
    }
  },
  "additionalProperties": false,
  "x-agentic-workspace-doc-role": "contract-reference"
}
