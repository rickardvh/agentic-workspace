{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/python-contract-consumption.schema.json",
  "title": "Python Contract Consumption Policy",
  "description": "Schema for Python Contract Consumption Policy.",
  "type": "object",
  "required": [
    "schema_version",
    "loader_boundary",
    "dynamic_validated_loader_boundary",
    "validated_at_consumption",
    "model_derivation_policy"
  ],
  "properties": {
    "schema_version": {
      "const": "agentic-workspace/python-contract-consumption/v1",
      "description": "The schema version constant field in Python Contract Consumption Policy."
    },
    "loader_boundary": {
      "type": "object",
      "required": [
        "module",
        "generic_loader",
        "validated_loader",
        "error_type"
      ],
      "properties": {
        "module": {
          "type": "string",
          "description": "The module string field in Python Contract Consumption Policy."
        },
        "generic_loader": {
          "type": "string",
          "description": "The generic loader string field in Python Contract Consumption Policy."
        },
        "validated_loader": {
          "type": "string",
          "description": "The validated loader string field in Python Contract Consumption Policy."
        },
        "error_type": {
          "type": "string",
          "description": "The error type string field in Python Contract Consumption Policy."
        }
      },
      "additionalProperties": false,
      "description": "The loader boundary object field in Python Contract Consumption Policy."
    },
    "validated_at_consumption": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "contract",
          "schema",
          "loader",
          "consumer"
        ],
        "properties": {
          "contract": {
            "type": "string",
            "description": "The contract string field in Python Contract Consumption Policy."
          },
          "schema": {
            "type": "string",
            "description": "The schema string field in Python Contract Consumption Policy."
          },
          "loader": {
            "type": "string",
            "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
            "description": "The loader string field in Python Contract Consumption Policy."
          },
          "consumer": {
            "type": "string",
            "description": "The consumer string field in Python Contract Consumption Policy."
          }
        },
        "additionalProperties": false,
        "description": "Item object accepted by validated at consumption in Python Contract Consumption Policy."
      },
      "description": "The validated at consumption array field in Python Contract Consumption Policy."
    },
    "dynamic_validated_loader_boundary": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "loader",
          "schema",
          "reason"
        ],
        "properties": {
          "loader": {
            "type": "string",
            "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$",
            "description": "The loader string field in Python Contract Consumption Policy."
          },
          "schema": {
            "type": "string",
            "description": "The schema string field in Python Contract Consumption Policy."
          },
          "reason": {
            "type": "string",
            "description": "The reason string field in Python Contract Consumption Policy."
          }
        },
        "additionalProperties": false,
        "description": "Item object accepted by dynamic validated loader boundary in Python Contract Consumption Policy."
      },
      "description": "The dynamic validated loader boundary array field in Python Contract Consumption Policy."
    },
    "model_derivation_policy": {
      "type": "object",
      "required": [
        "authority_rule",
        "first_schema_derived_target",
        "handwritten_model_boundary"
      ],
      "properties": {
        "authority_rule": {
          "type": "string",
          "description": "The authority rule string field in Python Contract Consumption Policy."
        },
        "first_schema_derived_target": {
          "type": "string",
          "description": "The first schema derived target string field in Python Contract Consumption Policy."
        },
        "handwritten_model_boundary": {
          "type": "string",
          "description": "The handwritten model boundary string field in Python Contract Consumption Policy."
        }
      },
      "additionalProperties": false,
      "description": "The model derivation policy object field in Python Contract Consumption Policy."
    }
  },
  "additionalProperties": false,
  "x-agentic-workspace-doc-role": "contract-reference"
}
