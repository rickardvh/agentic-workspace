{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/workspace-local-override.schema.json",
  "title": "Agentic Workspace Local Mixed-Agent Override",
  "description": "Machine-local override schema for invocation preferences, delegation capabilities, safety posture, and local memory.",
  "type": "object",
  "required": [
    "schema_version"
  ],
  "properties": {
    "schema_version": {
      "const": 1,
      "description": "Local override contract version."
    },
    "workspace": {
      "type": "object",
      "properties": {
        "cli_invoke": {
          "type": "string",
          "minLength": 1,
          "description": "Command prefix this machine should show in copyable Agentic Workspace commands, such as `uv run agentic-workspace`."
        },
        "shared_config_path": {
          "type": "string",
          "minLength": 1,
          "description": "Optional path to another machine-local Agentic Workspace local config file. Relative paths resolve from the repository root; repo-local local config values override shared values."
        }
      },
      "additionalProperties": false,
      "description": "Local workspace preferences that should not be checked into shared repo config."
    },
    "runtime": {
      "type": "object",
      "properties": {
        "supports_internal_delegation": {
          "type": "boolean",
          "description": "Whether the current runtime can hand work to an internal subagent or equivalent in-session worker."
        },
        "strong_planner_available": {
          "type": "boolean",
          "description": "Whether this local environment has a stronger planning or reasoning path available for quality-sensitive work."
        },
        "cheap_bounded_executor_available": {
          "type": "boolean",
          "description": "Whether this local environment has a lower-cost executor suitable for bounded implementation when quality is not compromised."
        }
      },
      "additionalProperties": false,
      "description": "Capabilities of the current agent/runtime on this machine."
    },
    "handoff": {
      "type": "object",
      "properties": {
        "prefer_internal_delegation_when_available": {
          "type": "boolean",
          "description": "Prefer an in-session or same-runtime handoff when it is available and safe, instead of preparing an external handoff."
        }
      },
      "additionalProperties": false,
      "description": "Local preferences for internal or external delegation handoff."
    },
    "safety": {
      "type": "object",
      "properties": {
        "safe_to_auto_run_commands": {
          "type": "boolean",
          "description": "Whether this local environment may automatically run configured commands when another local policy explicitly permits automation."
        },
        "requires_human_verification_on_pr": {
          "type": "boolean",
          "description": "Whether pull request or review closeout on this machine should remain lower-trust until a human verifies it."
        }
      },
      "additionalProperties": false,
      "description": "Local safety limits for command execution and verification."
    },
    "delegation": {
      "type": "object",
      "properties": {
        "mode": {
          "type": "string",
          "enum": [
            "off",
            "manual",
            "suggest",
            "auto"
          ],
          "default": "suggest",
          "description": "Local control mode for delegation: disable target recommendations, prepare manual handoff prompts, suggest targets without execution, or allow automatic delegation only when safety settings permit it."
        }
      },
      "additionalProperties": false,
      "description": "Machine-local delegation control. This decides how far local tooling may go after recommending stay-local, stronger-reasoning, external-delegation, or manual-handoff."
    },
    "clarification": {
      "type": "object",
      "properties": {
        "mode": {
          "type": "string",
          "enum": [
            "ask-first",
            "suggest",
            "auto-continue"
          ],
          "default": "suggest",
          "description": "Local control mode for clarification: stop and ask before proceeding when intent is unclear, surface the ask-human option without forcing it, or continue with the best bounded interpretation unless a hard blocker is present."
        }
      },
      "additionalProperties": false,
      "description": "Machine-local clarification control for when agents should stop for human input instead of guessing or widening scope."
    },
    "local_memory": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "Whether this machine may use local-only repo memory in addition to checked-in shared Memory."
        },
        "path": {
          "type": "string",
          "minLength": 1,
          "description": "Repository-relative path for local-only memory; it must stay ignored and non-authoritative."
        }
      },
      "additionalProperties": false,
      "description": "Machine-local memory opt-in and path configuration."
    },
    "delegation_targets": {
      "type": "object",
      "patternProperties": {
        "^.+$": {
          "type": "object",
          "required": [
            "strength",
            "execution_methods"
          ],
          "properties": {
            "strength": {
              "type": "string",
              "enum": [
                "strong",
                "medium",
                "weak"
              ],
              "description": "Human- or agent-declared capability strength for this target. Use it as a routing hint, not as proof that the target can close the work."
            },
            "location": {
              "type": "string",
              "enum": [
                "local",
                "external",
                "either"
              ],
              "description": "Where this target runs from the current machine's perspective: local, external, or either. This is a local availability and boundary hint."
            },
            "confidence": {
              "type": "number",
              "minimum": 0,
              "maximum": 1,
              "description": "Advisory confidence in this target profile from 0 to 1. Agents may tune it from local outcome evidence, but it does not create execution authority."
            },
            "task_fit": {
              "type": "array",
              "items": {
                "type": "string",
                "minLength": 1,
                "description": "One kind of task this target has proven or is expected to handle well."
              },
              "description": "Advisory task-fit labels used to explain when this target is appropriate."
            },
            "capability_classes": {
              "type": "array",
              "items": {
                "type": "string",
                "enum": [
                  "boundary-shaping",
                  "reasoning-heavy",
                  "mixed",
                  "mechanical-follow-through"
                ],
                "description": "One capability class this target may be suitable for."
              },
              "description": "Advisory capability classes for routing quality-sensitive or mechanical work. These are estimates, not guarantees."
            },
            "execution_methods": {
              "type": "array",
              "minItems": 1,
              "items": {
                "type": "string",
                "enum": [
                  "internal",
                  "cli",
                  "api",
                  "manual"
                ],
                "description": "One locally available way to reach this target."
              },
              "description": "Human-owned execution boundary for this target. Automatic use still requires delegation.mode = \"auto\" and local safety permission."
            },
            "model_family": {
              "type": "string",
              "minLength": 1,
              "description": "Optional opaque model or family identifier for this local target. This is local descriptive metadata, not package-owned vendor policy."
            },
            "provider": {
              "type": "string",
              "minLength": 1,
              "description": "Optional provider or runtime name for this local target."
            },
            "context_capacity": {
              "type": "string",
              "enum": [
                "small",
                "medium",
                "large",
                "unknown"
              ],
              "default": "unknown",
              "description": "Advisory context capacity class. Use this to avoid handing broad or context-heavy tasks to constrained targets."
            },
            "reasoning_profile": {
              "type": "string",
              "enum": [
                "weak",
                "balanced",
                "strong",
                "unknown"
              ],
              "default": "unknown",
              "description": "Advisory reasoning profile for the target. Structural task classification still outranks this self-description."
            },
            "cost_class": {
              "type": "string",
              "enum": [
                "cheap",
                "standard",
                "premium",
                "unknown"
              ],
              "default": "unknown",
              "description": "Advisory relative cost class used for down-routing only when quality and proof remain safe."
            },
            "latency_class": {
              "type": "string",
              "enum": [
                "fast",
                "standard",
                "slow",
                "unknown"
              ],
              "default": "unknown",
              "description": "Advisory relative latency class for local routing tradeoffs."
            },
            "safe_task_classes": {
              "type": "array",
              "items": {
                "type": "string",
                "enum": [
                  "boundary-shaping",
                  "reasoning-heavy",
                  "mixed",
                  "mechanical-follow-through"
                ]
              },
              "description": "Capability classes this target is explicitly considered safe for when proof remains appropriate."
            },
            "forbidden_task_classes": {
              "type": "array",
              "items": {
                "type": "string",
                "enum": [
                  "boundary-shaping",
                  "reasoning-heavy",
                  "mixed",
                  "mechanical-follow-through"
                ]
              },
              "description": "Capability classes this target must not execute directly, regardless of model confidence."
            },
            "escalation_target": {
              "type": "string",
              "minLength": 1,
              "description": "Optional configured target name or route to prefer when this target is overmatched."
            },
            "confidence_source": {
              "type": "string",
              "minLength": 1,
              "description": "Optional source for the confidence estimate, such as human-declared, evaluation-derived, or agent-managed."
            },
            "last_evaluation": {
              "type": "string",
              "minLength": 1,
              "description": "Optional local note or date for the last capability evaluation used to tune this profile."
            },
            "human_control_modes": {
              "type": "array",
              "items": {
                "type": "string",
                "enum": [
                  "off",
                  "manual",
                  "suggest",
                  "auto"
                ]
              },
              "description": "Delegation modes in which this target may be considered. This keeps human control explicit."
            }
          },
          "additionalProperties": false,
          "description": "One local delegation target profile. Availability fields are human-owned controls; confidence, task fit, and capability classes are advisory estimates."
        }
      },
      "additionalProperties": false,
      "description": "Named local delegation targets available to this runtime."
    }
  },
  "additionalProperties": false,
  "x-agentic-workspace-doc-role": "contract-reference"
}
