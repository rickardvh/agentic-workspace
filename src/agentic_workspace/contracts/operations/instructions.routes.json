{
  "schema_version": "agentic-workspace/operation/v1",
  "id": "instructions.routes",
  "command_surface": {"command": "instructions", "subcommand": "routes", "format_option": "format"},
  "summary": "Progressively discover repo-owned semantic task routes derived from registered capabilities.",
  "classification": "read-only-report",
  "inputs": [
    {"name": "target", "source": "cli-option", "required": false},
    {"name": "format", "source": "cli-option", "required": false},
    {"name": "parent", "source": "cli-option", "required": false},
    {"name": "exact", "source": "cli-option", "required": false}
  ],
  "output": {"kind": "text-or-json", "schema_ref": "schemas/scoped_instruction_operation_result.schema.json"},
  "effects": {"read_only": true, "destructive": false, "idempotent": true, "writes_repo_state": false, "requires_preflight_gate": false},
  "locality": {"requires_repo": true, "network": "forbidden", "outside_repo_writes": "forbidden"},
  "reads": [{"surface": "canonical skill REGISTRY.json files", "required": true}],
  "writes": [],
  "steps": [
    {"id": "resolve_root", "uses": "workspace.target-root.resolve", "description": "Resolve the target root."},
    {"id": "execute_instruction_operation", "uses": "instructions.execute", "description": "Derive and progressively disclose semantic routes."},
    {"id": "emit_output", "uses": "output.emit", "description": "Emit the route discovery result."}
  ],
  "ir_plan": {"status": "complete", "steps": [
    {"id": "resolve_root", "uses": "workspace.target-root.resolve", "description": "resolve_root", "outputs": ["target_root"]},
    {"id": "execute_instruction_operation", "uses": "instructions.execute", "description": "execute_instruction_operation", "outputs": ["result"]},
    {"id": "emit_output", "uses": "output.emit", "description": "emit_output", "outputs": ["emitted"]}
  ]},
  "guards": ["Derive identities from canonical capability declarations.", "Do not emit the full route tree without exact or branch discovery."],
  "proof": ["instructions.routes.process", "root, branch, and direct-leaf discovery fixtures"],
  "migration_status": "runtime-consumed"
}
