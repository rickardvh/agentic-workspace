{
  "schema_version": "agentic-workspace/operation/v1",
  "id": "proof.report",
  "command_surface": {
    "command": "proof",
    "format_option": "format"
  },
  "summary": "Show canonical proof routes, execute a selected proof set as one resumable receipt transaction, preserve machine-local evidence locally, record explicit interoperability receipts, and repair guarded proof routes.",
  "classification": "derived-output",
  "inputs": [
    {"name": "target", "source": "cli-option", "required": false},
    {"name": "route", "source": "cli-option", "required": false},
    {"name": "current", "source": "cli-option", "required": false},
    {"name": "changed", "source": "cli-option", "required": false},
    {"name": "execute_selected", "source": "cli-option", "required": false},
    {"name": "proof_run_id", "source": "cli-option", "required": false},
    {"name": "proof_timeout_seconds", "source": "cli-option", "required": false},
    {"name": "proof_cancel_file", "source": "cli-option", "required": false},
    {"name": "record_receipt", "source": "cli-option", "required": false},
    {"name": "receipt_command", "source": "cli-option", "required": false},
    {"name": "receipt_result", "source": "cli-option", "required": false},
    {"name": "receipt_plan", "source": "cli-option", "required": false},
    {"name": "receipt_log", "source": "cli-option", "required": false},
    {"name": "receipt_route_id", "source": "cli-option", "required": false},
    {"name": "receipt_command_id", "source": "cli-option", "required": false},
    {"name": "receipt_duration_seconds", "source": "cli-option", "required": false},
    {"name": "receipt_timeout", "source": "cli-option", "required": false},
    {"name": "receipt_exit_state", "source": "cli-option", "required": false},
    {"name": "receipt_environment", "source": "cli-option", "required": false},
    {"name": "receipt_claim_sufficiency", "source": "cli-option", "required": false},
    {"name": "receipt_route_budget_seconds", "source": "cli-option", "required": false},
    {"name": "receipt_repair_finding_id", "source": "cli-option", "required": false},
    {"name": "receipt_repair_authority_revision", "source": "cli-option", "required": false},
    {"name": "receipt_repair_disposition", "source": "cli-option", "required": false},
    {"name": "receipt_repair_idempotency_key", "source": "cli-option", "required": false},
    {"name": "route_repair_mode", "source": "cli-option", "required": false},
    {"name": "route_repair_finding_id", "source": "cli-option", "required": false},
    {"name": "route_repair_authority_path", "source": "cli-option", "required": false},
    {"name": "route_repair_field_selector", "source": "cli-option", "required": false},
    {"name": "route_repair_expected_revision", "source": "cli-option", "required": false},
    {"name": "route_repair_delta_json", "source": "cli-option", "required": false},
    {"name": "route_repair_disposition", "source": "cli-option", "required": false},
    {"name": "route_repair_idempotency_key", "source": "cli-option", "required": false},
    {"name": "dry_run", "source": "cli-option", "required": false},
    {"name": "select", "source": "cli-option", "required": false},
    {"name": "verbose", "source": "cli-option", "required": false},
    {"name": "format", "source": "cli-option", "required": false}
  ],
  "output": {
    "kind": "text-or-json",
    "description": "Reports preserve existing proof fields; selected execution returns compact run identity, owner coverage, failure/cancel outcomes, receipt references, claim boundary, and one next action."
  },
  "effects": {
    "read_only": false,
    "destructive": false,
    "idempotent": false,
    "writes_repo_state": true,
    "requires_preflight_gate": false
  },
  "locality": {
    "requires_repo": false,
    "network": "forbidden",
    "outside_repo_writes": "forbidden"
  },
  "reads": [
    {"surface": "proof route contracts", "required": true},
    {"surface": "active planning state", "required": false},
    {"surface": "changed path arguments", "required": false},
    {"surface": "field selector for exact proof drill-down", "required": false},
    {"surface": "surface-value guardrail defaults", "required": false},
    {"surface": ".agentic-workspace/OWNERSHIP.toml [[subsystems]]", "required": false},
    {"surface": "verbose flag for broad diagnostic output", "required": false},
    {"surface": "optional repo-local proof failure log", "required": false},
    {"surface": "structured proof execution metadata supplied by receipt caller", "required": false},
    {"surface": "subject-bound prior selected-proof run receipt for resume or fresh reuse", "required": false},
    {"surface": "route-repair authority revision, field selector, semantic delta, and idempotency identity", "required": false}
  ],
  "writes": [
    {
      "surface": ".agentic-workspace/local/proof-receipts/runs/<run-id>/run.json and <command-id>.json",
      "required": false,
      "when": "--execute-selected and not a dry run",
      "description": "bounded local run aggregate and individually addressable command receipts; intentionally no shared repository proof artifact"
    },
    {
      "surface": ".agentic-workspace/local/proof-receipts/last.json",
      "required": false,
      "when": "--execute-selected completes or --record-receipt is used, and not a dry run",
      "description": "one aggregate selected-proof receipt for automatic execution, or one explicit interoperability receipt"
    },
    {
      "surface": ".agentic-workspace/local/proof-receipts/history.jsonl",
      "required": false,
      "when": "--execute-selected completes or --record-receipt is used, and not a dry run"
    },
    {
      "surface": ".agentic-workspace/config.toml [assurance.domain_proof_lanes]",
      "required": false,
      "when": "--route-repair-mode apply with a matching expected authority revision and successful independent validation",
      "description": "guarded canonical proof-route mutation; pre-apply bytes are restored when schema, route audit, validation, or receipt persistence fails"
    },
    {
      "surface": ".agentic-workspace/local/proof-route-repairs/history.jsonl",
      "required": false,
      "when": "a non-dry-run guarded route repair is applied and validated",
      "description": "apply receipt containing the fresh authority revision and validation/rollback outcome"
    }
  ],
  "steps": [
    {"id": "resolve_root", "uses": "workspace.target-root.resolve", "description": "Resolve the target root."},
    {"id": "resolve_proof", "uses": "proof.routes.resolve", "description": "Resolve route/current/changed-path proof; when requested, bind and execute remaining selected commands, persist compact local receipts, reconcile owner coverage and claim scope, or apply a revision-guarded route repair."},
    {"id": "review_surface_value", "uses": "proof.routes.resolve", "description": "Attach durable-surface value review when changed paths touch durable surfaces."},
    {"id": "select_profile", "uses": "output.profile.select", "description": "Select verbose proof detail or default next-validation action output."},
    {"id": "select_fields", "uses": "output.fields.select", "description": "Return only requested JSON fields when --select is provided."},
    {"id": "emit_output", "uses": "output.emit", "description": "Emit proof data in the requested format."}
  ],
  "guards": [
    "Unknown proof routes should be treated as usage errors.",
    "Additive-only durable surfaces should be flagged unless repeated cost, ownership, discovery, and validation answers are explicit.",
    "Subsystem proof hints must not replace selected package proof lanes; they add host-repo validation context for matching paths.",
    "Tiny output must preserve the first validation action, all required commands, and blocking warnings.",
    "For ordinary changed-path proof, proof_next_decision is authoritative: use its execute-selected action for an admitted command set, preserve manual/runtime/receipt blockers and claim_boundary, and use selectors or verbose output only for optional explanation.",
    "Proof recommendations must not record receipts; --record-receipt requires caller-supplied execution command and result.",
    "--execute-selected runs only commands from the current selected proof decision, reuses only an exact current subject, and fails closed when an explicit run id names stale evidence.",
    "Machine-local config evidence can authorize only the effective-local-configuration claim and must never satisfy repository, PR, release, or parent claims.",
    "Selected execution writes command receipts plus one aggregate receipt only under the existing ignored local proof-receipt owner, keeps command output behind exact receipt references, and never publishes the tracked trusted-producer receipt store.",
    "Proof receipts use caller-supplied structured duration, timeout, environment, route identity, and claim-sufficiency fields; route-health must not infer authoritative timeout or sufficiency from result/log strings.",
    "Route repair receipts must name the stable finding id, authority revision, and disposition before route-health retirement is allowed.",
    "Failed proof receipts may include repair retry ladders, but those ladders guide iteration only and must not replace the originally selected closeout proof.",
    "Failed proof log summaries must stay compact, keep full-log access as a reference, and avoid semantic correctness claims from log text.",
    "Route repair apply accepts only the declared canonical config proof-route surface, requires an exact authority revision plus idempotency key, and restores pre-apply bytes when deterministic validation fails."
  ],
  "proof": [
    "operation contract validates against operation.schema.json",
    "proof changed-path tests remain compatible",
    "proof changed-path tests cover accepted existing durable-surface updates and flagged additive-only surfaces",
    "proof changed-path tests cover subsystem proof hints",
    "proof tiny profile omits selected-lane and provenance detail while preserving required commands",
    "proof receipt tests cover explicit execution evidence recording, failed-receipt retry ladders, route repair receipts, failed-log summaries, and planning closeout consumption",
    "selected execution tests cover success, failure, timeout/cancel, partial resume, fresh reuse, stale subject rejection, owner coverage, and compact detail routes",
    "machine-local config tests prove focused selection, effective-local claim scope, shared-claim rejection, invalidation, and unchanged git status",
    "bounded adaptation conformance proves the declared canonical write surface matches the runtime authority path and that stale revisions supersede without mutation"
  ],
  "migration_status": "runtime-consumed"
}
