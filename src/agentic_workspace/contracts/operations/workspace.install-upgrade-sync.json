{
  "schema_version": "agentic-workspace/operation/v1",
  "id": "workspace.install-upgrade-sync",
  "summary": "Own explicit installed dependency identity transitions and admit before/after identity.",
  "classification": "lifecycle-mutation",
  "inputs": [
    {"name": "mode", "source": "derived", "command_visibility": "runtime-only", "required": true},
    {"name": "before_identity", "source": "derived", "command_visibility": "runtime-only", "required": true}
  ],
  "output": {"kind": "module-result", "description": "An admitted receipt containing the dependency identity before and after the explicit transition."},
  "effects": {
    "read_only": false,
    "destructive": false,
    "idempotent": true,
    "writes_repo_state": false,
    "requires_preflight_gate": true
  },
  "locality": {"requires_repo": true, "network": "allowed", "outside_repo_writes": "allowed"},
  "reads": [{"surface": "installed contract pair", "required": true}],
  "writes": [{"surface": "dependency environment identity", "required": true}],
  "steps": [
    {"id": "observe_before", "uses": "installed-contract.resolve", "description": "Record the admitted before identity."},
    {"id": "perform_transition", "uses": "workspace.lifecycle.apply", "description": "Perform the explicit install, upgrade, or sync transition through the production lifecycle primitive."},
    {"id": "observe_after", "uses": "installed-contract.resolve", "description": "Resolve and admit the after identity."}
  ],
  "guards": ["Ordinary startup, doctor, skills, and managed mutation surfaces are read-only for dependency identity."],
  "proof": ["Receipt contains admitted before and after identity digests."]
}
