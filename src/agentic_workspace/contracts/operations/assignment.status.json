{
  "classification": "read-only-report",
  "command_surface": {"command": "assignment", "format_option": "format", "subcommand": "status"},
  "effects": {"destructive": false, "idempotent": true, "read_only": true, "requires_preflight_gate": false, "writes_repo_state": false},
  "guards": [
    "Resolve one exact assignment/run identity without materializing or mutating lifecycle state.",
    "Return bounded currentness, retention, admission, integration, proof, completion, and exact continuation semantics.",
    "Missing, stale, unknown, and cleaned-up runs remain structured bounded dispositions; raw local state inspection is never the recovery."
  ],
  "id": "assignment.status",
  "inputs": [
    {"name": "target", "required": false, "source": "cli-option"},
    {"name": "format", "required": false, "source": "cli-option"},
    {"name": "assignment_id", "required": false, "source": "cli-option"},
    {"name": "assignment_revision", "required": false, "source": "cli-option"},
    {"name": "run_id", "required": false, "source": "cli-option"}
  ],
  "intent": "Inspect one known delegated assignment/run and return its current lifecycle and exact continuation without mutation or startup takeover.",
  "ir_plan": {
    "status": "complete",
    "steps": [
      {"description": "resolve_root", "id": "resolve_root", "outputs": ["target_root"], "uses": "workspace.target-root.resolve"},
      {"description": "inspect_assignment_lifecycle", "id": "inspect_assignment_lifecycle", "outputs": ["result"], "uses": "assignment.lifecycle.apply"},
      {"description": "emit_output", "id": "emit_output", "outputs": ["emitted"], "uses": "output.emit"}
    ]
  },
  "locality": {"network": "forbidden", "outside_repo_writes": "forbidden", "requires_repo": true},
  "migration_status": "runtime-consumed",
  "output": {"kind": "text-or-json", "schema_ref": "schemas/assignment_lifecycle_result.schema.json"},
  "proof": [
    "assignment status tests cover current, stale, missing, unknown, and cleaned-up run identities without writes",
    "generated Python, TypeScript, and external consumer clients expose the same operation identity and result"
  ],
  "reads": [
    {"description": "checked-in Planning assignment authority resolved by assignment id", "required": false, "surface": ".agentic-workspace/planning/assignments/<assignment-id>.assignment.json"},
    {"description": "local disposable state for the exact run id", "required": false, "surface": ".agentic-workspace/local/assignment-runs/<run-id>/state.json"}
  ],
  "schema_version": "agentic-workspace/operation/v1",
  "steps": [
    {"description": "Resolve the target repository root.", "id": "resolve_root", "uses": "workspace.target-root.resolve"},
    {"description": "Inspect the exact assignment/run state without writing.", "id": "inspect_assignment_lifecycle", "uses": "assignment.lifecycle.apply"},
    {"description": "Emit the bounded status result.", "id": "emit_output", "uses": "output.emit"}
  ],
  "summary": "Inspect one exact assignment run and return its current lifecycle and recovery action.",
  "writes": []
}
