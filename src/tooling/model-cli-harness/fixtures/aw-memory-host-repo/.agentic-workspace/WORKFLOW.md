# Agentic Workspace Workflow

Read the repository instructions. Keep direct work direct and load only task-relevant owner detail.

When current facts or effects matter, use `agentic-workspace start --target . --task "<task>" --format json`, adding repeated `--changed` arguments for known paths. Follow exact returned references and requests; execute only an admitted action with `invoke`.

If native execution is unavailable, report that boundary and use only repository-readable facts. Do not reconstruct runtime output or mutate managed owner state. Startup-only and orientation-only work remains read-only unless the user requests durable handoff.

Create Planning custody only when the work needs continuity. Validate the changed surface narrowly and distinguish proof from intent satisfaction. This file is procedure, not task state.
