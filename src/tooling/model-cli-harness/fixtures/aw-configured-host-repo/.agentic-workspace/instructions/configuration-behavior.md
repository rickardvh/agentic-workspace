# Configuration-sensitive work

Before claiming completion, inspect the effective config when settings affect the
task and report their actual effect. Prefer concise agent-readable output.

Target-selection preferences in this synthetic fixture: the docs worker is suited
to mechanical documentation, narrow validation and fixture maintenance under
explicit scope; the planner is suited to workflow design, planning contracts and
review requiring judgment. Keep configured hard restrictions and human review.
The docs worker has no executable command; its handoff requires manual transport.
Runtime availability must be observed. These preferences grant no capability.
