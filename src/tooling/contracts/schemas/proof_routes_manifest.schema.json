{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic-workspace/proof-routes-manifest.schema.json",
  "title": "Proof Routes Manifest",
  "description": "Source-maintenance model only; not native/public command authority. Manifest of named proof routes and their validation commands.",
  "type": "object",
  "required": [
    "schema_version",
    "canonical_doc",
    "command",
    "default_routes"
  ],
  "properties": {
    "schema_version": {
      "const": "proof-routes-manifest/v1",
      "description": "Contract version for this document."
    },
    "canonical_doc": {
      "type": "string",
      "description": "Canonical doc text value used by this contract."
    },
    "command": {
      "type": "string",
      "description": "Command string or command name used by this contract."
    },
    "default_routes": {
      "type": "object",
      "minProperties": 1,
      "additionalProperties": {
        "type": "string",
        "description": "Named entry in the default routes mapping."
      },
      "description": "Default routes details used by this contract."
    },
    "migration_status": {
      "const": "source-maintenance-only",
      "description": "Retained source-maintenance metadata; not an executable native/public CLI contract. Current commands belong to source_decision_contract.json native_cli."
    }
  },
  "additionalProperties": false,
  "x-agentic-workspace-doc-role": "contract-reference"
}
